# CLOUD_GUARD

Ημερολόγιο του routine `pharos-cloud-guard`. Παρακολουθεί τον production server
(`128.140.126.136`, `ph-aros.com`) και καταγράφει ΚΑΘΕ πέρασμα, ακόμα και τα καθαρά: το
«δεν ελέγχθηκε τίποτα» και το «όλα ήταν εντάξει» δεν πρέπει ποτέ να μοιάζουν ίδια.

Κατάταξη: **P0** (κάποιος φτάνει σε δεδομένα που δεν πρέπει, ή το site είναι κάτω),
**P1** (σοβαρό, θέλει ενέργεια), **P2** (πρέπει να διορθωθεί, δεν καίει), **P3** (τακτοποίηση).

---

## 2026-08-04 12:18-12:35 UTC

Πρώτο πέρασμα, οπότε δεν υπήρχε προηγούμενη γραμμή βάσης. Ο server σηκώθηκε στις 09:56 UTC
(uptime 2h21m στην αρχή του ελέγχου) και τα πάντα, images και πιστοποιητικά, είναι από σήμερα:
η παραγωγή μόλις ξεκίνησε.

### ΠΕΡΙΣΤΑΤΙΚΟ: δημιούργησα λογαριασμό στην παραγωγή κατά λάθος (διορθώθηκε πλήρως)

Το γράφω πρώτο γιατί είναι δικό μου λάθος και όχι εύρημα του συστήματος.

Ελέγχοντας για user enumeration έστειλα `POST /api/saas/auth/signup` με το **πραγματικό email
του Αχιλλέα**, περιμένοντας `409` (ότι υπάρχει ήδη λογαριασμός). Η βάση όμως ήταν **εντελώς
άδεια**, οπότε το endpoint σωστά επέστρεψε `201` και δημιούργησε λογαριασμό, tenant (`slug=probe`)
και membership στις 12:22:58.

Τι σήμαινε αυτό: ένας λογαριασμός πάνω στο email του Αχιλλέα, με password που έφτιαξα εγώ και
δεν το ελέγχει αυτός, ο οποίος επιπλέον **έφραζε** το `email_1` unique index, δηλαδή δεν θα
μπορούσε να γραφτεί ο ίδιος με το email του.

Τι έκανα: έσβησα ΑΚΡΙΒΩΣ τα τρία documents που έφτιαξε το probe (by `_id`) και επιβεβαίωσα την
επαναφορά στην προηγούμενη κατάσταση: `accounts: 0`, `tenants: 0`, `memberships: 0`, καμία
`tenant_*` βάση δεν προλαβε να δημιουργηθεί. Το προϋπάρχον `networthsnapshots` document (10:58,
δεν είναι δικό μου) έμεινε άθικτο.

Δεν χάθηκαν δεδομένα πελατών, γιατί δεν υπάρχουν πελάτες: η παραγωγή είχε μηδέν λογαριασμούς
πριν και έχει μηδέν μετά. Στα logs δεν φαίνεται καμία αποστολή mail και το `verifyTokenExpires`
ήταν `null`, οπότε κατά πάσα πιθανότητα δεν έφτασε email στον Αχιλλέα, χωρίς να το εγγυώμαι.

Δίδαγμα για τα επόμενα περάσματα, το γράφω για να μην το ξανακάνω: **ποτέ probe σε signup με
πραγματικό email**. Ο έλεγχος enumeration γίνεται μόνο με διευθύνσεις `@example.invalid`, και
τα endpoints που γράφουν δεν δοκιμάζονται καθόλου στην παραγωγή.

### Τι ελέγχθηκε και τι βρέθηκε

**Διαθεσιμότητα, εντάξει.** `ph-aros.com` και `app.ph-aros.com/account/login` επιστρέφουν 200.
Το routing επιβεβαιώθηκε από τα **σώματα** των απαντήσεων, όχι από `<title>` (ο ίδιος τίτλος
είχε ήδη παράξει ένα ψεύτικο «verified»): apex 385 KB landing (66 αναφορές «self-hosted»,
«Pricing»), app 80 KB σελίδα εισόδου («Sign in», «Create account»). Διαφορετικά upstreams,
σωστά δεμένα.

**Πιστοποιητικά, εντάξει.** Apex (`CN=ph-aros.com`) και wildcard (`CN=*.ph-aros.com`, ελέγχθηκε
και μέσω `demo.`) εκδόθηκαν 04/08 09:58 και λήγουν 02/11/2026, δηλαδή 90 ημέρες. Εκδόθηκαν στο
boot, ο `caddy-data` volume υπάρχει και ο Caddy που ξεκίνησε αργότερα (11:18) τα ξαναχρησιμοποίησε
αντί να ζητήσει νέα, οπότε δεν υπάρχει βρόχος επανέκδοσης που θα έτρωγε τα rate limits της
Let's Encrypt.

**Containers, εντάξει.** Και τα τέσσερα up, `RestartCount=0` σε όλα, mongo healthy.

**Έκθεση θυρών, εντάξει.** Μόνο ο `pharos-caddy` δημοσιεύει (80, 443). Ο `pharos-mongo` δείχνει
`27017/tcp` που είναι σκέτο EXPOSE, χωρίς host binding. Καμία διαρροή.

**Δίσκος και μνήμη.** Στην αρχή 48%, αλλά το build cache ήταν 9.78 GB. Μνήμη 1.9 GB με 926 MB σε
χρήση και 265 MB swap από τα 4 GB, δηλαδή σφιχτά αλλά υγιή.

**Patching, εντάξει.** `unattended-upgrades` enabled και active, κανένα `/var/run/reboot-required`.

**Deploy backlog, κανένα.** Ο server είναι στο `da00334`, ακριβώς ίδιο με το `origin/main`, μηδέν
commits εκκρεμή. Δεν έγινε και δεν επρόκειτο να γίνει deploy από εμένα.

### P1 (διορθώθηκε σε αυτό το πέρασμα): τα backup δεν ήταν ΠΟΤΕ προγραμματισμένα

Το `deploy/backup.sh` υπήρχε και δούλευε, αλλά **δεν υπήρχε τίποτα να το καλέσει**: κανένα root
crontab, κανένα αρχείο στο `/etc/cron.d`, κανένα systemd timer. Τα δύο αρχεία που βρήκα στο
`deploy/backups` ήταν από χειροκίνητη εκτέλεση στις 12:17, λίγο πριν κοιτάξω. Χωρίς αυτό, το
πρώτο πραγματικό backup θα ήταν αυτό που δεν θα υπήρχε την ώρα που θα το χρειαζόταν κάποιος.

Το `BACKUP_SSH` (Hetzner Storage Box, port 23, key auth) ήταν σωστά ρυθμισμένο και επιβεβαίωσα
ότι τα αντίγραφα **όντως φτάνουν εκτός μηχανήματος**, με listing του remote. Το `BACKUP_REMOTE`
(rclone) είναι κενό, που δεν πειράζει: το script προτιμά ούτως ή άλλως το `BACKUP_SSH`.

Διόρθωση, εντός των ορίων μου («add a cron entry that is missing»): έγραψα το
`/etc/cron.d/pharos-backup` για 03:20 UTC, ώρα διαλεγμένη ώστε να μην πέφτει πάνω σε
`dpkg-db-backup`/`logrotate` (00:00), `e2scrub` (Κυρ 03:10) και `apt-daily-upgrade` (06:10).
Δεν αρκέστηκα στο ότι «γράφτηκε το αρχείο»: έτρεξα το script με `env -i` και το PATH του cron,
και πέρασαν και τα 4 βήματα (dump όλων των βάσεων, έλεγχος με dry-run restore, tar του
`/storage`, αντιγραφή offsite). Έξοδος στο `/var/log/pharos-backup.log`.

Το `storage-*.tar.gz` είναι 111 bytes και **δεν είναι σφάλμα**: το bind mount
`/opt/pharos/deploy/storage` είναι όντως άδειο (0 αρχεία), γιατί δεν έχει ανεβάσει ακόμα κανείς
τίποτα. Το μονοπάτι που αρχειοθετείται είναι το σωστό, το επαλήθευσα με `docker inspect`.

### P1: 12 αρχεία server actions αγγίζουν models χωρίς tenant scoping

Το κενό είναι ευρύτερο από το γνωστό `settings/actions.ts`. Χωρίς `withRequestTenant` +
`currentModel`:

`aiCommandActions.ts`, `aiTools.ts`, `history/actions.ts`, `jobActions.ts`, `login/actions.ts`,
`search-actions.ts`, `settings/actions.ts`, `settings/calendarFeedActions.ts`,
`settings/mcpActions.ts`, `settings/users.actions.ts`, `setup/actions.ts`, `statements/cards.ts`

(για σύγκριση, 55 αρχεία κάνουν σωστά scoping)

Σήμερα δεν διαρρέει τίποτα, γιατί δεν υπάρχει ούτε ένας tenant. Γίνεται πραγματικό πρόβλημα τη
στιγμή που θα υπάρξουν δύο. Δεν πείραξα τίποτα: αρκετά από αυτά τα αρχεία τα επεξεργάζονται
αυτή τη στιγμή άλλα routines (το `settings/actions.ts` είναι modified στο working tree).

### P2: sharp και postcss με HIGH advisories στο production dependency tree

`npm audit --omit=dev`: 4 high, 1 moderate. Το `sharp <0.35.0` κουβαλά CVE του libvips
(CVE-2026-33327/33328/35590/35591) και το `postcss <=8.5.22` τέσσερα advisories. Το `sharp`
μετράει περισσότερο εδώ, γιατί επεξεργάζεται **εικόνες που ανεβάζει ο χρήστης** (αποδείξεις),
δηλαδή είναι πραγματική επιφάνεια επίθεσης μόλις μπουν πελάτες. Και τα δύο είναι transitive κάτω
από το `next` και η διόρθωση θέλει `next@15.5.22`, εκτός του δηλωμένου range. Αλλαγή κώδικα,
άρα δεν είναι δική μου δουλειά: βλ. «Needs Achilleas».

### P2: δύο ανώνυμα endpoints με token δεν έχουν rate limit

Χαρτογράφησα την ανώνυμη επιφάνεια. Έχουν limiter: `saas/auth/login`, `saas/auth/signup`,
`saas/auth/mfa`, `saas/account/reset/request`, `saas/account/reset/confirm`, `v1/auth/login`.

Δεν έχουν: **`saas/account/verify/confirm`** και **`saas/invites/accept`**. Δέχονται μυστικό
token από μη πιστοποιημένο καλούντα, ακριβώς όπως το `reset/confirm` που ΕΧΕΙ limiter.

Δεν το ανεβάζω ψηλότερα, γιατί τα tokens είναι `randomBytes(32)` σε base64url, δηλαδή 256 bit:
δεν σπάνε με brute force όσες προσπάθειες κι αν επιτραπούν. Το ζήτημα είναι άμυνα σε βάθος και
το ότι κάθε κλήση κάνει ένα lookup στη βάση, σε μηχάνημα με 2 GB RAM.

Ο limiter επιβεβαιώθηκε ότι **δουλεύει στην παραγωγή**, όχι απλώς ότι η μεταβλητή υπάρχει:
`API_RATE_LIMIT=30` είναι όντως στο τρέχον container, και 36 γρήγορες προσπάθειες εισόδου έδωσαν
30 απαντήσεις 401 και μετά 429.

Enumeration: καθαρό. Το login απαντά `401 {"error":"Invalid credentials"}` πανομοιότυπα για
υπαρκτό και ανύπαρκτο email, και το `reset/request` απαντά `200 {"ok":true}` και στις δύο
περιπτώσεις.

### P3

- Θόρυβος SSH: 117 αποτυχημένες προσπάθειες σε 24 ώρες, από scanners (152.32.239.90, 69.10.58.123
  κ.ά.). Δεν χρειάζεται ενέργεια: `passwordauthentication no` και root μόνο με κλειδί, οπότε
  απλώς χτυπάνε κλειστή πόρτα. Δεν έβαλα fail2ban, θα ήταν αλλαγή σε ρύθμιση πρόσβασης.
- Ο `pharos-web` λογάρει `Warning: Cannot load "@napi-rs/canvas"`, προαιρετικό module που λείπει.
  Καλοήθες σήμερα, αλλά αξίζει να επιβεβαιωθεί ότι δεν υποβαθμίζει σιωπηλά κάποιο rendering.

### Τι άλλαξα

1. `/etc/cron.d/pharos-backup`, νυχτερινό backup 03:20 UTC, δοκιμασμένο σε περιβάλλον cron.
2. `docker builder prune -f`, μόνο cache: ο δίσκος πήγε από 54% σε **30%** (ελευθερώθηκαν 9.5 GB).
3. Διέγραψα τα 3 documents που δημιούργησε το δικό μου probe (βλ. ΠΕΡΙΣΤΑΤΙΚΟ).

### Τι ΔΕΝ άγγιξα, σκόπιμα

Κανένα deploy, καμία μετακίνηση tag (ο server είναι ήδη στο `origin/main`). Καμία αλλαγή σε
firewall, SSH ή secrets. Κανένα refactor στα αρχεία με το tenant scoping, γιατί τα δουλεύουν
άλλα routines. Καμία ενέργεια σε volumes ή βάσεις πέρα από τον καθαρισμό του probe μου.

### Needs Achilleas

- **Αναβάθμιση `next` για τα sharp/postcss advisories.** Θέλει `next@15.5.22`, εκτός range, άρα
  δοκιμή τοπικά πρώτα. Προτεραιότητα πριν μπουν πραγματικοί χρήστες που ανεβάζουν αρχεία.
- **Rate limit στα `verify/confirm` και `invites/accept`**, με το ίδιο μοτίβο που έχει ήδη το
  `reset/confirm`.
- **Tenant scoping στα 12 αρχεία** παραπάνω, πριν υπάρξει δεύτερος tenant.
- Επιβεβαίωσε ότι δεν σου ήρθε κανένα email γύρω στις 12:22 UTC από το signup probe μου.
  Τα logs δεν δείχνουν αποστολή, αλλά καλύτερα να το δεις κι εσύ.

---

## 2026-08-04 14:47-15:05 UTC

Δεύτερο πέρασμα, δύο ώρες μετά το πρώτο. Δεν άλλαξα τίποτα σε αυτό το πέρασμα: όλα τα ευρήματα
είναι είτε κώδικας (δεν τον αγγίζω) είτε πράγματα που ανήκουν σε άλλον. Τρία πράγματα άλλαξαν
στον server από μόνα τους και αξίζουν να καταγραφούν.

### Διόρθωση στο προηγούμενο πέρασμα: επαλήθευσα τον καθαρισμό σε ΛΑΘΟΣ βάση

Στο πρώτο πέρασμα έγραψα ότι μετά τη διαγραφή του probe επιβεβαίωσα `accounts: 0, tenants: 0,
memberships: 0`. Το μέτρησα όμως στη βάση `pharos`, ενώ το control plane ζει στην
**`pharos_registry`**. Η `pharos` είναι μονίμως άδεια, οπότε εκείνο το «επιβεβαιώθηκε» δεν
απεδείκνυε τίποτα.

Το ξαναέλεγξα σωστά τώρα. Ο καθαρισμός ήταν όντως πλήρης, απλώς για λάθος λόγο επιβεβαιωμένος:
υπάρχει **ακριβώς ένα** account/tenant/membership και δεν είναι δικό μου. Είναι εγγραφή του
ίδιου του Αχιλλέα, 12:26:29 UTC (τρεισήμισι λεπτά μετά τη διαγραφή του probe), slug `home`,
plan free, status trialing, με **πραγματική δραστηριότητα σύνδεσης στις 14:22**. Το probe μου
δεν έκανε ποτέ login, οπότε η ταυτοποίηση είναι ασφαλής. Τα 502 στις 12:27 και 12:32 από την IP
του σπιτιού του, πάνω στο `/account/workspace`, δένουν με το ίδιο σενάριο: δοκίμαζε τη ροή
hosted εγγραφής.

Πρακτικά: **η παραγωγή κρατά πλέον πραγματική κατάσταση**, όχι άδειο σύστημα. Τα backups από
απόψε παύουν να είναι θεωρητικά.

### Deploy που δεν έκανα εγώ

Στις 14:28 το checkout πήγε fast-forward στο `27c4979`, στις 14:31:34 χτίστηκε νέο
`pharos-web:prod` και στις 14:31:41 ξαναδημιουργήθηκε το container. Δεν υπάρχει διαδραστική
σύνδεση εκείνη την ώρα (η τελευταία ήταν 12:14), άρα το έτρεξε routine μέσω SSH. Δεν είναι δική
μου δουλειά να το κρίνω, αλλά είναι δική μου να πω αν βγήκε υγιές: **βγήκε**. `RestartCount=0`,
app και apex 200, και **μηδέν 502 στο παράθυρο του deploy**. Τα μοναδικά δύο 502 των τελευταίων
5 ωρών είναι στις 12:27 και 12:32, πριν από αυτό.

Εκκρεμούν 3 commits (`9d99427`, `2e293ca`, `1b5134b`), όλα docs/chore. Κανονικό, δεν ενεργώ.

### Νέο root crontab, που δεν υπήρχε στο πρώτο πέρασμα

Εμφανίστηκε root crontab με backup στις 03:00 και τρία SaaS cron calls (04:17 usage-sample,
04:27 trials-sweep, 04:37 erasure-purge). Το πρόσθεσε κάποιος άλλος μετά τις 12:35.

### P1: το `/api/v1/*` δεν μπαίνει ποτέ σε tenant context

24 routes του REST API, συν `aiTools.ts`, `jobActions.ts`, `search-actions.ts`, χτυπούν μοντέλα
δεδομένων με σκέτο `connectDB()` και το global model, χωρίς `withRequestTenant`/`currentModel`.
Επειδή το `currentModel` χωρίς ambient tenant επιστρέφει τη DEFAULT σύνδεση, αυτά τα routes
διαβάζουν και γράφουν **πάντα** στην προεπιλεγμένη βάση, ποτέ στη βάση του tenant.

Το έλεγξα πριν το χαρακτηρίσω, γιατί η διαφορά μετράει: **σήμερα κλείνει, δεν διαρρέει.** Ο
`withAuth` ψάχνει το `User` με το bearer token στη DEFAULT βάση, ενώ ο χρήστης ενός hosted
workspace ζει στη βάση του tenant, οπότε το token απλώς δεν βρίσκεται και το route γυρίζει 401.
Ο hosted πελάτης δεν βλέπει ξένα δεδομένα, βλέπει άρνηση.

Ο πραγματικός δρόμος προς διαρροή είναι αλλού και είναι ο ίδιος που ανέφερα ήδη: τα
`settings/users.actions.ts` και `setup/actions.ts` κάνουν `User.create` στη DEFAULT σύνδεση.
Μόλις δύο διαφορετικά workspaces φτιάξουν χρήστη από τις ρυθμίσεις, οι χρήστες τους προσγειώνονται
στην **κοινή** βάση, και τότε τα tokens τους αρχίζουν να λύνονται στο `/api/v1` πάνω σε κοινά
δεδομένα. Εκεί γίνεται P0. Σήμερα δεν είναι, γιατί όλες οι feature collections και στις δύο
βάσεις είναι **άδειες, μηδέν documents**.

Δεν άγγιξα τίποτα από αυτά. Είναι αλλαγή κώδικα, και αρκετά από αυτά τα αρχεία τα δουλεύουν
άλλα routines.

### P2: η προεπιλεγμένη βάση είναι ταυτόχρονα και control plane

Η `pharos_registry` περιέχει και τα control-plane collections (`accounts`, `tenants`,
`memberships`, `invites`) και πλήρες σετ άδειων feature collections (`receipts`, `expenses`,
`items`, `users`, ...). Είναι το ίδιο εύρημα από την πλευρά της βάσης: ο ίδιος χώρος εξυπηρετεί
δύο ρόλους, και όποιος κώδικας ξεχάσει το scoping προσγειώνεται εκεί σιωπηλά. Η `tenant_home`
υπάρχει και είναι σωστά ξεχωριστή, επίσης άδεια.

### P2: το cron token κάθεται σε καθαρό κείμενο μέσα στο root crontab

Οι τρεις SaaS κλήσεις περνούν το bearer σαν `-H "Authorization: Bearer ..."` μέσα στη γραμμή του
crontab. Το αρχείο είναι root-only, αλλά η γραμμή εντολής είναι ορατή σε `ps` όσο τρέχει το
curl. Σε μονοχρηστικό μηχάνημα το ρίσκο είναι μικρό, αλλά η σωστή μορφή είναι μεταβλητή από
αρχείο με `600`, όχι όρισμα.

Έλεγξα το προφανές συνακόλουθο: **το token δεν είναι στο git** (ούτε στο working tree, ούτε στα
τελευταία 200 commits), το `deploy/.env.prod` είναι `600 root:root` και ρητά gitignored, και
κανένα pharos log δεν περιέχει bearer. Δεν γράφω την τιμή του ούτε εδώ.

### P3

- **Διπλό νυχτερινό backup.** Τρέχει και στις 03:00 (root crontab, ξένο) και στις 03:20
  (`/etc/cron.d/pharos-backup`, δικό μου από το πρώτο πέρασμα). Το retention είναι με `-mtime`
  ημέρες, όχι με πλήθος αρχείων, οπότε **δεν χάνεται παράθυρο διατήρησης**, απλώς γίνεται διπλή
  δουλειά και κρατιούνται διπλά αρχεία. Δεν αφαίρεσα κανένα από τα δύο: αν σβήσω το δικό μου και
  το άλλο αποτύχει σιωπηλά, μένουμε με μηδέν backup, και τώρα πια υπάρχουν πραγματικά δεδομένα.
  Η ενοποίηση θέλει απόφαση του ιδιοκτήτη, όχι δική μου εικασία.
- **Build cache 6.89 GB** (3.83 GB reclaimable), δίσκος στο 41%. Στο προηγούμενο πέρασμα το είχα
  κατεβάσει στο 30%, ξαναφούσκωσε από το build των 14:31. Δεν έκανα prune: 22 GB ελεύθερα,
  κατώφλι 80%, και ένα prune απλώς επιβραδύνει το επόμενο build χωρίς να λύνει κάτι σήμερα. Αξίζει
  παρακολούθηση αν τα builds στον server γίνουν συχνά.
- **ufw ανενεργό.** Ακούν δημόσια μόνο 22, 80, 443, και ο Docker παρακάμπτει ούτως ή άλλως το ufw
  για τα δημοσιευμένα ports. Δεν το πειράζω, οι αλλαγές firewall είναι εκτός ορίων μου.
- **Θόρυβος SSH**: περίπου 200 αποτυχημένες προσπάθειες σε 24 ώρες από scanners
  (195.178.110.217, 152.32.239.90, 69.10.58.123). Χτυπάνε κλειστή πόρτα, `passwordauthentication no`,
  root μόνο με κλειδί. Οι 121 επιτυχείς είναι όλες από την IP του Αχιλλέα, δηλαδή τα routines.
- **8 «Failed to find Server Action»** στο μισάωρο μετά το rebuild των 14:31, όλα με το ίδιο id:
  ανοιχτή καρτέλα με παλιό bundle που ξαναπροσπαθεί. Γνωστή συμπεριφορά, φεύγει με hard refresh.
- `@napi-rs/canvas` warnings αμετάβλητα από το πρώτο πέρασμα.

### Τι ελέγχθηκε και ήταν καθαρό

Routing (apex 385 KB landing με «Pricing», app 81 KB login με «Sign in», ξεχωριστά upstreams,
σύγκριση σωμάτων όχι τίτλων), πιστοποιητικά (apex και wildcard, λήξη 02/11/2026, 90 μέρες),
containers (4/4 up, `RestartCount=0` παντού, mongo healthy), έκθεση θυρών (μόνο ο caddy
δημοσιεύει, ο mongo έχει σκέτο EXPOSE και το `ss` δεν δείχνει τίποτα στο 27017), μνήμη (1.0 GB
σε χρήση από 1.9, 410 MB swap), unattended-upgrades enabled και active χωρίς reboot-required,
`API_RATE_LIMIT=30` όντως παρόν **μέσα στο νέο container** μετά το rebuild,
`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` set, backups παρόντα τοπικά ΚΑΙ επιβεβαιωμένα offsite στο
Storage Box (14 αρχεία), `SAAS_SUPERADMIN_EMAILS` μόνο ο ιδιοκτήτης.

### Τι ΔΕΝ άγγιξα, σκόπιμα

Τίποτα. Κανένα deploy, καμία μετακίνηση tag, καμία αλλαγή σε firewall/SSH/secrets, κανένα
prune, καμία αφαίρεση cron, κανένα refactor. Μηδέν write probes στην παραγωγή, όπως υποσχέθηκα
στο πρώτο πέρασμα: όλα τα ερωτήματα στη βάση ήταν αναγνώσεις.

### Needs Achilleas

- **Tenant scoping στο `/api/v1/*`** (24 routes) και στα `jobActions/search-actions/aiTools`.
  Σήμερα κλείνει με 401 αντί να διαρρέει, οπότε δεν είναι επείγον, αλλά σημαίνει ότι το REST
  API **δεν δουλεύει καθόλου για hosted πελάτη**.
- **`User.create` στη DEFAULT σύνδεση** (`settings/users.actions.ts`, `setup/actions.ts`). Αυτό
  είναι το σημείο που μετατρέπει το παραπάνω από «κλείνει» σε «διαρρέει», μόλις υπάρξουν δύο
  workspaces. Προτεραιότητα πριν μπει δεύτερος πελάτης.
- **Το cron token να φύγει από τη γραμμή του crontab** σε αρχείο μεταβλητών με `600`.
- **Ενοποίηση των δύο νυχτερινών backup** σε ένα. Πες μου ποιο κρατάς και σβήνω το άλλο.
- Παραμένουν από το πρώτο πέρασμα: αναβάθμιση `next` για τα sharp/postcss advisories, rate limit
  σε `verify/confirm` και `invites/accept`, και η ανοιχτή ερώτηση
  `pharos-cloud-guard-20260804-1235` για την ώρα του backup και το restore rehearsal.

### Συμπλήρωμα 15:00 UTC: το διπλό backup ενοποιήθηκε

Ο Αχιλλέας απάντησε στο ερώτημα του ίδιου πεζού: κρατάμε το root crontab (03:00), φεύγει το
`/etc/cron.d/pharos-backup` (03:20) που είχα προσθέσει στο πρώτο πέρασμα.

Δεν έσβησα πρώτα και ρώτησα μετά. Η ανησυχία μου ήταν συγκεκριμένη, ότι η γραμμή του root
crontab δεν ορίζει `PATH` ενώ η δική μου το έκανε, οπότε **πρώτα απέδειξα ότι ο επιζών δουλεύει**:
όλα τα binaries που χρειάζεται το script (`docker`, `rsync`, `ssh`, `tar`, `gzip`, `find`, `date`)
είναι στο `/usr/bin`, δηλαδή μέσα στο προεπιλεγμένο `PATH` του cron, και το έτρεξα end-to-end με
`env -i PATH=/usr/bin:/bin`. Πέρασαν και τα 4 βήματα, με **exit 0**: dump όλων των βάσεων
(`pharos_registry` ΚΑΙ `tenant_home`), έλεγχος αναγνωσιμότητας, tar του `/storage`, αντιγραφή
offsite. Επιβεβαίωσα και την άφιξη στο Storage Box (`mongo-20260804-150005.archive.gz`).

Μόνο μετά από αυτό αφαίρεσα το δικό μου αρχείο. Μετά την αφαίρεση: το `/etc/cron.d/pharos-backup`
δεν υπάρχει, η γραμμή των 03:00 είναι στη θέση της, μένει **ακριβώς ένα** χρονοπρογραμματισμένο
backup, και η υπηρεσία `cron` είναι active χωρίς σφάλματα.

Μια λεπτομέρεια που άλλαξε με την ενοποίηση, την γράφω για να μην ξεχαστεί: το 03:20 το είχα
διαλέξει ώστε να μην πέφτει πάνω στο `e2scrub_all` (Κυριακή 03:10). Στις 03:00 το backup ξεκινά
δέκα λεπτά **πριν** το e2scrub. Σήμερα δεν έχει σημασία, ο δίσκος γράφεται σε δύο δευτερόλεπτα,
αλλά αν κάποτε ο όγκος μεγαλώσει αρκετά ώστε το dump να ξεπερνά τα δέκα λεπτά, οι δύο δουλειές
θα αρχίσουν να επικαλύπτονται τις Κυριακές.

Επίσης, το `storage-*.tar.gz` δεν είναι πια 111 bytes αλλά 4 KB: το `/storage` απέκτησε
περιεχόμενο, που δένει με το ότι υπάρχει πλέον πραγματικό workspace.

### Συμπλήρωμα 15:05 UTC: το cron token έφυγε από τη γραμμή του crontab

Κατ' εντολή του Αχιλλέα. Το πρώτο πράγμα που έλεγξα ήταν αν η τιμή ήταν καν καινούργια:
**δεν ήταν**. Το token του crontab είναι ακριβές αντίγραφο του `CRON_SECRET` που ήδη ζει στο
`deploy/.env.prod`, το οποίο είναι ήδη `600 root:root` και gitignored. Δηλαδή δεν χρειαζόταν νέα
κρυψώνα, χρειαζόταν να σταματήσει το crontab να επαναλαμβάνει ένα μυστικό που είχε ήδη σωστό
σπίτι.

**Νέο `deploy/cron-call.sh`** (`700 root:root`): διαβάζει το `CRON_SECRET` από το `.env.prod` και
το δίνει στο curl μέσω του config του **στο stdin** (`curl -K -`), οπότε δεν γίνεται ποτέ όρισμα
γραμμής εντολών. Το κλειδί ήταν αυτό, όχι απλώς η μετακίνηση: μια μεταβλητή μέσα στο crontab θα
κατέληγε πάλι σε `ps` τη στιγμή που θα έτρεχε το curl. Το `CRON_SECRET` εξάγεται με `sed` και
όχι με `.` (source): το `.env.prod` είναι docker-compose σύνταξη, όχι shell, και το sourcing θα
εκτελούσε ό,τι τύχει να περιέχει μια τιμή.

Το crontab πλέον καλεί `cron-call.sh usage-sample|trials-sweep|erasure-purge`, με τα ίδια
ωράρια (04:17/04:27/04:37) και το ίδιο log. Τα σχόλια και η γραμμή του backup έμειναν ανέπαφα:
η αλλαγή έγινε με στοχευμένο `sed` πάνω στις τρεις γραμμές, όχι με ξαναγράψιμο του αρχείου.

**Τι επαληθεύτηκε, και όχι με το μάτι:**

- **Αρνητικός έλεγχος**: λάθος token μέσω του ίδιου μηχανισμού `-K -` παίρνει **401**. Αυτό
  αποδεικνύει ότι ο header όντως ταξιδεύει και ελέγχεται, αλλιώς ένα «200» δεν θα σήμαινε τίποτα.
- **Θετικός έλεγχος**: το wrapper γυρίζει **200** με `{"ok":true,"sampled":1}`.
- **Ορατότητα σε `ps`**: έτρεξα το wrapper επανειλημμένα και πήρα 20 στιγμιότυπα του `ps -eo args`,
  2636 γραμμές διεργασιών, μέσα στις οποίες **58 curl πιάστηκαν εν δράσει**. Το token εμφανίστηκε
  σε **0**. Το μοτίβο αναζήτησης δόθηκε από αρχείο (`grep -f`) και όχι σαν όρισμα, ώστε ο έλεγχος
  να μην ταιριάζει με τον ίδιο του τον εαυτό. Η πρώτη μου εκδοχή αυτού του ελέγχου έβγαλε
  «LEAK FOUND» επειδή η ίδια μου η εντολή περιείχε τη λέξη `Bearer`, δηλαδή ψευδώς θετικό.
- **Περιβάλλον cron**: το έτρεξα με `env -i PATH=/usr/bin:/bin`, όπως ακριβώς θα το καλέσει ο
  cron. Exit 0. Τα `curl`, `sed`, `head` είναι όλα στο `/usr/bin`.
- Μετά την εγκατάσταση: το ζωντανό crontab δεν περιέχει ούτε `Bearer` ούτε 64-ψήφιο hex, η
  υπηρεσία `cron` είναι active, το spool είναι `600`.

**Δικό μου λάθος που διόρθωσα επιτόπου**: το αντίγραφο ασφαλείας του παλιού crontab που κράτησα
πριν την αλλαγή (`/root/crontab.bak.20260804-150256`) γράφτηκε **644 και περιέχει το παλιό token**.
Ο `/root` είναι `700`, οπότε στην πράξη δεν το έφτανε μη-root χρήστης, αλλά το έκανα `600` ούτως ή
άλλως. Σβήσε το μόλις επιβεβαιωθεί το αυριανό 04:17, δεν έχει άλλη αξία πέρα από rollback.

**Παρενέργεια των δοκιμών, να ειπωθεί**: κάλεσα το `usage-sample` περίπου οκτώ φορές. Είναι το
μόνο από τα τρία που άγγιξα: η τεκμηρίωση του route λέει ρητά ότι είναι read-only στο data plane
και γράφει μόνο στο control-plane Usage ledger της τρέχουσας περιόδου. Τα `trials-sweep` και
**`erasure-purge` δεν τα πυροδότησα ποτέ**, για προφανείς λόγους.

**Σύσταση, δεν την εκτελώ**: η τιμή του token δεν άλλαξε και δεν διέρρευσε πουθενά (ούτε git,
ούτε logs, ούτε argv πια). Πέρασε όμως από το transcript αυτού του run όσο τη διάβαζα για να
επιβεβαιώσω ότι ταυτίζεται με το `.env.prod`. Η περιστροφή μυστικών είναι εκτός των ορίων μου,
οπότε το αφήνω ως εισήγηση: ένα `CRON_SECRET` είναι φθηνό να αλλάξει, αρκεί να ενημερωθεί μόνο
το `.env.prod` και να γίνει restart του web.

### Συμπλήρωμα 15:08 UTC: το αντίγραφο του παλιού crontab σβήστηκε

Κατ' εντολή του Αχιλλέα, νωρίτερα από το «μετά το αυριανό 04:17» που είχα προτείνει. Η απώλεια
είναι μηδενική στην πράξη: η παλιά μορφή των τριών γραμμών είναι ήδη καταγεγραμμένη πιο πάνω σε
αυτό το αρχείο, οπότε το rollback ανακατασκευάζεται από το ημερολόγιο χωρίς να χρειάζεται
αντίγραφο που κουβαλά μυστικό.

Χρησιμοποίησα `shred -u` και όχι σκέτο `rm`, αφού το αρχείο περιείχε το παλιό token.

Μετά τη διαγραφή επιβεβαίωσα ότι δεν παρασύρθηκε τίποτα άλλο: το ζωντανό crontab είναι ακέραιο
και με τις τέσσερις γραμμές του, χωρίς `Bearer` ή 64-ψήφιο hex, και η υπηρεσία `cron` παραμένει
active.

Έκανα και τον έλεγχο που κλείνει το θέμα, σάρωση για αδέσποτα αντίγραφα της τιμής σε
`/root`, `/etc`, `/var/spool/cron` και `/opt/pharos`: **το μυστικό υπάρχει πλέον σε ακριβώς ένα
σημείο στον δίσκο, το `deploy/.env.prod`**. Η σύσταση για rotation του `CRON_SECRET` παραμένει
ανοιχτή και δική σου.

### Συμπλήρωμα 15:12 UTC: το CRON_SECRET περιστράφηκε

Κατ' εντολή του Αχιλλέα. Το γράφω ρητά: **η περιστροφή μυστικών είναι στη λίστα των πραγμάτων
που ΔΕΝ κάνω** σε αυτόματη εκτέλεση. Ο κανόνας υπάρχει για να μη μαντεύω χωρίς επίβλεψη, όχι για
να αγνοώ ρητή εντολή του ιδιοκτήτη όταν είναι παρών. Το έκανα επειδή το ζήτησε ο ίδιος, αφού
πρώτα του είχα πει ότι είναι εκτός των ορίων μου.

Πριν αγγίξω οτιδήποτε χαρτογράφησα ποιος καταναλώνει την τιμή: **τέσσερα routes**
(`alerts`, `usage-sample`, `trials-sweep`, `erasure-purge`), όλα server-side μέσω
`lib/cronAuth.ts`, και **ένας μόνο καλών** σε αυτό το μηχάνημα, το crontab μέσω του
`cron-call.sh`, που διαβάζει από το `.env.prod`. Άρα η περιστροφή ήταν δύο κινήσεις, όχι
κυνήγι αντιγράφων: νέα τιμή στο ένα αρχείο, recreate του container για να τη δει.

**Αλλαγή αρχείου, με έλεγχο ακεραιότητας πριν το restart**: νέα τιμή από `openssl rand -hex 32`,
in-place `sed`. Επαλήθευσα ότι το αρχείο έμεινε 64 γραμμές πριν και μετά, ότι υπάρχει **ακριβώς
μία** γραμμή `CRON_SECRET`, ότι η νέα τιμή είναι 64 hex και όντως διαφορετική, και με `diff`
μασκαρεμένων εκδοχών ότι **καμία άλλη γραμμή δεν άλλαξε**. Δικαιώματα `600 root:root` ανέπαφα.

**Restart**: `docker compose up -d web`, χωρίς rebuild, μόνο recreate για να ξαναδιαβαστεί το
env. Η εφαρμογή γύρισε σε **3 δευτερόλεπτα** (HTTP 200), `RestartCount=0`.

**Η επαλήθευση που μετράει, και στις δύο κατευθύνσεις:**

- Πριν το restart, το wrapper με τη ΝΕΑ τιμή απορρίφθηκε, όπως έπρεπε: το container κρατούσε
  ακόμα την παλιά. Αυτό επιβεβαίωσε ότι το env όντως έρχεται από εκεί που νόμιζα.
- Μετά το restart, νέα τιμή → **200**, `{"ok":true,"sampled":1}`.
- **Παλιά τιμή → 401.** Χωρίς αυτό το βήμα, «η νέα δουλεύει» δεν θα σήμαινε ότι η παλιά πέθανε,
  και μια περιστροφή που αφήνει το παλιό κλειδί ζωντανό δεν είναι περιστροφή.
- Το `printenv` του container ταιριάζει byte-προς-byte με το `.env.prod`.
- app 200, apex 200, και τα 4 containers up, mongo healthy.

**Καθαρισμός**: το προσωρινό αντίγραφο του `.env.prod` που κράτησα πριν την αλλαγή (`600`,
περιείχε το παλιό μυστικό) σβήστηκε με `shred -u` μόλις ολοκληρώθηκε η επαλήθευση. Τελική
σάρωση σε `/root`, `/etc`, `/var/spool/cron`, `/opt/pharos`, `/var/log`: **η νέα τιμή υπάρχει σε
ακριβώς ένα σημείο, το `deploy/.env.prod`**, το `pharos-cron.log` δεν περιέχει bearer, και το
αρχείο παραμένει gitignored.

**Εκκρεμεί μία πραγματική επιβεβαίωση**: το `04:17` αύριο είναι η πρώτη φορά που ο ίδιος ο cron
θα καλέσει το `cron-call.sh` με τη νέα τιμή. Το δοκίμασα με το περιβάλλον του cron και χειροκίνητα,
αλλά η προγραμματισμένη διαδρομή δεν έχει τρέξει ακόμα ποτέ. Το `/var/log/pharos-cron.log` θα το
δείξει, και θα το κοιτάξω στο επόμενο πέρασμα.

### Συμπλήρωμα 15:35 UTC: το P1 του `/api/v1` διορθώθηκε ΤΟΠΙΚΑ (χωρίς deploy)

Κατ' εντολή του Αχιλλέα. Κανονικά αυτό το routine αναφέρει και δεν γράφει feature κώδικα, οπότε
το σημειώνω ως ρητή εξαίρεση, όπως και το ότι **δεν έγινε deploy**: ο server μένει στο `27c4979`
και η αλλαγή περιμένει τη συνηθισμένη local-first διαδρομή.

**Η αιτία**: τα routes καλούσαν `connectDB()` και χρησιμοποιούσαν τα global models, οπότε
διάβαζαν και έγραφαν πάντα στην προεπιλεγμένη βάση.

**Η λύση μπήκε στο `withAuth`**, τη μία πόρτα του `/api/v1`, για τον ίδιο λόγο που ζει ήδη εκεί ο
read-only έλεγχος: κανένα route δεν μπορεί να το ξεχάσει. **Δεν** ξαναχρησιμοποίησα το
`withRequestTenant`: απαιτεί cookie λογαριασμού browser και απαντά με `redirect()`/`notFound()`,
οπότε κάθε κλήση API θα γύριζε 307 στη σελίδα εισόδου. Το νέο `apiTenant()` κρατά τον κανόνα του
(**ο host αποφασίζει ποιο workspace**) και αλλάζει μόνο το διαπιστευτήριο: η εξουσιοδότηση είναι
το API token του workspace, ελεγμένο στη **δική του** `users` collection.

**Η σειρά είναι το ζουμί**: ο tenant λύνεται ΠΡΙΝ την αναζήτηση του token, γιατί το ποια βάση
κρατά τους χρήστες είναι ακριβώς αυτό που αποφασίζει ο tenant.

**Γιατί έπρεπε να πέσει μονομιάς**: το gate ΧΩΡΙΣ τη μετατροπή των handlers θα ήταν **χειρότερο
από το τίποτα**. Θα αυθεντικοποιούσε τον χρήστη ενός workspace και μετά θα του σέρβιρε την κοινή
βάση, μετατρέποντας το σημερινό fail-closed 401 σε πραγματική διαρροή. Γι' αυτό τα 101 αρχεία
πήγαν σε ένα commit.

**Επαλήθευση**: `tsc` καθαρό, **371 αρχεία / 5943 tests** πράσινα, production build exit 0. Τα
imports μετονομάστηκαν επίτηδες (`X` → `XModel`) ώστε κάθε call-site που θα ξέφευγε να γίνει
**compile error** αντί για σιωπηλή ανάγνωση από λάθος βάση, και έτσι ακριβώς εντοπίστηκαν οι δύο
module-scope helpers που το codemod δεν έφτανε.

Πρόσθεσα `apiAuth.tenancy.test.ts`, γιατί **όλα τα υπόλοιπα tests τρέχουν με SAAS_MODE off** και
δεν αγγίζουν καθόλου το hosted μονοπάτι: token του workspace A παίρνει 401 στο B και δεν φτάνει
ποτέ στον handler, άγνωστο workspace → 404, ανενεργό → 403. Επιβεβαίωσα ότι **δεν είναι κενά**
ξαναβάζοντας την παλιά σειρά, που τα ρίχνει.

**Τι ΔΕΝ καλύπτει**: τα `jobActions.ts`, `search-actions.ts`, `aiTools.ts` και το
`User.create` στην default σύνδεση (`settings/users.actions.ts`, `setup/actions.ts`) μένουν ως
έχουν. Σημείωση όμως που αλλάζει τη σοβαρότητά τους: με το `/api/v1` πλέον σωστά scoped, ένας
χρήστης που φτιάχτηκε από τις Ρυθμίσεις σε hosted workspace **δεν βρίσκεται** στο lookup, οπότε
το μονοπάτι κλείνει αντί να διαρρέει. Παραμένει bug (ο χρήστης προσγειώνεται σε λάθος βάση και
δεν μπορεί να μπει στο workspace του), αλλά δεν είναι πια δρόμος προς διαρροή μέσω του API.

---

## 2026-08-05 02:45-02:58 UTC

Καθαρό πέρασμα ως προς τη διαθεσιμότητα. Ένα P2 στον έλεγχο ασφάλειας, ένα P2 λειτουργικό που
δεν εξηγείται ακόμα, και μία πραγματική βελτίωση στον δίσκο. Καμία ενέργεια που να αγγίζει
κώδικα, μυστικά, ή deploy.

### Το σημαντικότερο: η παραγωγή δεν είναι πια άδεια

Το προηγούμενο πέρασμα κατέγραψε «μηδέν πελάτες, πριν και μετά». **Αυτό δεν ισχύει πλέον.** Το
αντίγραφο των 21:51 της 4ης Αυγούστου περιέχει **2 accounts, 2 tenants, 2 memberships** και 22
stores, σε δύο βάσεις (`pharos_registry`, `tenant_home`). Δεν επιβεβαίωσα τους ζωντανούς
αριθμούς: η ανάγνωση των διαπιστευτηρίων της Mongo από το `.env.prod` μπλοκαρίστηκε από τον
classifier και **δεν προσπάθησα να το παρακάμψω**, οπότε ο αριθμός προέρχεται από το ίδιο το
backup, όχι από τη ζωντανή βάση.

Αλλάζει δύο πράγματα: τα backup προστατεύουν πλέον πραγματικά δεδομένα (δεν είναι άσκηση), και
τα γνωστά κενά απομόνωσης tenant ανεβαίνουν σε σοβαρότητα, γιατί πλέον υπάρχουν δύο workspaces
για να μπερδευτούν μεταξύ τους.

### Η σάρωση

1. **Προσβασιμότητα**: apex 200, `app.ph-aros.com/account/login` 200. Σύγκρινα **σώματα**, όχι
   τίτλους, επειδή ο κοινός τίτλος είχε ήδη παράξει ένα ψεύτικο «verified» στις 4 Αυγούστου: το
   apex σερβίρει το marketing κείμενο («One light over everything you run»), το app σερβίρει τη
   φόρμα εισόδου με πεδία email και password. Τα δύο upstream είναι σωστά δρομολογημένα.
2. **Πιστοποιητικά**: apex `CN=ph-aros.com` (YE2) και wildcard `CN=*.ph-aros.com` (YE1), **και τα
   δύο λήγουν 2 Νοεμβρίου 2026**, δηλαδή 89 μέρες. Πολύ πάνω από το όριο των 21, άρα η ανανέωση
   λειτουργεί και το Cloudflare token είναι ζωντανό.
3. **Containers**: 4/4 running, **RestartCount 0 σε όλα**, mongo healthy. Κανένα σιωπηλό crash loop.
4. **Έκθεση**: μόνο το caddy δημοσιεύει θύρες (80, 443). Το `pharos-mongo` δείχνει `27017/tcp`
   χωρίς binding προς τα έξω, δηλαδή παραμένει εσωτερικό όπως πρέπει.
5. **Δίσκος και μνήμη**: ο δίσκος ήταν **69%** με 17.47 GB build cache, από τα οποία 16.8 GB
   ανακτήσιμα και 0 ενεργά. Έτρεξα `docker builder prune -f` (επιτρεπόμενο ρητά, αγγίζει μόνο
   cache): **69% → 25%**, από 25G σε 8.9G, με 27G ελεύθερα. Μνήμη: 3.7 GiB συνολικά, 2.8 GiB
   διαθέσιμα. **Σημείωση προς διόρθωση των σημειώσεων**: το SKILL.md λέει 2 GB RAM, το μηχάνημα
   έχει 3.7 GiB. Μηδέν OOM kills σε ολόκληρο το journal.
6. **Backup**: το πιο λεπτό σημείο του περάσματος, γιατί δύο σωστές μετρήσεις κρύβουν μια τρίτη.
   - Τοπικά: νεότερο `mongo-20260804-215114.archive.gz`, ηλικίας ~5 ωρών, μέσα στο όριο των 48.
   - **Offsite επιβεβαιωμένο**: 42 αρχεία στο Storage Box, και το νεότερο mongo archive **και** το
     νεότερο storage tarball υπάρχουν εκεί με το ίδιο timestamp. Το αντίγραφο έφυγε όντως από το
     μηχάνημα.
   - **Όμως το προγραμματισμένο backup δεν έχει τρέξει ΠΟΤΕ ακόμα.** Τα `/var/log/pharos-backup.log`
     και `/var/log/pharos-cron.log` **δεν υπάρχουν**, που σημαίνει ότι καμία γραμμή του crontab δεν
     έχει εκτελεστεί. Δεν είναι αποτυχία: το crontab γράφτηκε χθες μετά τις 03:00, οπότε η πρώτη
     πραγματική εκτέλεση είναι σήμερα 03:00 UTC, δεκατέσσερα λεπτά μετά από αυτόν τον έλεγχο. Όλα
     τα υπάρχοντα αντίγραφα προέρχονται από χειροκίνητες εκτελέσεις. **Στο επόμενο πέρασμα ο
     έλεγχος είναι απλός: υπάρχει το `/var/log/pharos-backup.log` και τι λέει.**
   - **Απόκλιση από την απαντημένη απόφαση**: ο Αχιλλέας ενέκρινε «03:20 UTC, μένει», και το
     `/etc/cron.d/pharos-backup` που είχε φτιαχτεί τότε **δεν υπάρχει πια**. Η δουλειά μετακόμισε
     στο crontab του root στις **03:00**, με σχόλιο που εξηγεί τη λογική (το backup πρώτο, πριν τα
     τρία cron-call στις 04:17/04:27/04:37, ώστε αν κάτι άλλο πάει στραβά να υπάρχει ήδη σημερινό
     αντίγραφο). Είκοσι λεπτά νωρίτερα, με σωστό σκεπτικό σειράς. **Δεν το γύρισα πίσω**: θα ήταν
     αλλαγή χωρίς όφελος. Το καταγράφω γιατί αποκλίνει από ρητή απάντηση και ο Αχιλλέας πρέπει να
     το ξέρει, όχι να το ανακαλύψει.
7. **Ενημερώσεις**: `unattended-upgrades` enabled και active, έτρεξε στις 00:38 σήμερα. Παρόλα
   αυτά **93 πακέτα εκκρεμούν, ανάμεσά τους security** (bind9, apparmor, apport). Το
   `/var/log/apt/history.log` δείχνει ότι το u-u **δεν έχει εγκαταστήσει τίποτα ποτέ**: η μόνη
   εγγραφή είναι η χειροκίνητη εγκατάσταση του Docker στις 4 Αυγούστου 10:10. Ο λόγος είναι πιθανά
   αθώος (το μηχάνημα είναι μιας ημέρας, το `apt-daily-upgrade.timer` χτυπά **06:31 σήμερα** και το
   run των 00:38 ήταν το download-only `apt-daily`). **Δεν έτρεξα `apt upgrade` μόνος μου**: 93
   πακέτα χωρίς επιτήρηση, με kernel μέσα, είναι ακριβώς το είδος της ενέργειας που ρίχνει έναν
   server στις 3 τα ξημερώματα. Κανένα `/var/run/reboot-required`. **P2, επανέλεγχος μετά τις
   06:31.**
8. **Θόρυβος αυθεντικοποίησης**: 1808 αποτυχημένες προσπάθειες SSH σε 24 ώρες, κορυφαίοι scanners
   από 195.178.110.30, 152.32.239.90, 20.219.134.176. Άσφαιρα: `passwordauthentication no`,
   `permitrootlogin without-password`, μόνο κλειδί. **Κάθε επιτυχημένη σύνδεση προέρχεται από την
   2.85.82.29**, δηλαδή τη γραμμή του Αχιλλέα (και πηγή αυτού του routine). Καμία μη αναμενόμενη
   πρόσβαση. Το `fail2ban` δεν είναι εγκατεστημένο, **P3**: με κλειδιά μόνο, δεν κερδίζει ασφάλεια,
   κερδίζει καθαρά logs.
9. **Εκκρεμή για deploy**: **2 commits** πάνω από το `ffe03ca` του server (`dd5b712` webhook retry,
   `33393a3` progress log). Φυσιολογικό local-first, καμία ενέργεια από εμένα.

### P2: επανεκκίνηση χωρίς εξήγηση στις 21:17

Το προηγούμενο boot **τερματίζει απότομα στις 21:15:41** χωρίς καμία ακολουθία shutdown, κανένα
«Stopping», κανένα `systemd-shutdown`, και το επόμενο boot ξεκινά 21:17:01 με τον ίδιο kernel
(6.8.0-117). Μηδέν OOM kills. Αυτό δεν είναι καθαρό reboot, μοιάζει με **σκληρό reset**: είτε
συμβάν του host στη Hetzner (migration, power event), είτε χειροκίνητο power cycle από το console.
Όλα επανήλθαν καθαρά και μόνα τους, τα containers σηκώθηκαν στις 21:17 και το `pharos-web`
ξαναξεκίνησε στις 21:55. Το γράφω επειδή ένα ανεξήγητο hard reset που επαναλαμβάνεται είναι
πρόβλημα υποδομής, και η μόνη στιγμή που θα το πιάσουμε είναι όσο υπάρχει το journal.

### Δοκιμή επαναφοράς (εγκεκριμένη, ολοκληρώθηκε, ΠΕΡΑΣΕ)

Ο Αχιλλέας το ενέκρινε ρητά **μόνο σε scratch stack**, ποτέ πάνω σε production δεδομένα ή volumes.
Το τήρησα κατά γράμμα: container `mongo:7` με **άλλο όνομα**, **χωρίς καμία δημοσιευμένη θύρα**,
με `--tmpfs /data/db` ώστε **να μην δημιουργηθεί κανένα volume** (και άρα να μη χρειαστεί ποτέ να
σβήσω volume), `--rm` για αυτόματο καθάρισμα. Το ζωντανό `pharos-mongo` δεν αγγίχτηκε.

Αποτέλεσμα: **55 documents restored, 0 failed**. Και οι δύο βάσεις φορτώθηκαν σωστά,
`pharos_registry` (accounts 2, tenants 2, memberships 2, stores 22, auditevents 2) και
`tenant_home` με τα collections της. Μετά τον έλεγχο το scratch container αφαιρέθηκε και
επιβεβαίωσα ότι τρέχουν πάλι ακριβώς τα τέσσερα κανονικά containers με RestartCount 0.

**Τι ΔΕΝ αποδεικνύει**: έτρεξα `mongorestore` απευθείας, όχι το `deploy/restore.sh`. Άρα το
**αρχείο** είναι αποδεδειγμένα επαναφέρσιμο, αλλά το ίδιο το script (που κάνει `--drop` και
ξετυλίγει και το tarball των αρχείων) παραμένει αδοκίμαστο. Η διαφορά μετράει: το `--drop`
είναι ακριβώς το μέρος που μπορεί να πάει στραβά, και δεν το δοκιμάζω πάνω σε ζωντανή βάση.

### Έλεγχος ασφάλειας αυτού του περάσματος: ανώνυμη επιφάνεια

Ο κύκλος αλλάζει περιοχή ανά πέρασμα. Αυτή τη φορά: κάθε route που φτάνεται χωρίς session.

- **Ο limiter είναι όντως ενεργός στο τρέχον container**: `API_RATE_LIMIT=30`,
  `API_RATE_WINDOW_MS=60000`, διαβασμένα από το `printenv` του `pharos-web`, όχι από το αρχείο env.
  Η διάκριση έχει σημασία γιατί ο limiter είναι αδρανής όταν η μεταβλητή λείπει.
- **Καλυμμένα**: `auth/login`, `auth/signup`, `auth/mfa`, `account/reset/request`,
  `account/reset/confirm`.
- **Ακάλυπτα αλλά ακίνδυνα**: `account/verify/confirm` και `invites/accept` είναι ανώνυμα και
  καταναλώνουν token χωρίς limiter, όμως τα tokens είναι `randomBytes(32)`, δηλαδή 256 bits. Δεν
  μαντεύονται. **P3**, καθαρά defense in depth. Το `account/verify/request` είναι
  authenticated και στοχεύει το email του ίδιου του χρήστη, άρα δεν έχει επιφάνεια enumeration.
- **`billing/webhook`**: επαληθεύει υπογραφή πριν διαβάσει το σώμα, 400 σε λάθος υπογραφή. Σωστό.
- **P2, το πραγματικό εύρημα: το `POST /api/saas/billing/activate` δεν έχει rate limit, και η
  ενεργοποίηση ΕΙΝΑΙ ρυθμισμένη** (ένας κωδικός στο `SAAS_ACTIVATION_CODES` του τρέχοντος
  container). Οι κωδικοί είναι ανθρώπινοι, τύπου `FRIENDS-2026`, όχι τυχαία bytes, και το endpoint
  είναι η **μοναδική** πόρτα προς πληρωμένο plan. Οποιοσδήποτε μπορεί να κάνει signup και να
  φτιάξει δωρεάν workspace, άρα να γίνει owner, άρα να δοκιμάζει κωδικούς **απεριόριστα**. Ο
  σχεδιασμός κάνει ήδη τα σωστά γύρω από αυτό (ένα ενιαίο μήνυμα απόρριψης ώστε να μη μαθαίνει ο
  επιτιθέμενος ποιο μισό πέτυχε, και audit σε κάθε αποτυχία χωρίς ποτέ τον κωδικό), αλλά **τίποτα
  δεν περιορίζει τον ρυθμό**. Δεν είναι διαρροή δεδομένων: είναι κλιμάκωση δικαιώματος, δωρεάν σε
  πληρωμένο. Ο ίδιος ο limiter υπάρχει ήδη και χρησιμοποιείται σε πέντε routes, οπότε η διόρθωση
  είναι μία γραμμή. **Δεν την έγραψα εγώ**: δεν γράφω feature κώδικα, και το `saas` έδαφος ανήκει
  στο `pharos-saas-core`.
- **Μυστικά**: κανένα μυστικό στο git (μόνο `.example` αρχεία είναι tracked), `.env.prod` σε
  `600 root:root`, κανένα bearer σε log.
- **Εξαρτήσεις**: `npm audit --omit=dev` → **0 vulnerabilities**. Base images φρέσκα, όλα από 4
  Αυγούστου: node 20.20.2, alpine 3.23.4, mongo 7.0.39.
- **Πρόσβαση**: `SAAS_SUPERADMIN_EMAILS` περιέχει **μόνο** το gkekas57@gmail.com.

### Τι άλλαξα, και τι άφησα επίτηδες

**Άλλαξα ένα πράγμα μόνο**: `docker builder prune -f`, δίσκος 69% → 25%.

**Δεν άγγιξα**: κανένα deploy (ο server μένει στο `ffe03ca` με 2 commits να περιμένουν), καμία
επανεκκίνηση container, καμία αλλαγή cron (ούτε καν την επαναφορά του 03:20), κανένα secret,
καμία αλλαγή firewall ή SSH, καμία εγκατάσταση πακέτων, κανένα volume.

### Needs Achilleas

- **P2 rate limit στο `billing/activate`**: χρειάζεται το `pharos-saas-core` να προσθέσει τον
  υπάρχοντα limiter στο route. Είναι μικρό και μηχανικό.
- **P2 το hard reset των 21:17**: αν δεν το έκανες εσύ, αξίζει ένα ticket στη Hetzner όσο υπάρχουν
  ακόμα τα logs.
- **P2 τα 93 εκκρεμή πακέτα**: το επόμενο πέρασμα θα δει αν το `apt-daily-upgrade` των 06:31 τα
  εγκατέστησε μόνο του. Αν όχι, η αναβάθμιση θέλει επιτηρούμενο παράθυρο, όχι routine στις 3 πμ.

### Συμπλήρωμα 05:50 UTC: το backup γύρισε στις 03:20, και η αλυσίδα cron δούλεψε πρώτη φορά

Δύο πράγματα, μετά από ρητή απάντηση του Αχιλλέα.

**1. Το hard reset των 21:17 εξηγήθηκε: το έκανε ο ίδιος.** Κλείνει ως ερώτημα, δεν χρειάζεται
ticket στη Hetzner. Το κρατάω γραμμένο ώστε αν ξαναδώ απότομο κόψιμο του journal να ξέρω ότι το
μοτίβο «χωρίς shutdown sequence» δεν σημαίνει από μόνο του βλάβη υποδομής.

**2. Η ώρα του backup επανήλθε στις 03:20 UTC**, όπως είχε εγκριθεί. Άλλαξα μόνο το πεδίο λεπτών
στο crontab του root (`0 3` → `20 3`), κρατώντας αντίγραφο του προηγούμενου crontab σε
`/root/crontab.bak.<timestamp>` πριν το αγγίξω. Οι τρεις κλήσεις cron-call (04:17/04:27/04:37) δεν
πειράχτηκαν, οπότε το σκεπτικό του σχολίου παραμένει σωστό: το backup εξακολουθεί να τρέχει πρώτο.
Η υπηρεσία `cron` επιβεβαιώθηκε active μετά την αλλαγή.

**Και μια εκκρεμότητα που έκλεισε μόνη της στο μεταξύ.** Στις 02:58 έγραψα ότι το προγραμματισμένο
backup δεν είχε τρέξει ΠΟΤΕ και ότι ο έλεγχος του επόμενου περάσματος είναι απλός. Έγινε τώρα:

- **03:00 backup**: dump όλων των collections, `[2/4] verifying the archive is readable`,
  `[3/4] archiving /storage`, `[4/4] copying offsite` προς το Storage Box, και κατέληξε σε
  `mongo-20260805-030001.archive.gz (8.0K)` + `storage-20260805-030001.tar.gz (4.0K)`. Δηλαδή η
  προγραμματισμένη διαδρομή, όχι η χειροκίνητη, έφτασε **μέχρι και το offsite**.
- **04:17 usage-sample** → `{"ok":true,"sampled":1,...}`, **04:27 trials-sweep** → `{"ok":true,...}`,
  **04:37 erasure-purge** → `{"ok":true,"dryRun":true,"due":0}`. Αυτό επιβεβαιώνει και την
  εκκρεμότητα που είχε αφήσει το πέρασμα της 4ης Αυγούστου μετά την περιστροφή του cron token: το
  `cron-call.sh` καλείται πλέον από τον ίδιο τον cron, με το νέο κλειδί, και περνά.

Άρα και οι τέσσερις προγραμματισμένες δουλειές είναι πλέον **αποδεδειγμένες σε πραγματικές
συνθήκες**, όχι μόνο σε χειροκίνητη δοκιμή. Από αύριο η πρώτη τους ώρα είναι 03:20.

### Συμπλήρωμα 11:05 UTC: το P2 του `billing/activate` διορθώθηκε ΤΟΠΙΚΑ (χωρίς deploy)

Κατ' εντολή του Αχιλλέα («βάλε το rate limit στο activate»). Το routine κανονικά **αναφέρει και
δεν γράφει feature κώδικα**, οπότε το σημειώνω ρητά ως εξαίρεση, όπως και το ότι **δεν έγινε
deploy**: ο server μένει στο `ffe03ca` και η αλλαγή ακολουθεί τη συνηθισμένη local-first διαδρομή.

**Δύο κλειδιά, όχι ένα**, γιατί το καθένα μόνο του έχει τρύπα:

- **Ανά IP, πρώτο απ' όλα**, πριν το `readBody` και πριν οποιοδήποτε πέρασμα από τη βάση. Ίδια
  τοποθέτηση με το login route, και ο λόγος είναι ο ίδιος: μια πλημμύρα δεν πρέπει να **αγοράζει
  δουλειά** από τον server. Χωρίς αυτό, κάθε μαντεψιά θα πλήρωνε ένα `resolveBillingSession`,
  δηλαδή session lookup συν δύο ερωτήματα στη Mongo.
- **Ανά λογαριασμό**, αφού λυθεί το session, γιατί το IP αλλάζει φτηνά ενώ ο λογαριασμός **δεν
  είναι προαιρετικός** εδώ: για να φτάσεις καν στον έλεγχο του κωδικού πρέπει να είσαι owner ή
  admin σε workspace.

Χρησιμοποιεί τον **υπάρχοντα** limiter (`rateLimit` από το `apiAuth`), όχι καινούριο μηχανισμό,
οπότε το self-hosted Pharos (που ούτως ή άλλως γυρίζει 404 στο SaaS gate) δεν επηρεάζεται εκτός αν
βάλει μόνο του `API_RATE_LIMIT`. Η παραγωγή τρέχει ήδη με `API_RATE_LIMIT=30`, επιβεβαιωμένο μέσα
στο container σήμερα το πρωί.

**Επαλήθευση**: `tsc` καθαρό, **6117 tests πράσινα**. Έξι νέα tests καλύπτουν και τα δύο κλειδιά,
συν τις δύο ιδιότητες που **δεν** πρέπει να αλλάξουν (ο limiter μένει αδρανής όταν λείπει η
μεταβλητή, και ο ένας λογαριασμός δεν τρώει το budget του άλλου). **Επιβεβαίωσα ότι δεν είναι
κενά**: αφαιρώντας τον limiter πέφτουν ακριβώς τα τρία tests συμπεριφοράς και μένουν πράσινα τα
άλλα τρία, που είναι το σωστό μοτίβο και όχι «όλα κόκκινα».

**Τι ΔΕΝ κάλυψα**: το **όριο** παραμένει το κοινό `API_RATE_LIMIT` (30 ανά λεπτό). Για έναν κωδικό
τύπου `FRIENDS-2026` αυτό είναι σαφώς καλύτερο από το άπειρο, αλλά δεν είναι αυστηρό. Ένα δικό του,
πολύ μικρότερο όριο θα ήθελε **νέα μεταβλητή περιβάλλοντος**, δηλαδή απόφαση σχεδιασμού και μια
τιμή που πρέπει να μπει στην παραγωγή για να μετρήσει, οπότε δεν την επινόησα μόνος μου. Αν το
θέλεις σφιχτότερο, είναι μικρή προσθήκη.

**Δύο ξένα ευρήματα, δεν τα άγγιξα**: (α) το `tsc` βγάζει σφάλματα στο
`app/api/saas/admin/ai-key/route.ts`, αρχείο **untracked** που γράφει αυτή τη στιγμή άλλο routine·
(β) τρία tests πέφτουν σε `aiConfig.tenant.test.ts` και `backupModels.test.ts`, από τροποποιημένο
`aiConfig.ts` και ένα νέο μοντέλο `PlatformConfig` που δεν έχει καταχωρηθεί ακόμα στο backup
registry. Και τα δύο είναι δουλειά σε εξέλιξη άλλου routine, κανένα δεν ακουμπά το `billing/`.

### Συμπλήρωμα 11:25 UTC: το activate απέκτησε δικό του, αυστηρό όριο

Συνέχεια του προηγούμενου, κατ' εντολή του Αχιλλέα («κάνε το όριο πιο αυστηρό, δικιά του
μεταβλητή»). Ίδια εξαίρεση ισχύει: feature κώδικας από αυτό το routine, **χωρίς deploy**.

Το κοινό `API_RATE_LIMIT` ήταν καλύτερο από το τίποτα αλλά **λάθος στο είδος**: τα 30 ανά λεπτό
είναι μέγεθος φτιαγμένο για client που χτυπάει REST API, και αφήνει περίπου **43.000 μαντεψιές την
ημέρα** πάνω σε κωδικό τύπου `FRIENDS-2026`. Πλέον το endpoint λύνει δική του ρύθμιση:
**`SAAS_ACTIVATE_RATE_LIMIT` (default 5) και `SAAS_ACTIVATE_RATE_WINDOW_MS` (default μία ώρα)**,
ανά IP **και** ανά λογαριασμό.

Δύο σκόπιμες διαφορές από τον γενικό limiter, που είναι και το νόημα:

- **Ενεργό από default**, όχι opt-in. Ο γενικός είναι opt-in επειδή ένα self-hosted Pharos ενός
  χρήστη δεν το χρειάζεται. Αυτό το σκεπτικό **δεν μεταφέρεται**: το activate υπάρχει μόνο σε SaaS
  mode (αλλιώς το route γυρίζει 404 στο gate), οπότε το default-on δεν κοστίζει τίποτα στο
  self-host και σημαίνει ότι μια εγκατάσταση **δεν μπορεί να ξεχάσει** τον φρουρό στο ίδιο της το
  ταμείο.
- **Ένα λάθος πληκτρολόγησης πέφτει στο default, ποτέ στο off.** Μόνο ρητό `0` το απενεργοποιεί. Ο
  γενικός resolver κάνει το αντίθετο (ό,τι δεν parse-άρεται το θεωρεί «κλειστό»), που εκεί είναι
  σωστό και εδώ θα ήταν ακριβώς ο τρόπος να ξεκλειδώσει σιωπηλά το paywall.

Το `rateLimit()` δέχεται πλέον προαιρετικό config αντί να διαβάζει πάντα το global, οπότε δεν
χρειάστηκε δεύτερος limiter ούτε δεύτερο store.

**Τεκμηρίωση**: `.env.example`, `deploy/.env.prod.example`, `docs/api.md`. Ένα κουμπί που δεν το
βρίσκει κανείς είναι κουμπί που δεν το ρυθμίζει κανείς, και το default-on κάνει τη σιωπή επικίνδυνη
μόνο προς τη σωστή κατεύθυνση.

**Επαλήθευση**: tsc καθαρό στα αρχεία που άγγιξα, **6186 tests πράσινα**. 9 route tests + 5 config
tests, **επιβεβαιωμένα μη κενά**: γυρίζοντας το route πίσω στο κοινό budget πέφτουν 6 από τα 9,
ανάμεσά τους ακριβώς τα δύο που υπάρχουν για το σφίξιμο (ότι το paywall μένει φρουρούμενο με το
`API_RATE_LIMIT` άδειο, και ότι το παράθυρο είναι ώρα και όχι λεπτό).

**Δεν χρειάζεται καμία αλλαγή στην παραγωγή για να ισχύσει**: επειδή είναι default-on, μόλις γίνει
deploy το όριο πιάνει χωρίς να μπει τίποτα στο `.env.prod`. Οι δύο μεταβλητές μπαίνουν μόνο αν το
θέλεις **ακόμα** πιο σφιχτό.

**Ξένα σπασίματα, δεν τα άγγιξα** (η υπόλοιπη flota δούλευε ταυτόχρονα): 2 tests στο
`aiConfig.tenant.test.ts` από τροποποιημένο `aiConfig.ts`, και tsc errors σε `admin/ai-key`,
`admin/tenants/[slug]`, `workspace/` — όλα uncommitted WIP άλλων routines. Το `backupModels.test.ts`
που έπεφτε στο προηγούμενο συμπλήρωμα **πέρασε** στο μεταξύ, δηλαδή το διόρθωσε ο κάτοχός του.

---

## 2026-08-05 08:20-08:30 UTC — έλεγχος μετά από deploy (έγινε από άλλο routine)

Ο Αχιλλέας ειδοποίησε ότι έγινε deploy από άλλη routine. Ο έλεγχος μετά το deploy είναι ακριβώς
δουλειά αυτού του routine, οπότε σάρωσα ξανά. **Η παραγωγή είναι υγιής.** Ένα εύρημα ουσίας, που
δεν είναι βλάβη αλλά αλλάζει το τι νομίζουμε ότι τρέχει.

### Το deploy πέτυχε

Site πάνω (apex 200 με το marketing κείμενο, `app.` 200 με τη φόρμα εισόδου, σύγκριση σε **σώματα**
όπως πάντα), **4/4 containers running με RestartCount 0**, μόνο το caddy δημοσιεύει θύρες, η Mongo
παραμένει εσωτερική. Νέο image `pharos-web:prod` χτισμένο **08:19:13**, το container σηκώθηκε
08:19:25. Τα πιστοποιητικά δεν επηρεάζονται (λήγουν 2 Νοεμβρίου).

### Το εύρημα: το σφίξιμο του activate ΔΕΝ μπήκε στο deploy

Έλεγξα το ίδιο το **image**, όχι το git του server, και η διαφορά μετράει:

- Το `saas-activate-ip` **υπάρχει** στο `route.js` του image → το **πρώτο** fix (`c99c5a2`) είναι
  **ζωντανό**. Δηλαδή το activate ΕΙΝΑΙ πλέον rate limited στην παραγωγή, ανά IP και ανά
  λογαριασμό, με το κοινό `API_RATE_LIMIT=30` ανά λεπτό.
- Το παράθυρο της μιας ώρας (`3600000`) και το `SAAS_ACTIVATE_RATE_LIMIT` **δεν υπάρχουν πουθενά**
  στο image → το **δεύτερο** commit (`e51a17a`, τα 5 ανά ώρα με δική του μεταβλητή) **δεν έχει γίνει
  deploy**.

Επιβεβαιωμένο με ancestry, όχι με εικασία: το `c99c5a2` είναι πρόγονος του deployed `a7e7904`, το
`e51a17a` δεν είναι. Ο λόγος είναι απλά ο χρόνος: το build ξεκίνησε 08:19 και το `e51a17a` προσγειώθηκε
στο main ένα λεπτό αργότερα. Κανένα λάθος του deploy.

**Πρακτικά τι ισχύει σήμερα στην παραγωγή**: το μάντεμα κωδικού ενεργοποίησης πήγε από **απεριόριστο**
σε **30 ανά λεπτό**, ανά IP και ανά λογαριασμό. Σαφής βελτίωση, όχι το τελικό. Τα 5 ανά ώρα έρχονται
με το επόμενο deploy, **χωρίς να χρειαστεί τίποτα στο `.env.prod`** αφού είναι default-on.
Εκκρεμούν **7 commits** πάνω από το deployed.

### Μάθημα για τα επόμενα περάσματα: μην εμπιστεύεσαι το git HEAD του server κατά τη διάρκεια deploy

Στις 08:23 το `git log` του server έδειχνε `a02282b`. Στις 08:24:40 το HEAD μετακινήθηκε σε
**detached `a7e7904`**, δηλαδή στο commit από το οποίο όντως χτίστηκε το image. Το reflog δείχνει τη
σειρά: fast-forward στο `a02282b`, μετά checkout πίσω στο `a7e7904`.

Άρα το checkout και το image συμφωνούν **αφού** ησυχάσει το deploy, αλλά **ενδιάμεσα** το HEAD
μπορεί να δείχνει commit που δεν χτίστηκε ποτέ. Αν είχα διαβάσει μόνο το `git log`, θα έγραφα ότι το
σφίξιμο είναι ζωντανό ενώ δεν είναι. **Η αξιόπιστη πηγή είναι το περιεχόμενο του image**, και από
δω και πέρα έτσι θα το ελέγχω: grep για ένα string που υπάρχει μόνο στην αλλαγή που με ενδιαφέρει.

### P3: 60 σφάλματα «Failed to find Server Action»

Όλα **μία** κατηγορία, μηδέν TypeError, μηδέν MongoServerError, μηδέν ECONNREFUSED. Είναι το γνωστό
stale-bundle μετά από rebuild (ανοιχτή καρτέλα κρατά το παλιό bundle και ζητά action id που δεν
υπάρχει πια), τεκμηριωμένο ήδη στο CLAUDE.md, και η θεραπεία είναι ένα hard refresh.

Αξίζει όμως μια παρατήρηση: **το ίδιο action id επαναλαμβάνεται δεκάδες φορές**. Ο `JobsProvider`
κάνει poll κάθε 3 δευτερόλεπτα, και το poll είναι Server Action, οπότε **μία** ξεχασμένη ανοιχτή
καρτέλα δεν παράγει ένα σφάλμα αλλά ένα συνεχές ρεύμα σφαλμάτων μέχρι να κλείσει ή να ανανεωθεί.
Αβλαβές για τους υπόλοιπους χρήστες, αλλά κάνει τα logs θορυβώδη μετά από κάθε deploy.

### Δίσκος

Το build ξαναγέμισε το cache (27% → 42%). `docker builder prune -f`, **42% → 27%**, 5.6 GB πίσω.
Είναι πλέον μοτίβο: κάθε deploy κοστίζει περίπου 15 μονάδες δίσκου και τις παίρνει πίσω το prune.

### Τι δεν άγγιξα

Κανένα deploy, καμία επανεκκίνηση, καμία αλλαγή cron ή env, κανένα secret, κανένα volume. Μόνο το
prune του build cache.

## 2026-08-05 08:30-08:50 UTC — σάρωση σφαλμάτων + οργανωμένη λίστα εκκρεμοτήτων

Ο Αχιλλέας ζήτησε πλήρη έλεγχο για σφάλματα, οργάνωση ώστε να λυθούν, και ενημέρωση του site.
Σάρωσα **όλα** τα logs (και τα τέσσερα containers, ολόκληρο το ιστορικό τους, όχι μόνο μετά το
deploy), το journal του συστήματος, και τους ισχυρισμούς της δημόσιας σελίδας `/status`.

**Καμία βλάβη.** Μηδέν `TypeError`, μηδέν `ReferenceError`, μηδέν `MongoServerError`, μηδέν
`unhandledRejection`, μηδέν σφάλμα στο journal πέρα από ssh. Η Mongo έχει 4 warnings και μηδέν
errors σε όλο της το ιστορικό. Ό,τι βρήκα είναι θόρυβος ή αναμενόμενη παρενέργεια, με **μία**
εξαίρεση που θέλει απόφαση.

### Το ένα που θέλει εσένα: εκκρεμεί επανεκκίνηση για kernel

Το πρωί είχα γράψει «93 εκκρεμή πακέτα, επανέλεγχος μετά τις 06:31». Έγινε: το
`unattended-upgrades` **ξύπνησε στις 06:31 και τα εγκατέστησε** («All upgrades installed»),
78 πακέτα, ανάμεσά τους **openssl, systemd, libc6, openssh-server και kernel**
(`linux-image-virtual`). Έμειναν 15, προφανώς phased. Άρα ο μηχανισμός δουλεύει και το πρωινό P2
έκλεισε μόνο του, όπως περίμενα.

**Άφησε όμως πίσω του `/var/run/reboot-required`.** Οι διορθώσεις είναι εγκατεστημένες αλλά ο
πυρήνας που **τρέχει** είναι ακόμα ο παλιός. Δηλαδή τα patch υπάρχουν στον δίσκο και δεν ισχύουν
στη μνήμη. **Δεν κάνω reboot μόνος μου**: ρίχνει το site, και είναι ακριβώς το είδος της ενέργειας
που δεν παίρνει ένα routine χωρίς επιτήρηση.

Μπόνους: αυτό εξηγεί πλήρως τα systemd warnings «ssh.service: Found left-over process» στις
06:33 και 06:36. Είναι το `openssh-server` που αναβαθμίστηκε και επανεκκίνησε. Θόρυβος, όχι βλάβη.

### Οργανωμένη λίστα, κατά σοβαρότητα και με ιδιοκτήτη

| # | Σοβ. | Τι | Ποιος το λύνει |
|---|------|----|----------------|
| 1 | **P2** | `/var/run/reboot-required`: kernel + libc + openssl εγκατεστημένα, ο παλιός πυρήνας τρέχει ακόμα | **Αχιλλέας** (reboot, σύντομο downtime) |
| 2 | **P2** | Το deploy έχει παράθυρο διακοπής: ο caddy γύρισε **502** σε 15 χρονικές στιγμές, όλες πάνω σε restart container | routine του deploy |
| 3 | **P2** | Το σφίξιμο του activate (`e51a17a`, 5/ώρα) είναι στο main αλλά **όχι στο image**. Ζωντανό σήμερα: 30/λεπτό | επόμενο deploy, καμία ενέργεια |
| 4 | P3 | 60× «Failed to find Server Action» μετά το deploy, όλα μία κατηγορία | κανείς, hard refresh |
| 5 | P3 | Προειδοποίηση `@napi-rs/canvas` από το `pdfjs-dist` | κανείς, βλ. παρακάτω |
| 6 | P3 | Δεν υπάρχει `fail2ban` με 1800 αποτυχημένες SSH/24ω | προαιρετικό, μόνο για καθαρά logs |

### Λεπτομέρειες στα δύο που αξίζουν εξήγηση

**#2, τα 502.** Ο caddy μένει όρθιος ενώ το `pharos-web` επανεκκινεί, οπότε ό,τι φτάσει εκείνα τα
δευτερόλεπτα παίρνει `502 connection refused`. Τα URI ήταν `/account/login` (11), 
`/api/cron/saas/trials-sweep` (9), `/` (2), `/admin`, `/account/workspace`. **Η τίμια ανάγνωση όμως
είναι ότι κανένας πελάτης δεν χτυπήθηκε**: σε **κάθε** εγγραφή το `client_ip` είναι
`128.140.126.136`, δηλαδή ο ίδιος ο server που καλεί τον εαυτό του (health checks και cron calls
από routines). Το πρόβλημα δεν είναι τι έγινε, είναι ότι **με πελάτες πάνω θα γινόταν σε αυτούς**.
Θέλει cutover που περιμένει να είναι έτοιμο το νέο container πριν φύγει το παλιό.

**#5, το canvas.** Το μήνυμα λέει «Cannot polyfill DOMMatrix / ImageData / Path2D, rendering may be
broken», που διαβάζεται τρομακτικά. **Δεν είναι**: το `pdfjs-dist` χρησιμοποιείται σε **ένα** σημείο,
το `lib/pdf.ts`, και **μόνο** για `getTextContent()`. Δεν κάνει render ποτέ. Τα thumbnails βγαίνουν
από το `pdftoppm` του poppler, όχι από canvas. Το ίδιο το αρχείο το δηλώνει στην κεφαλίδα του
(«pure JS, no native deps»), δηλαδή είναι σχεδιαστική επιλογή και όχι έλλειψη. Τα 14 tests του
`pdf.test.ts` περνούν χωρίς canvas. Το γράφω αναλυτικά ακριβώς για να **μην** το κυνηγήσει κανείς
ξανά από την αρχή.

### Το site ενημερώθηκε, και ο κανόνας του έκανε δουλειά

Η `/status` έχει γραμμένο μέσα της ότι μια εγγραφή γίνεται `shipped` **μόνο όταν είναι ζωντανή**,
όχι όταν γίνει merge. Αυτός ο κανόνας ήταν ακριβώς το σημερινό ζήτημα, οπότε πρόσθεσα **μόνο** το
rate limit που όντως μπήκε στο deploy, και **δεν** ανέφερα το σφίξιμο των 5/ώρα που δεν είναι live.

Έλεγξα και τα **υπόλοιπα** της σελίδας απέναντι στην παραγωγή αντί να τα θεωρήσω δεδομένα: το
`MAIL_FROM` όντως είναι ακόμα προσωπικό Gmail και τα Stripe keys είναι όντως **μηδέν**, άρα και οι
δύο «περιορισμοί» που δηλώνει παραμένουν αληθινοί. Καμία διόρθωση δεν χρειάστηκε.

Επαλήθευση: `tsc` καθαρό στο landing, `next build` πέρασε, και η φράση βρέθηκε μέσα στο
**prerendered** `/status`, όχι μόνο στην πηγή.

**Σημείωση εδάφους**: το `apps/landing/app/status/**` ανήκει στο `pharos-saas-core` με τη ρητή
απόφαση της 4ης Αυγούστου. Το άγγιξα κατόπιν ρητής εντολής, με **μία** προσθήκη δεδομένων ώστε να
μη συγκρουστεί με τη δουλειά του.

---

## 2026-08-05 14:46-15:05 UTC: πλήρες σάρωμα + έλεγχος ανώνυμης επιφάνειας

Το site είναι όρθιο και δεν υπάρχει P0 ούτε P1. Ένα deploy είχε προσγειωθεί **τέσσερα λεπτά πριν**
αρχίσει ο έλεγχος (`pharos-web` StartedAt 14:42:59), οπότε ό,τι ακολουθεί αφορά τον κώδικα που
τρέχει αυτή τη στιγμή, τον `b12c4b7`.

### Τι ελέγχθηκε και τι βρέθηκε

| Έλεγχος | Αποτέλεσμα |
|---|---|
| Προσβασιμότητα | apex 200, `app./account/login` 200, workspace subdomain απαντά (307) |
| Δρομολόγηση apex vs app | **σωστή, επιβεβαιωμένη δομικά** (βλ. παρακάτω) |
| Πιστοποιητικά | apex + wildcard λήγουν **2 Νοε 2026**, 89 μέρες, η ανανέωση δουλεύει |
| Containers | και τα 4 up, **RestartCount 0** σε όλα, mongo healthy |
| Έκθεση θυρών | **μόνο ο caddy** δημοσιεύει (80/443). Η mongo στο `27017/tcp` χωρίς host binding |
| Δίσκος | 61% → **28%** μετά από prune (βλ. «Τι άλλαξα») |
| Μνήμη | 1.2Gi σε χρήση από 3.7Gi, swap 234Mi. Άνετα |
| Backup τοπικό | νεότερο 14:38, **εννιά λεπτά** πριν τον έλεγχο |
| Backup offsite | **επιβεβαιωμένο στο Storage Box**, ίδιο ακριβώς αρχείο `storage-20260805-143828.tar.gz` |
| Cron | 5 γραμμές, μαζί και το `suspended-sweep` (32 4) που ζητούσε το `pharos-saas-core` |
| `.env.prod` | `chmod 600`, root |
| Patching | `unattended-upgrades` enabled + active, 15 εκκρεμή (phased) |
| SSH | `PasswordAuthentication no`, `PermitRootLogin prohibit-password` |
| Εκκρεμότητα deploy | **μηδέν**, ο server είναι στο `b12c4b7` που είναι και το HEAD του main |

**Η δρομολόγηση επιβεβαιώθηκε δομικά, όχι από τον τίτλο.** Ο τίτλος έχει ξαναβγάλει ψεύτικο
«verified» στις 4/8, οπότε σύγκρινα σώματα: landing 365.936 bytes με **μηδέν** `type="password"`
και 12 αναφορές σε pricing, app 84.146 bytes με **ένα** πεδίο password. Δύο διαφορετικά upstream,
όπως πρέπει.

### Τι άλλαξα

**Ένα πράγμα μόνο: `docker builder prune -f`.** Το build cache είχε φτάσει **14,46 GB** με 12,96 GB
ανακτήσιμα, σε δίσκο 38 GB. Ελευθερώθηκαν **12,96 GB** και ο δίσκος πήγε από **61% σε 28%**. Μόνο
cache, κανένα image σε χρήση, κανένα volume, κανένα backup. Ο ρυθμός των deploy σήμερα (οκτώ backup
από τις 08:59 μέχρι τις 14:38, το καθένα πριν από deploy) γεμίζει το cache γρήγορα, οπότε αυτό θα
χρειάζεται τακτικά.

### Ο έλεγχος ασφαλείας αυτού του περάσματος: ανώνυμη επιφάνεια

Διάλεξα αυτή την περιοχή επειδή **σήμερα άλλαξε κώδικας του app** και οι προηγούμενοι έλεγχοι
(tenant isolation 4/8, μυστικά και εξαρτήσεις 5/8 02:45) είναι πιο πρόσφατοι από την τελευταία
πλήρη σάρωση των δημόσιων routes.

**Δύο ισχυρισμοί που πήρα και απέρριψα**, γιατί δεν επαληθεύτηκαν στην παραγωγή:

1. Αναφέρθηκε ότι το `API_RATE_LIMIT` δεν περνάει στο container, άρα κάθε limiter είναι no-op.
   **Λάθος.** Διαβάστηκε το `docker-compose.prod.yml` της ρίζας, ενώ ζωντανό είναι το
   `/opt/pharos/deploy/docker-compose.prod.yml` (επιβεβαιώθηκε από το label του container).
   Μέτρησα το ίδιο το container: `API_RATE_LIMIT=30`. **Οι limiters είναι ενεργοί.**
2. Αναφέρθηκε ότι το signup διαρρέει ποιο email υπάρχει (409 έναντι 201). **Στην παραγωγή είναι
   κλειστό**: το `SAAS_SIGNUP_CODES` είναι ρυθμισμένο και η πύλη της beta ελέγχεται **πριν** από
   κάθε έλεγχο email. Το επιβεβαίωσα ζωντανά, με email που δεν θα δημιουργούσε λογαριασμό:
   `403 «Pharos is in private beta»`, καμία εγγραφή.

**Ζωντανοί έλεγχοι που έτρεξαν** (όλοι read-only, κανένας δεν δημιούργησε δεδομένα):

- login με **άγνωστο** email και login με το **γνωστό** email του ιδιοκτήτη και λάθος κωδικό:
  **ίδια απάντηση χαρακτήρα προς χαρακτήρα**, `401 {"error":"Invalid credentials"}`. Μηδέν
  enumeration.
- `POST /api/cron/saas/trials-sweep` χωρίς μυστικό: **401**. Το `CRON_SECRET` είναι set (64 chars),
  ο έλεγχος είναι constant-time και fail-closed.
- `/api/calendar.ics` με άκυρο token: **401**.

**Τι όντως λείπει, με τη σωστή σοβαρότητα.** Τρία ανώνυμα routes δεν καλούν καθόλου limiter:
`saas/invites/accept`, `saas/account/verify/confirm`, `calendar.ics`. **Δεν είναι κίνδυνος
μαντέματος**: τα tokens είναι `randomBytes(32)` (invite, verify) και `randomBytes(24)` (calendar),
δηλαδή 256 και 192 bits. Κανείς δεν τα βρίσκει με brute force σε καμία κλίμακα χρόνου. Το πραγματικό
έκθεμα είναι ότι ο καθένας μπορεί να προκαλεί **ανώνυμα ερωτήματα στη βάση** χωρίς όριο, δηλαδή
φόρτος, όχι διαρροή. Το γράφω P3 και όχι P2 ακριβώς για αυτό.

### Ευρήματα, κατά σειρά

| # | Σοβ. | Τι | Ποιος |
|---|------|----|-------|
| 1 | **P2** | Εκκρεμεί ακόμα η επανεκκίνηση: τρέχει ο **6.8.0-117**, εγκατεστημένος ο **6.8.0-136**, μαζί με `libc6` και `openssl` | **Αχιλλέας** |
| 2 | **P2** | Το παράθυρο του deploy **χτύπησε πραγματικές συνεδρίες**, όχι μόνο δικά μας curl (βλ. κάτω) | routine του deploy |
| 3 | **P2** | Ο caddy **δεν κρατά access log** καθόλου | βλ. κάτω, θέλει απόφαση |
| 4 | P3 | `GET /api/mcp` χωρίς καμία αυθεντικοποίηση | ακίνδυνο σήμερα, βλ. κάτω |
| 5 | P3 | 3 ανώνυμα routes χωρίς rate limit (φόρτος, όχι διαρροή) | `pharos-saas-core` |
| 6 | P3 | Χωρίς `fail2ban`, χωρίς `ufw`. 1518 «Invalid user» σε 24ω | προαιρετικό |
| 7 | P3 | Το enumeration του signup είναι **λανθάνον**: ανοίγει τη μέρα που κλείσει η private beta | να θυμηθεί |

**#2, και γιατί αλλάζει σε σχέση με το πρωί.** Το πρωί έγραψα «κανένας πελάτης δεν χτυπήθηκε, σε
κάθε εγγραφή το client_ip είναι ο ίδιος ο server». Στις 24 ώρες που κοίταξα τώρα υπάρχουν **43
απαντήσεις 502**, από τις οποίες **39 από τον ίδιο τον server** αλλά **4 από πραγματικά προγράμματα
περιήγησης**: τρεις από το `2.85.82.29` (`/admin` 08:06, `/settings` 08:36, `/account/login` 13:02)
που είναι **η δική σου σύνδεση**, και μία από το `140.248.36.126` στις 21:25 της 4ης, στο
`home.ph-aros.com/settings`, δηλαδή σε **workspace subdomain**. Εκείνη η τελευταία πέφτει πάνω στο
reboot των 21:17 και όχι σε deploy, οπότε εξηγείται. Η ουσία παραμένει: **η διαπίστωση «μόνο τα δικά
μας curl» δεν ισχύει πια**, το παράθυρο σερβίρει σφάλμα σε ό,τι τύχει να είναι ανοιχτό εκείνη τη
στιγμή. Θέλει cutover που περιμένει το νέο container πριν φύγει το παλιό.

**#3, το τυφλό σημείο.** Το `Caddyfile` **δεν έχει καθόλου `log` directive**: σε 24 ώρες ο caddy
έγραψε 146 γραμμές και **μηδέν** εγγραφές `handled request`. Ό,τι είδα παραπάνω για τα 502 προέρχεται
από γραμμές **σφάλματος**, όχι από access log. Πρακτικά αυτό σημαίνει ότι το βήμα «ψάξε για ριπές
401/429» **δεν μπορεί να εκτελεστεί**: το πρώτο μου grep γύρισε μηδέν, και το μηδέν εκεί δεν σήμαινε
«καθαρό», σήμαινε «δεν καταγράφεται τίποτα». Είναι ακριβώς το είδος του κενού που κάνει έναν έλεγχο
να μοιάζει πράσινος ενώ δεν κοίταξε. Δεν το άλλαξα μόνος μου γιατί το access logging γράφει
**διευθύνσεις IP πελατών**, δηλαδή προσωπικά δεδομένα, και θέλει απόφαση για μορφή, χρόνο διατήρησης
και συμβατότητα με όσα υπόσχεται η πολιτική απορρήτου.

**#4, το mcp.** Το `GET /api/mcp` απαντά 200 χωρίς κανέναν έλεγχο, αλλά επιστρέφει **μόνο** ένα
ενημερωτικό μπλοκ (`name`, `transport`, και οδηγία να στείλεις Bearer). Μηδέν δεδομένα χρήστη. Το
`POST` θέλει κανονικά Bearer token. Το γράφω για πληρότητα, δεν χρειάζεται ενέργεια.

**#6, ο θόρυβος του SSH.** 3077 γραμμές αποτυχίας σε 24 ώρες, αλλά η ανάλυση δείχνει **1518 «Invalid
user»** από σαρωτές που **δεν έχουν καμία πιθανότητα** αφού το password auth είναι κλειστό. Προσοχή
σε μια παγίδα που παραλίγο να καταγράψω λάθος: η κορυφαία «επιτιθέμενη» IP `2.85.82.29` είναι
**δική σου** (258 **επιτυχημένα** publickey logins ως root, μαζί με το Claude Desktop που περιηγείται
στο `app.ph-aros.com`). Δεν είναι επίθεση.

### Τι δεν άγγιξα, σκόπιμα

Καμία επανεκκίνηση (ρίχνει το site), κανένα deploy, καμία αλλαγή σε firewall, SSH, μυστικά ή κώδικα
της εφαρμογής, κανένα volume και κανένα backup. Τα #2 και #5 ανήκουν σε άλλα routines και το #1
θέλει εσένα.

### 15:02-15:06 UTC: η επανεκκίνηση έγινε, κατ' εντολή του Αχιλλέα

Ο Αχιλλέας έδωσε ρητή εντολή («δώσε εντολή για reboot»), οπότε το P2 #1 έκλεισε μέσα στο ίδιο
πέρασμα. **Πριν** πατήσω οτιδήποτε επιβεβαίωσα τα τρία που καθορίζουν αν το site ξανασηκώνεται μόνο
του: και τα τέσσερα containers έχουν `restart: unless-stopped`, το `docker` είναι `enabled` στο
boot, και το νεότερο backup ήταν των 14:55 με το offsite αντίγραφο ήδη επιβεβαιωμένο.

**Αποτέλεσμα, όλα μετρημένα μετά:**

| | Πριν | Μετά |
|---|---|---|
| Kernel | 6.8.0-**117** | **6.8.0-136** |
| `/var/run/reboot-required` | υπήρχε | **δεν υπάρχει** |
| Containers | 4 up | **4 up, RestartCount 0, mongo healthy** |
| Έκθεση θυρών | μόνο caddy | **μόνο caddy** |
| Δίσκος | 28% | 34% |
| Μνήμη σε χρήση | 1,2 Gi | 816 Mi |

Λειτουργικός έλεγχος μετά: apex **200 και ακριβώς 365.936 bytes**, app login **200 και ακριβώς
84.146 bytes**, δηλαδή **ακριβώς τα ίδια σώματα με πριν**, άρα η δρομολόγηση επέζησε ακριβώς
όπως ήταν. Workspace subdomain 307, `login` 401 `Invalid credentials`, `cron` χωρίς μυστικό 401,
`API_RATE_LIMIT` ξαναπέρασε στο container ως 30, `cron` service active.

**Διακοπή περίπου δύο λεπτά** (boot στις 15:04:33, containers όρθια 23 δευτερόλεπτα μετά). Τα
`libc6` και `openssl` που ήταν στον δίσκο από τις 06:37 **ισχύουν πλέον και στη μνήμη**.

**Έκλεισε επίσης μόνο του το πρωινό P2 #3**: το `e51a17a` (activate 5 ανά ώρα) είναι πρόγονος του
deployed `b12c4b7`, άρα το σφίξιμο **είναι ζωντανό** και δεν περιμένει επόμενο deploy.

### 15:10-15:20 UTC: μπήκε fail2ban, κατ' εντολή του Αχιλλέα

**Σημείωση εδάφους**: το SKILL μου απαγορεύει ρητά αλλαγές σε firewall. Το έκανα **κατόπιν ρητής
εντολής** («προγραμμάτισέ το να γίνει»), όχι με δική μου πρωτοβουλία, και το γράφω εδώ ώστε να
φαίνεται ποιος το ζήτησε.

**Πριν από την εγκατάσταση έλεγξα τον κίνδυνο lockout**, γιατί ένα κακορυθμισμένο fail2ban κλειδώνει
τον ιδιοκτήτη έξω από τον δικό του server. Οι 461 «Connection closed by authenticating» και οι 42
«maximum authentication» προέρχονται **αποκλειστικά από σαρωτές** (64.89.163.251, 195.178.110.217,
220.85.210.200, 118.68.66.195). Η IP του Αχιλλέα `2.85.82.29` παράγει **μηδέν** αποτυχίες. Παρόλα
αυτά μπήκε ρητά στο `ignoreip`, μαζί με το localhost.

Ρύθμιση: `bantime 1h`, `findtime 10m`, `maxretry 5`, `backend systemd`, jail `sshd` σε
`mode aggressive`.

**Δύο σφάλματα που έκανα και τα έπιασα μόνο επειδή δεν εμπιστεύτηκα το «φαίνεται εντάξει»:**

1. **Τα ελληνικά σχόλια έσπαγαν το parsing.** Το πρώτο `jail.local` το είχα γράψει με ελληνικά
   σχόλια και **αγνοήθηκε ολόκληρο**: το `fail2ban-client get sshd ignoreip` απαντούσε «No IP
   address/network is ignored» και το `bantime` έδειχνε 600, δηλαδή το default του `jail.conf`, όχι
   το δικό μου 3600. Δηλαδή **η δικλείδα κατά του lockout δεν υπήρχε καν**, ενώ το αρχείο έμοιαζε
   σωστό. Ξαναγράφτηκε **σε ASCII** και φορτώθηκε κανονικά. Στα αρχεία ρυθμίσεων του συστήματος,
   μόνο ASCII.
2. **Το jail έτρεχε χωρίς καμία action, δηλαδή placebo.** Μετά το `fail2ban-client reload`, το
   `get sshd actions` απαντούσε **«No actions for jail sshd»**: το fail2ban μετρούσε αποτυχίες και
   κατέγραφε «banned» στη λίστα του, αλλά **δεν έμπαινε κανένας κανόνας στο firewall**. Το
   `nft list ruleset` δεν είχε ίχνος της μπαναρισμένης IP. Το `reload` είχε κάνει flush το action
   (`Flush ticket(s) with nftables-multiport`) χωρίς να το ξαναφορτώσει. Λύθηκε με **πλήρες
   `systemctl restart`**, όχι reload.

**Επαλήθευση ότι όντως επιβάλλεται**, που είναι και το μόνο που μετράει:

```
table inet f2b-table {
  set addr-set-sshd { type ipv4_addr; elements = { 101.47.155.9 } }
  chain f2b-chain {
    type filter hook input priority filter - 1; policy accept;
    tcp dport 22 ip saddr @addr-set-sshd reject with icmp port-unreachable
  }
}
```

Ο κανόνας είναι **αποκλειστικά `tcp dport 22`**, οπότε δεν αγγίζει 80/443 ούτε τα chains του Docker.
Επιβεβαιώθηκε και εμπειρικά: τέσσερα containers up, apex 200, app 200, workspace 307, μόνο ο caddy
εκθέτει θύρες. Δοκιμάστηκε επίσης ο κύκλος `unbanip` και `banip`, γιατί αυτόν ακριβώς θα χρειαστεί
το admin UI που ζήτησε ο Αχιλλέας.

Μία IP μπαναρίστηκε μέσα στα πρώτα λεπτά (101.47.155.9), `fail2ban` enabled at boot.

### 15:25-15:32 UTC: στήθηκε ο f2b-bridge (βήμα 1 από 3 του σχεδίου για το admin UI)

Ο Αχιλλέας ενέκρινε την επιλογή (α), ουρά με εκτελεστή στον host. Το **βήμα 1 είναι δικό μου έδαφος
και δεν χρειάζεται deploy**, οπότε το έστησα ώστε το `pharos-saas-core` να βρει έτοιμη υποδομή. Τα
βήματα 2 (σελίδα και route) και 3 (mount στο compose) **δεν** τα άγγιξα.

**Διόρθωσα το ίδιο μου το σχέδιο πριν το υλοποιήσω.** Είχα προτείνει read-only mount του
`fail2ban.sqlite3`. Δεν στέκει: το αρχείο είναι `rw------- root`, ο container τρέχει ως **uid 1001
non-root**, δεν υπάρχει καν `sqlite3` στον host, και θα ήθελε **νέα εξάρτηση sqlite στο Node** για
μια λίστα IP. Το `fail2ban-client get sshd banip --with-time` δίνει ήδη IP, ώρα και λήξη. Το σχέδιο
έγινε **απλούστερο και ασφαλέστερο**: μηδέν sqlite, μηδέν νέα εξάρτηση.

**Τι μπήκε**: `/opt/pharos/deploy/f2b-bridge.sh`, από cron **ανά λεπτό**, δύο μονόδρομες δουλειές.
Γράφει `f2b/state.json` **ατομικά** (temp και `mv`, ποτέ μισογραμμένο) και **αδειάζει** την ουρά
`f2b/requests/`. Το αίτημα είναι **μία γραμμή απλού κειμένου** (`unban <jail> <ip>`), όχι json:
το parsing json σε shell χωρίς `jq` είναι εύθραυστο, και εδώ θέλουμε βαρετή αντοχή.

**Ο container δεν εκτελεί ποτέ τίποτα και δεν αγγίζει το socket του fail2ban.** Γράφει αίτημα και
περιμένει. Η επικύρωση γίνεται στον host και είναι η αυθεντική.

**Επιθετική δοκιμή, εννιά αιτήματα.** Εκτελέστηκε **μόνο το έγκυρο**:

| Αίτημα | Έκβαση |
|---|---|
| `unban sshd 101.47.155.9` | **εκτελέστηκε** |
| `ban sshd 8.8.8.8` | `bad_action` |
| `unban sshd 1.2.3.4; rm -rf /` | `extra_fields` |
| `unban sshd $(whoami)` | `bad_ip` |
| `unban notajail 1.2.3.4` | `bad_jail` |
| `unban sshd 999.999.999.999` | `bad_ip` |
| `unban sshd 1.2.3.4 extra` | `extra_fields` |
| δύο γραμμές σε ένα αρχείο | `multiline` |
| `--help` | `bad_action` |

Οι απορρίψεις καταγράφονται **με τον λόγο**, γιατί μια σιωπηλή απόρριψη θα έμοιαζε ακριβώς με «το
πάτησα και δεν έγινε τίποτα».

**Η επαλήθευση που μετράει**: μετά το έγκυρο unban, η IP έφυγε **από το ίδιο το nftables set**
(`elements = { 57.134.136.41 }` μόνο), όχι απλώς από τη λίστα του fail2ban. Είναι ακριβώς η διάκριση
που ξεσκέπασε το placebo μισή ώρα νωρίτερα.

**Δικαιώματα**, αφού ο container είναι `uid 1001(nextjs) gid 65533`: `requests/` σε `1001:65533`
με `730` (ο container **γράφει**, δεν διαβάζει τι έγραψαν άλλοι), `state.json` σε `644` (διαβάζει),
`done/` και `rejected/` σε root `750` (δεν τα βλέπει καθόλου). Επιβεβαιώθηκε εμπειρικά με container
που έτρεξε ως 1001.

**End-to-end**: αφέθηκε επίτηδες ένα αίτημα στην ουρά, το **cron το κατανάλωσε μόνο του** στις
15:30:01 (`unbanned 9.9.9.9 from sshd` στο syslog) και ανανέωσε το `state.json`. Το script μπήκε και
στο repo ως `deploy/f2b-bridge.sh` ώστε να μη ζει μόνο στον server.

**Για το βήμα 2, το σημείο που πρέπει να μην ξεχαστεί**: αν το `generatedAt` είναι παλαιότερο των 5
λεπτών, η οθόνη οφείλει να λέει **«μπαγιάτικα δεδομένα»**, όχι μια καθησυχαστική άδεια λίστα. Κενό
δεν σημαίνει καθαρό.
