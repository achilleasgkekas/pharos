# Pharos — Web features → Mobile parity

What the **web app** (`apps/web`) does, and where the **mobile app** (`apps/mobile`, Expo) stands.
Legend: ✅ done · 🟡 partial · ❌ missing. This is the mobile roadmap — work top-down by priority.

## Navigation & shell
| Web | Mobile |
|-----|--------|
| Top nav: grouped menus (Stuff/Money/Plan/Activity), central AI/Search bar, AI-online dot, theme toggle, language switcher, notification bell, settings, user menu | 🟡 Drawer menu (Stuff/Money/Plan + Activity + Search/AI/Settings/Sign out), AppBar (☰ + 🔍 + 🔔-με-badge + logo). No theme toggle, no language switcher. Notification-bell badge ✅ (unread count, tap → Activity → Alerts) |
| Dark/light theme + 8-language i18n | ❌ dark only, English only |
| Lighthouse logo / favicon / PWA | ✅ logo in-app + app icon + splash |

## Home / dashboard
| Web | Mobile |
|-----|--------|
| Hero + AI command bar + module nav cards | ✅ hub: overview stats (owed) + nav tiles |

## Items — Inventory & Shopping (`/items`, `/shopping`)
| Web | Mobile |
|-----|--------|
| Grid/list, e-shop sidebar filters (status/store/category/sort/flags), select-mode + bulk AI fill | 🟡 list + All/Owned/Shopping filter |
| Add item + **URL import** (fetch + AI parse → preview → approve), dedup | 🟡 add by title **+ URL import** (paste link → ✦ AI fetch+parse → add to Shopping, auto-detected). No preview-before-approve step, no select-mode/bulk-AI |
| Item detail: specs, **PricePanel** (best price, where-to-buy, price position, log price, search online, full history chart), links, photos, warranty, purchase & payment, **link to installment plan**, AI specs, convert-to-task | 🟡 tap-to-edit (title, status, category, price, target, specs) + **PricePanel** (best-now + verdict, position bar low/target/high, where-to-buy tap→open store, log-a-price, full history, photos strip, links, warranty/purchase) + **link to installment plan** (linked δόσεις with payoff + unlink, collapsible picker of available plans → tap to attach) + **convert-to-task** (button → seeds a Task from the item) + **AI specs / AI info** (✦ κουμπιά → `aiFillItem(id, 'specs'\|'info')`, re-prefill specs/price/links in-place) |
| Delete | ✅ long-press delete |

## Shopping list (`/shopping-list`)
| Web | Mobile |
|-----|--------|
| Quick add, **photo→AI→verify→add**, check-off, Bought section, quick-verify, category color/eyebrow | ✅ add, photo scan, check-off, delete, categories |

## Receipts (`/receipts`)
| Web | Mobile |
|-----|--------|
| Dropzone upload + AI parse, e-shop layout, **email import**, find-duplicates + merge | 🟡 list + camera scan→save |
| Detail: image/PDF, **editable line items (net/VAT/gross)**, store select, ∑-items, **re-scan OCR/text**, **verify**, **archive (not a receipt)**, add items to library | ✅ detail = image + **editable** store/date/payment/total/notes + **editable line items** (name/qty/net/VAT%, gross shown, add/remove) + **∑-items** + **verify** + **archive (not a receipt)** + **add items to inventory** + **re-scan text/OCR** (re-prefill in-place) |

## Expenses & Income (`/expenses`, `/income`)
| Web | Mobile |
|-----|--------|
| **Scan a bill/payslip** (AI), e-shop layout, recurring series + auto-generate, **anomaly badges**, vendor autocomplete | 🟡 list + manual add (vendor+amount) + **AI scan-a-bill** (✦ camera → parse → confirm draft → add; carries date/period/recurring/payment). **Anomaly badges ✅** (2026-07-02: v1 list route εκθέτει additive `anomaly?` ±% μέσω `computeAnomalies` στο `serialize.ts`, mirror του web `page.tsx` median-deviation· gold `⚠ ±N%` badge δίπλα στο amount στο MoneyScreen· skip σε incremental sync). **vendor autocomplete ✅** (2026-07-02: distinct-vendor suggestion chips από την ήδη-φορτωμένη λίστα, substring-match, tap→fill, μηδέν endpoint). |
| Detail + **edit** (vendor/amount/category/date/period/recurring/payment/notes) + re-scan | 🟡 tap-to-edit **vendor/amount/category/date/period/recurring+cycle/payment/notes** (full-field) + long-press delete. Scanned-bill image στο detail ✅ (2026-07-02, `fileSource(editing.file)` → `<Image>`). **Re-scan (AI) text/OCR ✅** (2026-07-02, νέο `POST /api/v1/expenses/[id]/rescan` byte-mirror του receipts rescan → `rescanExpense(id, ocr)` σε api.ts → «Re-scan text/OCR» bar στο edit modal όταν `editing.file`, re-prefill in-place, μένει unverified) |

## Statements (`/statements`)
| Web | Mobile |
|-----|--------|
| PDF import, transactions, **installment plans** (compute, link to products, merge/bind, payoff), per-card outstanding, re-scan | 🟡 list + **installment-plan overview** (cross-statement, active-first: label, card, €/mo, paid/total, €left, payoff month; done plans dimmed) + **detail με transactions** (sorted, amount, date, **per-charge installment badge** current/total) + totals (total/min/paid). No merge/bind, no re-scan, no PDF import |

## Subscriptions (`/subscriptions`)
| Web | Mobile |
|-----|--------|
| e-shop layout, **AI fill from name**, add/edit, cycle, next renewal | ✅ list + add (name+amount, ✦ AI fill) + tap-to-edit (name, amount, **billing cycle picker**, **next renewal date**, active) + delete |

## Vouchers (`/vouchers`)
| Web | Mobile |
|-----|--------|
| e-shop layout, **AI scan (paste/image)**, add/edit | ✅ list + quick-add (title+code) + **AI scan (paste or 📷 photo → full draft)** + tap-to-edit full fields (title, code, store, discount, expiry, url, used) + delete |

## Calendar (`/calendar`)
| Web | Mobile |
|-----|--------|
| 3-month money agenda: renewals stepped per cycle, **installments aggregated per month**, recurring bills/income projected, warranty + voucher expiries, monthly in/out totals | ✅ 3-month sectioned agenda: renewals stepped, installments aggregated/month (pinned), recurring bills/income projected, warranty/voucher expiries, per-month in/out totals |

## Reports (`/reports`)
| Web | Mobile |
|-----|--------|
| Net-position banner, monthly-spend chart, upcoming-installments, spend-by-store, inventory-value pie, subs-by-category, warranties expiring, biggest purchases, **installment payoff**, **budgets**, income-vs-expense, date-range | 🟡 **net-position banner** (inventory − installments owed), net month/year, by-category, **budget · this month** (per-category progress bars, gold ≥80%, red over), **upcoming-installments · next 6 months** (purple bars), last-6-months bars, **spend-by-store · top 8** (accent bars), **biggest purchases** (list: store/date/total), **warranties expiring · next 150 days** (list: title + Nd-left, red ≤30d / gold ≤90d), **subscriptions · monthly by category** (purple bars, monthly-equivalent), **cash flow · 12 months** (grouped income-green / expense-red stacked mini-bars per month, με legend), **inventory value · by category** (cyan bars, owned-value ανά κατηγορία — το web pie, στο mobile bars χωρίς RN charting lib), **installment payoff** (2026-07-06: additive `installmentPayoff` field στο `GET /api/v1/reports`, mirror του web page.tsx — linked plans resolve item-titles ως label· mobile `PayoffRow`: label + accent-dot όταν linked + paid/total counter, progress bar purple/accent-όταν-done, `€/mo · €left / paid off ✓`), **date-range selector** (TRENDS `6m/12m/24m` pills, default 12· φιλτράρει τα SPEND + CASH FLOW windowed charts, mirror του web `?months=`· endpoint `?months=`, additive `months` field). **Reports parity ΠΛΗΡΩΣ ΚΛΕΙΣΤΟ** (όλα τα web charts + payoff + date-range καλυμμένα) |

## Tasks (`/tasks`)
| Web | Mobile |
|-----|--------|
| **Kanban** (todo/in-progress/blocked/done, drag + ←/→), board/list toggle, #tags, steps/checklist, project progress by tag | 🟡 list + add (**#tag-parse**) + toggle done + **inline ←/→ quick-move** (κύκλος status κατά μήκος todo→in-progress→blocked→done, mirror του web Kanban ←/→) + tap-to-edit (title + **4-status picker** + **priority picker** low/normal/high + **tags editor** + **steps/checklist** [add/toggle/remove, optimistic] + card badge `☑ done/total`) + status chips + tag/priority display + **tag filter row** (horizontal chips από όλα τα task tags, tap→φιλτράρει τη λίστα, tap ξανά→καθαρίζει) + **project-progress-by-tag** (όταν επιλεγεί tag: bar done/total + % — ο επιλεγμένος tag λειτουργεί ως project/phase, mirror του web) + delete. No Kanban board |

## Settings (`/settings`)
| Web | Mobile |
|-----|--------|
| 7 tabs: appearance/defaults, budgets/cards, **AI engine/features/prompts**, storage/backup/CSV/trash/OneDrive, stores/lists, **ntfy notifications** | 🟡 account + **editable preferences** (currency, VAT, warranty months/alert, auto-add stores) + **editable budgets** + **payment cards CRUD** + **stores CRUD** (search/add/edit/delete, name/url/aliases, needs-review badge) + **dropdown lists editor** (3 category taxonomies, add/remove chips, reset-to-default) + **ntfy URL/enable/test** + server/version. No theme/language, AI engine, storage |

## Activity
| Web | Mobile |
|-----|--------|
| Jobs (background AI), History (AI conversations), Trash (restore/purge) | ✅ Activity screen με 4 tabs: **Alerts** (in-app notification feed, mark-read/mark-all), **Trash** (restore + delete-forever[admin]), **Jobs** (background AI, live progress auto-refresh, read-only), **History** (AI conversations, expand thread) |

## AI & search
| Web | Mobile |
|-----|--------|
| Conversational command bar (add/search/overview…), global search | ✅ AI assistant screen, ✅ global search |

## Notifications
| Web | Mobile |
|-----|--------|
| In-app notification center (bell), ntfy push | 🟡 in-app feed ✅ (Activity → Alerts, με unread state) + **bell-with-badge στο AppBar** (🔔 + κόκκινο unread count, tap → Activity/Alerts). Remote push pipeline buildable αλλά αδοκίμαστο (χρειάζεται EAS dev build + APNs key) |

---

## Roadmap (priority order)
1. ✅ **Edit existing records** — tap-to-edit modals for Tasks, Expenses, Income, Subscriptions, Vouchers, **Items** (PATCH endpoints + forms).
2. ✅ **Tasks statuses** — todo/in-progress/blocked/done picker + status chips.
3. ✅ **Receipt verify/edit** — edit fields + line items + ∑-items + verify + archive + add-to-library + **re-scan text/OCR** (2026-07-01).
4. ✅ **AI fill** — subscription-from-name, voucher scan (paste+image), item URL import, expenses scan-a-bill.
5. ✅ **Statement transactions** — per-statement detail με transactions + per-charge installment badges + **aggregated installment-plan overview** (cross-statement payoff, active-first) + **link plans to products** (item PlansBlock) + **merge/bind plans** (overview «⑂ Merge into…» picker + «Unmerge», 2026-07-05). *Complete — closes the last #5 write-op gap.*
6. 🟡 **Settings** — editable preferences (currency/VAT/warranty/auto-add), editable budgets, payment cards CRUD, stores CRUD, dropdown-lists editor, ntfy URL/enable/test ✅. *Remaining: theme toggle, language, AI engine, storage/OneDrive (βλ. Needs Achilleas).*
7. ✅ **Activity** — Alerts (notification feed) + Trash (restore/purge) + Jobs + History.
8. 🟡 **Push notifications** — in-app feed ✅· remote push pipeline **ΗΔΗ ΧΤΙΣΜΕΝΟ** (commit `2156a83`, 2026-06-29 — token
   registry, `sendExpoPush`/`pushAllDevices`, `/api/v1/push/register`, mobile `registerForPush`/`unregisterForPush` wired
   στο `App.tsx`, `runAlertChecks` fan-out). Doc ήταν stale (έλεγε "buildable αλλά" σαν να μην είχε χτιστεί ακόμα) — fixed
   2026-07-14. Μόνο E2E delivery σε πραγματική συσκευή (EAS dev build + APNs/FCM) μένει αδοκίμαστο, βλ. Needs Achilleas.

> Progress: tap any row on Tasks / Expenses / Income / Subscriptions / Vouchers / **Items** to **edit**; long-press to delete. Items edit covers title, status, category, price, target, specs + full PricePanel + plan-link.

---

## UI Debt Queue

> **ui-auditor re-audit 2026-07-18 (41η σάρωση; confirmation, μηδέν νέο finding):** fresh read-only grep audit (όχι docs read-back). **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (C/SPACE/RADIUS/SIZE/scrim/alpha) + `ui.tsx` primitives (ΟΛΑ παρόντα). mobile `npx tsc --noEmit` → **EXIT 0**. **(Tokens)** hardcoded hex εκτός `theme.ts` = **0**, rgba = **0** — token layer πλήρως καθαρό (αμετάβλητο από 40ο). **(Reusable)** raw `<TextInput>` = **3** (ReceiptsScreen cellInput για QTY/NET/VAT; τότε 11, ο Αχιλλέας έχει migrate-άρει τα 8 σε `<Input>` primitives). Ghost buttons = **3 uses** του `Button variant="ghost"`, καλή primitive adoption. **(Theme)** dark-only, μηδέν light context (P3/L αμετάβλητο). **(Adaptive)** `contentWidth` ✓, safe-area απών (P2/M αμετάβλητο). **(Touch)** Settings `rm`/`swatch` χωρίς `hitSlop` (WIP-blocked, αμετάβλητο). **Μηδέν νέα violations.** Queue αμετάβλητο.

> **UI-consistency σάρωση 2026-07-10 (ui-auditor, 1η αποκλειστικά UI):** στόχος = πόσο «universal» είναι το mobile UI vs το web design system, ΟΧΙ functional parity (αυτό ζει στο Build Queue). **Θεμέλιο ΥΠΑΡΧΕΙ ήδη**, οπότε ΔΕΝ χρειάζεται P1 foundation item: `apps/mobile/src/theme.ts` καθρεφτίζει πλήρως τα web tokens (`apps/web/src/app/globals.css:8-22` — bg/surface/border/text/accent/cyan/red/gold/purple/orange ολα ταιριαστα· + SPACE/RADIUS/SIZE/scrim/alpha scales) και `apps/mobile/src/ui.tsx` εκθέτει centralized primitives (`Button`/`IconButton`/`Card`/`ListItem`/`Input`/`TextArea`/`Badge`/`Chip`/`Check`/`Header`/`Spinner`/`Empty`/`ErrorText`/`ModalSheet`/`contentWidth`). **Καθαρές διαστάσεις:** hardcoded hex στα screens = **0**· rgba literals στα screens = **0** (ολα μεσω `alpha()`)· `contentWidth` cap εφαρμοσμενο και στα 14 list screens· state-primitives (`<Spinner>`/`<Empty>`/`<ErrorText>`) χρησιμοποιουνται απο 10-12 screens το καθενα. **Παραβιασεις που βρεθηκαν (5 dimensions):** reusable-component re-impl ×2 (Shopping inline loading/empty· Receipts cell-inputs)· token magic-number ×1 (Receipts `borderRadius: 8` εκτος RADIUS scale)· adaptive/safe-area ×1 (legacy `SafeAreaView`, χωρις landscape/bottom insets)· theme/dark-mode ×1 (mobile dark-only ενω web εχει `data-theme='light'`). `apps/mobile npx tsc --noEmit` → **EXIT 0**. Σειρα: μικρα + υψηλη προτεραιοτητα πρωτα.

### ShoppingScreen: inline loading/empty → shared `<Spinner>`/`<Empty>`
- Priority: P2
- Size: S
- Web ref: state primitives (file: apps/mobile/src/ui.tsx:31-39 `Spinner`/`Empty`)
- Mobile files: apps/mobile/src/screens/ShoppingScreen.tsx
- Depends on: none
- Acceptance:
  - Η γραμμη `if (loading) return <View style={s.center}><ActivityIndicator color={C.accent} /></View>;` (ShoppingScreen.tsx:69) αντικαθισταται με `<Spinner />`.
  - Το `ListEmptyComponent={<Text style={s.empty}>…</Text>}` (ShoppingScreen.tsx:97) χρησιμοποιει το shared `<Empty>` primitive.
  - Τα local StyleSheet entries `center` και `empty` (byte-dupes του ui.tsx `center`/`empty`, ShoppingScreen.tsx:152) διαγραφονται, μηδεν αχρησιμοποιητο style μενει.
  - `apps/mobile npx tsc --noEmit` → EXIT 0.
- Status: TODO

### ReceiptsScreen: line-item cell inputs → shared `<Input>` (kill raw TextInput + magic radius)
- Priority: P2
- Size: S
- Web ref: Input token (file: apps/mobile/src/ui.tsx:77-85 `Input`, RADIUS scale apps/mobile/src/theme.ts:26)
- Mobile files: apps/mobile/src/screens/ReceiptsScreen.tsx (+ ίσως compact/`cell` variant στο apps/mobile/src/ui.tsx)
- Depends on: none
- Acceptance:
  - Τα 3 raw `<TextInput>` cells (QTY/NET/VAT %, ReceiptsScreen.tsx:279-281) χρησιμοποιουν το shared `<Input>` primitive (νεο compact `cell` variant αν χρειαζεται συμπαγες padding), οχι raw TextInput + inline `placeholderTextColor`.
  - Το local `cellInput` style (ReceiptsScreen.tsx:388) φευγει· κανενα `borderRadius: 8` magic number (χρησιμοποιησε RADIUS token).
  - Raw-`<TextInput>` grep στο `src/screens/` = 0 (μονο ο import μενει αν χρησιμοποιειται αλλου).
  - `apps/mobile npx tsc --noEmit` → EXIT 0.
- Status: TODO

### Safe-area: adopt `react-native-safe-area-context` (landscape + bottom home-indicator inset)
- Priority: P2
- Size: M
- Web ref: adaptive/safe layout (no direct web token — RN-specific· web reference = mobile-first CLAUDE.md principle)
- Mobile files: apps/mobile/App.tsx, apps/mobile/package.json (add dep), apps/mobile/src/nav.tsx (Drawer bottom items)
- Depends on: none
- Acceptance:
  - Το legacy `SafeAreaView` απο `react-native` (App.tsx:2,88 — iOS-only top/bottom, μηδεν left/right insets σε landscape) αντικαθισταται με `SafeAreaProvider` + `useSafeAreaInsets` απο `react-native-safe-area-context` (Expo SDK 54 συμβατο).
  - Το bottom home-indicator inset εφαρμοζεται στο shell/Drawer ωστε τα bottom items να μην κρυβονται κατω απο το indicator.
  - Το χειροκινητο `paddingTop` Android hack (App.tsx:111) καλυπτεται απο τα insets, οχι διπλο padding.
  - `apps/mobile npx tsc --noEmit` → EXIT 0.
- Status: TODO

### Light-theme parity: mobile dark-only vs web `data-theme='light'`
- Priority: P3
- Size: L
- Web ref: light theme override palette (file: apps/web/src/app/globals.css:33-50 `html[data-theme='light']`)
- Mobile files: apps/mobile/src/theme.ts (light palette + theme context), apps/mobile/src/ui.tsx (primitives να διαβαζουν context αντι const `C`), apps/mobile/src/screens/SettingsScreen.tsx (theme toggle)
- Depends on: none
- Acceptance:
  - Ορισμος light palette (mirror του globals.css:37-50) + theme provider/context· τα primitives διαβαζουν την ενεργη παλετα αντι για το const `C`.
  - Settings theme toggle (dark/light/follow-system) που περνα ολα τα screens.
  - **NEEDS DECISION (Αχιλλεας):** θελει καν light mode στο mobile, ή follow-system, ή μενει dark-only για τωρα; Το web light theme ειναι «for later» (CLAUDE.md), οποτε χαμηλη προτεραιοτητα μεχρι απαντηση. Μεχρι τοτε ΔΕΝ ειναι unattended auto-buildable.
- Status: TODO

---

## Build Queue
> **Re-audit 2026-07-18 (mobile-parity-auditor, 50η σάρωση· 1 νέο auto-buildable GAP από P12, 2 web features σωστά κρίθηκαν εκτός mobile scope):** Inventory επιβεβαιώθηκε από τον κώδικα, όχι από τα docs: **51 v1 routes**, **16 mobile screens**, **84 exported api-functions** — και τα τρία αμετάβλητα vs την 49η σάρωση. `git log --since=2026-07-13 -- apps/web/src apps/mobile/src` δείχνει **5 commits**: 4 νέα web features (P1 demo/sample-data mode, P26 onboarding checklist, P12 savings/financial goals, P21 document/manual vault) + 1 SaaS admin surface (`9fee9b0`, admin tenant actions — εκτός mobile scope, ήδη confirmed σε προηγούμενη σάρωση) — **μηδέν από αυτά άγγιξε `apps/web/src/app/api/v1` ή `apps/mobile/src`** (επιβεβαιωμένο ανά `git show --stat` για κάθε commit). **P21 βρέθηκε ήδη σωστά speced στο Build Queue** (η 49η σάρωση το είχε προβλέψει σωστά ΠΡΙΝ καν χτιστεί το web feature στις 2026-07-14 — το attachments[] shape/actions/component ταιριάζουν 1:1 με ό,τι shipped· το entry παραμένει αμετάβλητο, ακόμα `Status: TODO`, μηδέν αλλαγή χρειάζεται). **P26 + P1 επιβεβαιώθηκαν ΟΧΙ mobile gap** — τα ίδια τα daily-dev PROGRESS entries (2026-07-16, 2026-07-17) το έκριναν ρητά έτσι (onboarding = καθαρά web-only homepage state, δεν υπάρχει "getting started" concept στο mobile roadmap· sample-data = admin-only Settings dev-tool, κανένα precedent για data-management actions στο mobile app) — κρίση με την οποία συμφωνώ, δεν μπαίνουν στο queue. **1 νέο auto-buildable PARITY GAP:** savings/financial goals (P12, `78ebd76`) — εντελώς νέο entity (`models/Goal.ts`, με `updatedAt` index ήδη σχολιασμένο ρητά στον κώδικα για μελλοντικό `GET /api/v1/goals`), μηδέν v1 route, mirror ακριβώς του ήδη-ουρασμένου Bill/GiftCard pattern (P28/P32). mobile `npx tsc --noEmit` → **EXIT 0**. **Ranked top-3+1 (unattended-safe, τα πρώτα 3 αμετάβλητα vs την 49η):** (1) **Expenses category-rule wiring στο v1 POST route** [P1/S, functional bug fix, καθαρά web-side]· (2) **Subscriptions trial field exposure** [P2/S]· (3) **Reports net-worth headline/breakdown** [P2/M]· (4, νέο) **Goals entity exposure** [P2/L, νέο entity, βλ. entry παρακάτω]. **Needs Achilleas (αμετάβλητα):** receipt↔transaction reconciliation scope (P18), SaaS multi-tenant surfaces confirm-out-of-scope, safe-area dep, theme/light-dark + language switcher, AI-engine/storage/OneDrive Settings, statements PDF-import, remote push E2E σε πραγματική συσκευή (EAS+APNs), Tasks Kanban board, lucide icon set, rate-limit 429 backoff.
> **Re-audit 2026-07-13 (mobile-parity-auditor, 49η σάρωση· 8 ΝΕΑ auto-buildable GAP από backlog P-items shipped 2026-07-11/13, μηδέν από αυτά ported στο v1 API):** Inventory ξαναχτισμένο από τον κώδικα. **51 v1 routes** (αμετάβλητο), **16 mobile screens**, **84 exported api-functions**, mobile `npx tsc --noEmit` → **EXIT 0**. Επιβεβαίωσα πρώτα ότι τα top-2 items της 48ης (pricehike icon, return-window badge) όντως shipped σήμερα (`6ea29a0`) — το doc είχε μείνει με stale `Status: TODO` σε αυτά τα 2 items ενώ το PROGRESS.md ήδη τα έλεγε κλειστά· διορθώθηκε. **Νέο εύρημα:** `git log --since=2026-07-10 -- apps/web/src apps/mobile/src` δείχνει **9 approved backlog P-items** (P7/P15/P18/P19/P22/P25/P28/P32/P33) shipped ως web features (pure libs + server actions), ΑΛΛΑ **μηδέν από αυτά άγγιξε `apps/web/src/app/api/v1` ή `apps/mobile/src`** (επιβεβαιωμένο ανά commit `git show --stat`) — η "Approved P-items queue" του daily-dev routine χτίζει web-first και δεν κλείνει αυτόματα το mobile loop, σε αντίθεση με ό,τι υπονοούσε η σημερινή PROGRESS καταχώρηση. Live-verified με grep στα ίδια routes: `GET /api/v1/reports` δεν έχει `safeToSpend` ούτε rollover-adjusted budgets (μόνο raw `budgets[]`), `GET/PATCH /api/v1/settings` δεν εκθέτει `categoryRules`/`budgetRollover`/`trialAlertDays`/`billAlertDays`, `POST /api/v1/expenses` (route.ts:47) βάζει literal `'other'` όταν λείπει category αντί να καλέσει το ήδη-υπάρχον `matchCategoryRule()` (**functional inconsistency, όχι απλά missing UI** — μια απόδειξη/έξοδο που δημιουργείται από mobile παίρνει ΛΑΘΟΣ category ενώ η ίδια θα έπαιρνε σωστή από το web), `subscriptions` routes δεν έχουν `trialEndsAt`, `GET /api/v1/search` δεν έχει matched-line-item snippet, και δύο εντελώς νέα entities (**Bill**, **GiftCard**) δεν έχουν v1 route καθόλου. **Counts: DONE 11 (+2 σήμερα) / auto-buildable functional GAP 8 νέα / NEEDS DECISION 1 νέο.** **Ranked top-3 (unattended-safe):** (1) **Expenses category-rule wiring στο v1 POST route** [P1/S, functional bug fix, ΜΗΔΕΝ mobile UI change, καθαρά web-side]· (2) **Subscriptions trial field exposure** [P2/S, additive 2 πεδία]· (3) **Reports net-worth headline/breakdown** [P2/M, ήδη speced από την 48η, παραμένει ανοιχτό]. Οι SaaS multi-tenant επιφάνειες (`feat(saas)` commits: account/workspace/billing/members/admin, Activity audit-trail tab) **ΔΕΝ μπήκαν στο queue** — προορίζονται για το hosted-SaaS web account portal, όχι για το self-hosted personal-use mobile companion app (καμία ένδειξη ότι το mobile πρέπει να γίνει multi-tenant-aware)· flagged ως προς επιβεβαίωση στο PROGRESS. **Needs Achilleas (νέο):** receipt↔transaction reconciliation (P18, `07fba9f`) — interactive matching UI (`ReconcilePanel.tsx`, 233 γραμμές) στο web· ασαφές αν το mobile θέλει πλήρες interactive matching ή απλή read-only λίστα προτεινόμενων ταιριασμάτων πρώτα, βλ. PROGRESS.
> **Update 2026-07-13 (pharos-daily-dev): top-2 items ΕΚΛΕΙΣΑΝ.** (1) **pricehike notification icon** — έγινε ευρύτερο
> από το flag: το `models/Notification.ts` enum έχει πλέον **4** κενά kinds στο mobile (`pricehike` [P14] + `trialend`
> [P33] + `giftcard` [P32] + `bill` [P28], όχι μόνο pricehike — τα 3 τελευταία shipped ΜΕΤΑ την 48η σάρωση). Fix:
> `NotifKind` (`api.ts:466`) + `NOTIF_ICON` (`ActivityScreen.tsx:23`) πήραν όλα τα 4 (glyphs 📈/⏰/🎁/📄). (2)
> **return-window badge στα receipts** — νέο optional 2ο param `trimReceipt(r, returnDaysLeft?)` (`serialize.ts`, ίδιο
> spread-pattern με το expenses `anomaly`)· το list route (`route.ts`) + το detail route (`[id]/route.ts`) υπολογίζουν
> το πεδίο μέσω του υπάρχοντος `lib/returnWindow.ts` (store override → global default, skip σε archived, skip τα
> cross-doc lookups σε incremental `updatedSince` sync). Mobile: `ReceiptSummary.returnDaysLeft?` + `<Badge>` chip
> (cyan/gold ≤3d) στη λίστα ΚΑΙ στο modal header, mirror του web `ReturnBadge`. **Verified:** web type-check EXIT 0,
> mobile `tsc --noEmit` EXIT 0, `npx vitest run` **2227 passed / 172 files** (+9 νέα return-window route tests +2
> serialize tests, μηδέν regression — τα προϋπάρχοντα exact-shape `toEqual` πέρασαν αμετάβλητα γιατί το test
> default είναι `defaultReturnWindowDays:0` → το πεδίο παραμένει απόν). Safe Docker rebuild: `homepage-web` clean
> restart (ExitCode 0, no OOM), `/login` 200, `/receipts` 307 (auth-gated, compiled OK), build cache pruned 2.18GB.
> Item #3 (net-worth headline/breakdown στο Reports) παραμένει ανοιχτό.
> **Re-audit 2026-07-10 (parity-auditor, 48η σάρωση· 3 ΝΕΑ auto-buildable functional GAP από τα PA/P shipped features):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 route.ts κάτω από `api/v1`** (login + 49 bearer), **17 mobile screens** (16 + Assistant), mobile `npx tsc --noEmit` → **EXIT 0**. Route↔consumer loop: μηδέν orphan endpoint. **Δέλτα από 46η (`git log --since=2026-07-06 -- apps/web/src apps/mobile/src`):** το web shipped **5 features με ρητά flagged mobile follow-ups** που οι endpoint-centric σαρώσεις δεν έπιασαν (γιατί κάποια είναι server-actions ή computed πεδία, όχι νέα routes): **(1) P14 price-hike watch** (`74f9cd4`) — το `models/Notification.ts:10` enum πρόσθεσε kind **`pricehike`**, αλλά το mobile `NOTIF_ICON` (`ActivityScreen.tsx:22`) + `NotifKind` (`api.ts:465`) ΔΕΝ το έχουν → μια `pricehike` ειδοποίηση renderάρει **κενό icon** (`NOTIF_ICON['pricehike']===undefined`). **(2) PA3 return-window tracker** (`0514499`) — το web receipts δείχνει chip «Nd return» (computed `returnDaysLeft` via pure `lib/returnWindow.ts`), αλλά ο v1 receipts serializer (`serialize.ts trimReceipt`) ΔΕΝ εκθέτει το πεδίο → το mobile δεν μπορεί να το δείξει. **(3) PA2 net-worth** (`67bffc9`) — το web `/reports` δείχνει πλέον εμπλουτισμένο «Net worth» (inventory + manual assetAccounts − installments − card balances) + AreaChart snapshot trend, αλλά το v1 reports route επιστρέφει ΜΟΝΟ το παλιό `netPosition` (inventory − installmentsOwed)· το mobile ReportsScreen δείχνει το παλιό. Το **headline/breakdown** slice είναι deterministic/auto-buildable (chart deferred). **Και τα 3 είναι PARITY GAP χωρίς product decision.** Οι υπόλοιπες mobile-src αλλαγές (`b397bc0` Receipts einput→Input, `00d6f46` quick-verify [ΕΚΛΕΙΣΕ το 47η top item], `f7527e0` ShoppingScreen ModalSheet) είναι DONE. **NEEDS DECISION (νέα, δεν πάνε στον builder):** PA1 bank/CSV import (server-action, κανένα v1 endpoint· θέλει upload-endpoint + mobile file-picker decision), P6 iCal feed Settings connector (server-actions calendarToken, κανένα v1 endpoint), net-worth AreaChart trend (RN charting lib). **Counts: DONE 9 / auto-buildable functional GAP 3 (νέα) / NEEDS DECISION 3 νέα.** **Ranked top-3 (unattended-safe):** (1) **pricehike notification icon** [P2/S, pure mobile, μηδέν API, μηδέν rebuild]· (2) **return-window badge receipts** [P2/M, v1 serializer field via existing lib + mobile chip]· (3) **net-worth headline/breakdown στο Reports** [P2/M, v1 netWorth object + mobile chips, chart deferred]. **Needs Achilleas (αμετάβλητα):** safe-area dep, theme/light-dark/language switcher, AI-engine/storage/OneDrive Settings, statements PDF-import, remote push (EAS+APNs), Tasks Kanban board, lucide icon set, rate-limit 429 backoff, + τα 3 νέα NEEDS DECISION παραπάνω.
> **Re-audit 2026-07-09 (parity-auditor, 47η σάρωση· ΝΕΟ functional GAP βρέθηκε — quick-verify):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **51 route.ts κάτω από `api/v1`** (login + 50 bearer), **19 web pages** (calendar/expenses/history/income/items/jobs/login/receipts/reports/settings/setup/shopping/shopping-list/statements/subscriptions/tasks/trash/vouchers· **ΟΧΙ /network** — δεν υπάρχει πια web page ούτε v1 route, άρα δεν είναι mobile gap), **16 mobile screens**, **84 exported api-functions**. **api.ts↔screens (μηχανικός loop):** και οι 84 api-functions + τα 51 route bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint, μηδέν unwired wrapper** (spot-check: addReceiptToLibrary/aiFillItem/convertItemToTask/getItemPlans/linkItemPlan/unlinkItemPlan/logItemPrice/getInstallmentPlans/mergePlans/unmergePlan/scanProduct/importItemUrl/suggestSub/getHistory/getTrash/restoreTrash/purgeTrash/createCard/…/saveList = όλα used σε ≥1 screen). mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **ΝΕΟ εύρημα (1 auto-buildable functional GAP που οι endpoint-centric προηγ. σαρώσεις έχασαν):** **Receipts quick-verify rapid queue.** Το web έχει `apps/web/src/app/receipts/QuickVerify.tsx` (focused queue: μία unverified απόδειξη τη φορά, editable store/date/total + Verify/Skip/Edit/Not-a-receipt + progress). Το mobile ReceiptsScreen έχει ΜΟΝΟ per-receipt detail-modal με «Verified» toggle (line 222) — **καμία rapid queue**. Γιατί χάθηκε: το QuickVerify δεν εκθέτει νέο endpoint (χρησιμοποιεί web server-actions `quickVerifyReceipt`+`archiveReceipt`), αλλά το mobile PATCH `/api/v1/receipts/[id]` κάνει **partial `$set`** (route.ts:37-58 — θέτει ΜΟΝΟ τα passed fields, δεν αγγίζει lineItems), οπότε `updateReceipt(id,{store,date,total,verified:true})` + `updateReceipt(id,{archived:true})` **αναπαράγουν πλήρως** τη συμπεριφορά με τα ΥΠΑΡΧΟΝΤΑ endpoints — μηδέν νέο route, μηδέν AI cost, μηδέν product decision. → μπήκε στην κορυφή του Build Queue (P1/M). **Counts: DONE 8 / auto-buildable functional GAP 1 (νέο· quick-verify) / NEEDS DECISION 0 νέα.** **Ranked top-3 (unattended-safe):** (1) **Receipts quick-verify queue** [P1/M, νέο, endpoint υπάρχει, structural verify — δεν καλεί AI]· (2) fresh pure-lib **vitest coverage** (`lib/cards.ts`/`lib/taxonomies.ts normalizeList`/`lib/itemStatus.ts`, μηδέν rebuild)· (3) ReceiptsScreen `einput`→`<Input>` [P3/S, tsc-verifiable, μηδέν rebuild]. **Needs Achilleas (αμετάβλητα + 2 confirm):** duplicate-detection στο mobile (receipts/stores/items — web `DuplicatesModal`/`StoreDuplicatesModal`/`ItemDuplicatesModal` = server-actions, **ΚΑΝΕΝΑ v1 endpoint** → θέλει endpoint-design + mobile-UX decision), backup/restore + CSV export στα Settings (web server-actions `exportData`/`importData`, **κανένα v1 endpoint**, file-download UX awkward σε mobile), safe-area dep, language switcher + light/dark theme context, AI-engine/storage/OneDrive στα Settings, statements PDF-import (upload endpoint), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board, lucide icon set (cosmetic swap).
> **Re-audit 2026-07-06 (parity-auditor, 46η σάρωση· CONFIRMATION + 2 σημαντικά state changes, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **51 route.ts κάτω από `api/v1`** (login + 50 bearer· **+1 vs 45η**), **51 μοναδικά base paths**, **16 mobile screens**. **Το +1 route = `statements/plans/merge` (POST+DELETE)** που προστέθηκε με το `08ced6d` και το mobile ΤΟ ΚΑΤΑΝΑΛΩΝΕΙ (`api.ts` `statements/plans/merge` ×2 → `mergePlans`/`unmergePlan`). **Route↔consumer (μηχανικός loop όλων των base paths vs `apps/mobile/src/api.ts`):** και τα 51 bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint**. **Δέλτα portable-surface από την 45η (`git log --since=2026-07-05 -- apps/mobile/src apps/web/src/app/api/v1`):** μόνο **1 feat commit** → **`08ced6d`** (`feat(mobile): installment plan merge/bind (last #5 parity gap)`) — **ΕΚΛΕΙΣΕ ΤΟ ΤΕΛΕΥΤΑΙΟ #5 write-op parity gap** (statements merge/bind· ήταν μέχρι τώρα «Needs Achilleas»). Roadmap #5 πλέον 100% ✅. Οι υπόλοιποι commits = test-only (`test(api/v1)` receipts/statements/vouchers/merge coverage) → **μηδέν v1 shape change, μηδέν νέο portable feature**. **2 STATE CHANGES από την 45η:** (α) **working tree ΚΑΘΑΡΟ** (τα πρώην WIP Receipts/Settings/Shopping έγιναν commit — `6835e6a` `<Input>` primitive + `0ac7a37` hitSlop· άρα το προηγούμενο «WIP-blocked» Input/hitSlop debt ξεμπλόκαρε)· (β) το πρώην «statements merge-bind = Needs Achilleas» ΔΕΝ ισχύει πια (χτίστηκε). mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Raw-`<TextInput>` audit (live grep `src/screens/`):** μόνο **ReceiptsScreen 9** (`einput` ×6 store/date/pay/total/name/notes + `cellInput` ×3 qty/net/vat) — **τώρα committed/non-WIP** (όχι πια WIP-blocked)· ItemsScreen/SettingsScreen/ShoppingScreen = 0 raw. **Counts: DONE 8 (parity queue 6/6 + Activity + πλέον merge/bind) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό auto-buildable **functional** parity TODO **κανένα** — πλήρης functional parity. **Ranked top-3 (unattended-safe, non-WIP):** (1) **ReceiptsScreen `einput`→`<Input>`** (τα 5-6 top-level fields store/date/pay/total/notes· P3/S, tsc-verifiable, mobile-only, μηδέν rebuild· άνοιξε τώρα που έγινε commit· τα 3 compact `cellInput` qty/net/vat = attended-preferred visual delta)· (2) fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts`, μηδέν rebuild)· (3) `<Chip>`/`<Badge>`/`<ListItem>` holdout σάρωση για non-WIP raw clusters (P3/S, tsc-verifiable). **Needs Achilleas (ενημερωμένα — αφαιρέθηκε το statements merge-bind, χτίστηκε):** safe-area dep (`react-native-safe-area-context` ΑΠΟΝ), theme toggle + language switcher + light/dark context, AI-engine/storage/OneDrive στα Settings, statements **PDF-import** (θέλει upload endpoint), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board, lucide icon set (μεγάλο cosmetic swap), rate-limit 429 backoff στο mobile `api.ts` (config-gated, off by default).
> **Re-audit 2026-07-05 (parity-auditor, 45η σάρωση· CONFIRMATION, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 route.ts κάτω από `api/v1`** (login + 49 bearer· αμετάβλητο vs 44η), **39 μοναδικά base paths**, **16 mobile screens**. **Route↔consumer (μηχανικός loop των base paths vs `apps/mobile/src/api.ts`):** και τα 39 bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint**. **Δέλτα portable-surface από την 44η (`git log --since=2026-07-04 -- apps/mobile/src apps/web/src/app/api/v1`):** μόνο 2 relevant commits: **`ad226a7`** (`refactor(mobile): ItemsScreen price-log inputs → shared <Input>` — ΕΚΛΕΙΣΕ το top-2 recommendation των 43η/44η· επιβεβαιωμένο live: ItemsScreen raw `<TextInput>` = 0) και **`8d4ab98`** (`feat(api): config-gated rate limiting` — **off by default**, additive· επιστρέφει 429 + Retry-After/X-RateLimit-* μόνο όταν ενεργό μέσω `API_RATE_LIMIT` env· καμία v1 shape change, καμία νέα portable δυνατότητα). Οι υπόλοιποι commits = test-only/marketing (landing PWA, api tests) → **μηδέν νέο mobile feature, μηδέν v1 shape change**. mobile `tsc --noEmit` → **EXIT 0**. **Raw-`<TextInput>` audit (live grep `src/screens/`):** μόνο **ReceiptsScreen 9** (WIP-blocked· ReceiptsScreen στο working tree = uncommitted edits του Αχιλλέα)· ItemsScreen/SettingsScreen/ShoppingScreen = 0 raw → το εναπομείναν Input-debt είναι **εξ ολοκλήρου WIP-blocked**. Working tree στην αρχή: `.claude/launch.json` + 2 deleted `.github/workflows` + Receipts/Settings/Shopping screens = WIP του Αχιλλέα (δεν αγγίχτηκαν). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό auto-buildable functional parity TODO **κανένα**· τα 2 εναπομείναντα `Status: TODO` items (lucide icons P2/M attended-preferred· language switcher P3/L needs-decision) ΔΕΝ είναι unattended auto-buildable. **Ranked top-3 (unattended-safe, non-WIP):** (1) fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts`, μηδέν rebuild)· (2) `<Chip>`/`<Badge>`/`<ListItem>` holdout σάρωση για non-WIP raw clusters (P3/S, tsc-verifiable)· (3) (κανένα άλλο mobile-only unattended-safe· τα υπόλοιπα WIP-blocked ή needs-decision). **Needs Achilleas (αμετάβλητα):** safe-area dep (`react-native-safe-area-context` ΑΠΟΝ), theme toggle + language switcher + light/dark context, AI-engine/storage/OneDrive στα Settings, statements merge-bind + PDF-import (θέλουν write/upload endpoints), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board, lucide icon set (μεγάλο cosmetic swap). **+νέο minor consideration:** το config-gated rate limiting (`8d4ab98`) όταν ενεργοποιηθεί επιστρέφει 429· το mobile `api.ts` δεν έχει 429 retry/backoff handling — off by default τώρα, αλλά αν ο Αχιλλέας το ανάψει σε SaaS χρειάζεται decision για client-side backoff (βλ. ## Needs Achilleas στο PROGRESS).
> **Re-audit 2026-07-04 (parity-auditor, 44η σάρωση· CONFIRMATION, μηδέν νέο functional GAP + 1 doc-fix):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (`find api/v1 -name route.ts` = 50· 50 μοναδικά route bases· αμετάβλητο vs 43η), **16 mobile screens**. **Route↔consumer (μηχανικός loop των 50 base paths vs `apps/mobile/src/api.ts`):** και τα 50 bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint**. **Δέλτα portable-surface από την 43η (`git log eecf89c..HEAD -- apps/mobile/src apps/web/src/app/api/v1`):** **1 μόνο** commit → **`ad226a7`** (`refactor(mobile): ItemsScreen price-log inputs → shared <Input> primitive`) — **ΕΚΛΕΙΣΕ το top-2 item της 43ης** (ItemsScreen `logInput`→`<Input>`). Οι υπόλοιποι 8 commits από την 43η (`ec1bf52` SaaS cost-helper, `0c010e0` test(app-settings), `c914f1d` docs, `66c7903` feat(landing) app-icons, `7873cba` test(storage-config), `388246d`+`ac35ca3` SaaS invites-guard/index, `ba1a725` docs) = **όλοι web-only (SaaS control-plane / test infra / marketing landing / docs)** → **καμία νέα portable mobile δυνατότητα, μηδέν v1 shape change**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Raw-`<TextInput>` audit (live grep `src/screens/`):** ΜΟΝΟ **ReceiptsScreen 9** (WIP-blocked)· ItemsScreen/SettingsScreen/ShoppingScreen = **0 raw** → το εναπομείναν Input-debt είναι **ΑΠΟΚΛΕΙΣΤΙΚΑ** ReceiptsScreen (WIP του Αχιλλέα, δεν αγγίζεται). **DOC-FIX:** ενημέρωσα το UI-Debt «Input primitive» Status (10/11 screens· `ad226a7` έκλεισε το ItemsScreen `logInput`· απομένει μόνο ReceiptsScreen). Working tree στην αρχή: `.claude/launch.json` + Receipts/Settings/Shopping screens = WIP του Αχιλλέα (δεν αγγίχτηκαν). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα**· ο builder δουλεύει UI Debt Queue. **Ranked top-3 (unattended-safe, non-WIP):** (1) fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts` — καθαρά pure, μηδέν rebuild)· (2) **`<Chip>`/`<Badge>` holdout σάρωση** για τυχόν εναπομείναντα non-WIP raw clusters [P3/S, tsc-verifiable]· (3) (κανένα άλλο mobile-only unattended-safe· ReceiptsScreen Input-debt = WIP-blocked, lucide icons + language switcher = attended/needs-decision). **Needs Achilleas (αμετάβλητα):** safe-area dep (`react-native-safe-area-context` ΑΠΟΝ), theme toggle + language switcher + light/dark context, AI-engine/storage/OneDrive στα Settings, statements merge-bind + PDF-import (write/upload endpoints), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board, lucide icon set (cosmetic swap, οπτικό verify). Το ReceiptsScreen WIP κρατά το τελευταίο mobile Input-debt· commit/revert θα το ξεμπλόκαρε.
> **Re-audit 2026-07-03 (parity-auditor, 43η σάρωση· CONFIRMATION, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (`find api/v1 -name route.ts` = 50· login + 49 bearer· αμετάβλητο vs 42η), **39 μοναδικά route bases**, **16 mobile screens**. **Route↔consumer (μηχανικός loop των 39 base paths vs `apps/mobile/src/api.ts`):** και τα 39 bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint**. **Δέλτα portable-surface από την 42η (`git log acba60a..HEAD -- apps/mobile/src apps/web/src/app/api/v1`):** 3 commits, όλα ήδη γνωστά/ακίνδυνα: **`7151d47`** (`refactor(mobile): StatementsScreen installment badge → shared <Badge>` — ΕΚΛΕΙΣΕ το top-3 item #1 της 42ης· επιβεβαιωμένο live: `grep -c '<Badge' StatementsScreen.tsx` = 1, μηδέν raw `s.badge`), **`3d7e3e3`** + **`4f3ea11`** (`test(api)` tasks GET/POST/PATCH/DELETE coverage — test-only, μηδέν runtime/shape change). Μετέπειτα commits (`bf49975` feat(landing) PWA manifest, `7eeb319` test(store)) = **web-only (marketing/test)** → **καμία νέα portable mobile δυνατότητα, μηδέν v1 shape change**. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Raw-`<TextInput>` audit (live grep `src/screens/`):** ItemsScreen 2 (`logInput` ×2, **non-WIP**), ReceiptsScreen 9 (WIP-blocked)· **SettingsScreen + ShoppingScreen = 0 raw** → το εναπομείναν non-WIP Input-debt είναι **ΜΟΝΟ** τα 2 ItemsScreen `logInput` (compact radius 10/pad 10-8/fontSize 14 → visual delta, attended-preferred). Working tree στην αρχή: `.claude/launch.json` + Receipts/Settings/Shopping screens = WIP του Αχιλλέα (δεν αγγίχτηκαν). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα**· τα 2 εναπομείναντα `Status: TODO` items (lucide icons P2/M attended-preferred· language switcher P3/L needs-decision) ΔΕΝ είναι auto-buildable. Ο builder δουλεύει UI Debt Queue. **Ranked top-3 (unattended-safe, non-WIP):** (1) fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts` — καθαρά pure, μηδέν rebuild· ή `lib/scrape.ts` internals με minimal `export`)· (2) **ItemsScreen `logInput`→`<Input>`** [P2/S, το τελευταίο non-WIP raw-input site· visual delta → attended-preferred]· (3) (κανένα άλλο mobile-only unattended-safe· τα υπόλοιπα είναι WIP-blocked ή needs-decision). **Needs Achilleas (αμετάβλητα):** safe-area dep (`react-native-safe-area-context` ΑΠΟΝ), theme toggle + language switcher + light/dark context, AI-engine/storage/OneDrive στα Settings, statements merge-bind + PDF-import (θέλουν write/upload endpoints), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board, lucide icon set (μεγάλο cosmetic swap, οπτικό verify). Τα 3 WIP screens (Receipts/Settings/Shopping) κρατούν το εναπομείναν Receipts Input/hitSlop debt· commit/revert θα το ξεμπλόκαρε.
> **Re-audit 2026-07-03 (parity-auditor, 42η σάρωση· CONFIRMATION, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (`find api/v1 -name route.ts` = 50· login + 49 bearer· αμετάβλητο vs 41η), **16 mobile screens**. **Route↔consumer (μηχανικός loop route-base-paths vs `apps/mobile/src/api.ts`):** και τα 50 route bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint** (incl. `push/register`, `expenses/[id]/rescan`, `items/[id]/{ai-fill,convert-to-task,link-plan,plans,price}`, `receipts/[id]/{add-to-library,rescan}`, `scan/{product,expense,receipt,voucher}`, `trash/[type]/[id]`). **Δέλτα portable-surface:** τελευταίο commit που άγγιξε `apps/mobile/src` = **`acba60a`** (SearchScreen `row`→`<ListItem>`, UI-debt consolidation ήδη committed + reviewed· ΟΧΙ νέα δυνατότητα)· τελευταίο σε `api/v1` = **`7670bf9`** (anomaly badge, ήδη audited). `git log 96150c5..HEAD -- apps/mobile/src apps/web/src/app/api/v1` → μόνο `acba60a`. Μετέπειτα commits (`2fc4893` test(notifiers), `7f4e99e`/`df5f376` feat(landing), `eab4ac9`/`28e0575` docs) = **όλοι web-only (test infra / marketing landing / docs)** → **καμία νέα portable mobile δυνατότητα, μηδέν v1 shape change, μηδέν doc-fix**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Raw-`<TextInput>` audit (live grep `src/screens/`):** ItemsScreen 2 (`logInput` ×2, **non-WIP**), ReceiptsScreen 9 (WIP-blocked)· **SettingsScreen + ShoppingScreen = 0 raw TextInput** (όλα ήδη `<Input>`) → το εναπομείναν non-WIP Input-debt είναι **ΜΟΝΟ** τα 2 ItemsScreen `logInput` (compact radius 10/pad 10-8/fontSize 14 → visual delta, attended-preferred). Working tree στην αρχή: `.claude/launch.json` + Receipts/Settings/Shopping screens = WIP του Αχιλλέα (δεν αγγίχτηκαν). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder δουλεύει UI Debt Queue. **Ranked top-3 (unattended-safe, non-WIP):** (1) **StatementsScreen `badge`→`<Badge>`** [P3/S· επόμενο shared-primitive debt μετά το SearchScreen `acba60a`· tsc-verifiable]· (2) fresh pure-lib **vitest coverage** (`lib/scrape.ts` internal `parsePriceNum`/`extractPrimaryPrice`/`decodeEntities`/`isBotChallenge` — θέλει minimal `export`, αγγίζει web runtime → safe rebuild· ή `lib/cards.ts`/`lib/taxonomies.ts normalizeList`/`lib/itemStatus.ts` — καθαρά pure, μηδέν rebuild)· (3) **ItemsScreen `logInput`→`<Input>`** [P2/S, το τελευταίο non-WIP raw-input site· αλλά visual delta → attended-preferred]. **Needs Achilleas (αμετάβλητα):** safe-area dep (`react-native-safe-area-context` ΑΠΟΝ), theme toggle + language switcher + light/dark context, AI-engine/storage/OneDrive στα Settings, statements merge-bind + PDF-import (θέλουν write/upload endpoints), extra reports charts (charting lib), remote push (EAS + APNs), Tasks Kanban board. Τα 3 WIP screens (Receipts/Settings/Shopping) κρατούν το εναπομείναν Receipts Input/hitSlop debt· commit/revert θα το ξεμπλόκαρε.
> **Re-audit 2026-07-03 (parity-auditor, 41η σάρωση· CONFIRMATION, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (`find api/v1 -name route.ts` = 50· login + 49 bearer· αμετάβλητο vs 40ή), **16 mobile screens**. **Route↔consumer (μηχανικό loop route-base-paths vs `apps/mobile/src/api.ts`):** και τα 50 route bases έχουν ≥1 mobile consumer → **μηδέν orphan endpoint** (incl. `push/register`)· deep-route spot-check: `ai-fill`×1, `convert-to-task`×1, `link-plan`×2, `add-to-library`×1, `rescan`×4, `expenses/[id]/rescan`×1, `price`×11 → όλα ≥1. **Δέλτα portable-surface:** τελευταίο commit που άγγιξε `apps/mobile/src` = **`96150c5`** (Button variants, ήδη audited)· τελευταίο σε `api/v1` = **`7670bf9`** (anomaly badge, ήδη audited). Οι μετέπειτα commits (`061b3fa` test(notify), `88e44c6`/`cfaddd8` docs, `12c46e8` feat(landing)) είναι **όλοι web-only (test infra / docs / marketing landing)** → **καμία νέα portable mobile δυνατότητα, μηδέν v1 shape change, μηδέν doc-fix**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). Working tree στην αρχή: `.claude/launch.json` + Receipts/Settings/Shopping screens = WIP του Αχιλλέα (δεν αγγίχτηκαν). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable functional GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe, non-WIP· mirror του 38ου ui-audit):** (1) **Input finish σε ItemsScreen `logInput`** [P2/S, `<Input>` ήδη σε `ui.tsx`, 9/11 screens migrated· structural, tsc-verifiable]· (2) **SearchScreen `row`→`<ListItem>`** [P3/S]· (3) **StatementsScreen `badge`→`<Badge>`** [P3/S]. Εναλλακτικά unattended-safe: fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts`). Attended-preferred: lucide icons [P2/M], Settings `rm`/`swatch` hitSlop [P2/S, WIP-blocked]. **Needs Achilleas (αμετάβλητα):** safe-area dep, language switcher + light/dark theme, AI-engine/storage/OneDrive στα Settings, statements merge-bind + PDF-import, extra reports charts (charting lib), remote push (EAS + APNs). Τα 3 WIP screens (Receipts/Settings/Shopping) κρατούν το μεγαλύτερο UI debt· commit/revert θα το ξεμπλόκαρε.
> **Builder 2026-07-02 (Expenses/Income re-scan):** ✅ **Expenses/Income AI re-scan DONE** (top auto-buildable P2/M item). Νέο `POST /api/v1/expenses/[id]/rescan` = byte-mirror του `receipts/[id]/rescan` (`withAuth` + `isObjectId` 400-guard + `readBody {ocr?}` → `rescanExpense(id, useOcr)` → re-read `-rawAiResponse` → GET-expenses shape). Εξήγαγα κοινό `expenses/serialize.ts` (`trimExpense` + `ExpenseLean`, μοναδική πηγή αλήθειας του v1 Expense JSON) και refactor-άρισα το list `route.ts` να το χρησιμοποιεί (μηδέν shape drift). mobile `rescanExpense(id, ocr)` σε api.ts + «Re-scan text/OCR» bar στο MoneyScreen edit modal (μόνο όταν `editing.file`, re-prefill in-place, μένει unverified). Δομικό verify μόνο (no-auth 401 + tsc EXIT 0)· κανένα πραγματικό AI scan (κόστος). Εναπομείνας auto-buildable: 0· attended-preferred: lucide icons [P2/M], language switcher [P3/L].
> **Builder 2026-07-03 (Button ghost/danger variant, 4 screens):** ✅ **`<Button variant>` DONE** (το reusable holdout που πρότεινε ο Chip note `681098f`, ~7 sites). Το `Button` primitive (`ui.tsx`) πήρε `variant?: 'primary' | 'danger' | 'ghost'` (default `primary` = αμετάβλητη accent pill). `danger` = το byte-identical `delBtn`+`delBtnText` text-button (padH12/padV12 = SPACE.md, red/SIZE.md/600) που ήταν duplicated σε Money/Subscriptions/Items/Vouchers· `ghost` = dim "Cancel" (ίδιο container, C.dim/SIZE.md). Migrated **6 call sites σε 4 screens**: MoneyScreen (Discard + Delete), SubscriptionsScreen (Delete), ItemsScreen (Delete), VouchersScreen (Delete danger + Cancel ghost) → διαγράφηκαν τα πλέον-unused `delBtn`/`delBtnText`/`cancelText` StyleSheet entries και στα 4. Pixel-identical (SPACE.md=12, SIZE.md=15 → 1:1 με τα literals). Ανέγγιχτα (WIP Αχιλλέα): Receipts/Settings/Shopping — τα δικά τους cancel/del buttons + το Settings bordered `cancelBtn` μένουν για attended follow-up. mobile `npx tsc --noEmit` **EXIT 0**. Καθαρά mobile-only → καμία Docker rebuild, κανένα AI call.
> **Builder 2026-07-03 (Input primitive migration, 3 screens):** ✅ **`<Input>` migration προχώρησε 6/11 → 9/11 screens.** Μετέτρεψα τα raw `<TextInput style={s.input}>` σε `<Input>` (`ui.tsx`) στα **LoginScreen** (3 fields: server/username/password), **SearchScreen** (1 field), **AssistantScreen** (1 chat field). Καθένα έριξε το τοπικό `placeholderTextColor={C.faint}` + το dead `s.input` StyleSheet entry (η primitive τα ορίζει). Layout-affecting props διατηρήθηκαν μέσω `style` override: Search `{flex:1}`, Assistant `{flex:1, maxHeight:120, borderRadius:14}`· Login fields παίρνουν την default surface variant (spacing από `label` marginTop, μηδέν margin στο input → αμετάβλητο). **Token normalization (σκόπιμο, ο σκοπός της primitive):** paddingHorizontal 14→12 (SPACE.md) και τα τρία· Search fontSize 16→15 (SIZE.md)· Login paddingVertical 12→11· Assistant borderRadius 14 διατηρήθηκε ρητά. Έμειναν 2 screens: **ReceiptsScreen** (WIP του Αχιλλέα, δεν αγγίχτηκε) + **ItemsScreen** (compact paired price/store log inputs `logInput` borderRadius:10/pad 10-8/fontSize:14 → μεγαλύτερο visual delta, attended-preferred). mobile `npx tsc --noEmit` **EXIT 0**. Καθαρά mobile-only → καμία Docker rebuild, κανένα AI call.
> **Builder 2026-07-02 (commit `681098f`):** ✅ **`<Chip>` primitive DONE.** Το toggle-pill holdout (6 byte-identical clusters / 5 screens: Items filter+status, Subscriptions cycle, Settings currency/card-kind/type, Tasks tag, Reports range) ενοποιήθηκε σε `ui.tsx` `<Chip label on onPress style? textStyle?>` (style-override idiom, pixel-identical). Άφησα ανέγγιχτα τα Settings `addChip`/`valChip` (μη-toggle). Επόμενο reusable holdout = **ghost `<Button variant>`** (~7 sites). mobile tsc EXIT 0.
> Ranked για τον builder routine· paίρνει το πρώτο TODO. P1+S πρώτα. Ο πυρήνας του parity είναι κλειστός (CRUD/scan/PricePanel/Activity/Calendar/Reports), οπότε ΔΕΝ υπάρχουν P1/S· ό,τι έμεινε είναι secondary. Όσα έχουν AI cost → δομικό verify μόνο (no token-spend).
> **Re-audit 2026-07-03 (parity-auditor, 40ή σάρωση· CONFIRMATION, μηδέν νέο functional GAP):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (`find api/v1 -name route.ts` = 50· login + 49 bearer· +1 vs προηγ. «49» = το `expenses/[id]/rescan` του 2201932, ήδη consumed), **16 mobile screens**. **Route↔consumer:** mechanical loop route-base-paths vs `apps/mobile/src/api.ts` → **κάθε route base έχει ≥1 mobile consumer, μηδέν orphan endpoint**· spot-check deep routes: `expenses/[id]/rescan`→`rescanExpense` ✓, expenses GET `anomaly`→3 refs στο MoneyScreen ✓. **Δέλτα από τελευταίο audit (38η/39η):** τα 2 flagged gaps (anomaly badge + vendor autocomplete) **ΕΚΛΕΙΣΑΝ** (commits `7670bf9` + `148fb06`, ήδη αποτυπωμένα στο parity table γρ.40-41) → **μηδέν doc-fix**. Post-`148fb06` commits (`c9367e2` landing 404, `7de30bc` test(ocr), `2fe2483`/`4a240aa` docs) είναι **όλοι web-only** (marketing site `apps/landing` / test infra / docs) → **καμία νέα portable mobile δυνατότητα**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). Working tree στην αρχή: μόνο `.claude/launch.json` (foreign tooling, δεν το αγγίζω). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe πρώτο):** (1) **Input primitive migration finish** [P1/S, `<Input>` ήδη σε `ui.tsx`, 6/11 screens migrated → 5 απομένουν· structural, μηδέν dep, tsc-verifiable]· (2) **Button ghost-variant finish** [P2/S, ~7 holdout sites κατά τον builder note `681098f`· structural, unattended-safe]· (3) **icons→lucide** [P2/M, `react-native-svg@15.12.1` ΥΠΑΡΧΕΙ → μηδέν νέα native dep, αλλά attended-preferred για οπτικό verify]. Εναλλακτικά unattended-safe: fresh pure-lib **vitest coverage** (`lib/cards.ts` / `lib/taxonomies.ts` / `lib/itemStatus.ts`). **Safe-area insets** (native dep `react-native-safe-area-context` ΑΠΟΝ), **language switcher** (persist dep + 16-screen string-extract), **light/dark theme context** = attended/Needs Achilleas. Statements merge-bind / PDF-import + extra reports charts + theme/language/AI-engine/storage/OneDrive = **Needs Decision** (νέα endpoints / charting lib / credentials / product).
> **Re-audit 2026-07-02 (parity-auditor, 5η σάρωση ημέρας· CONFIRMATION, ουρά αμετάβλητη):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**. **Route↔consumer:** grep στο `apps/mobile/src/api.ts` → κάθε ένα από τα 49 route paths έχει ≥1 mobile consumer (deep sub-routes επιβεβαιωμένα ένα-ένα: `ai-fill`/`convert-to-task`/`link-plan`/`plans`/`price`/`add-to-library`/`rescan` όλα ≥1) → **1:1, μηδέν orphan endpoint**. **Δέλτα portable-surface:** `git log 268efb4..HEAD -- apps/mobile/src apps/web/src/app/api/v1` = **ΚΕΝΟ**. Το τελευταίο commit που άγγιξε mobile/src είναι `268efb4` (Tasks tag-filter, ήδη audited)· api/v1 στο `bba44ff` (reports date-range). Οι μετέπειτα commits (`e741999`/`5544822` landing, `100d1d6` SaaS invites `?status`, `2e5e7da` test, `94a5d20`/`4eaa702`/`e7cd4d6`/`bc525f6` docs/health) είναι **όλοι web-only (landing / SaaS control-plane / tests / docs)** → **καμία νέα portable mobile δυνατότητα**. **Spot-check DONE claims στον κώδικα (όχι doc):** ItemsScreen wiring (PricePanel/link-plan/convert-to-task/ai-fill/log-price) ✓, ReceiptsScreen (rescan/verify/add-to-library/line-items) ✓, StatementsScreen (plans overview + per-charge installment) ✓, TasksScreen (tag-filter + progress + steps + status) ✓ → doc rows ακριβή, **μηδέν doc-fix**. `statements/route.ts` + `statements/plans/route.ts` + `reports/route.ts` = **GET-only** (grep επιβεβαίωση) → merge/bind + PDF-import + extra charts θέλουν νέα write/upload endpoints (Needs Decision). **Deps (live grep `apps/mobile/package.json`):** `react-native-svg@15.12.1` ΥΠΑΡΧΕΙ· `safe-area-context`/`async-storage`/gesture/reanimated/charting-lib ΑΠΟΝ. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). Working tree στην αρχή: μόνο `.claude/launch.json` (foreign tooling, δεν το αγγίζω). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe πρώτο):** (1) **Max content width** `<Screen>` wrapper [P3/S· ο builder να επιβεβαιώσει αν ΗΔΗ DONE από `28c943c` πριν το ξαναχτίσει]· (2) fresh pure-lib **vitest coverage** σε untested helper (`lib/cards.ts` statement-title / `lib/taxonomies.ts normalizeList` / `lib/itemStatus.ts`) [P3/S, unattended-safe, vitest-verifiable]· (3) **`<Chip>` primitive** [P2/M, attended-preferred για οπτικό verify]. **icons→lucide** (P2/M, μηδέν native dep αλλά attended-preferred) εναλλακτικά. **Safe-area / language-persist / charting-lib** = Needs Achilleas (native dep απόν). Statements merge-bind / PDF-import + extra reports charts + theme/language/AI-engine/storage = **Needs Decision** (νέα endpoints / charting lib / credentials).
> **Re-audit 2026-07-02 (parity-auditor, 4η σάρωση ημέρας· CONFIRMATION, ουρά αμετάβλητη):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`**. **Route↔consumer (μηχανικό `comm` route-paths vs consumed-paths από `apps/mobile/src/api.ts`):** και τα 49 normalized route paths έχουν ≥1 mobile consumer (`comm -23` = ΚΕΝΟ)· το φαινομενικό `tasksID` είναι grep artifact template-literal → **1:1, μηδέν orphan endpoint**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (web-only first-run wizard· N/A). **Δέλτα portable-surface:** το **τελευταίο** commit που άγγιξε είτε `apps/mobile/src` είτε `api/v1` είναι **`e6ad795`** (feat(mobile) Reports parity: spend-by-store + biggest-purchases + warranties-expiring — mobile screen + reports data)· `git log e6ad795..HEAD -- apps/mobile/src apps/web/src/app/api/v1` = **ΚΕΝΟ**. Οι μετέπειτα commits (`a082819` SaaS account self-service, `35953c6`/`b4ec753` SaaS password-reset+harden, `4dfac8d`/`c253bab` landing JSON-LD+a11y, `d6b1ef9`/`34045b9` i18n vitest suites, review/docs markers) είναι **όλοι web-only (SaaS control-plane / landing / tests / docs)** → **καμία νέα portable mobile δυνατότητα**. **SaaS API surface (15 routes κάτω από `api/saas/*`: account/auth/billing/members/usage) είναι control-plane, gated πίσω από `SAAS_MODE`, ΟΧΙ `v1` → ΟΧΙ mobile parity items** (multi-tenancy + billing = product/architecture decisions, βλ. Needs Achilleas). Το e6ad795 έχει ΗΔΗ αποτυπωθεί στο row Reports (γραμμή 66) από την 31η ui-σάρωση → **κανένα doc-fix αυτόν τον γύρο**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). Working tree στην αρχή: **μόνο** `.claude/launch.json` (foreign tooling, δεν το αγγίζω). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe πρώτο):** (1) **Max content width** `<Screen>` wrapper [P3/S· ΣΗΜ: πιθανώς ΗΔΗ DONE από `28c943c` shared max-content-width — ο builder να το επιβεβαιώσει πριν το ξαναχτίσει]· (2) **`<Chip>` primitive** [P2/M, τελευταίο reusable-component holdout· `chip`/`chipOn`/`cChip`/`sChip` σε 3 screens· token-drift → attended-preferred για οπτικό verify]· (3) **icons→lucide** [P2/M, μηδέν native dep — `react-native-svg@15.12.1` ΥΠΑΡΧΕΙ — αλλά attended-preferred]. Εναλλακτικά unattended-safe: fresh pure-lib **test coverage** (`lib/cards.ts` / `lib/taxonomies.ts` / `lib/itemStatus.ts`, pure+untested+vitest-verifiable). **Safe-area insets** παραμένει Needs Achilleas (native dep `react-native-safe-area-context` ΑΠΟΝ). Οι «partial» rows (extra reports charts [inventory-pie/subs-by-cat/income-vs-expense-12mo → charting lib] / statements merge-bind / PDF-import → νέα endpoints· theme/language/AI-engine/storage → decision/credentials) παραμένουν **Needs Decision**.
> **Re-audit 2026-07-02 (parity-auditor, 3η σάρωση ημέρας· ουρά αμετάβλητη + doc-fix):** inventory ξαναχτισμένο από τον κώδικα. **49 v1 routes** (`find api/v1 -name route.ts` = 49), **16 mobile screens**, **19 web `page.tsx`**. **Route↔consumer:** grep στο `apps/mobile/src/api.ts` → κάθε endpoint ≥1 mobile consumer (1:1, μηδέν orphan). **Δέλτα από τον marker `9283367`:** `git log 9283367..HEAD -- apps/mobile/src apps/web/src/app/api/v1` = **ΚΕΝΟ** → μηδέν νέο mobile-src, μηδέν v1-API shape change. Οι νέοι commits (`31663ae` landing footer, `9ecb86c` SaaS billing GET, `56bb22d` landing JSON-LD, `561b92a`/`063192a` vitest suites) είναι **όλοι web-only (landing/SaaS/tests)** → καμία portable mobile δυνατότητα. **`git log -- apps/mobile/src` τελευταίο ΑΚΟΜΑ `0f69116`** (ListItem)· **`git log -- api/v1` τελευταίο ΑΚΟΜΑ `6fd1075`** (readBody, byte-identical shapes). mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **DOC-FIX:** το row Reports (γραμμή 66) έλεγε λανθασμένα ότι το mobile δεν έχει «upcoming-installments», αλλά ο κώδικας (`ReportsScreen.tsx:85` «UPCOMING INSTALLMENTS · NEXT 6 MONTHS», τροφοδοτείται από `getReports().upcomingInstallments`) το ΗΔΗ renders → διορθώθηκε (flag από pharos-daily-dev entry 2026-07-02). **Counts: DONE 7 (parity queue 6/6 + Activity) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Top-3 (unattended-safe πρώτο):** (1) **Max content width** `<Screen>` wrapper [P3/S, μηδέν dep, structural, no-op σε phone]· (2) **icons→lucide** [P2/M, μηδέν native dep — `react-native-svg@15.12.1` ΥΠΑΡΧΕΙ — αλλά attended-preferred για οπτικό verify]· (3) **Safe-area insets** [P2/M, ΜΟΝΟ αν εγκριθεί native dep `react-native-safe-area-context`, ΑΠΟΝ]. Οι «partial» rows (extra reports charts / statements merge-bind / PDF-import → νέα endpoints· theme/language/AI-engine/storage → decision/credentials) παραμένουν **Needs Decision**.
> **Re-audit 2026-07-02 (parity-auditor, 2η σάρωση ημέρας· CONFIRMATION, ουρά αμετάβλητη):** inventory ξαναχτισμένο από τον κώδικα. **49 v1 routes** (`find api/v1 -name route.ts` = 49), **16 mobile screens**, **19 web `page.tsx`**. **Route↔consumer:** `grep` στο `apps/mobile/src/api.ts` → **50 distinct `/api/v1/*` paths** consumed (49 routes + `auth/login`) = **1:1, μηδέν orphan endpoint**. **Δέλτα από την 1η σάρωση:** μόνο 2 νέα commits, αμφότερα web-only — `3d7e50f` (test(web) `cardFields.ts` pure-lib suite, μηδέν runtime) + `a615c15` (feat(landing) self-host vs hosted comparison table, `apps/landing` isolated). **`git log -- apps/mobile/src` τελευταίο ΑΚΟΜΑ `0f69116`** (ListItem)· **`git log -- api/v1` τελευταίο ΑΚΟΜΑ `6fd1075`** (readBody, byte-identical shapes) → **μηδέν νέα portable web δυνατότητα, μηδέν νέο mobile-src, μηδέν API shape change**. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue 6/6 + activity) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue** (top unattended-safe: Max-content-width P3/S, μετά Safe-area αν εγκριθεί το dep). Working tree στην αρχή: μόνο `.claude/launch.json` (foreign tooling, δεν το αγγίζω). Οι «partial» rows παραμένουν **Needs Decision** (extra reports charts / statements merge-bind / PDF-import → νέα endpoints· theme/language/AI-engine/storage → decision/credentials boundary).
> **Re-audit 2026-07-02 (parity-auditor, 1η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά). **Route↔consumer επαλήθευση (live grep στο `apps/mobile/src/api.ts`):** κάθε ένα από τα 49 routes έχει ≥1 mobile consumer· grep-count των deep sub-routes: `ai-fill` 1, `convert-to-task` 1, `link-plan` 2, `plans` 5, `price` 11, `add-to-library` 1, `rescan` 2, `push/register` (registerPush/unregisterPush POST+DELETE), `scan/{product,expense,receipt,voucher}` όλα consumed → **μηδέν «endpoint χωρίς mobile consumer» gap, 1:1**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (web-only first-run wizard· N/A). **App-code diff από `43d3b3c` (τελευταίο parity baseline):** οι μόνοι app-src commits είναι (α) pure-lib **test suites** (`ca2d79f` money, `71fd79d` storagePath, `58b250d` dates, `9ebeea2` apiBody, `0b136b1` installments — test infra, μηδέν runtime code)· (β) **API refactors** readBody/isObjectId (`18b611f`, `f17f279`, `4503640`, `6fd1075` = **Web Debt cleanup**, byte-identical body-parse, response shapes αμετάβλητα → mobile ασφαλές)· (γ) mobile **UI-Debt primitives** `Badge` (`ee9d413`) + `<ListItem>` (`0f69116`, πλέον committed → κλείνει το set «Card+Badge+ListItem»)· (δ) **SaaS scaffold** `ceb65c6` (control-plane models Tenant/Account/Membership + `SAAS_MODE`), `1e5dc4c`/`1a7d16e` (tenant resolver + per-tenant connection), `2b803cc` (account auth signup/login/logout/session), **`3c6bcc4` billing scaffold** (plans/entitlements/Stripe client + `api/saas/billing/webhook`) — **όλα server-only infra, gated πίσω από `SAAS_MODE`, μηδέν user-facing route/page στο single-user path → ΟΧΙ mobile parity items** (multi-tenancy + billing = product/architecture decisions, βλ. Needs Achilleas). **Καμία νέα portable web δυνατότητα προς port.** Working tree στην αρχή: **μόνο** `.claude/launch.json` (foreign tooling, δεν το αγγίζω· μηδέν WIP src του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): `reports/route.ts` & `statements/plans/route.ts` **GET-only** → extra charts / merge-bind / PDF-import θέλουν νέα endpoints· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe πρώτο):** (1) **Max content width** (P3/S, UI Debt Queue) — καθαρά auto-buildable: μηδέν dep, μικρό, structural verify (νέο `<Screen>` wrapper με maxWidth+center, no-op σε phone/μόνο tablet centering)· (2) **icons→lucide** (P2/M) — μηδέν native dep (`react-native-svg@15.12.1` ΥΠΑΡΧΕΙ) αλλά attended-preferred για οπτικό verify· (3) **Safe-area insets** (P2/M) — ΜΟΝΟ αν εγκριθεί το native dep `react-native-safe-area-context` (ΑΠΟΝ στο package.json· βλ. Needs Achilleas). Εναλλακτικά (η raw-body web ουρά ΕΚΛΕΙΣΕ πλήρως — 0 raw routes): fresh pure-lib **test coverage** (`lib/cards.ts` / `lib/taxonomies.ts` / `lib/itemStatus.ts`, όλα pure+untested+unattended-safe+vitest-verifiable). «Partial» rows: `reports/route.ts` & `statements/plans/route.ts` **GET-only** → extra charts / merge-bind / PDF-import θέλουν νέα endpoints· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary.
> **Re-audit 2026-07-01 (parity-auditor, 22η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens** (`src/screens/*`), mobile `api.ts`. Επαλήθευσα coverage προγραμματιστικά: normalized route-paths vs `/api/v1/*` refs στο `api.ts` → η **μόνη** φαινομενική διαφορά ήταν `/trash/:type/:id`, που είναι **artifact κανονικοποίησης** (χρησιμοποιείται στο `api.ts:400/403` ως `` `/api/v1/trash/${type}/${id}` `` PATCH/DELETE) → **49/49 routes με ≥1 mobile consumer, μηδέν «endpoint χωρίς consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (web-only first-run wizard· N/A). **Νέο από την 21η:** το mobile `<ListItem>` (top UI-Debt build item) **έγινε commit** (`0f69116`, ήταν uncommitted WIP στην 25η ui-σάρωση) → κλείνει το set «Card + Badge + ListItem»· τελευταίος mobile-src commit = `0f69116`. **Dep facts για το ranking (live grep στο `package.json`):** (α) `react-native-svg@15.12.1` **ΥΠΑΡΧΕΙ** → `lucide-react-native` = **μηδέν νέα native dep** (μόνο JS πακέτο)· όμως **attended-preferred** (10 αρχεία με glyph icons, οπτικό verify)· (β) `react-native-safe-area-context` **ΑΠΟΝ** → το Safe-area item **χρειάζεται native dep-add** (`npx expo install`), **ΟΧΙ unattended-safe / non-verifiable χωρίς simulator**· (γ) `@react-native-async-storage/async-storage` **ΑΠΟΝ** → το language-switcher persist χρειάζεται dep-add ή reuse του υπάρχοντος `expo-secure-store`. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό functional parity TODO **κανένα** → ο builder πέφτει στο **UI Debt Queue**. **Ranked top-3 (unattended-safe πρώτο):** (1) **Max content width** (P3/S, UI Debt Queue) — καθαρά auto-buildable: μηδέν dep, μικρό, structural verify (νέο `<Screen>` wrapper με maxWidth+center)· (2) **icons→lucide** (P2/M) — μηδέν native dep αλλά attended-preferred για οπτικό verify· (3) **Safe-area insets** (P2/M) — ΜΟΝΟ αν εγκριθεί το native dep από τον Αχιλλέα (βλ. Needs Achilleas). Εναλλακτικά fresh web-debt σάρωση (εναπομείναντες raw-body routes). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο GAP): `reports/route.ts` & `statements/plans/route.ts` **GET-only** → extra charts / merge-bind / PDF-import θέλουν νέα endpoints· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary.
> **Re-audit 2026-07-01 (parity-auditor, 21η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **130 exported symbols** στο `apps/mobile/src/api.ts`. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes· επιβεβαιωμένα ξεχωριστά με grep-count ΟΛΑ τα deep sub-routes: `ai-fill` 1, `convert-to-task` 1, `link-plan` 2, `plans` 2, `price` 11, `add-to-library` 1, `rescan` 2, `test-notify` 1, `items/import` 1 → όλα ≥1 consumer, **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (web-only first-run wizard· N/A). **App-code diff από `43d3b3c` (τελευταίο parity baseline):** 23 files / +272 −36, **καμία νέα portable web δυνατότητα προς port**: (α) API refactors readBody/isObjectId (`18b611f`, `f17f279` κ.ά. = **Web Debt cleanup**, byte-identical body-parse, response shapes αμετάβλητα)· (β) `ee9d413` mobile `Badge` primitive (**UI Debt**, ήδη DONE)· (γ) `ca2d79f` Vitest + `money.test.ts` (test infra)· (δ) **`ceb65c6` SaaS control-plane models `Tenant`/`Account`/`Membership` + `SAAS_MODE` flag** — **server-only infra, ΟΧΙ user-facing web δυνατότητα** (μηδέν route/page, gated behind `SAAS_MODE`) → **δεν είναι mobile parity item** (βλ. Needs Achilleas: πολυ-tenancy = product/architecture decision, όχι auto-buildable). Working tree στην αρχή: **μόνο** `.claude/launch.json` (foreign tooling, δεν το αγγίζω· μηδέν WIP src του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): `reports/route.ts` **GET-only**, `statements/plans/route.ts` **GET-only** → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (κορυφαίο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· μετά `<ListItem>` primitive· `<Chip>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify) ή fresh web-debt σάρωση (εναπομείναντες raw-body routes: `auth/login`, `items/[id]/ai-fill`, `shopping-list`, `scan/expense`, `scan/voucher`).
> **Re-audit 2026-07-01 (parity-auditor, 20η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **81 exported api fns** στο `apps/mobile/src/api.ts`. **Route↔consumer diff (μηχανικό `comm`):** εξήγαγα τα 49 route paths + τα consumed v1 paths από το mobile `api.ts` (normalized `${...}`→`[id]`)· 45/49 ταίριαξαν άμεσα, τα 4 «no consumer» (`receipts`, `search`, `statements`, `trash/[type]/[id]`) ήταν **artifacts** query-string/`[type]` (grep επιβεβαίωσε: `receipts?limit=100` :188, `search?q=` :383, `statements?limit=100` :283, `trash/${type}/${id}` PATCH :400 + DELETE :403) → **μηδέν «endpoint χωρίς mobile consumer» gap, 1:1**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (web-only first-run wizard· N/A). **App-code diff από `43d3b3c` (προηγ. parity marker = 19η σάρωση):** μόλις **2** app-src commits, **καμία νέα portable web δυνατότητα**: `f17f279` (`refactor(api)` isObjectId dedup 4η/τελική παρτίδα, 7 deep sub-route files items/[id]/{ai-fill,convert-to-task,link-plan,plans,price}+receipts/[id]/{add-to-library,rescan}· `--stat` 7 files/+15 −8· byte-identical guard `/^[a-f0-9]{24}$/i`, response shapes αμετάβλητα = **Web Debt cleanup**) + `ee9d413` (`refactor(mobile)` shared `Badge` primitive για status/type chips· `--stat` 4 files/+37 −14 = **UI Debt**, byte-identical με overrides). Τα ενδιάμεσα commits = docs (review/docker-health) + marker. Working tree **καθαρό** στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): `reports/route.ts` **GET-only**, `statements/plans/route.ts` **GET-only** → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (κορυφαίο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· μετά `<ListItem>` primitive· `<Chip>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify) ή fresh web-debt σάρωση (readBody adoption στα ~13 raw-body routes).
> **Re-audit 2026-07-01 (parity-auditor, 19η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (`find api/v1 -name route.ts` = 49· login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **72 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes· επιβεβαιωμένα ξεχωριστά ΟΛΑ τα deep sub-routes με ακριβές grep (interpolated path, όχι substring): `items/[id]/plans` (getItemPlans :237), `link-plan` (:240/:243), `convert-to-task` (:246), `ai-fill` (:253), `price` (:226), `items/import` (:509), `receipts/[id]/rescan` (:210), `add-to-library` (:458), `statements/plans` (:298), `settings/test-notify` (:334), `scan/{product,expense,receipt,voucher}` (:108/:168/:198/:502) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `0fa3365` (προηγ. parity marker = 18η σάρωση):** **1 μόνο** app-src commit — `c5cec57` (`refactor(api)` isObjectId dedup 3η παρτίδα, 5 route files cards/[id]+stores/[id]+statements/[id]+notifications+trash/[type]/[id]· `--stat` = 5 files / +13 −10, byte-identical guard `/^[a-f0-9]{24}$/i` μετακόμισε στο `@/lib/apiBody`, response shapes αμετάβλητα) → **καμία νέα portable web δυνατότητα προς port**. Μηδέν mobile-src commit (`git diff --stat 0fa3365..HEAD -- apps/mobile/src` = κενό). Τα ενδιάμεσα commits = docs (review/docker-health) + το marker. Working tree **καθαρό** στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports `route.ts` GET-only, Statements `plans/route.ts` GET-only → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (κορυφαίο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· μετά `<ListItem>` primitive· `<Chip>`/`<Badge>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify) ή fresh web-debt σάρωση (isObjectId 4η παρτίδα = items/[id]/{ai-fill,convert-to-task,link-plan,plans,price} + receipts/[id]/{add-to-library,rescan}).
> **Re-audit 2026-07-01 (parity-auditor, 18η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά). Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list, 50 distinct route-strings ≥ 49 routes· κάθε deep sub-route [`ai-fill`/`convert-to-task`/`link-plan`/`plans`/`price`/`add-to-library`/`rescan`/`test-notify`/`items/import`] ≥1 consumer) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `ed352c4` (προηγ. parity marker = 17η σάρωση):** **1 μόνο** app-src commit — `f1413c3` (`refactor(api)`: shared `isObjectId()` guard μεταφέρθηκε στο `@/lib/apiBody`, dedup 2η παρτίδα σε 5 `[id]` routes `expenses`/`receipts`/`subscriptions`/`tasks`/`vouchers` = **Web Debt cleanup**· `git diff --stat` = 5 files / +15 −14, byte-identical body-parse, response shapes `{item}`/`{voucher}`/`{ok:true}` αμετάβλητα) → **καμία νέα portable web δυνατότητα προς port**. Τα ενδιάμεσα commits = docs (web-debt 20ή, ui-auditor 19η, monitor, review) + docker-health. Working tree **καθαρό** στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports `route.ts` GET-only, Statements `plans/route.ts` GET-only → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (κορυφαίο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· μετά `<ListItem>` primitive· `<Chip>`/`<Badge>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify) ή fresh web-debt σάρωση.
> **Re-audit 2026-07-01 (parity-auditor, 17η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **81 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes· επιβεβαιωμένα ξεχωριστά με grep-count ΟΛΑ τα deep sub-routes: `items/[id]/ai-fill` 1, `convert-to-task` 1, `link-plan` 2, `plans` 2, `price` 1, `receipts/[id]/add-to-library` 1, `rescan` 2, `settings/test-notify` 1, `items/import` 4 → όλα ≥1 consumer, **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `bead394` (προηγ. parity marker `bead394` = 15η/16η):** μόλις **3 web files + 2 mobile files** — `d259a55` (shared `isObjectId()` guard + `readBody` σε items/[id] & shopping-list/[id] PATCH = **Web Debt cleanup**, byte-identical body-parse, response shapes `{item}`/`{ok:true}` αμετάβλητα) + `3a272c1` (mobile Button-family finish = **UI Debt**). `git diff bead394..HEAD -- apps/web/src` = 3 files, όλα refactor· **καμία νέα portable web δυνατότητα προς port**. Τα ενδιάμεσα commits = docs (web-debt 18η/19η, ui-auditor 17η/18η, monitor, review) + docker-health. Working tree **καθαρό** στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports `route.ts` GET-only, Statements `plans/route.ts` GET-only → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Button-family ΕΚΛΕΙΣΕ [3a272c1]· κορυφαίο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· μετά `<ListItem>` primitive· `<Chip>`/`<Badge>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify) ή fresh web-debt σάρωση.
> **Re-audit 2026-07-01 (parity-auditor, 16η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **81 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes· επιβεβαιωμένα ξεχωριστά ΟΛΑ τα deep sub-routes (`items/[id]/{ai-fill,convert-to-task,link-plan,plans,price}`, `receipts/[id]/{add-to-library,rescan}`, `settings/test-notify`, `push/register`, `items/import` όλα ≥1 consumer) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `bead394` (προηγ. parity marker):** **1 μόνο** app-src commit — `3a272c1` (mobile Button-family finish: ShoppingScreen `addBtnWide` + ItemsScreen `save` outlier → `<Button>` = **UI Debt**, byte-identical-ish)· `git diff bead394..HEAD -- apps/web/src` = **ΚΕΝΟ** → **καμία νέα portable web δυνατότητα προς port**. Τα ενδιάμεσα commits = docs (web-debt 17η/18η, ui-auditor 16η/17η, monitor) + docker-health. Working tree **καθαρό** στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports `route.ts` **GET-only** (line 24), Statements `plans/route.ts` **GET-only** (line 16) → extra charts / merge-bind / PDF-import θέλουν νέα endpoints + RN charting lib· Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Button-family ΕΚΛΕΙΣΕ [3a272c1]· επόμενο unattended-safe = Safe-area insets [`SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift]· `<Chip>`/`<Badge>`/`<ListItem>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify). Αλλιώς fresh web-debt σάρωση.
> **Re-audit 2026-07-01 (parity-auditor, 15η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income + setup ξεχωριστά), **72 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list· τα `tasks[id]`/`trash/[id]/[id]` = grep artifacts από template-literal concat) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `d235eff` (προηγ. parity marker):** 10 app commits, **καμία νέα portable web δυνατότητα**: (α) 7 `refactor(api)` που υιοθετούν τα apiBody helpers (`readBody`/`strField`/`enumField`) + το `lib/cardFields.ts` dedup σε items/vouchers/tasks/stores/cards/expenses/subscriptions/notifications/lists routes — **behaviorally identical** (ίδιο body-parse, ίδια response shapes, επιβεβαιωμένο από reviewers `7f18222`/`b9b2725`) → **Web Debt cleanup, ΟΧΙ feature**· το μόνο νέο vs 14η σάρωση = `0c866eb` (readBody σε notifications + lists PATCH, ίδιο μοτίβο)· (β) `e0f7a97` (Tasks steps API + mobile modal) + `47a9574` (mobile Tasks ←/→ quick-move) + `929ca4d`/`9d226cf` (mobile IconButton/Card primitives) = **ήδη DONE** στην ουρά. Working tree: μόνο `STATUS.md` = WIP του Αχιλλέα (δεν το αγγίζω). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports (`route.ts` **GET-only** @ line 24 → extra charts θέλουν endpoint-extension + RN charting lib), Statements (`plans/route.ts` **GET-only** @ line 16 → merge/bind/PDF-import θέλουν νέα write/upload endpoints), Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (IconButton + Card ΕΚΛΕΙΣΑΝ [929ca4d/9d226cf]· επόμενο unattended-safe = Button-family finish [ShoppingScreen `addBtnWide` + ItemsScreen `save` outlier → `<Button>`] ή Safe-area insets· `<Chip>`/`<Badge>`/lucide-icons/language-switcher = attended-preferred για οπτικό verify). Αλλιώς fresh web-debt σάρωση.
> **Re-audit 2026-07-01 (parity-auditor, 14η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income ξεχωριστή), **81 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list· τα `tasks[id]`/`trash/[id]/[id]` = grep artifacts από template-literal concat) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `d235eff` (προηγ. parity marker):** 9 app commits, **καμία νέα portable web δυνατότητα**: (α) 8 `refactor(api)` που υιοθετούν τα apiBody helpers (`readBody`/`strField`/`enumField`) + το νέο `lib/cardFields.ts` dedup σε cards/expenses/subscriptions/tasks/vouchers/stores/items routes — **behaviorally identical** (ίδιο body-parse, ίδια response shapes `{item}`/`{voucher}`/`{card}`/`{ok,id}`, επιβεβαιωμένο line-by-line από reviewer `4c6856f`) → **Web Debt cleanup, ΟΧΙ feature**· (β) `e0f7a97` (Tasks steps API + mobile edit modal) + `47a9574` (mobile Tasks ←/→ quick-move) + `929ca4d`/`9d226cf` (mobile IconButton/Card primitives) = **ήδη DONE** στην ουρά (χτίστηκαν μαζί με το mobile half). Working tree καθαρό στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): Reports (`route.ts` GET-only + σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib), Statements (`plans/route.ts` GET-only → merge/bind/PDF-import θέλουν νέα write/upload endpoints), Tasks Kanban board, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (IconButton + Card primitives ΕΚΛΕΙΣΑΝ [929ca4d/9d226cf]· επόμενο unattended-safe = `<ListItem>` ή `<Chip>` primitive [`<Badge>` token-divergent → attended-preferred]· lucide-icons/language-switcher/swipe-gesture = attended-preferred για οπτικό verify). Αλλιώς fresh web-debt σάρωση.
> **Re-audit 2026-07-01 (parity-auditor, 13η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income ξεχωριστή), **81 exported api fns**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list· τα `tasks[id]`/`trash/[id]/[id]` = grep artifacts) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `d235eff` (προηγ. parity marker):** μόνη web αλλαγή = `37fca25` (adopt `apiBody` helpers [`readBody`/`strField`/`numField`/`enumField`] σε `items` + `vouchers` POST) — **behaviorally identical** (ίδιο body-parsing, ίδιο response shape `{item}`/`{voucher}` 201· επιβεβαιωμένο line-by-line: `String(b.title||'').trim()`→`strField(b,'title','',true)`, `enumField` == η παλιά whitelist-includes) → **Web Debt cleanup, ΟΧΙ νέα δυνατότητα προς port**. Μηδέν mobile-src commit ενδιάμεσα εκτός του `effd90d` (Button primitive, ήδη στην 12η σάρωση). Working tree καθαρό στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** `route.ts` **GET-only** + σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans/route.ts` **GET-only** → merge/bind/PDF-import θέλουν νέα write/upload endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Button primitive ΕΚΛΕΙΣΕ [effd90d]· επόμενο unattended-safe = IconButton/`addBtn` variant [4 byte-identical 46-wide `＋` accent + Items cyan `importBtn`] ή Card+Badge+ListItem· Chip + Input-outlier screens attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, 12η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income ξεχωριστή). Το mobile `api.ts` (**81 exported fns**) καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list· τα `tasks[id]`/`trash/[id]/[id]`/double-slash = grep artifacts) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `d235eff` (προηγ. parity marker) = ΚΕΝΟ** (`git diff d235eff..HEAD -- apps/web/src apps/mobile/src` δεν επιστρέφει τίποτα)· ο μόνος ενδιάμεσος commit `5ebf9c2` = docker-health docs → **καμία νέα web/mobile δυνατότητα προς port**. Working tree καθαρό στην αρχή (μηδέν WIP του Αχιλλέα). «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** `route.ts` **GET-only** + σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans/route.ts` **GET-only** → merge/bind/PDF-import θέλουν νέα write/upload endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Button primitive ΕΚΛΕΙΣΕ [effd90d, 5 screens]· επόμενο unattended-safe = IconButton/`addBtn` variant ή Card+Badge+ListItem· Chip + Input-outlier screens attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, όψιμο απογευματινό run):** inventory ξαναχτισμένο από τον κώδικα (όχι docs), ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`** (home + 18· income ξεχωριστή σελίδα). Το mobile `api.ts` (81 exported fns) καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list· τα `receiptsX_rescan`/`tasks:id`/`trash/:id/:id` = grep artifacts) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `b6a3a36` (προηγ. parity marker):** **3 commits** — `a582ac5` (web `ai/route.ts`: cap AI history 20 turns / 8000 chars = **cost/DoS hardening**, response shape `{reply,actions}` ΑΜΕΤΑΒΛΗΤΟ → μηδέν κίνδυνος για το mobile), `91a5fe3` + `1791295` (mobile scrim token + Input primitive = **UI Debt**, byte-identical). `git diff b6a3a36..HEAD -- apps/web/src` = μόνο το `ai/route.ts` (hardening, όχι νέα feature) → **καμία νέα web δυνατότητα προς port**. Working tree καθαρό στην αρχή (μηδέν WIP του Αχιλλέα). **DOC-FIX:** το row Items έλεγε λανθασμένα «No AI specs (AI cost)» — ο κώδικας δείχνει `aiFillItem(id,'specs'|'info')` @ `api.ts:250` + «✦ AI specs / ✦ AI info» κουμπιά @ ItemsScreen:438-442 (re-prefill specs/price/links in-place) → **DONE στο mobile**, το doc row διορθώθηκε. «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** `route.ts` **GET-only** (`export async function GET` μόνο) + σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans/route.ts` **GET-only** → merge/bind/PDF-import θέλουν νέα write/upload endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Input primitive 6/11 screens· scrim token ΕΚΛΕΙΣΕ· επόμενο unattended-safe = Button+Chip token/primitive, αλλιώς Input-outlier screens attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, 10η σάρωση ημέρας):** inventory ξαναχτισμένο από τον κώδικα, ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens** (+ App.tsx/nav), **18 web `page.tsx`** (home + 17). Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep normalized consumed-paths == route list, ακριβές match· τα `tasks:id`/`trash/:id/:id` = grep artifacts) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `b6a3a36` (προηγ. parity marker) = ΚΕΝΟ** (`git diff b6a3a36..HEAD -- apps/web/src` δεν επιστρέφει τίποτα)· τα ενδιάμεσα commits = docs + `refactor(mobile)` (`1791295` Input primitive, `91a5fe3` scrim token = **UI Debt**, byte-identical) → **καμία νέα web feature προς port**. Working tree καθαρό στην αρχή. «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** `route.ts` **GET-only + σκόπιμα trimmed** → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans/route.ts` **GET-only** (μόνο `export async function GET`) → merge/bind/PDF-import θέλουν νέα write/upload endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Input primitive 6/11 screens· scrim token ΕΚΛΕΙΣΕ· επόμενο unattended-safe = Button+Chip token/primitive, αλλιώς Input-outlier screens attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, όψιμο νυχτερινό run):** inventory ξαναχτισμένο από τον κώδικα, ουρά **αμετάβλητη**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **18 web `page.tsx`** (home + 17). Το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 49 routes 1:1 (grep consumed-paths == route list· επιβεβαιωμένα και τα deep sub-routes: `items/[id]/{ai-fill,convert-to-task,link-plan,plans,price}`, `receipts/[id]/{add-to-library,rescan}`, `settings/test-notify`, `push/register` όλα ≥1 consumer) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `b6a3a36` (προηγ. parity marker):** **1 μόνο** commit — `1791295` (mobile Input primitive migration Items+Settings = **UI Debt**, byte-identical)· `git diff b6a3a36..HEAD -- apps/web/src` = **κενό** → **καμία νέα web feature προς port**. Working tree καθαρό στην αρχή. «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** endpoint σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans` GET-only → merge/bind/PDF-import θέλουν νέα write/upload endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage/OneDrive → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Input primitive 6/11 screens· επόμενο unattended-safe = `scrim`/`backdrop` token, αλλιώς Button+Chip / Input-outlier screens attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, βραδινό run):** inventory ξαναχτισμένο από τον κώδικα. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **19 web `page.tsx`**. Το mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** ένα από τα 49 routes (grep consumed-paths == route list· κάθε route ≥1 consumer) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web pages έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). Το `shopping-list` καλύπτεται από ShoppingScreen (getShoppingList/addListItem/toggle/delete). **App-code diff από `eec8a8a` (προηγ. parity marker):** ΜΙΑ μόνο web αλλαγή `c540322` (inline error → shared `apiError()`, Web Debt refactor, behaviorally identical)· τα υπόλοιπα = mobile UI Debt (`6a9dc33` Input primitives, `3bc8a3d` onAccent→`C.onAccent`) + docker/docs → **καμία νέα web feature προς port**. «Partial» rows ξανα-επιβεβαιωμένα live ως **Needs Decision** (όχι κρυμμένο auto-buildable GAP): (α) **Reports** endpoint σκόπιμα trimmed → extra charts θέλουν endpoint-extension + RN charting lib· (β) **Statements** `plans` GET-only → merge/bind θέλει νέα write endpoints· (γ) Tasks Kanban/steps, Settings theme/language/AI-engine/storage → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 (parity queue) / auto-buildable GAP 0 / NEEDS DECISION 0 νέα.** Ενεργό parity TODO κανένα → ο builder πέφτει στο **UI Debt Queue** (Input primitive IN PROGRESS 4/11· μετά Button+Chip / Card+Badge+ListItem, attended-preferred για οπτικό verify).
> **Re-audit 2026-07-01 (parity-auditor, απογευματινό run):** ουρά ξαναμετρημένη από τον κώδικα (όχι docs), **κανένα ενεργό parity TODO**. Inventory: **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **18 web `page.tsx`** (home + 17). Το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 49 routes 1:1 (grep των consumed paths == route list· τα `/api/v1/tasksID`, `/api/v1/trash/ID/ID` είναι grep artifacts, όχι νέα routes) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Όλα τα web screens έχουν mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `eec8a8a` (προηγ. parity marker):** μόλις **2 commits** και τα δύο refactor, ΟΧΙ νέα δυνατότητα — `c540322` (web: inline error → shared `apiError()` στα 4 routes = Web Debt, behaviorally identical) + `6a9dc33` (mobile: Input/TextArea primitives migration = UI Debt) → **καμία νέα web feature προς port**. Άρα **7/7 queue items DONE**. **«Partial» rows ξανα-επιβεβαιωμένα live ως Needs Decision (όχι κρυμμένο auto-buildable GAP):** (α) **Reports** — `GET /api/v1/reports` γυρνά σκόπιμα trimmed set (inventoryValue/installmentsOwed/net/budgets/byCategory/monthly/thisMonth/thisYear/upcomingInstallments, route.ts:105), όλο consumed από ReportsScreen· τα extra charts (spend-by-store/inventory-pie/subs-by-cat/warranties/biggest/income-vs-expense-12mo) θέλουν endpoint-extension + RN charting lib. (β) **Statements merge/bind** — `statements/plans` **GET-only** (μόνο `export async function GET`, route.ts:16)· write-ops θέλουν νέα endpoints. (γ) Tasks Kanban/steps-checklist, Settings theme/language/AI-engine/storage → decision/credentials boundary. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 / GAP 0 (auto-buildable) / NEEDS DECISION 0 νέα.** Ο builder ΔΕΝ έχει ενεργό parity TODO → κορυφαίο εναπομείναν auto-buildable = **UI Debt** (Input primitive συνέχεια 4/11 screens, attended-preferred λόγω οπτικού verify· ή Button+Chip primitives P2/M unattended-safe).
> **Re-audit 2026-07-01 (parity-auditor, μεσημεριανό run):** ουρά ξαναμετρημένη από τον κώδικα, **κανένα ενεργό parity TODO**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **81** exported api fns· το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 49 routes 1:1 (grep των consumed paths == route list· τα φαινομενικά extra `/api/v1/tasks[id]`, `/api/v1/trash/[id]/[id]` είναι grep artifacts, όχι νέα routes) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Web user-facing screens: **18** `page.tsx` (home + 17), ΟΛΕΣ με mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). **App-code diff από `eec8a8a` (προηγ. parity marker):** **1 commit** — `6a9dc33` (mobile Input/TextArea primitives refactor + migrate 4 record-form screens = **UI Debt work, ΟΧΙ parity feature**· τα επόμενα `3ae313f`/`f11ce26` = docs/docker) → **καμία νέα web δυνατότητα προς port**. Άρα τα **6 αρχικά Build-Queue items + το Tasks tags/priority GAP = ΟΛΑ DONE (7/7)**. **Έλεγχος «partial» rows για κρυμμένο auto-buildable GAP (ξανα-επιβεβαιωμένο live):** (α) **Reports** — `GET /api/v1/reports` επιστρέφει σκόπιμα trimmed set (inventoryValue/installmentsOwed/activePlans/net/budgets/thisMonth/thisYear) που το mobile ReportsScreen καταναλώνει ΟΛΟ· τα extra web charts (spend-by-store/inventory-pie/subs-by-cat/warranties/biggest-purchases/income-vs-expense-12mo) θέλουν ΚΑΙ endpoint-extension ΚΑΙ charting lib → **Needs Decision**. (β) **Statements merge/bind** — `statements/plans` είναι **GET-only** (μόνο `export async function GET`)· τα web write-ops (link-to-product, merge/bind) θέλουν νέα write endpoints + UX → **Needs Decision**. (γ) **Tasks Kanban/steps-checklist** — UX + API-extension → **Needs Decision**. (δ) Settings theme/language/AI-engine/storage → **Needs Decision** (credentials/product boundary). mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Counts: DONE 7 / GAP 0 (auto-buildable) / NEEDS DECISION 0 νέα.** Ο builder ΔΕΝ έχει ενεργό parity TODO → να πάρει το κορυφαίο **UI Debt** = **Input primitive** (P1/M, IN PROGRESS: 4/11 record-form screens migrated στο `6a9dc33`· απομένουν Items/Receipts/Settings/Shopping/Search/Assistant/Login).
> **Re-audit 2026-07-01 (parity-auditor, πρωινό run):** ουρά ξαναμετρημένη από τον κώδικα, **κανένα ενεργό parity TODO**. **49 v1 routes** (login + 48 bearer), **16 mobile screens**, **81** exported api fns· το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 49 routes 1:1 (grep των consumed paths == route list, incl. `push/register` @ api.ts:444/447) → **μηδέν «endpoint χωρίς mobile consumer» gap**. Web user-facing screens: **18** `page.tsx`, ΟΛΕΣ με mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). App-code diff από `245be25` (προηγ. parity marker): **3 commits** — `6a5809a` (serializeLineItems refactor, όχι feature), `86c6ada` (ai-fill parity, queue item → **DONE**), `2223203` (Tasks tags/priority, το τελευταίο GAP → **DONE**, επιβεβαιωμένο: `addTask(title,{tags?,priority?})` api.ts:141 + `splitTitleTags` TasksScreen:18 + edit-modal priority/tags → `updateTask(id,{title,status,priority,tags})` :62). Άρα **6/6 αρχικά queue items + το νέο Tasks GAP = ΟΛΑ DONE**· καμία νέα web δυνατότητα προς port. **Έλεγχος «partial» rows για κρυμμένο auto-buildable GAP:** το **Reports** (docs 🟡) ΔΕΝ είναι endpoint gap — το `GET /api/v1/reports` επιστρέφει σκόπιμα trimmed set (netPosition/thisMonth/thisYear/byCategory/budgets/monthly/upcomingInstallments) και το mobile ReportsScreen τα καταναλώνει **ΟΛΑ** (τα extra web charts [spend-by-store/inventory-pie/subs-by-cat/warranties/biggest-purchases/income-vs-expense-12mo] θέλουν ΚΑΙ endpoint-extension ΚΑΙ charting lib → **Needs Decision**, όχι clean gap). Ομοίως Statements merge/bind/PDF-import (write endpoints/upload) + Tasks Kanban/steps-checklist (UX + API-extension) + Settings theme/language/AI-engine/storage = **Needs Decision**. mobile `tsc --noEmit` → **EXIT 0**. **Counts: DONE 7 / GAP 0 (auto-buildable) / NEEDS DECISION 0 νέα.** Ο builder ΔΕΝ έχει ενεργό parity TODO → να πάρει το κορυφαίο **UI Debt** = **Input primitive** (P1/M, pure-mobile).
> **Re-audit 2026-07-01 (parity-auditor, νυχτερινό run #2):** **49 v1 routes** (login + 48 bearer· το `items/[id]/ai-fill` προστέθηκε με το `86c6ada`, 48→49), **16 mobile screens**, **81** exported api fns· το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 49 routes 1:1 (grep των consumed paths == route list) → **κανένα «endpoint χωρίς mobile consumer» gap**. Web user-facing screens: **18** `page.tsx` (home + 17), ΟΛΕΣ με mobile equivalent εκτός `/setup` (first-run admin wizard, web-only by design· N/A). Από `245be25` (προηγ. parity audit): μόνο **2** app-code commits (`86c6ada` ai-fill parity = το τελευταίο queue TODO, **τώρα DONE**· `6a5809a` serializeLineItems dedup) → **καμία νέα web δυνατότητα προς port**, άρα **τα 6 αρχικά Build-Queue items = 6/6 DONE**. mobile `tsc --noEmit` → **EXIT 0**. **ΝΕΟ GAP από fresh code-diff (όχι στα 6 αρχικά):** **Tasks — tags/priority read-only στο mobile** (P3/S, no AI, no decision): το POST `/api/v1/tasks` δέχεται ήδη `tags`+`priority` (route.ts:52,58), το PATCH δέχεται `tags`/`priority`/`content`/`dueDate`, αλλά το mobile `addTask(title)` στέλνει ΜΟΝΟ `{title}` (TasksScreen:32) + το edit modal καλεί `updateTask(id,{title,status})` (TasksScreen:44) → tags/priority μόνο display, μη επεξεργάσιμα. Auto-buildable ΧΩΡΙΣ νέο endpoint. Είναι το **μόνο ενεργό TODO** του builder (βλ. queue item παρακάτω, ranked στην κορυφή).
> **Re-audit 2026-07-01 (parity-auditor, βραδινό run):** queue ξαναμετρημένη ολόκληρη από τον κώδικα, **αμετάβλητη**. **48 v1 routes** (login + 47 bearer· `items/[id]/` = {route, convert-to-task, link-plan, plans, price}, **κανένα ai-fill**), **16 mobile screens**, **71** exported api fns· το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 48 routes 1:1 (το grep των consumed paths == το route list) → **κανένα «endpoint χωρίς mobile consumer» gap**. Cross-check web user-facing screens (`page.tsx`): 18 routes (home/calendar/expenses/income/items/shopping/shopping-list/receipts/statements/subscriptions/vouchers/tasks/reports/settings/history/jobs/trash/login) → ΟΛΕΣ έχουν mobile equivalent· το μόνο web screen χωρίς mobile = **`/setup`** (first-run admin wizard, web-only by design — το mobile μπαίνει με bearer token· **N/A, όχι gap**). DONE επιβεβαιωμένα live από κώδικα: convert-to-task (`api.ts:244` + ItemsScreen:341), rescanReceipt (`api.ts:207`), getInstallmentPlans (`api.ts:287`), Notifications badge, Expenses full-field PATCH. Από `245be25` (προηγ. parity audit): **0 web feature commits** στο app dir (μόνο `6a5809a` = serializeLineItems refactor, dedup) + **0 mobile commits** → καμία νέα δυνατότητα προς port, η ουρά by-construction αμετάβλητη. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Από τα 6 Build-Queue items: 5 DONE, 1 TODO** = Items AI specs / ai-fill (P3/M, full-stack — το `items/[id]/ai-fill` route **ΔΕΝ υπάρχει**, οι web actions `aiFillSpecs:533`/`aiFillInfo:571` υπάρχουν προς port· AI cost → δομικό verify μόνο). Είναι το **μόνο** ενεργό TODO.
> **Re-audit 2026-07-01 (parity-auditor):** queue ξαναμετρημένη από τον κώδικα, **αμετάβλητη**. **48 v1 routes** (login + 47 bearer), **16 mobile screens**, **80** exported api fns· το mobile `api.ts` καταναλώνει **ΚΑΘΕ** ένα από τα 48 routes 1:1 (grep των paths == route list) → **κανένα «endpoint χωρίς mobile consumer» gap**. Επιβεβαιωμένο από κώδικα ότι ο Receipts re-scan mobile half είναι DONE (`fbda2b3`): `rescanReceipt` στο `api.ts:207` + «Re-scan text/OCR» bar στο `ReceiptsScreen.tsx:171-178`. **0 web feature commits** στο `apps/web/src/app` από το προηγ. full audit (`e8a963a..HEAD` κενό για το app dir) → καμία νέα web δυνατότητα προς port. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). **Από τα 6 queue items: 5 DONE, 1 TODO** = **Items AI specs / ai-fill** (P3/M, full-stack: το `items/[id]/ai-fill` route **ΔΕΝ υπάρχει** ακόμα — επιβεβαιωμένο: `items/[id]/` έχει μόνο {route.ts, convert-to-task, link-plan, plans, price}· AI cost → δομικό verify μόνο). Είναι το **μόνο** ενεργό TODO του builder· χτίζει ΚΑΙ web endpoint ΚΑΙ mobile half.
> **Builder 2026-06-30 (βραδινό):** Expenses/Income full-field edit **DONE** (PATCH επεκτάθηκε + MoneyScreen edit modal full-field) → queue **5 → 4 GAP** (1 P2 + 3 P3). Επόμενο TODO: Items convert-to-task (P3/S, no AI) ή Receipts re-scan (P2/M, AI → δομικό verify).
> **Re-audit 2026-06-30 (cont.² — parity-auditor, 3η σάρωση ημέρας):** queue (πριν το builder) **αμετάβλητο** (5 GAP). Επιβεβαιωμένο ξανά από τον κώδικα: **46 v1 routes** (login + 45 bearer), **16 mobile screens**, **78** exported api fns· το mobile `api.ts` καταναλώνει 1:1 ΚΑΘΕ υπάρχον endpoint (grep των paths ταιριάζει με το route list) → κανένα «web endpoint χωρίς mobile consumer» gap. Το μόνο P1 (Statements installment-plan overview) είναι **DONE** (api.ts:274 καλεί `/api/v1/statements/plans`). Ο ενεργός κορυφαίος TODO είναι **Receipts re-scan (P2/M)**. Τα 5 εναπομείναντα items μένουν γνήσια gaps (ξαναελεγμένα από τα directory listings): `receipts/[id]/` έχει μόνο {route.ts, add-to-library} (κανένα rescan), `items/[id]/` έχει μόνο {route.ts, link-plan, plans, price} (κανένα convert-to-task/ai-fill), το `PATCH /api/v1/expenses/[id]` δέχεται μόνο vendor/amount/category/kind/notes/date (route.ts:11-29 επαληθευμένο, PARTIAL — λείπουν period/recurring/recurringCycle/paymentMethod), `GET /api/v1/notifications` υπάρχει+καταναλώνεται και ήδη γυρνά `unread` count (api.ts:412, badge = pure-mobile UI). Από το προηγ. parity audit (`afbccb3`) μόνο 1 code commit άγγιξε web runtime: `5457339` (updatedAt indexes σε 7 synced models = db perf, ΟΧΙ parity feature)· τα υπόλοιπα = docs/docker. mobile `tsc --noEmit` → exit 0 (μηδέν P1 type errors).
>
> **Re-audit 2026-06-30 (cont.³ — parity-auditor, 5η σάρωση ημέρας):** queue ξαναμετρημένη από τον κώδικα. **48 v1 routes** (ανέβηκαν από 46: προστέθηκαν `items/[id]/convert-to-task` + `receipts/[id]/rescan`), **16 mobile screens**, **70** exported api fns (όχι 78, η παλιά μέτρηση ήταν χαλαρή). Το mobile `api.ts` καταναλώνει **ΟΛΑ** τα 48 routes εκτός **ενός**: `POST /api/v1/receipts/[id]/rescan` (χτίστηκε σήμερα ως web endpoint, `d24be27`, **κανένας mobile consumer ακόμα**) → αυτό ΕΙΝΑΙ ο μοναδικός «endpoint χωρίς mobile half» gap. **ΚΡΙΣΙΜΗ αλλαγή vs προηγ. audit:** το queue item «Receipts re-scan» έλεγε `API exists: no` — πλέον **exists: yes** (το endpoint είναι registered + serving, no-token 401 verified). Άρα το TODO ΔΕΝ είναι πια full-stack· είναι **μόνο το mobile half** (api.ts `rescanReceipt` + ReceiptsScreen buttons). Από τα 6 queue items: **4 DONE** (Statements overview, Items convert-to-task, Notifications badge, Expenses full-field edit), **2 TODO GAP**: (α) Receipts re-scan mobile half (P2/M, web endpoint ΕΤΟΙΜΟ, AI → δομικό verify), (β) Items AI specs (P3/M, web endpoint `items/[id]/ai-fill` **ΔΕΝ υπάρχει** — full-stack, AI). DONE επιβεβαιωμένα από κώδικα: `items/[id]/convert-to-task/route.ts` υπάρχει + `convertItemToTask` στο api.ts· `expenses/[id]/route.ts` PATCH έχει period/recurring/recurringCycle/paymentMethod (γραμμές 23-27)· `statements/plans` + `getInstallmentPlans` παρόντα. mobile `tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). Web commits από `afbccb3`: 4 feature (item-status whitelist, expenses PATCH, convert-to-task, rescan endpoint) + 2 refactor (listEnvelope `{data}`, apiBody helpers) + 1 perf (updatedAt indexes) — όλα ήδη reviewed/clean.

> **Re-audit 2026-07-02 (mobile-parity-auditor, 38η σάρωση):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **50 v1 routes** (login + 49 bearer, `find api/v1 -name route.ts`), **16 mobile screens**, mobile `api.ts` καταναλώνει **1:1 ΚΑΘΕ** route → **μηδέν endpoint-level gap** (και τα νέα `expenses/[id]/rescan` + `statements/plans` δεμένα). Mobile `npx tsc --noEmit` → **EXIT 0**. Επιβεβαίωσα στον κώδικα ότι τα 2 GAP της προηγ. σάρωσης έκλεισαν: bill image (`fileSource(editing.file)` στο MoneyScreen) + Expenses/Income re-scan (`rescanExpense` + rescanBar) → **DONE**. **ΝΕΟ εύρημα (2 auto-buildable GAP, τοποθετημένα στην κορυφή):** (α) **Expense anomaly badges** — το web υπολογίζει `anomaly` (±% απόκλιση από το vendor median, `page.tsx:25-46`, pure stats μηδέν AI) και δείχνει `AnomalyBadge`, αλλά ο v1 serializer (`serialize.ts`) ΔΕΝ εκθέτει το πεδίο → το mobile δεν μπορεί να το δείξει· port = additive `anomaly?` στο list route (ήδη import-άρει `vendorKey`) + badge στο MoneyScreen. (β) **Expense vendor autocomplete** — το web add-form έχει vendor autocomplete· το mobile MoneyScreen add-form είναι plain `Input` (line 147), μηδέν suggestions· auto-buildable client-side (derive distinct vendors από την ήδη-φορτωμένη λίστα, μηδέν endpoint). Τα εναπομείναντα παλιά TODO (lucide icons, language switcher) μένουν attended-preferred → κάτω από τα 2 νέα un-attended.

### Expenses — vendor→category auto-rules δεν εφαρμόζονται στο v1 create route (P15 gap, functional bug)
- Priority: P1 | Size: S | no AI, no decision, no mobile UI change — web-side wiring only
- Web ref: P15 vendor→category auto-rules (`5f13b7c`) — `matchCategoryRule()` (apps/web/src/lib/categoryRules.ts:71) εφαρμόζεται σε ΚΑΘΕ web expense-creation path (apps/web/src/app/expenses/actions.ts:234,349,502,587) μέσω `(await getAppSettings()).categoryRules`.
- API: POST /api/v1/expenses (exists: yes, apps/web/src/app/api/v1/expenses/route.ts:47) — όταν το mobile client παραλείπει category, μπαίνει literal `'other'` (`strField(b,'category','other')`), **παρακάμπτοντας εντελώς το rule engine**. Αποτέλεσμα: το ίδιο vendor δίνει ΔΙΑΦΟΡΕΤΙΚΗ category ανάλογα με το αν το expense καταχωρήθηκε από web ή mobile — silent data inconsistency, όχι απλά missing UI. Έλεγξε επίσης το PATCH (`expenses/[id]/route.ts`) για το ίδιο pattern αν το vendor/notes αλλάζουν.
- Mobile files: apps/web/src/app/api/v1/expenses/route.ts (πριν το create: αν δεν δόθηκε ρητή category, `matchCategoryRule((await getAppSettings()).categoryRules, {vendor, description: notes})?.category` ίδιο με το web action) — **μηδέν mobile-side αλλαγή**, το MoneyScreen add-form ήδη παραλείπει category by default οπότε το fix διορθώνει άμεσα ό,τι βλέπει ο mobile χρήστης.
- Acceptance:
  - `POST /api/v1/expenses` με vendor που ταιριάζει σε configured rule, χωρίς ρητή category → response category = της rule, όχι `'other'`
  - explicit-category requests ανεπηρέαστα (η rule εφαρμόζεται μόνο όταν η category λείπει/κενή)
  - tsc καθαρό (web)· +1 νέο route test για το rule-applied path
- Status: ✅ DONE 2026-07-19 (pharos-daily-dev) — `matchCategoryRule((await getAppSettings()).categoryRules, {vendor, description: notes})`
  εφαρμόζεται πριν το `Expense.create()`, μόνο όταν η category είναι κενή/absent (explicit category, ΚΑΙ literal
  `'other'`, δεν αλλάζει — presence-check, όχι το web form's `!== 'other'` heuristic, καθαρότερο για JSON API). Το
  PATCH (`expenses/[id]/route.ts`) ελέγχθηκε — ήδη σωστό, ΔΕΝ default-άρει category όταν λείπει (partial-update
  semantics), άρα δεν είχε το ίδιο bug· σκόπιμα ΔΕΝ προστέθηκε auto-rule-on-vendor-change εκεί (θα άλλαζε silently
  μια ήδη-καθορισμένη category σε κάθε edit — surprising behavior, εκτός acceptance criteria). +4 νέα route tests
  (rule-applied/explicit-wins/no-match-fallback/text-matchType) — 27/27 passed. tsc καθαρό, full vitest 2341/181.

### Subscriptions — free-trial πεδίο στο mobile create/edit (P33 gap, notif icon ήδη shipped)
- Priority: P2 | Size: S | no AI, no decision
- Web ref: P33 (`bfd96ba`) — `Subscription.trialEndsAt` (apps/web/src/models/Subscription.ts) settable στο web SubscriptionsClient form· `trialAlertDays` στα Settings ελέγχει το lead time. Το notif icon (`trialend`) shipped σήμερα, αλλά το ίδιο το πεδίο δεν είναι readable/settable στο mobile.
- API: GET/POST/PATCH /api/v1/subscriptions(/[id]) (exist: yes) — κανένα δεν διαβάζει/γράφει/επιστρέφει `trialEndsAt` (grep confirmed). GET/PATCH /api/v1/settings — κανένα `trialAlertDays`.
- Mobile files: apps/web/src/app/api/v1/subscriptions/route.ts + [id]/route.ts (+`trialEndsAt` GET/POST/PATCH, ίδιο optional-date pattern με `nextRenewal`), apps/web/src/app/api/v1/settings/route.ts (+`trialAlertDays` GET/PATCH), apps/mobile/src/api.ts (`Subscription` += `trialEndsAt?: string | null`), apps/mobile/src/screens/SubscriptionsScreen.tsx (optional date field στο add/edit + μικρό «Trial» badge όταν μελλοντικό)
- Acceptance:
  - Create/edit subscription με `trialEndsAt` από mobile → persists, round-trips
  - tsc καθαρό web+mobile
- Status: ✅ DONE 2026-07-19 (pharos-daily-dev) — `trialEndsAt`+`firstChargeAmount` στο v1 subscriptions GET/POST/PATCH
  (trim/route.ts), `trialAlertDays` στο v1 settings GET/PATCH (clamp 0–60, ίδιο με το web action). PATCH υποστηρίζει
  explicit `null` για clear του trial (πέρα από set-with-date/omit). Mobile: `Subscription` type +2 πεδία,
  `addSubscription`/`updateSubscription` δέχονται τα νέα πεδία, `SubscriptionsScreen` edit modal νέο «FREE TRIAL ENDS»
  date input (blank=clear) + gold `<Badge label="Trial">` στη λίστα όταν η ημερομηνία είναι μελλοντική (ίδιο pattern
  με το ήδη-shipped return-window badge στο Receipts). +8 νέα unit tests (POST/PATCH valid/invalid/clear/no-change).
  tsc καθαρό web+mobile, vitest 2349/181 (+8, μηδέν regression). Docker rebuild clean, browser-verified /login.

### Reports — net-worth headline + breakdown στο mobile (PA2 gap)
> Ήδη speced στην προηγούμενη σάρωση (48η, 2026-07-10) — βλ. πλήρες entry παρακάτω («Reports — net-worth headline + breakdown στο mobile (PA2 gap)»). Παραμένει ανοιχτό, item #3 του builder queue.

### Reports — safe-to-spend forward cashflow κάρτα στο mobile (P19 gap)
- Priority: P2 | Size: M | no AI, no decision
- Web ref: P19 (`d3e191d`) — pure `computeSafeToSpend(agendaMonths)` (apps/web/src/lib/safeToSpend.ts:34) τροφοδοτείται από το ήδη-computed `computeMoneyAgenda` series, renders ως κάρτα στο `/reports` κάτω από το net-worth banner (apps/web/src/app/reports/ReportsClient.tsx, apps/web/src/app/reports/page.tsx:315-319).
- API: GET /api/v1/reports (exists: yes, κανένα `safeToSpend` πεδίο) — additive: το route ήδη χτίζει agenda-months structure για άλλους υπολογισμούς· κάλεσε `computeSafeToSpend` και πρόσθεσε το αποτέλεσμα.
- Mobile files: apps/web/src/app/api/v1/reports/route.ts (+`safeToSpend`), apps/mobile/src/api.ts (`Reports` += `safeToSpend?: {...}` mirror του web type), apps/mobile/src/screens/ReportsScreen.tsx (νέα κάρτα, 30/60/90-day windows)
- Acceptance:
  - GET /api/v1/reports επιστρέφει `safeToSpend` (additive, no-auth ακόμα 401 όχι 500)
  - ReportsScreen δείχνει «Safe to spend» κάρτα με τα 3 windows όταν παρόν
  - tsc καθαρό web+mobile
- Status: TODO

### Reports/Settings — budget envelope / rollover mode στο mobile (P25 gap)
- Priority: P2 | Size: M | no AI, no decision
- Web ref: P25 (`9dabfe9`) — `AppConfig.budgetRollover: boolean` toggle στα Settings· όταν on, `categoryRollover()` (apps/web/src/lib/budgetRollover.ts:34) μεταφέρει unspent budget από τους προηγ. `ROLLOVER_WINDOW`(=3) μήνες στο effective budget του τρέχοντος μήνα (apps/web/src/app/reports/page.tsx:163-176).
- API: GET/PATCH /api/v1/settings (exists: yes) — ΔΕΝ εκθέτει/δέχεται `budgetRollover` (grep confirmed 0 matches). GET /api/v1/reports (exists: yes) — `budgets[]` (route.ts:44-51) χρησιμοποιεί raw per-category limit, όχι το rollover-adjusted `effective`.
- Mobile files: apps/web/src/app/api/v1/settings/route.ts (GET επιστρέφει `budgetRollover`, PATCH το δέχεται· boolean coerce, ίδιο pattern με άλλα flags), apps/web/src/app/api/v1/reports/route.ts (όταν `settings.budgetRollover`, τρέξε `categoryRollover` ανά budgeted category, πρόσθεσε `effective`/`carried` στο κάθε `budgets[]` row), apps/mobile/src/screens/SettingsScreen.tsx (toggle), apps/mobile/src/screens/ReportsScreen.tsx (budget κάρτα δείχνει carried/effective όταν παρόν)
- Acceptance:
  - PATCH /api/v1/settings `{budgetRollover:true}` persists, GET το αντανακλά
  - GET /api/v1/reports budget rows έχουν `effective`/`carried` ΜΟΝΟ όταν rollover on (additive, off = αμετάβλητο shape)
  - tsc καθαρό web+mobile
- Status: TODO

### Subscriptions — auto-discover untracked recurring charges στο mobile (P7 gap)
- Priority: P2 | Size: M | no AI (καθαρή heuristic), no decision — builder defaults ήδη locked (>=3 occurrences, ±5 μέρες tolerance)
- Web ref: P7 (`97a7d66`) — `discoverRecurringCandidates(expenses, {excludeVendorKeys})` (apps/web/src/lib/recurringDiscovery.ts:69) τρέχει πάνω στο Expense history μέσα στο subscriptions server action (apps/web/src/app/subscriptions/actions.ts:145), εμφανίζεται ως Add/Dismiss suggestion chips (dismiss = session-only, καμία persisted ignore-list).
- API: GET /api/v1/subscriptions (exists: yes) — κανένα suggestions πεδίο. Χρειάζεται νέο additive computation (reuse το pure lib, χρειάζεται `Expense.find()` ίδιο με το web action) — inline στο GET response (`suggestions: RecurringCandidate[]`) είναι το απλούστερο (ίδιο idiom με τα υπόλοιπα additive πεδία σε αυτό το file).
- Mobile files: apps/web/src/app/api/v1/subscriptions/route.ts (+`suggestions` στο GET, reuse `discoverRecurringCandidates` + τα υπάρχοντα subscription vendorKeys ως excludeVendorKeys), apps/mobile/src/api.ts (list response type += `suggestions?: RecurringCandidate[]`), apps/mobile/src/screens/SubscriptionsScreen.tsx (suggestion chips block: «Add» → `createSubscription` με name/amount/cycle του candidate, «Dismiss» → local state μόνο, mirror του web's non-persisted dismiss)
- Acceptance:
  - GET /api/v1/subscriptions επιστρέφει `suggestions` (additive, κενό array όταν τίποτα — φθηνή heuristic, μηδέν AI)
  - mobile δείχνει Add/Dismiss chips όταν υπάρχουν suggestions· Add δημιουργεί πραγματική subscription μέσω του υπάρχοντος POST
  - tsc καθαρό web+mobile
- Status: TODO

### Bills — payable/due tracker στο mobile (P28 gap, νέο entity — μηδέν v1 route ακόμα)
- Priority: P2 | Size: L | no AI, no decision — mirrors υπάρχον CRUD entity pattern (vouchers/subscriptions), απλά μεγαλύτερο σε επιφάνεια
- Web ref: P28 (`a737bbc`) — νέο `models/Bill.ts` (amount, dueDate, paidAt, recurring/cycle, archived) + pure `lib/bill.ts` (`billStatus` παράγει paid/overdue/due-soon/upcoming από dueDate+paidAt, ποτέ αποθηκευμένο) + Settings `billAlertDays` + NotificationBell integration. Ξεχωριστή λίστα recurring payables (ενοίκιο/λογαριασμοί), διαφορετικό από Expense.
- API: κανένα δεν υπάρχει. Χρειάζεται νέο `GET/POST /api/v1/bills` + `GET/PATCH/DELETE /api/v1/bills/[id]`, mirror του shape/auth/error-envelope του `apps/web/src/app/api/v1/vouchers/route.ts` (πλησιέστερο sibling: απλό CRUD + derived status πεδίο, μηδέν AI-scan variant).
- Mobile files: νέα v1 routes παραπάνω, apps/mobile/src/api.ts (+`Bill` type, +CRUD fns), νέο apps/mobile/src/screens/BillsScreen.tsx (ή section μέσα στο MoneyScreen — η τοποθέτηση δεν μπλοκάρει το build, default σε ξεχωριστή screen αφού το web το βλέπει ως distinct λίστα) + nav entry
- Acceptance:
  - Πλήρες CRUD round-trip μέσω των νέων v1 routes, auth-gated 401 χωρίς token
  - mobile list δείχνει bill rows με derived status badge (paid/overdue/due-soon/upcoming), mark-paid action
  - tsc καθαρό web+mobile
- Status: TODO

### Gift cards — store-credit balance tracker στο mobile (P32 gap, νέο entity — μηδέν v1 route ακόμα)
- Priority: P2 | Size: L | no AI, no decision — mirrors υπάρχον CRUD entity pattern
- Web ref: P32 (`052ee64`) — νέο `models/GiftCard.ts` (title, store, initialAmount, uses[] με signed amounts spend/reload) + pure `lib/giftcard.ts` (`giftCardBalance`, `giftCardSpentPct`, `giftCardDaysLeft`, `giftCardIsLive`) — ζει ως tab μέσα στο web `/vouchers` (apps/web/src/app/vouchers/page.tsx:3-13 φορτώνει Voucher+GiftCard παράλληλα).
- API: κανένα δεν υπάρχει. Χρειάζεται νέο `GET/POST /api/v1/giftcards` + `GET/PATCH/DELETE /api/v1/giftcards/[id]` (+ τρόπος να κατοχυρωθεί ένα use/spend — additive sub-array push).
- Mobile files: νέα v1 routes παραπάνω, apps/mobile/src/api.ts (+`GiftCard` type, +CRUD fns), apps/mobile/src/screens/VouchersScreen.tsx (πρόσθεσε gift-card section/tab δίπλα στη λίστα vouchers, mirror του web's ίδιας-σελίδας tab — κρατά το mobile nav 1:1 με το web αντί για νέα top-level screen)
- Acceptance:
  - Πλήρες CRUD + log-a-use round-trip μέσω των νέων v1 routes, auth-gated
  - mobile VouchersScreen δείχνει gift-card λίστα με live balance + days-left + spent%
  - tsc καθαρό web+mobile
- Status: TODO

### Reports — savings/financial goals στο mobile (P12 gap, νέο entity — μηδέν v1 route ακόμα)
- Priority: P2 | Size: L | no AI, no decision — mirrors υπάρχον CRUD entity pattern (Bill/GiftCard), plus ένα nested contributions ledger σαν το GiftCard uses[]
- Web ref: P12 (`78ebd76`) — νέο `models/Goal.ts` (title, targetAmount, optional targetDate, free-form category, notes, archived, embedded `contributions[]` {amount,date,note}, ίδιο σχήμα με `GiftCard.uses[]`) + pure `lib/goals.ts` (`goalCurrent` = Σ contributions, πάντα derived, ποτέ stored· `goalProgress` = target/remaining/pct/done/monthsLeft/perMonth) + `apps/web/src/app/reports/goalsActions.ts` (create/update/archive/delete + add/remove-contribution) — ζει ως «Goals» κάρτα μέσα στο web `/reports` (id="goals" anchor) + homepage NavCard. Το `Goal.ts` έχει ήδη `updatedAt` index με ρητό σχόλιο στον κώδικα «for a future GET /api/v1/goals» — το route δεν χτίστηκε ακόμα.
- API: κανένα δεν υπάρχει. Χρειάζεται νέο `GET/POST /api/v1/goals` + `GET/PATCH/DELETE /api/v1/goals/[id]` (mirror shape/auth/error-envelope του `apps/web/src/app/api/v1/vouchers/route.ts`) + τρόπος να κατοχυρωθεί μια contribution (additive sub-array push/pull, ίδιο pattern με το gift-card use όταν χτιστεί).
- Mobile files: νέα v1 routes παραπάνω, apps/mobile/src/api.ts (+`Goal`/`GoalContribution` types, +CRUD fns +add/remove-contribution fns), νέο apps/mobile/src/screens/GoalsScreen.tsx (ή section μέσα στο ReportsScreen — το web το δείχνει μέσα στο Reports, default να ακολουθήσει το ίδιο) + nav entry αν γίνει standalone screen
- Acceptance:
  - Πλήρες CRUD + add/remove-contribution round-trip μέσω των νέων v1 routes, auth-gated 401 χωρίς token
  - mobile δείχνει goal cards με derived current/target/pct progress bar + «€X/mo needed» hint όταν έχει deadline
  - tsc καθαρό web+mobile
- Status: TODO

### Items — document/manual vault στο mobile (P21 gap, νέο πεδίο σε υπάρχον entity — μηδέν v1 exposure ακόμα)
- Priority: P2 | Size: M | no AI, no decision — reuse του `/api/files` serving pattern (ίδιο με photos)
- Web ref: P21 (2026-07-14) — `models/Item.ts` νέο `attachments[]` ({path, name, mimeType, size, uploadedAt}), server actions `uploadItemAttachments`/`deleteItemAttachment` (equipment bucket, reuse `saveFile`), `apps/web/src/app/items/ItemDocuments.tsx` (list + upload + delete UI στο item detail modal).
- API: `GET/PATCH /api/v1/items/[id]` δεν εκθέτει ακόμα `attachments` (mirror του πώς εκτέθηκαν τα `photos` ήδη). Upload χρειάζεται multipart endpoint (δεν υπάρχει σήμερα κανένα v1 file-upload route — το mobile στέλνει JSON, όχι FormData· πρώτο instance αυτού του pattern στο v1 API, βλ. Needs Achilleas για upload-endpoint design).
- Mobile files: v1 serializer (+`attachments` read-only πρώτα), apps/mobile/src/api.ts (+`Attachment` type), apps/mobile/src/screens/ItemsScreen.tsx (read-only λίστα εγγράφων στο item detail με tap-to-open· upload = phase 2 μετά το endpoint decision)
- Acceptance:
  - GET item detail δείχνει `attachments[]` με path/name/size
  - mobile item detail rendάρει λίστα εγγράφων, tap ανοίγει το αρχείο (browser/in-app viewer)
  - tsc καθαρό web+mobile
- Status: TODO (read-only πρώτα· upload χρειάζεται νέο v1 multipart pattern, βλ. Needs Achilleas)

### Search — εμφάνιση matched receipt line-item στα mobile αποτελέσματα (P22 gap)
- Priority: P3 | Size: S | no AI, no decision
- Web ref: P22 (`68acb9a`) — `apps/web/src/lib/receiptSearch.ts` (pure, 27 γραμμές) βρίσκει ποιο line-item ταίριαξε σε ένα search query· το web global search (`search-actions.ts:10-13`) περιλαμβάνει το snippet.
- API: GET /api/v1/search (exists: yes) — δεν περιλαμβάνει το matched line-item snippet.
- Mobile files: apps/web/src/app/api/v1/search/route.ts (reuse `receiptSearch.ts`, πρόσθεσε snippet field στα receipt hits), apps/mobile/src/api.ts (search result type += `matchedItem?: string`), apps/mobile/src/screens/SearchScreen.tsx (δείξε το snippet κάτω από τη receipt row όταν παρόν)
- Acceptance:
  - Search όρου που ταιριάζει ΜΟΝΟ σε line-item (όχι store/vendor) επιστρέφει το receipt hit με το matched item name
  - tsc καθαρό web+mobile
- Status: TODO

### Notifications — pricehike icon στο mobile Activity (P14 gap)
- Priority: P2 | Size: S | no AI, no decision, no endpoint, no native dep
- Web ref: P14 price-hike/drop watch (`74f9cd4`) πρόσθεσε kind `pricehike` στο enum (file: apps/web/src/models/Notification.ts:10 `enum: ['deal','installment','warranty','pricehike','system']`)· το web NotificationBell το χειρίζεται, το mobile όχι.
- API: GET /api/v1/notifications (exists: yes) — **καμία αλλαγή**· το route περνά το `kind` string ως έχει. Το mobile απλώς λείπει το icon mapping + type entry.
- Mobile files: apps/mobile/src/api.ts (`NotifKind` [line 465] → πρόσθεσε `'pricehike'` στο union), apps/mobile/src/screens/ActivityScreen.tsx (`NOTIF_ICON` [line 22] → πρόσθεσε `pricehike: '📈'` ή `'⚠'`, ώστε το `Record<NotifKind,string>` να μένει exhaustive)
- Acceptance:
  - `NOTIF_ICON` καλύπτει και τα 5 kinds (deal/installment/warranty/pricehike/system)· καμία `pricehike` ειδοποίηση δεν renderάρει κενό glyph
  - `NotifKind` union περιλαμβάνει `pricehike` (αλλιώς tsc σπάει στο `Record` map)
  - tsc καθαρό (mobile)· μηδέν νέο endpoint/dep, μηδέν rebuild
- Status: ✅ DONE 2026-07-13 (pharos-daily-dev, `6ea29a0`) — επιβεβαιωμένο στον κώδικα: `NotifKind` (api.ts:466) έχει και τα 4 (pricehike/trialend/giftcard/bill), `NOTIF_ICON` (ActivityScreen.tsx:22) όλα mapped. mobile tsc EXIT 0.

### Receipts — return-window badge στο mobile (PA3 gap)
- Priority: P2 | Size: M | no AI, no decision · web-half (v1 serializer) + mobile-half
- Web ref: PA3 return-window tracker (`0514499`) — το web receipts δείχνει chip «Nd return» (Undo2 icon, cyan κανονικά / gold ≤3 μέρες) σε ReceiptCard + ReceiptRow (files: apps/web/src/app/receipts/page.tsx:41-43 computes `returnDaysLeft`, apps/web/src/app/receipts/ReceiptsClient.tsx renders το chip). Pure lib `lib/returnWindow.ts` (`effectiveReturnWindow`+`returnDaysLeft`) ΥΠΑΡΧΕΙ ήδη, μηδέν AI, deterministic.
- API: GET /api/v1/receipts (exists: yes, ΑΛΛΑ δεν εκθέτει το πεδίο) — ο builder πρέπει ΠΡΩΤΑ (web-half): στο v1 receipts route (apps/web/src/app/api/v1/receipts/route.ts) φόρτωσε `getAppSettings()` (για `defaultReturnWindowDays`) + `Store.find().lean()` (για per-store `returnWindowDays`), μετά ανά μη-archived απόδειξη υπολόγισε `returnDaysLeft(r.date, effectiveReturnWindow(r.store, stores, settings.defaultReturnWindowDays), now)` και πρόσθεσε το ως additive optional πεδίο στο `trimReceipt` (apps/web/src/app/api/v1/receipts/serialize.ts) — ίδιο pattern με το expense `anomaly` (computed, ΔΕΝ αποθηκεύεται· null → παραλείπεται). ΣΗΜ: το `trimReceipt` είναι pure τώρα (δέχεται μόνο `ReceiptLean`)· πέρασε το computed `returnDaysLeft` ως 2ο arg ή annotate τα docs πριν το map (μη-breaking για rescan/detail που δεν το δίνουν → undefined).
- Mobile files: apps/web/src/app/api/v1/receipts/route.ts + serialize.ts (web-half παραπάνω), apps/mobile/src/api.ts (`ReceiptRow`/λίστα type += `returnDaysLeft?: number`), apps/mobile/src/screens/ReceiptsScreen.tsx (στο list card, δίπλα στο verified/archived badge: `{typeof item.returnDaysLeft === 'number' && <Badge label={`${item.returnDaysLeft}d return`} .../>}` cyan κανονικά, gold όταν ≤3· mirror του web chip)
- Acceptance:
  - `GET /api/v1/receipts` επιστρέφει `returnDaysLeft` σε αποδείξεις εντός παραθύρου (additive· no-auth ακόμα 401, όχι 500)· αρχειοθετημένες/εκτός-παραθύρου → χωρίς το πεδίο
  - mobile receipts card δείχνει «Nd return» chip· ≤3 μέρες → gold· χωρίς πεδίο → κανένα chip
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
- Status: ✅ DONE 2026-07-13 (pharos-daily-dev, `6ea29a0`) — επιβεβαιωμένο στον κώδικα: `trimReceipt(r, returnDaysLeft?)` (serialize.ts:33) + list/detail routes το υπολογίζουν, mobile `ReceiptSummary.returnDaysLeft?` + `<Badge>` σε λίστα+modal (ReceiptsScreen.tsx:223-224,239-240). +9 route tests, vitest 2227/172 EXIT 0.

### Reports — net-worth headline + breakdown στο mobile (PA2 gap)
- Priority: P2 | Size: M | no AI, no decision (chart deferred → Needs Achilleas)
- Web ref: PA2 net-worth (`67bffc9`) — το web `/reports` net-position banner έγινε «Net worth» = inventory + manual assetAccounts − installments − card balances, με 4 breakdown chips (files: apps/web/src/app/reports/page.tsx:250-263 computes `accountsTotal`+`netWorth`, apps/web/src/app/reports/ReportsClient.tsx renders). Το v1 reports route επιστρέφει ΜΟΝΟ το παλιό `netPosition` (inventory − installmentsOwed, route.ts:71-76)· το mobile ReportsScreen δείχνει το παλιό (ReportsScreen.tsx:111-112).
- API: GET /api/v1/reports (exists: yes, παλιό shape) — additive πεδίο `netWorth: { assetsInventory, assetsAccounts, liabInstallments, liabCards, net }`. Ο builder: στο route φόρτωσε `getAppSettings().assetAccounts` (manual accounts, ίδιο pattern με budgets) → `assetsAccounts = Σ values`· `liabInstallments` = ήδη computed `installmentsOwed`· `liabCards` = card outstanding (last-statement-per-card balance· mirror του web getReports `outstanding`· το route έχει ήδη `Statement.find().lean()` στη γρ.44)· `net = assetsInventory + assetsAccounts − liabInstallments − liabCards`. ΟΛΑ deterministic, μηδέν AI, μηδέν νέο query πέρα από το appSettings read. Το `netPosition` ΜΕΝΕΙ (το χρησιμοποιούν aiTools/υπάρχον mobile).
- Mobile files: apps/web/src/app/api/v1/reports/route.ts (νέο `netWorth` object), apps/mobile/src/api.ts (`Reports` type += `netWorth?: {assetsInventory:number; assetsAccounts:number; liabInstallments:number; liabCards:number; net:number}`), apps/mobile/src/screens/ReportsScreen.tsx (η υπάρχουσα «Net position» κάρτα → «Net worth» headline `netWorth.net` + 4 breakdown chips [inventory / accounts / −installments / −cards] ως `<Chip>` rows· fallback στο παλιό `netPosition` όταν `netWorth` undefined· ΧΩΡΙΣ chart — mirror του chart-free ReportsScreen pattern)
- Acceptance:
  - `GET /api/v1/reports` επιστρέφει `netWorth` με τα 5 πεδία· additive → no-auth 401, όχι 500
  - mobile Reports δείχνει net-worth headline + 4 breakdown chips· manual accounts κενά → assetsAccounts 0 (δεν σπάει)
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
  - ΣΗΜ: το snapshot AreaChart trend (`captureAndListSnapshots` series) ΔΕΝ μπαίνει εδώ — θέλει RN charting lib (Needs Achilleas)· αυτό το item είναι το chart-free headline/breakdown slice
- Status: TODO

### Receipts — quick-verify rapid queue στο mobile — ✅ DONE 2026-07-09 (daily-dev, `00d6f46`)
- Priority: P1 | Size: M
- Web ref: rapid review queue (file: apps/web/src/app/receipts/QuickVerify.tsx· κουμπί «⚡ Quick verify (N)» στο ReceiptsClient header, N = parsed-but-unverified)
- API: GET /api/v1/receipts?limit=… (exists: yes· λίστα με `verified`/`archived`/`total`/`itemCount`/`store`/`date`/`thumb`) + PATCH /api/v1/receipts/[id] (exists: yes· partial `$set`, δέχεται `{store,date,total,verified,archived}` χωρίς να αγγίζει lineItems) — **κανένα νέο endpoint**
- Mobile files: apps/mobile/src/screens/ReceiptsScreen.tsx (νέο quick-verify modal/queue + «⚡ Quick verify (N)» κουμπί στο header)· καμία αλλαγή σε api.ts (τα `getReceipts` + `updateReceipt` αρκούν)
- Acceptance:
  - Header κουμπί «⚡ Quick verify (N)» όπου N = πλήθος parsed-but-unverified (`!verified && !archived && (total>0 || itemCount>0)`)· κρυφό όταν N=0
  - Focused queue που περνά μία απόδειξη τη φορά: thumbnail/preview + editable store (text ή picker) + date + total inline
  - Actions: **Verify & next** → `updateReceipt(id,{store,date,total,verified:true})` + προχωρά· **Skip** → επόμενη χωρίς αλλαγή· **Not a receipt** → `updateReceipt(id,{archived:true})` + προχωρά· **Edit fully** → ανοίγει το υπάρχον detail modal
  - Progress indicator «i / N · done» + κλείσιμο όταν τελειώσει η ουρά· η λίστα κάνει refresh (`load()`) στο τέλος
  - Το queue snapshot-άρεται στο άνοιγμα (δεν reshuffle-άρει καθώς verify-άρεις)
  - mobile `npx tsc --noEmit` EXIT 0· structural verify μόνο (δεν καλεί AI, μηδέν κόστος)
- Status: ✅ DONE 2026-07-09 (commit `00d6f46`· κουμπί «⚡ Quick verify (N)» κάτω από το scan, snapshot queue bottom-sheet με editable store/date/total + Verify & next / Skip / Edit fully / Not a receipt + progress bar· «Edit fully» κλείνει την ουρά και ανοίγει το detail modal· λίστα reload στο κλείσιμο)

### Expenses/Income — anomaly badge (±% vs vendor median) στο mobile — ✅ DONE 2026-07-02
- Priority: P2 | Size: S | no AI (pure stats), no decision, no dep, no native dep
- **DONE (pharos-daily-dev 2026-07-02):** νέα exported pure `computeAnomalies(docs)` στο `serialize.ts` (byte-mirror του web `page.tsx:28-46` median-deviation· returns array aligned στα docs)· `trimExpense` πήρε optional 2ο param `anomaly?` (μπαίνει στο output μόνο όταν defined → το single-doc `rescan` το παραλείπει). Το GET list route καλεί `computeAnomalies(docs)` μετά το `.lean()` και το περνά per-index· **skip όταν `updatedSince`** (incremental sync = partial slice → wrong medians). `ExpenseLean` += `vendorKey?`. Mobile: `Expense` type += `anomaly?`, gold `⚠ ±N%` badge δίπλα στο amount στο MoneyScreen row. +4 `computeAnomalies` tests + 1 anomaly-key test (serialize.test.ts, 18/18 green). Verify: web+mobile tsc EXIT 0· safe rebuild /login 200· GET no-auth (και με `updatedSince`) 401 όχι 500.
- Web ref: το web `/expenses` υπολογίζει ανά load `anomaly` = ±% απόκλιση από το vendor-series median (≥3 priced entries, |dev|>0.3) και δείχνει gold `AnomalyBadge` σε ExpenseRow/ExpenseCard (file: apps/web/src/app/expenses/page.tsx:25-46, apps/web/src/app/expenses/ExpensesClient.tsx:303,340)
- API: GET /api/v1/expenses (exists: yes) — χρειάζεται ΕΝΑ additive πεδίο `anomaly?: number` στο trimmed output. Το route ήδη import-άρει `vendorKey` (route.ts:7)· ο serializer (`serialize.ts`) όμως στρίβει vendorKey/anomaly. Υπολόγισε στο **list route** μετά το `.lean()`: group amounts ανά `doc.vendorKey`, median ανά group (byte-mirror του page.tsx:28-46), attach `anomaly` σε κάθε trimmed item. ΣΗΜ: το v1 list είναι paginated (skip/limit) → το median υπολογίζεται πάνω στη ΣΕΛΙΔΑ (το mobile φορτώνει με μεγάλο limit, οπότε συνήθως όλη η series είναι μέσα)· αποδεκτή προσέγγιση, τεκμηρίωσέ το. Το `rescan` single-record response ΔΕΝ χρειάζεται anomaly (μονήρες record → δεν υπάρχει series context· το web το υπολογίζει μόνο σε list).
- Mobile files: apps/web/src/app/api/v1/expenses/route.ts (compute + attach `anomaly` στο GET map· ΟΧΙ στον shared serializer, για να μην το ζητά το rescan), apps/mobile/src/api.ts (`Expense` type += `anomaly?: number`), apps/mobile/src/screens/MoneyScreen.tsx (μικρό gold `⚠ ±N%` badge σε row + detail όταν `anomaly != null`, tint με `C.gold`)
- Acceptance:
  - `GET /api/v1/expenses` επιστρέφει `anomaly` σε records που αποκλίνουν >30% από το vendor median· additive → no-auth ακόμα 401, όχι 500· υπόλοιπο shape byte-stable
  - mobile Expense/Income row + detail δείχνει gold `⚠ +N%`/`⚠ -N%` badge όταν anomaly· κρύβεται αλλιώς
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
- Status: ✅ DONE 2026-07-02 (επιβεβαιωμένο στον κώδικα, 39η σάρωση: `computeAnomalies`+`trimExpense(…, anomaly)` @ serialize.ts, list route καλεί μόνο όταν όχι `updatedSince`, mobile `Expense.anomaly?` + gold badge @ MoneyScreen:170,273· stale `Status: TODO` διορθώθηκε)

### Expenses/Income — vendor autocomplete στο add-form (mobile)
- Priority: P3 | Size: S | no AI, no decision, no endpoint, no native dep
- Web ref: το web add-expense form έχει vendor autocomplete/datalist από τα υπάρχοντα vendors (file: apps/web/src/app/expenses/ExpensesClient.tsx, page.tsx:49 `vendors` list)
- API: **κανένα νέο** — το mobile MoneyScreen ήδη φορτώνει όλη τη λίστα expenses (`load()`)· derive distinct vendors client-side από τα ήδη-φορτωμένα items
- Mobile files: apps/mobile/src/screens/MoneyScreen.tsx (add-form vendor `Input` [line 147] → πρόσθεσε suggestion chips/dropdown από `[...new Set(items.map(i=>i.vendor))]` filtered κατά το typed prefix· tap → set vendor)· προαιρετικά μικρό reusable helper
- Acceptance:
  - Typing στο vendor field δείχνει matching υπάρχοντες vendors· tap συμπληρώνει το field· κενό input → καμία λίστα
  - Μηδέν νέο endpoint/dep· tsc καθαρό (mobile)
- Status: ✅ DONE 2026-07-02 (pharos-daily-dev). `useMemo` `vendorList` = case-insensitive dedup των `rows.vendor` (κρατά first casing, sorted). `vendorSuggestions` = substring-match στο typed `vendor.trim()`, εξαιρεί exact match, cap 6. Render = horizontal `ScrollView` με `<Chip>` (keyboardShouldPersistTaps="handled" ώστε το tap να μη χάνεται) κάτω από το add-row· tap → `setVendor(v)`. Κενό input → καμία λίστα. Μηδέν endpoint/dep. mobile tsc EXIT 0. (apps/mobile/src/screens/MoneyScreen.tsx)

### Expenses/Income — δείξε το scanned bill image στο mobile detail
- Priority: P2 | Size: S | no AI, no decision, no native dep
- Web ref: το web expense detail δείχνει το αποθηκευμένο αρχείο (image/PDF thumb) πάνω από τη φόρμα (file: apps/web/src/app/expenses/ExpensesClient.tsx) — το mobile MoneyScreen detail το παραλείπει εντελώς.
- API: GET /api/v1/expenses (exists: yes) — **καμία αλλαγή**· ο serializer εκθέτει ήδη `file`/`thumb` (apps/web/src/app/api/v1/expenses/route.ts:32-33) και ο mobile `Expense` type τα φέρει (`file: string|null; thumb: string|null`).
- Mobile files: apps/mobile/src/screens/MoneyScreen.tsx (import `fileSource` από `../api`· στο detail modal, πάνω από τα edit fields: `{fileSource(sel.file) && <Image source={fileSource(sel.file)} ... resizeMode="contain" />}` — ίδιο pattern με ReceiptsScreen.tsx:170 + λίστα thumbnail με `fileSource(item.thumb || item.file)` όπως ReceiptsScreen:147)
- Acceptance:
  - Expense/Income με αποθηκευμένο αρχείο δείχνει το bill image στο detail (και προαιρετικά thumbnail στη λίστα)· manual entries (κενό `file`) δεν σπάνε (guard)
  - Χρήση του υπάρχοντος `fileSource()` helper (authed uri + headers)· μηδέν νέο endpoint, μηδέν νέα dep
  - tsc καθαρό (mobile)
- Status: **DONE 2026-07-02** — `MoneyScreen.tsx`: import `Image` (react-native) + `fileSource` (../api)· στο edit/detail modal, πρώτο child του ScrollView `{editing && fileSource(editing.file) && <Image source={fileSource(editing.file)} style={s.bigImg} resizeMode="contain" />}`· νέο style `bigImg` (width 100%, height 220, radius 12, marginTop 12). Manual entries (`file:null`) → guard short-circuit, καμία εικόνα. Μηδέν νέο endpoint/dep. `npx tsc --noEmit` → EXIT 0. (Λίστα thumbnail παραλείφθηκε σκόπιμα — το detail image καλύπτει το gap· η λίστα μένει καθαρή text-first, όπως το web ExpenseRow.)

### Expenses/Income — re-scan stored file (OCR/text) στο mobile detail
- Priority: P2 | Size: M | ⚠ AI cost → δομικό verify μόνο
- Web ref: rescanExpense (file: apps/web/src/app/expenses/actions.ts:348) — ξανατρέχει το AI parse στο αποθηκευμένο αρχείο του expense, mirror του `rescanReceipt`.
- API: POST /api/v1/expenses/[id]/rescan (exists: **no**) — ΝΕΟ endpoint, **byte-mirror** του `apps/web/src/app/api/v1/receipts/[id]/rescan/route.ts`: `withAuth` + `isObjectId` guard (bad id → 400), `readBody` → `{ocr?:boolean}`, καλεί `rescanExpense(id, useOcr)`, μετά re-read + επιστρέφει το ΙΔΙΟ shape με το GET expenses `trim()` (apps/web/src/app/api/v1/expenses/route.ts:18-35) ώστε το mobile detail να re-prefill in-place. 404 σε not found/missing file.
- Mobile files: apps/web/src/app/api/v1/expenses/[id]/rescan/route.ts (ΝΕΟ), apps/mobile/src/api.ts (νέο `rescanExpense(id, ocr)` → `POST /api/v1/expenses/${id}/rescan`, return type = `Expense`), apps/mobile/src/screens/MoneyScreen.tsx (detail modal: «Re-scan: text / OCR» buttons → spinner → re-prefill τα editable πεδία in-place· εμφανίζονται μόνο όταν `sel.file` υπάρχει)
- Acceptance:
  - `POST /api/v1/expenses/:id/rescan` no-token → 401· bad id → 400· ok → ίδιο shape με GET expenses [id]
  - Buttons εμφανίζονται μόνο για expenses με αποθηκευμένο αρχείο· re-prefill in-place χωρίς reopen
  - **Δομικό verify μόνο** (endpoint registered + no-token 401 + tsc καθαρό web+mobile)· **ΜΗΝ τρέξεις πραγματικό AI scan** (κόστος)
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
- Status: **DONE 2026-07-02** (builder `2201932` `feat(mobile): AI re-scan (text/OCR) for Expenses & Income`, επιβεβαιωμένο 37η σάρωση) — νέο `POST /api/v1/expenses/:id/rescan` (byte-mirror του receipts rescan route) + wired στο MoneyScreen edit modal ως «Re-scan text/OCR» bar (εμφανίζεται μόνο όταν `editing.file` υπάρχει· spinner ανά κουμπί, `prefill(updated)` re-prefill in-place, record μένει unverified). Εξήχθη shared `expenses/serialize.ts` (`trimExpense`+`ExpenseLean`) ώστε list & rescan routes να εκπέμπουν το ΙΔΙΟ v1 Expense shape. Token-clean (νέα styles `rescanBar`/`rescanLabel`/`rescanBtn`/`rescanText` όλα `C.*`, μηδέν hex). mobile `tsc --noEmit` EXIT 0. ΣΗΜ: ο νέος `rescanBtn` (cyan-border ghost, radius10) είναι +1 site στο ghost-`<Button variant>` holdout παρακάτω.

### Reports — inventory-value by category (bars) στο mobile
- Priority: P1 | Size: S
- Web ref: το web `/reports` pie «INVENTORY BY CATEGORY» ΔΕΝ είναι spend· τροφοδοτείται από owned-inventory-value grouped by category (`catSpend`/`spendByCategory` = sum `purchasedPrice ?? currentPrice` ΜΟΝΟ owned items) — file: apps/web/src/app/reports/page.tsx:163-184· consumed apps/web/src/app/reports/ReportsClient.tsx:254-268 (`<Pie data={data.spendByCategory}>`, παρά το όνομα var = inventory value)
- API: GET /api/v1/reports (exists: yes) — χρειάζεται ΕΝΑ additive πεδίο `inventoryByCategory: {name;value}[]`. ΣΗΜ: το υπάρχον `byCategory` του route είναι EXPENSE-based (route.ts:106, `byCat[cat]+=amt` σε expenses), ΟΧΙ inventory → δεν καλύπτει αυτό το gap· το route έχει ΗΔΗ `Item.find().select('status purchasedPrice currentPrice ...')` (route.ts:35), οπότε μηδέν νέο query
- Mobile files: apps/web/src/app/api/v1/reports/route.ts (νέος `inventoryByCategory` υπολογισμός — byte-mirror του page.tsx catSpend: loop owned items, `v = purchasedPrice ?? currentPrice ?? 0`, `if(v>0) map[cat]+=v`, `{name,value:Math.round}`, sort desc), apps/mobile/src/api.ts (`Reports` type += `inventoryByCategory:{name:string;value:number}[]`), apps/mobile/src/screens/ReportsScreen.tsx (νέα section «INVENTORY · VALUE BY CATEGORY» ως `Bar` rows, νέο `invMax`, guard `(d.inventoryByCategory?.length ?? 0) > 0`, mirror του υπάρχοντος subs-by-category/spend-by-store bar pattern — ΟΧΙ pie, μηδέν RN charting lib, μηδέν νέα native dep)
- Acceptance:
  - `GET /api/v1/reports` επιστρέφει `inventoryByCategory` (owned-value ανά κατηγορία, sorted desc)· additive → no-auth ακόμα 401, όχι 500
  - mobile Reports δείχνει νέα section με bars ανά κατηγορία (values = inventory value)· κρύβεται όταν κενό
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
  - ΚΛΕΙΝΕΙ το τελευταίο εναπομείναν Reports parity gap (το «inventory-pie» που ήταν flag-αρισμένο ως needs-lib· γίνεται bars αντί pie, όπως τα υπόλοιπα 8 Reports sections)
- Status: **DONE** (2026-07-02, pharos-daily-dev — endpoint `inventoryByCategory` additive + mobile «INVENTORY VALUE · BY CATEGORY» cyan bars· web+mobile tsc EXIT 0· safe rebuild /login 200, reports no-auth 401, web restarts 0)

### Tasks — steps / checklist στο mobile detail
- Priority: P2 | Size: M | no AI, decision ΕΓΙΝΕ (Αχιλλέας 2026-07-01: promote από Needs Achilleas)
- Web ref: addStep/toggleStep/deleteStep + Steps section στο modal (file: apps/web/src/app/tasks/actions.ts:81-96, apps/web/src/app/tasks/TasksClient.tsx:660-688)
- API: το `Task` model **έχει ήδη** `steps: [{ text, done, _id }]` (models/Task.ts:4-10) αλλά το REST **ΔΕΝ τα εκθέτει**: το `trim()` σε GET `/api/v1/tasks` (route.ts) + το PATCH `/api/v1/tasks/[id]` δεν περιλαμβάνουν `steps`. **Ο builder πρέπει ΠΡΩΤΑ (web half):** (α) πρόσθεσε `steps: (t.steps??[]).map(s=>({id:String(s._id), text:s.text, done:!!s.done}))` στο `trim()` **και** στα δύο routes (list + [id] PATCH response)· (β) στο PATCH `/api/v1/tasks/[id]` δέξου `steps` ως **full-array replacement** — `if (Array.isArray(b.steps)) set.steps = b.steps.map(s=>({text:String(s.text||'').trim(), done:!!s.done})).filter(s=>s.text)` (idempotent, mobile στέλνει όλο το updated array· απλούστερο από sub-routes). **Κανένα νέο route file.**
- Mobile files: apps/mobile/src/api.ts (`Task` type += `steps?:{id?:string;text:string;done:boolean}[]`· `updateTask` data type += `steps?`), apps/mobile/src/screens/TasksScreen.tsx (edit modal: section «STEPS» με λίστα `<Check>`+text ανά step [tap toggle], × remove, input+＋ add· κάθε mutation → `updateTask(id,{steps:next})` optimistic· στη λίστα/card: badge `doneCount/total` όταν total>0, mirror του web `ListChecks doneSteps/steps.length`)
- Acceptance:
  - GET tasks + PATCH επιστρέφουν `steps[]`· PATCH με `steps` array → replace· no-token → 401· bad id → 400
  - Edit modal: add/toggle/remove step → reflect μετά reload· card badge δείχνει `2/5`
  - tsc καθαρό (web + mobile)· safe rebuild → /login 200, web restarts 0
- Status: ✅ DONE (2026-07-01 builder). Web half: `steps[]` στο `trim()` (list GET) + PATCH response + PATCH δέχεται `steps` ως full-array replacement (idempotent, filter empty). Mobile: `TaskStep` type + `updateTask` δέχεται `steps` + edit-modal STEPS section (Check-toggle / × remove / input+＋ add, κάθε mutation → optimistic `updateTask(id,{steps})`) + card badge `☑ done/total` (accent όταν full). Verified: both tsc 0, safe rebuild /login 200 web restarts 0, POST tasks no-token → 401.

### Mobile — αντικατάσταση όλων των emoji/glyph icons με lucide (icon set)
- Priority: P2 | Size: M | no AI, decision ΕΓΙΝΕ (Αχιλλέας 2026-07-01: «δεν θέλω χαζά εικονίδια») · ⚠ attended-preferred (οπτικό verify)
- Web ref: `lucide-react` παντού (file: apps/web/package.json + apps/web/src/components/SiteNav*, nav cards με `Package/Cart/Receipt/CreditCard/CalendarClock/ListChecks/BarChart3` κλπ) — το mobile χρησιμοποιεί emoji/unicode glyphs αντί για icon set.
- API: **κανένα** — pure mobile UI.
- Πρόβλημα: **58 emoji/glyph icon sites / 12 αρχεία** (nav.tsx + 11 screens): `＋ ✦ 🔔 📷 🔍 🗂 🗄 🛡 ↑ ↗ ↘ ← → ∑ ⚡ 🎯 💳` κ.ά. ως `<Text>` glyphs. Ασυνεπή, «χαζά», δεν ταιριάζουν με το web design system.
- Λύση: **`lucide-react-native`** (`npx expo install lucide-react-native` — peer `react-native-svg` **ΕΙΝΑΙ ήδη dep** [15.12.1], μηδέν νέα native dep) → ίδιο icon vocabulary με το web. Νέο `apps/mobile/src/icons.tsx` (thin re-export των icons που χρειάζονται, default size/`color={C.text}`), μετά αντικατάσταση των glyphs. **Mapping** (mirror web): `＋`→`Plus`, `✦/AI`→`Sparkles`, `🔔`→`Bell`, `📷`→`Camera`, `🔍`→`Search`, `🗂/🗄`→`Archive`, `🛡`→`ShieldCheck`, `↑`→`TrendingUp`, `↘`→`TrendingDown`, `←/→`→`ChevronLeft/Right`, `∑`→`Sigma`, `⚡`→`Zap`, `🎯`→`Target`, `💳`→`CreditCard`· nav tiles → τα ίδια lucide με τα web NavCards (`Package/ShoppingCart/Receipt/CalendarClock/ListChecks/BarChart3/Ticket/Wallet`). ΣΗΜ: το `••${last4}` της κάρτας **δεν** είναι icon → μένει text· το `✓` έχει ήδη `<Check>` primitive.
- Mobile files: apps/mobile/package.json (+`lucide-react-native`), νέο apps/mobile/src/icons.tsx, apps/mobile/src/nav.tsx + 11 screens (nav/Activity/Assistant/Items/Money/Receipts/Shopping/Statements/Subscriptions/Tasks/Vouchers)
- Acceptance:
  - Μηδέν emoji/glyph icon σε `<Text>` για UI affordances (grep καθαρό στα 12 αρχεία)· όλα μέσω lucide components με χρώμα από `theme.ts`
  - Icon sizes/colors συνεπή (default μέσω `icons.tsx`)· nav tiles + AppBar (🔔/🔍) + κάθε κουμπί ταιριάζουν οπτικά με το web
  - tsc καθαρό (mobile)· οπτικό verify σε device/simulator (attended)
- Status: TODO

### Tasks — swipe-to-change-status (αντί Kanban)
- Priority: P3 | Size: M | no AI · ⚠ dep-add + attended-preferred (gesture + οπτικό verify)
- Web ref: Kanban ←/→ quick-move (file: apps/web/src/app/tasks/TasksClient.tsx) — mobile-friendly εκδοχή = swipe, ΟΧΙ board (columns δεν χωράνε σε phone)
- API: **κανένα νέο** — το `setTaskStatus(id,status)` υπάρχει ήδη (api.ts, PATCH `/api/v1/tasks/[id]`, TasksScreen:53 toggle το χρησιμοποιεί)
- Mobile files: apps/mobile/package.json (+`react-native-gesture-handler`, Expo-compatible· `npx expo install`), apps/mobile/App.tsx (wrap σε `GestureHandlerRootView`), apps/mobile/src/screens/TasksScreen.tsx (κάθε task row → `Swipeable`: swipe-right → επόμενο status στον κύκλο todo→in-progress→done, swipe-left → blocked/πίσω· `setTaskStatus` optimistic). **Εναλλακτικά (no-dep, unattended-safe):** inline ←/→ κουμπιά που κυκλώνουν status (mirror του web) αν ο builder τρέχει unattended χωρίς simulator.
- Acceptance:
  - Swipe (ή ←/→) αλλάζει status· reflect + persist· sort ξαναφέρνει done κάτω
  - tsc καθαρό· gesture verify σε device/simulator (attended) ή structural μόνο για το no-dep variant
- Status: ✅ DONE (no-dep ←/→ variant, 2026-07-01 builder). TasksScreen κάρτα: inline ←/→ κουμπιά (δεξιά, δίπλα στο title) που κινούν το status κατά μήκος του `STATUSES` flow (todo→in-progress→blocked→done, mirror του web `COLUMNS` order)· disabled+dimmed στα άκρα (idx 0 → no ←, idx last → no →)· `move(it, dir)` optimistic + `setTaskStatus` persist, catch → `load()`. Κανένα νέο dep/endpoint (χωρίς `react-native-gesture-handler`)· η swipe-gesture εκδοχή μένει attended-preferred (οπτικό verify). mobile tsc EXIT 0. **Το checkbox (toggle done↔todo) παραμένει** ως ξεχωριστό affordance.

### Settings — language switcher στο mobile
- Priority: P3 | Size: L | no AI, decision ΕΓΙΝΕ (Αχιλλέας 2026-07-01) · ⚠ string-extraction heavy — 16 screens
- Web ref: cookie-based i18n (file: apps/web/src/lib/i18n/server.ts + config.ts + dictionaries· 8 locales, `LOCALE_COOKIE`)
- API: **κανένα** — client-side i18n (persist locale σε `AsyncStorage`, όχι server). Το mobile έχει **μηδέν** i18n σήμερα (hardcoded English σε 16 screens).
- Mobile files: νέο apps/mobile/src/i18n.ts (mirror του web `config.ts`: LOCALES + `t(key)` + `LocaleProvider`/`useT` context, persist `AsyncStorage('pharosLocale')`), port των web dictionaries → mobile dict (ξεκίνα από el+en, τα υπόλοιπα incremental), apps/mobile/src/screens/SettingsScreen.tsx (language picker κάτω από preferences), **σταδιακή** αντικατάσταση hardcoded strings → `t('key')` σε ΟΛΑ τα 16 screens
- Acceptance:
  - Language picker αλλάζει locale· persisted μεταξύ launches· τα migrated screens ακολουθούν άμεσα
  - Ξεκίνα incremental: infra + el/en dict + 2-3 core screens (Home/Settings/Tasks) σε πρώτο pass· τα υπόλοιπα screens ως follow-up (μη-block)
  - tsc καθαρό (mobile)
- Status: TODO
- ΣΗΜ builder: **L λόγω string-extraction** (κάθε αγγλικό literal σε 16 screens). Σπάσε σε passes· μην περιμένεις one-shot. Attended-preferred για οπτικό verify των μεταφράσεων.

### Tasks — tags + priority στο add/edit (mobile) ✅ DONE (2026-07-01, builder)
- Priority: P3 | Size: S | no AI, no decision
- Web ref: createTask + UpdateTask (file: apps/web/src/app/tasks/actions.ts, apps/web/src/app/tasks/TasksClient.tsx) — το web new-task modal καταχωρεί tags + priority· το mobile όχι.
- API: POST /api/v1/tasks (exists: yes — δέχεται ήδη `tags` [array ή comma-string] + `priority` [low/normal/high], route.ts:52,58) · PATCH /api/v1/tasks/[id] (exists: yes — δέχεται ήδη `tags`/`priority`/`content`/`dueDate`). **Κανένα νέο endpoint· ο builder αγγίζει ΜΟΝΟ mobile.**
- Mobile files: apps/mobile/src/api.ts (`addTask` signature += `{title, tags?, priority?}`· `updateTask` data type += `tags?: string[]`), apps/mobile/src/screens/TasksScreen.tsx (στο `add()`: parse `#word` tokens από το title → tags[], strip από το title πριν το POST [mirror του web quick-add]· στο edit modal: priority picker low/normal/high + tags editor [chips add/remove ή comma input] → `updateTask(id, {title, status, priority, tags})`)
- Acceptance:
  - Quick-add «Buy switch #network #order» → δημιουργεί task title «Buy switch» με tags ['network','order'] (ορατά ως #network #order στη λίστα)
  - Edit modal αλλάζει priority + tags· reflect μετά το reload· POST/PATCH no-token → 401· bad id → 400
  - tsc καθαρό (mobile)
- Status: ✅ DONE (commit pending). `api.ts`: `addTask(title, {tags?,priority?})` + `updateTask` data type += `tags?: string[]`. `TasksScreen.tsx`: `splitTitleTags()` (#word parse με `\p{L}` για ελληνικά tags, strip από title, all-tags input κρατά raw ώστε title≠κενό) στο `add()`· edit modal += PRIORITY picker (low/normal/high, gold high) + TAGS input (space/comma → `parseTags()` dedupe+lowercase)· `updateTask(id,{title,status,priority,tags})`. mobile tsc EXIT 0. Καμία αλλαγή web runtime → χωρίς Docker rebuild.

### Statements — installment-plan overview στο mobile detail
- Priority: P1 | Size: M
- Web ref: InstallmentOverview + computeInstallmentPlans (file: apps/web/src/lib/installments.ts, apps/web/src/app/statements/StatementsClient.tsx)
- API: GET /api/v1/statements/plans (exists: no) — νέο endpoint, mirror του apps/web/src/app/api/v1/items/[id]/plans/route.ts ΧΩΡΙΣ το per-item `linked` filter (return όλα τα plans: label/card/perAmount/remaining*/totalAmount/projectedEndDate/done/itemCount). Η λογική (`computeInstallmentPlans(statements)`) είναι ήδη proven.
- Mobile files: apps/mobile/src/api.ts (νέο `getInstallmentPlans()` + type), apps/mobile/src/screens/StatementsScreen.tsx (header section «INSTALLMENT PLANS» πάνω από τη λίστα: ανά plan → label, card, remaining/total amount, παίδα N/M, payoff date, active-first)
- Acceptance:
  - GET /api/v1/statements/plans no-token → 401· με token → `{currency, plans:[…]}` ομαδοποιημένα cross-statement (ίδια signature → ένα plan)
  - StatementsScreen δείχνει active plans πρώτα με remaining amount + projected payoff· done plans μετά (ή κρυμμένα)
  - Ποσά μέσω `money()` + το σωστό currency· tsc καθαρό (web + mobile)
- Status: DONE (2026-06-30) — `GET /api/v1/statements/plans` + StatementsScreen ListHeader «INSTALLMENT PLANS». Authed round-trip: 9 plans, 5 active-first, real data, read-only.

### Receipts — re-scan stored file (OCR/text) στο detail [MOBILE HALF, web endpoint ΕΤΟΙΜΟ]
- Priority: P2 | Size: M | ⚠ AI cost → δομικό verify μόνο
- Web ref: rescanReceipt (file: apps/web/src/app/receipts/actions.ts)
- API: POST /api/v1/receipts/[id]/rescan (exists: **yes** — `d24be27`, `apps/web/src/app/api/v1/receipts/[id]/rescan/route.ts`) — body `{ocr?:boolean}`· καλεί το ίδιο pipeline με το web `rescanReceipt(id, useOcr)`, withAuth + 24-hex id-guard (bad id → 400), επιστρέφει **ΑΚΡΙΒΩΣ το shape του GET /api/v1/receipts/[id]** (re-query + trimReceipt + normalized lineItems `{name,qty,price,vatRate}` + notes) + `aiUsed/model/aiError`. **ΑΡΑ ΑΠΟΜΕΝΕΙ ΜΟΝΟ το mobile half** — μην ξαναχτίσεις το endpoint.
- Mobile files: apps/mobile/src/api.ts (νέο `rescanReceipt(id, ocr)` → `POST /api/v1/receipts/${id}/rescan`, type = το ίδιο με `getReceipt`), apps/mobile/src/screens/ReceiptsScreen.tsx (στο detail modal: «Re-scan: OCR / text» buttons → spinner → re-prefill τα editable πεδία [store/date/total/lineItems/notes] από το response, όπως μετά το camera scan)
- Acceptance:
  - `rescanReceipt` καλεί το υπάρχον endpoint· response έχει ίδιο shape με `getReceipt` → re-prefill in-place (χωρίς reopen)
  - Δομικό verify μόνο (το endpoint είναι ήδη registered + no-token 401 verified· tsc καθαρό web+mobile)· **ΜΗΝ τρέξεις πραγματικό AI scan** (κόστος)
- Status: DONE (2026-07-01) — `rescanReceipt(id, ocr)` στο `api.ts` (POST `/api/v1/receipts/${id}/rescan`, response = `{receipt:ReceiptDetail, aiUsed, model, aiError}`) + «Re-scan text/OCR» bar στο ReceiptsScreen detail modal (κάτω από το image, πάνω από STORE): spinner ανά κουμπί, `setDetail(r.receipt)` → ο υπάρχων prefill effect ξανα-γεμίζει store/date/total/lineItems/notes in-place + `load()` refresh της λίστας, `aiError`→Alert. Δομικό verify μόνο (tsc mobile EXIT 0)· **κανένα πραγματικό AI scan** δεν τρέχτηκε (κόστος).

### Items — convert to task
- Priority: P3 | Size: S | no AI
- Web ref: convertItemToTask (file: apps/web/src/app/items/actions.ts:649)
- API: POST /api/v1/items/[id]/convert-to-task (exists: **yes**) — δημιουργεί Task από το item (title + price + links σε HTML content, tag 'shopping'), επιστρέφει `{ok, taskId}`. Καθαρό wrap του `convertItemToTask` (μηδέν AI), 24-hex id-guard, item αμετάβλητο.
- Mobile files: apps/mobile/src/api.ts (`convertItemToTask(id)`), apps/mobile/src/screens/ItemsScreen.tsx (κουμπί «＋ Convert to task» στο item edit modal + Alert επιβεβαίωση)
- Acceptance:
  - POST .../convert-to-task no-token → 401· valid → νέο Task ορατό στο TasksScreen
  - tsc καθαρό (web + mobile)
- Status: **DONE** (2026-06-30 builder — `POST /api/v1/items/[id]/convert-to-task` + mobile button· tsc καθαρό web+mobile· structural 401 verified)

### Notifications — unread badge στο AppBar
- Priority: P3 | Size: S | no AI
- Web ref: notification bell με unread count (file: apps/web/src/components/SiteNav* / notifications)
- API: GET /api/v1/notifications (exists: yes) — επιστρέφει ήδη `items` με `read`
- Mobile files: apps/mobile/src/nav.tsx (AppBar: μικρό 🔔 με κόκκινο badge = unread count, tap → Activity/Alerts), apps/mobile/App.tsx (poll/refresh count· tap → setScreen('activity'))
- Acceptance:
  - Badge δείχνει τον αριθμό unread· μηδέν unread → χωρίς badge
  - Tap → ανοίγει Activity (Alerts tab)· tsc καθαρό
- Status: DONE (2026-06-30) — AppBar 🔔 με κόκκινο badge (unread count, 99+ cap)· tap → `setScreen('activity')` (ActivityScreen default tab = alerts). App.tsx: `unread` state, `getNotifications().unread` poll κάθε 60s + refresh σε κάθε screen change (πιάνει το mark-read στο Activity). Καμία αλλαγή σε api.ts (`getNotifications` γυρνά ήδη `unread`). mobile tsc EXIT 0.

### Expenses/Income — full-field edit
- Priority: P3 | Size: S | no AI
- Web ref: updateExpense full fields (file: apps/web/src/app/expenses/actions.ts)
- API: PATCH /api/v1/expenses/[id] (exists: PARTIAL) — δέχεται ΜΟΝΟ vendor/amount/category/kind/notes/date (apps/web/src/app/api/v1/expenses/[id]/route.ts:17-22). ⚠ ΛΕΙΠΟΥΝ period/recurring/recurringCycle/paymentMethod → ο builder πρέπει ΠΡΩΤΑ να επεκτείνει το PATCH (mirror του web `updateExpense`: `set.period`, `set.recurring`=bool, `set.recurringCycle` enum-guarded, `set.paymentMethod`) πριν τα δείξει στο mobile.
- Mobile files: apps/mobile/src/api.ts (`updateExpense` signature += period/recurring/recurringCycle/paymentMethod), apps/mobile/src/screens/MoneyScreen.tsx (το `openEdit`/`saveEdit` καλύπτουν μόνο vendor/amount/category → πρόσθεσε date, period, recurring toggle + cycle picker, payment, notes)
- Acceptance:
  - PATCH /api/v1/expenses/[id] δέχεται πλέον period/recurring/recurringCycle/paymentMethod (enum-guarded cycle)· no-token → 401· bad id → 400
  - Edit modal αποθηκεύει date/period/recurring/cycle/payment/notes· reflect μετά το reload
  - tsc καθαρό (web + mobile)
- Status: DONE (2026-06-30) — PATCH επεκτάθηκε με period/recurring/recurringCycle (enum-guard ίδιος με POST)/paymentMethod· MoneyScreen edit modal πλέον date/period/payment/recurring toggle+cycle picker/notes (ScrollView, maxHeight 88%)· mobile `Expense` type + `updateExpense` signature += τα 4+notes πεδία. Structural verify: safe rebuild → /login 200, web restarts 0, PATCH no-token/bogus-token → 401· web+mobile tsc EXIT 0.

### Items — AI specs / AI-fill info
- Priority: P3 | Size: M | ⚠ AI cost → δομικό verify μόνο
- Web ref: aiFillSpecs / aiFillInfo (file: apps/web/src/app/items/actions.ts:533,571)
- API: POST /api/v1/items/[id]/ai-fill (exists: yes, 2026-07-01) — body `{mode:'specs'|'info'}`· wraps τα web actions· returns `{ok, mode, specs?, filled?, error?}` (όχι item — ο client ξανακαλεί GET /items/:id)
- Mobile files: apps/mobile/src/api.ts (`aiFillItem(id, mode)`), apps/mobile/src/screens/ItemsScreen.tsx (κουμπιά «✦ AI specs» / «✦ AI info» στο item detail)
- Acceptance:
  - POST .../ai-fill no-token → 401· bad id → 400 ✓
  - Δομικό verify μόνο (δεν τρέχτηκε πραγματικό AI)· web+mobile tsc EXIT 0 ✓
- Status: DONE (2026-07-01) — νέο route `items/[id]/ai-fill` (id-guard 24-hex, withAuth, mode default 'specs', AI-feature-gated μέσω των actions)· mobile `aiFillItem(id, mode)` + AI bar (specs/info) στο edit modal· on-success → re-fetch detail + list refresh, 'specs' γεμίζει το textarea, 'info' → Alert με τα filled πεδία. Structural verify: safe rebuild → /login 200, web restarts 0, ai-fill no-token → 401. **Parity queue: 6/6 DONE.**

### Notifications — νέο `pricehike` kind (P14) δεν χειρίζεται στο mobile
- Priority: P3 | Size: S | no AI
- Web ref: P14 price-hike/drop watch (commit 74f9cd4)· `NotifKind` += `'pricehike'` (file: apps/web/src/app/notifications/actions.ts), bell icon `TrendingUp`/red + format «Rose {prev} → {curr} ({pct})» (file: apps/web/src/components/NotificationBell.tsx), body wire-format `"prev|curr|pct"`, title = vendor
- API: GET /api/v1/notifications (exists: yes, ΑΜΕΤΑΒΛΗΤΟ route) — μπορεί πλέον να επιστρέψει item με `kind:'pricehike'` (additive, μη-breaking). Το route.ts δεν άλλαξε· η νέα τιμή έρχεται από το `computeAlerts`.
- Πρόβλημα (graceful, ΟΧΙ crash): (1) mobile `NotifKind` union (apps/mobile/src/api.ts:465) = `'deal'|'installment'|'warranty'|'system'` → stale, λείπει `'pricehike'`· (2) `NOTIF_ICON` (apps/mobile/src/screens/ActivityScreen.tsx:22-24) δεν έχει key `pricehike` → **κενό emoji icon** στο alert row· (3) το `AlertsTab` δείχνει `item.body` **raw** (γρ.101) → «13|15|15» αντί «Rose €13 → €15 (+15%)» (ίδιο pre-existing debt με το `installment` body «222|5»).
- Mobile files: apps/mobile/src/api.ts (`NotifKind` += `'pricehike'`), apps/mobile/src/screens/ActivityScreen.tsx (`NOTIF_ICON.pricehike` = π.χ. '📈'· προαιρετικά split-format του `prev|curr|pct` body ώστε να διαβάζεται)
- Acceptance:
  - Alert row για price-hike δείχνει icon (όχι κενό) + ανθρώπινο κείμενο· mobile tsc EXIT 0
- Status: TODO (flagged by reviewer 2026-07-11). Αφέθηκε ΑΔΙΟΡΘΩΤΟ στο review: η επιλογή emoji + το body-formatting είναι product-level (και το raw-body είναι κοινό pre-existing gap με το installment). Χαμηλή προτεραιότητα — καθαρή graceful degradation, μηδέν crash.

---

## UI Debt Queue
> **ui-auditor re-audit 2026-07-09 (48η σάρωση· read-only live grep, όχι docs read-back):** mobile `npx tsc --noEmit` → **EXIT 0**. Working tree: `apps/mobile/src/ReceiptsScreen.tsx` uncommitted-modified (WIP Αχιλλέα) → τα **3 cellInput** raw `<TextInput>` (γρ.211-213, qty/net/vat) μένουν WIP-blocked/attended-preferred. **Δέλτα από 47η:** το ShoppingScreen ModalSheet holdout **ΕΚΛΕΙΣΕ** (raw fade `<Modal>` + το τελευταίο `borderRadius:18` magic literal έφυγαν, migrated σε `<ModalSheet>`). **Ανά διάσταση (file:line):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, inline `rgba(` εκτός `theme.ts` = **0** → χρωματικό token layer καθαρός. (Shared theme) `theme.ts` foundation DONE, αλλά **dark-only** (χωρίς light palette / context). (Reusable) `ui.tsx` 15 primitives centralized· Button/IconButton adoption 22 sites ΑΛΛΑ **14 inline button styles** ακόμη hand-rolled (existing partial item). (Touch targets) **μηδέν violation** — `lineDel` 30×30 (Receipts:276) έχει `hitSlop={8}` → 46×46. (Adaptive) `react-native-safe-area-context` **ΔΕΝ installed** (existing TODO)· max-width DONE. **3 ΝΕΑ ευρήματα → items προστέθηκαν:** (1) **RADIUS token drift** — **47** `borderRadius` literals που ταιριάζουν με token value (10/12/14/18) γραμμένα ως raw αριθμοί αντί `RADIUS.*`, σε 12 screens + **ίδιο το `ui.tsx`** (chip:279 radius10, iconBtn:287 radius12)· mechanical, tsc-verifiable, μηδέν οπτική αλλαγή. (2) **Brand typography ΑΠΟΝ** — **μηδέν `fontFamily`** σε ΟΛΟ το mobile (type hierarchy μόνο με `fontWeight` ×134)· το web brand = Outfit/Manrope/IBM Plex Mono → το mobile δείχνει σαν διαφορετικό προϊόν (system font, κανένα mono για labels/amounts). Ο μεγαλύτερος untracked consistency gap. (3) minor: 2 full-screen initial loaders hand-rolled (`<View center><ActivityIndicator>`, Shopping:69/Settings:91) αντί `<Centered><Spinner/>`. **Builder next-3: RADIUS adoption (P2/S) → Brand typography (P2/M) → Button+Chip completion (14 inline buttons, existing partial).**
>
> **ui-auditor re-audit 2026-07-06 (47η σάρωση· working tree ΚΑΘΑΡΟ, 2 πρώην WIP-blocked items ΞΕΜΠΛΟΚΑΡΑΝ, μηδέν token-regression):** fresh read-only grep audit από τον κώδικα (όχι docs read-back). **17 mobile source files** (16 screens + `ui.tsx`), mobile `npx tsc --noEmit` → **EXIT 0**. **Δέλτα από 46η:** `git log -- apps/mobile/src` τελευταίο = **`08ced6d`** (merge/bind, ήδη μετρημένο στην 46η) → **μηδέν νέο mobile-src commit**, η βάση αμετάβλητη· ΟΜΩΣ το **working tree είναι πλέον καθαρό** (`git status --short` κενό) άρα τα πρώην uncommitted Receipts/Settings/Shopping screens έγιναν commit (`6835e6a` Input primitive, `0ac7a37` touch-targets, `08ced6d`) → **ξεμπλόκαραν** τα 2 attended-preferred items. **Ανά διάσταση (live grep, file:line):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**· inline `rgba(`/`rgb(` εκτός `theme.ts` = **0** → token layer πλήρως καθαρός. **Το ΜΟΝΟ εναπομείναν magic literal:** `ShoppingScreen.tsx:165` `borderRadius: 18` (πρέπει `RADIUS.xl`, που **υπάρχει πλέον** στο `theme.ts:26` μετά το `c9fef1b`). (Shared theme) `theme.ts` (C/SPACE/RADIUS/SIZE/scrim/alpha) foundation DONE. (Reusable) `ui.tsx` = Header/Centered/Spinner/Empty/ErrorText/ModalSheet/Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge/Chip + `contentWidth`· όλα centralized. **`<ModalSheet>` DONE** (`c9fef1b`, 5 screens: Subscriptions/Money/Tasks/Items/Vouchers)· **holdout = ShoppingScreen.tsx:112** (raw `<Modal animationType="fade">` + `<View modalWrap>/<View modal borderRadius:18>` = χειροκίνητο αντίγραφο του ModalSheet pattern, ΧΩΡΙΣ tap-to-close backdrop) → τώρα non-WIP, buildable. Οι υπόλοιποι raw `<Modal>` (Statements:147, Settings:298/457, Receipts:161) είναι **`animationType="slide"`** (bottom-sheet/full-panel, ΔΙΑΦΟΡΕΤΙΚΟ pattern από το centered-fade ModalSheet) → δεν είναι holdout. **raw `<TextInput>` = 9, όλα ReceiptsScreen** (`einput` ×6 top-level store/date/pay/total/notes γρ.181/185/189/195/207/220 + `cellInput` ×3 qty/net/vat γρ.211-213)· τώρα non-WIP → τα 6 `einput` → `<Input>` buildable (radius 10→12 delta ή style-override), τα 3 compact `cellInput` = attended-preferred visual delta. (Touch targets) **0 violations:** Settings `rm` 36×36 +hitSlop8 (52²), `swatch` 32×32 +hitSlop6 (44²), Receipts `lineDel` 30×30 +hitSlop8 (46²), όλα ≥44 (`0ac7a37` έκλεισε το πρώην gap). (States) Spinner/Empty/ErrorText centralized → consistent. (Adaptive) `contentWidth` cap DONE (`28c943c`)· **safe-area ΑΚΟΜΑ plain RN `SafeAreaView`** top-only, `react-native-safe-area-context` **ΑΠΟΝ** από `package.json` (grep 0 imports) → bottom home-indicator + landscape side-notch ακάλυπτα (P2/M needs-decision, dep-add). (Theme) dark-only· web έχει πλήρες light `:root` (globals.css:33-51) + toggle → μεγαλύτερη web-parity διαφορά (P3/L needs-decision). **Working tree στην αρχή: καθαρό** (δεν αγγίχτηκε τίποτα πλην των 2 doc files). **Top-3 για τον builder (unattended-safe, non-WIP):** (1) **ShoppingScreen → `<ModalSheet>` + `RADIUS.xl`** [P3/S· κλείνει το τελευταίο ModalSheet holdout ΚΑΙ το μοναδικό `borderRadius:18` magic literal, tsc-verifiable, mobile-only, μηδέν rebuild]. (2) **ReceiptsScreen 6× `einput` → `<Input variant="surface">`** [P3/S· τα compact `cellInput` μένουν attended-preferred]. (3) καμία άλλη auto-buildable UI debt (safe-area/theme-toggle = needs-decision). Δεν προστέθηκε νέο `###` item· τα υπάρχοντα «Modal container primitive» + «Input primitive» καλύπτουν πλήρως τα 2 ευρήματα (statuses ενημερώθηκαν: Shopping holdout unblocked).
> **ui-auditor re-audit 2026-07-05 (46η σάρωση· confirmation, μηδέν νέο finding, μηδέν νέο mobile-src commit από 45η):** fresh read-only grep audit από τον κώδικα (όχι docs read-back). **17 mobile source files** (16 screens + `ui.tsx`), mobile `npx tsc --noEmit` → **EXIT 0**. **Δέλτα από 45η:** `git log --since=2026-07-04 -- apps/mobile/src` = **μηδέν commit** → η βάση αμετάβλητη. **Ανά διάσταση (live counts):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, rgba/rgb literals εκτός `theme.ts` = **0** (και στα 3 WIP screens Receipts/Settings/Shopping = 0 νέο hex) → token layer πλήρως καθαρός. (Shared theme) `theme.ts` υπάρχει (C/SPACE/RADIUS/SIZE/scrim/alpha), foundation DONE. (Reusable components) Card/Badge/ListItem/Input/TextArea/Button/IconButton/Chip/Check/Header όλα centralized στο `ui.tsx` — το **`<ModalSheet>` είναι το ΜΟΝΟ εναπομείναν consolidation TODO** (6× byte-identical `modal:` StyleSheet με `borderRadius:18` magic literal: Subscriptions:154, Money:294, Tasks:237, Items:468, Shopping:165, Vouchers:203· **5 non-WIP** buildable, Shopping = WIP-blocked). `RADIUS.xl=18` ΑΠΩΝ από `theme.ts` (grep `xl` πιάνει μόνο SPACE.xl/SIZE.xl). (Touch targets) Settings `rm` 36×36 (γρ.624) + `swatch` 32×32 (γρ.647) ακόμα ΧΩΡΙΣ `hitSlop` → attended-preferred (SettingsScreen = WIP-blocked). (States/Adaptive/Safe-area) αμετάβλητα από 45η. **Working tree στην αρχή:** `.claude/launch.json` + 2 deleted `.github/workflows` + Receipts/Settings/Shopping screens = uncommitted WIP του Αχιλλέα (δεν αγγίχτηκαν). **Top-3 για τον builder (unattended-safe, non-WIP):** (1) **`<ModalSheet>` + `RADIUS.xl=18`** [P2/M· structural, tsc-verifiable, byte-identical output στα 5 non-WIP screens· βλ. item παρακάτω] το top buildable. (2) **`<Chip>`/`<Badge>`/`<ListItem>` holdout σάρωση** για τυχόν non-WIP raw clusters [P3/S]. (3) κανένα άλλο mobile-only unattended-safe (ReceiptsScreen Input-debt + Settings touch-targets = WIP-blocked· safe-area/theme-toggle = needs-decision).
> **ui-auditor re-audit 2026-07-04 (45η σάρωση· ΕΝΑ νέο `###` item [Modal container primitive], μηδέν token-regression):** fresh read-only grep audit από τον κώδικα. **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha`) + `ui.tsx` primitives (`Header`/`Centered`/`Spinner`/`Empty`/`ErrorText`/`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip`/`contentWidth`) ΟΛΑ παρόντα. mobile `npx tsc --noEmit` → **EXIT 0**. **Δέλτα από 44η:** τελευταίο mobile-src commit **`ad226a7` (ItemsScreen `logInput` ×2 → `<Input>`)** → το τελευταίο non-WIP raw-`<TextInput>` holdout ΕΚΛΕΙΣΕ· απομένει **ΜΟΝΟ** ReceiptsScreen (9 raw, WIP-blocked). **Ανά διάσταση (live grep):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, inline `rgba(` εκτός `alpha()` = **0** — token layer πλήρως καθαρό, ξανα-επιβεβαιωμένο. (Reusable) raw `<TextInput>` = **9** (όλα ReceiptsScreen `einput`/`cellInput`, WIP του Αχιλλέα → blocked). **ΝΕΟ finding:** **6 byte-identical `modal:` container StyleSheet entries** (`backgroundColor:C.surface, borderWidth:1, borderColor:C.border, borderRadius:18, padding:20`) σε Subscriptions:154 / Tasks:237 / Money:294 / Items:468 / Vouchers:203 / Shopping:165 + **7 `modalWrap:` scrim-wrapper dupes** → κανένα `<ModalSheet>` primitive· επιπλέον το `borderRadius:18` είναι magic number **εκτός** του `RADIUS` scale (sm10/md12/lg14). Ghost/tint border-buttons ακόμα raw = **9 sites** (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`)· `<Button variant="ghost">` adopted 1 → holdout ανοιχτό (P2/M). (Theme) dark-only· web έχει πλήρες light `:root` (globals.css:33-51) → μεγαλύτερη web-parity διαφορά (P3/L). (States) `Spinner`/`Empty`/`ErrorText` centralized → consistent. (Adaptive) `contentWidth` DONE (`28c943c`)· **safe-area ΑΚΟΜΑ plain RN `SafeAreaView`** (`App.tsx:88` top-only· Android manual `RNStatusBar.currentHeight:111`)· `react-native-safe-area-context` **ΑΠΟΝ** από `package.json` (grep 0 imports) → bottom home-indicator + landscape side-notch ακάλυπτα (P2/M foundation). (Touch) Settings `rm`/`swatch` χωρίς `hitSlop` → WIP-blocked. **Git WIP block:** ReceiptsScreen/SettingsScreen/ShoppingScreen έχουν uncommitted edits του Αχιλλέα → items που τα αγγίζουν = attended-preferred. **Top-3 unblocked για builder:** (1) **`<ModalSheet>` primitive + `RADIUS.xl=18` token** για τα 5 non-WIP modal:18 sites (Subscriptions/Tasks/Money/Items/Vouchers· ShoppingScreen/ReceiptsScreen WIP-blocked) (P2/M· νέο item παρακάτω), (2) **safe-area-context adoption** (P2/M foundation· `npx expo install react-native-safe-area-context` + `SafeAreaProvider`/`useSafeAreaInsets`, App.tsx + package.json, ΟΧΙ screen-WIP), (3) **`<Button variant="ghost" tint?>`** για τα ~7 core cyan-ghost sites σε non-WIP screens (P2/M).
> **ui-auditor re-audit 2026-07-03 (41η σάρωση· ΕΝΑ leftover-migration ΕΚΛΕΙΣΕ [`7151d47`], μηδέν νέο finding):** fresh read-only grep audit από τον κώδικα (όχι docs). **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha`) + `ui.tsx` primitives (`Header`/`Centered`/`Spinner`/`Empty`/`ErrorText`/`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip`) ΟΛΑ παρόντα (`<Chip>` export επιβεβαιωμένο). mobile `npx tsc --noEmit` → **EXIT 0**. **Δέλτα από 40η:** τελευταίο mobile-src commit πλέον **`7151d47` (`refactor(mobile): StatementsScreen installment badge → shared <Badge>`)** — μεταναστεύει το τελευταίο «leftover local style» που σημειώθηκε από την 38η σάρωση (StatementsScreen cyan inline installment pill → `<Badge label color={C.cyan}>` @ StatementsScreen:122, μηδέν τοπικό `badge:` StyleSheet entry). → **Το «Card + Badge + ListItem primitives» item κλείνει τελείως** (και ο τελευταίος display-badge outlier μεταναστεύθηκε). **Ανά διάσταση (live grep):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, rgba/rgb εκτός `theme.ts` = **0** — token layer πλήρως καθαρό. (Reusable) raw `<TextInput>` = **11**: **ItemsScreen `logInput` ×2** (γρ.99-100· non-WIP → buildable, radius10/pad10-8/fs14) + **ReceiptsScreen ×9** (WIP του Αχιλλέα → blocked). `<Chip>` adopted σε **6 screens** (Money/Subscriptions/Tasks/Items ×2/Reports/Settings ×3) — Chip holdout ΕΚΛΕΙΣΕ. Ghost/tint border-buttons ακόμα raw = **9 sites** (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`)· `<Button variant="ghost">` adopted μόλις **1** → το `<Button variant="ghost" tint?>` holdout ανοιχτό (P2/M). (Theme) dark-only· web έχει πλήρες light `:root` (globals.css) → μεγαλύτερη εναπομείνασα web-parity διαφορά (P3/L, needs decision). (States) `Spinner`/`Empty`/`ErrorText` centralized → consistent. (Adaptive) `contentWidth` DONE· **safe-area ΑΚΟΜΑ plain RN `SafeAreaView`** (`App.tsx`, top-only)· `react-native-safe-area-context` **ΑΠΟΝ** από `package.json` (grep 0 imports) → bottom home-indicator + landscape side-notch ακάλυπτα (P2/M foundation). (Touch) Settings `rm` 36×36 + `swatch` 32×32 χωρίς `hitSlop` → WIP-blocked (SettingsScreen uncommitted). **Git WIP block αμετάβλητο:** ReceiptsScreen/SettingsScreen/ShoppingScreen έχουν uncommitted edits του Αχιλλέα → items που τα αγγίζουν = attended-preferred. **Top-3 unblocked για builder:** (1) **ItemsScreen `logInput` ×2 → `<Input variant="surface">`** (P2/S· κλείνει το τελευταίο non-WIP Input holdout, tsc-verify), (2) **safe-area-context adoption** (P2/M foundation· `npx expo install react-native-safe-area-context` + `SafeAreaProvider`/`useSafeAreaInsets`, package.json + App.tsx, ΟΧΙ screen-WIP), (3) **`<Button variant="ghost" tint?>`** για τα ~7 core cyan-ghost sites σε non-WIP screens (Subscriptions/Money/Items/Vouchers/Tasks) (P2/M). Δεν προστέθηκε νέο `###` item — τα υπάρχοντα καλύπτουν πλήρως τα ευρήματα.
> **ui-auditor re-audit 2026-07-03 (40η σάρωση· confirmation, μηδέν νέο finding, αμετάβλητο από 39η):** fresh read-only grep audit (όχι docs read-back). **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha`) + `ui.tsx` primitives (`Header`/`Centered`/`Spinner`/`Empty`/`ErrorText`/`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip`/`contentWidth`) ΟΛΑ παρόντα. mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν output). **Ανά διάσταση:** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, rgba/rgb εκτός `theme.ts` = **0** — token layer πλήρως καθαρό, ξανα-επιβεβαιωμένο. (Reusable) raw `<TextInput>` = **11**: **ItemsScreen `logInput` ×2** (γρ.99-100· non-WIP → buildable) + **ReceiptsScreen ×9** (`einput` ×6 incl. multiline notes γρ.181/185/189/195/207/220 + `cellInput` ×3 γρ.211-213· WIP του Αχιλλέα → blocked). Ghost/tint border-buttons ακόμα raw = **~17 sites** (το `<Button variant="ghost">` holdout ανοιχτό· core cyan-ghosts σε non-WIP screens = buildable). `<Chip>`/`<Card>`/`<Badge>`/`<ListItem>`/`<Button>` core όλα DONE/committed. (Theme) dark-only· web έχει πλήρες light `:root` (globals.css:26-50) → **η μεγαλύτερη εναπομείνασα web-parity διαφορά** (P3/L). (States) `Spinner`/`Empty`/`ErrorText` centralized → consistent. (Adaptive) `contentWidth` DONE (committed `28c943c`)· **safe-area ΑΚΟΜΑ plain RN `SafeAreaView`** (`App.tsx:88`, top-only· Android manual `RNStatusBar.currentHeight` γρ.111), `react-native-safe-area-context` **ΑΠΟΝ** από `package.json` → bottom home-indicator + landscape side-notch ακάλυπτα. (Touch) `hitSlop` σε 11 sites· Settings `rm`/`swatch` χωρίς → WIP-blocked. **Git WIP block αμετάβλητο:** ReceiptsScreen/SettingsScreen/ShoppingScreen έχουν uncommitted edits → items που τα αγγίζουν = attended-preferred. **Top-3 unblocked για builder:** (1) ItemsScreen `logInput` ×2 → `<Input variant="surface">` (P2/S, κλείνει το Input holdout), (2) safe-area-context adoption (P2/M, foundation· package.json + App.tsx, όχι screen-WIP), (3) `<Button variant="ghost" tint?>` για τα ~7 core cyan-ghost sites σε non-WIP screens (Subscriptions/Money/Items/Vouchers/Tasks) (P2/M). Δεν προστέθηκε νέο ### item — τα υπάρχοντα καλύπτουν πλήρως τα ευρήματα.
> **ui-auditor re-audit 2026-07-03 (39η σάρωση· confirmation, μηδέν νέο finding):** fresh read-only grep audit (όχι docs). **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha`) + `ui.tsx` primitives (`Header`/`Spinner`/`Empty`/`ErrorText`/`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip`/`contentWidth`) ΟΛΑ παρόντα. mobile `npx tsc --noEmit` → **EXIT 0**. (Tokens) hardcoded hex εκτός `theme.ts` = **0**, rgba/rgb εκτός `theme.ts` = **0** — token layer πλήρως καθαρό. (Reusable) raw `<TextInput>` εναπομείναντα = **13** (ItemsScreen `logInput` ×2 [non-WIP, buildable] + ReceiptsScreen `einput` ×7 / `cellInput` ×3 [WIP του Αχιλλέα → blocked] + notes TextArea Receipts). (Theme) dark-only, μηδέν light palette/context. (Adaptive) `contentWidth` DONE (committed `28c943c`)· safe-area ΑΚΟΜΑ plain RN `SafeAreaView` στο `App.tsx:88` (top-only· Android manual `RNStatusBar.currentHeight`), `react-native-safe-area-context` ΑΠΟΝ από `package.json` → bottom home-indicator + landscape side-notch ακάλυπτα. (Touch) οι μικροί targets έχουν `hitSlop` (11 sites)· Settings `rm`/`swatch` ακόμα χωρίς → WIP-blocked. **Git WIP block αμετάβλητο:** ReceiptsScreen/SettingsScreen/ShoppingScreen έχουν uncommitted edits του Αχιλλέα → όλα τα items που τα αγγίζουν παραμένουν attended-preferred. **Top-3 unblocked για builder:** (1) ItemsScreen `logInput` → `<Input>` (P2/S), (2) safe-area-context adoption (P2/M, foundation), (3) light/dark theme context (P3/L). Δεν προστέθηκε νέο ### item — τα υπάρχοντα καλύπτουν πλήρως τα ευρήματα.
> **ui-auditor re-audit 2026-07-03 (38η σάρωση· δύο in-flight items προχώρησαν, ΕΝΑ νέο touch-target finding):** fresh read-only audit από τον κώδικα (grep, όχι docs). **17 mobile source files** (16 screens + `ui.tsx`), foundation `theme.ts` (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha`) + `ui.tsx` primitives (`Header`/`Spinner`/`Empty`/`ErrorText`/`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip`/`contentWidth`) ΟΛΑ παρόντα. mobile `npx tsc --noEmit` → **EXIT 0**.
> **Ανά διάσταση (live counts):** (Tokens) hardcoded hex εκτός `theme.ts` = **0**, rgba/rgb literals εκτός `theme.ts` = **0** — το token layer είναι πλήρως καθαρό. (Reusable) `<Card>/<ListItem>/<Badge>/<Chip>/<Button>/<Input>` adoption σε 15/16 screens. (Theme) dark-only, μηδέν light palette/context. (Adaptive) `contentWidth` DONE· safe-area ΑΚΟΜΑ plain RN `SafeAreaView` (top-only), το `react-native-safe-area-context` ΑΠΟΝ από `package.json`. (Touch) οι περισσότεροι μικροί targets έχουν `hitSlop` → OK.
> **Δέλτα από 37η:** (α) **Input primitive → 9/11** — commit `53b861a` μετανάστευσε Login/Search/Assistant. Εναπομείναντα raw `<TextInput>`: **ItemsScreen `logInput`** (×2, radius10/pad10-8/fs14· ΟΧΙ WIP → buildable) + **ReceiptsScreen `einput`** (×7, radius10/pad12-10/fs15 = byte-identical με `inputModal`) + **`cellInput`** (×3, radius8/pad8/fs14)· τα Receipts είναι WIP του Αχιλλέα → blocked. (β) **Button family** — commit `96150c5` πρόσθεσε `danger`/`ghost` variants + migrate 4 screens (Money/Subscriptions/Items/Vouchers). Εναπομείναντα raw text-buttons: **ShoppingScreen** `cancelBtn`/`cancelText` + **SettingsScreen** `cancelBtn`/`cancelText` (×2· bordered variant) + `saveText` weight800 outlier — **ΟΛΑ σε WIP screens** (Shopping/Settings) → blocked.
> **ΝΕΟ finding (touch target):** **SettingsScreen `rm` ✕ buttons 36×36** (γρ.140/246/412) + **`swatch` 32×32** (γρ.336) **ΧΩΡΙΣ `hitSlop`** → effective hit area <44pt. Το «Touch targets ≥44pt» item είναι DONE αλλά κάλυψε μόνο τα checkboxes· αυτά ξέφυγαν. WIP-blocked (SettingsScreen). Αντίθετα `lineDel` (30×30 + hitSlop8), `menuBtn` (38×36 + hitSlop12), `backBtn` (Header hitSlop12) → OK.
> **Leftover local styles (μικρό debt):** `SearchScreen row` (radius12· ΟΧΙ WIP → migratable σε `<ListItem>`), `StatementsScreen badge` (cyan inline installment pill· ΟΧΙ WIP → `<Badge>` με textStyle), `ReportsScreen card` (radius16 stat-card, distinct pattern), `SettingsScreen card`/`row` (settings-group container/hairline row, distinct pattern → ΟΧΙ debt), `Shopping/Receipts row` (WIP).
> **Top-3 για τον builder (unattended-safe, non-WIP):** (1) **Input finish σε ItemsScreen `logInput`** [P2/S· structural, tsc-verify· 2px normalization προς `<Input surface>` τεκμηριωμένο]· (2) **SearchScreen `row` → `<ListItem>`** [P3/S· 2px radius delta]· (3) **StatementsScreen `badge` → `<Badge>`** [P3/S]. **Blocked (WIP):** Receipts `einput`/`cellInput` Input-migration, Shopping/Settings cancel-button + Settings `rm`/`swatch` hitSlop. **Needs Achilleas:** safe-area dep, light/dark theme, language switcher.
> **ui-auditor re-audit 2026-07-02 (37η σάρωση· ΕΝΑ νέο mobile-src commit `2201932` [Expenses/Income AI re-scan] → ΕΝΑ queue item ΕΚΛΕΙΣΕ, ghost-button holdout +1 site):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα. **16 screens**· `theme.ts` tokens + `ui.tsx` primitives (`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip` + `Header`/`Centered`/`Spinner`/`ErrorText`/`Empty` + `contentWidth`) **ΟΛΑ παρόντα**. mobile `npx tsc --noEmit` → **EXIT 0**. **Νέος κώδικας από την 36η (marker `df51e97`):** ένα mobile-src commit `2201932` (`feat(mobile): AI re-scan (text/OCR) for Expenses & Income`) — υλοποιεί το queue item «Expenses/Income — re-scan stored file (OCR/text)» (P2/M) → **DONE**. Νέο `POST /api/v1/expenses/:id/rescan` (byte-mirror του receipts route) + wired στο MoneyScreen edit modal ως Re-scan text/OCR bar (μόνο όταν `editing.file`), shared `expenses/serialize.ts` για identical v1 shape, re-prefill in-place, record unverified. Token-clean (`C.*`, μηδέν hex). **Ευρήματα ανά διάσταση (fresh grep):** Tokens (hex στα `screens/`+`nav.tsx`+`App.tsx`): **0** (incl. ο νέος κώδικας)· Shared theme file: **ΥΠΑΡΧΕΙ** → κανένα P1 foundation item ανοιχτό· Reusable components: εναπομένον holdout **ΜΟΝΟ** το ghost `<Button variant>` — το commit το μεγάλωσε σε **~13 border-ghost sites / 8 screens** (ο νέος `rescanBtn` MoneyScreen:273 cyan-ghost = +1)· ο πυρήνας ~7 AI/scan/rescan cyan-ghosts ενοποιείται σε `<Button variant="ghost" tint?>`· Theme/dark mode: **0** `useColorScheme`/`ThemeProvider`/`useTheme` (dark-only, P3/L decision)· States: complete trio (Spinner/ErrorText/Empty)· Adaptive: safe-area **ανοιχτό** (plain `SafeAreaView` @ App.tsx:88, `react-native-safe-area-context` **εκτός** package.json [grep 0], μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· max-content-width DONE (`contentWidth`)· Touch targets: DONE (`<Check>` 24×24 + hitSlop)· Input primitive: raw `<TextInput>` **22 sites / 7 screens** (Items/Shopping/Search/Settings/Receipts/Assistant/Login) — attended-preferred token-drift. **Ουρά:** ΕΝΑ item έκλεισε («Expenses/Income re-scan», P2/M)· μηδέν νέο item άνοιξε. **Top 3 για τον builder:** (1) Ghost `<Button variant>` (P2/M, τελευταίο reusable holdout, τώρα +1 site)· (2) Safe-area insets (P2/M, semi-attended)· (3) Input primitive full-adoption (P2/M, attended).
> **ui-auditor re-audit 2026-07-02 (36η σάρωση· ΕΝΑ νέο mobile-src commit `df51e97` [Expenses/Income scanned-bill image] → 0 νέο UI debt· CONFIRMATION):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (όχι docs). **16 screens**· `theme.ts` tokens (`C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`onAccent`/`alpha`) + `ui.tsx` primitives (`Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge`/`Chip` + `Header`/`Centered`/`Spinner`/`ErrorText`/`Empty` + `contentWidth`) **ΟΛΑ παρόντα**. mobile `npx tsc --noEmit` → **EXIT 0**. **Νέος κώδικας από την 35η (marker `681098f`):** ένα mobile-src commit `df51e97` (`feat(mobile): show scanned bill image in Expenses/Income detail`) — υλοποιεί το queue item «Expenses/Income scanned-bill image» (P2/S), που είναι ΗΔΗ **DONE 2026-07-02**· `Image` + `fileSource` στο MoneyScreen detail, νέο style `bigImg`, μηδέν hardcoded hex, guard για manual entries (`file:null`). Token-clean. **Ευρήματα ανά διάσταση (fresh grep):** Tokens (3/6/8-digit hex στα `screens/`+`nav.tsx`+`App.tsx`): **0** (όλα `C.*`, incl. ο νέος κώδικας)· Shared theme file: **ΥΠΑΡΧΕΙ** → κανένα P1 foundation item ανοιχτό· Reusable components: εναπομένον holdout ΜΟΝΟ το ghost `<Button variant>` (**~7 drift sites**: Subscriptions/Vouchers `aiBtn` [cyan border w40], Money `scanBtn` [cyan border w46 + surface bg], Shopping `scanBtn` [neutral `C.border`+surface2 padV12], Items `aiBtn` [accent border] + `importBtn` [cyan fill], Receipts `rescanBtn` [cyan radius10])· Theme/dark mode: **0** `useColorScheme`/`ThemeProvider`/`useTheme` (dark-only, P3/L decision)· States: complete trio· Adaptive: safe-area **ανοιχτό** (plain `SafeAreaView` @ App.tsx:88 + `import` react-native, `react-native-safe-area-context` **εκτός** package.json [grep 0], μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· max-content-width DONE (`contentWidth`)· Touch targets: DONE· Input primitive: raw `<TextInput>` **21 sites / 7 screens** (Receipts 9 / Shopping 4 / Login 3 / Items 2 / Assistant 1 / Settings 1 / Search 1). **ΕΠΙΠΤΩΣΗ ΣΤΗΝ ΟΥΡΑ:** μηδέν νέο item, μηδέν item έκλεισε (το `df51e97` υλοποίησε προϋπάρχον DONE)· ουρά ΑΜΕΤΑΒΛΗΤΗ. **Top-3 για builder (unattended-safe διάταξη):** (1) **Ghost `<Button variant>` (P2/M)** — τελευταίο reusable-component holdout· `variant?: 'primary'|'ghost'` (+ optional tint cyan/accent) στο υπάρχον `Button`· unattended-safe (tsc-verifiable, style-override idiom)· ΠΡΟΣΟΧΗ στο drift (borderColor cyan vs accent vs neutral, bg surface vs surface2, padding 8-12) → κράτα per-site override για pixel-identical. (2) **Safe-area insets (P2/M)** — μοναδικό πραγματικό adaptive gap· `react-native-safe-area-context` + `SafeAreaProvider`/`useSafeAreaInsets` (bottom inset για sheet buttons κάτω από home indicator)· package-add + simulator verify → semi-attended. (3) **Input primitive πλήρης υιοθέτηση (P2/M)** — 21 raw `<TextInput>` σε 7 screens να περάσουν στα υπάρχοντα `<Input>`/`<TextArea>`· token-drift → attended-preferred.
> **ui-auditor re-audit 2026-07-02 (34η σάρωση· ΔΥΟ νέα mobile-src commits `bba44ff` [Reports date-range 6/12/24m] + `268efb4` [Tasks tag-filter + project-progress] → 0 νέο TOKEN debt, ΑΛΛΑ ο `<Chip>` holdout ΜΕΓΑΛΩΣΕ):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (**16 screens**· `theme.ts` tokens `C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha` + `ui.tsx` primitives `Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` + `contentWidth` ΟΛΑ παρόντα, μηδέν `<Chip>` export). **Επιθεώρησα ΚΑΙ τα δύο diffs:** (α) `bba44ff` ReportsScreen +range-selector → νέο inline chip cluster `rangeChip`/`rangeChipOn`/`rangeText`/`rangeTextOn` (padH12/padV5/radius9· ΟΛΑ `C.surface2`/`C.border`/`C.accent`/`C.onAccent`)· (β) `268efb4` TasksScreen +tag-filter → νέο inline chip cluster `tagChip`/`tagChipOn`/`tagChipText`/`tagChipTextOn` (padH12/padV6/radius10· ΟΛΑ `C.*`) + νέο progress bar `progTrack`/`progFill` (token-clean, μιμείται το υπάρχον `<Bar>` track pattern). **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep 3/6/8-digit hex στα `screens/`+`nav.tsx`+`App.tsx`):** **0** (όλα `C.*`, incl. ο νέος κώδικας). **ΕΠΙΠΤΩΣΗ ΣΤΗΝ ΟΥΡΑ:** μηδέν νέο item, ΑΛΛΑ το «Button + Chip primitives» item (P2/M, IN PROGRESS) — μέρος (γ) `<Chip>` — **αναβαθμίστηκε σε scope**: το chip footprint πλέον **7 StyleSheet clusters σε 5 screens** (Items `chip`+`sChip`· Settings `chip`+`addChip`[cyan ghost]+`valChip`[with-X]· Subscriptions `cChip`· **Reports `rangeChip` [ΝΕΟ]**· **Tasks `tagChip` [ΝΕΟ]**), όλα byte-near-identical base (`surface2`/1px `border`/`accent`-on-state/`onAccent`-text). **Δομικά TODO αμετάβλητα (fresh grep):** `react-native-safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88, μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· `useColorScheme`/`ThemeProvider`/`useTheme` **0** (dark-only)· raw `<TextInput>` sites (attended-preferred token-drift). mobile `tsc --noEmit` → **EXIT 0**. **Top-3 για builder:** (1) **Safe-area insets (P2/M)** — μοναδικό πραγματικό adaptive gap· package-add + simulator → semi-attended. (2) **`<Chip>` primitive (P2/M)** — πλέον 7 clusters/5 screens, το πιο πολλαπλασιαζόμενο holdout (κάθε νέα οθόνη με chips προσθέτει άλλο ένα cluster)· attended-preferred για οπτικό verify αλλά η ενοποίηση ανοίγει με κοινό `<Chip on={}>`. (3) **Ghost `<Button variant>` (P2)** — κλείνει το button family (~7 sites). ΟΥΡΑ κατά τα άλλα ΑΜΕΤΑΒΛΗΤΗ — μηδέν item έκλεισε.
> **ui-auditor re-audit 2026-07-02 (33η σάρωση· ΝΕΟ mobile-src commit `6b5a10a` [Reports inventory-value by category] → 0 νέο UI debt· CONFIRMATION):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (**16 screens**· `theme.ts` tokens `C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha` + `ui.tsx` primitives `Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` + `contentWidth` ΟΛΑ παρόντα). **`git log -- apps/mobile/src` τελευταίο πλέον `6b5a10a`** — ΕΝΑ νέο mobile-src commit από την 32η (`d170d93`). **Επιθεώρησα το diff (+9 γραμμές· `api.ts` +1 / `ReportsScreen.tsx` +8):** ξαναχρησιμοποιεί το υπάρχον `<Bar>` component με `color={C.cyan}` + το υπάρχον `s.section` style, μηδέν νέο StyleSheet entry, **μηδέν hardcoded hex** → **0 νέο UI debt** (το flag «needs-pie-lib» έκλεισε ως bars, μηδέν native dep). **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep 3/6/8-digit hex στα `screens/`+`nav.tsx`+`App.tsx`):** **0** (όλα `C.*`). **Δομικά TODO αμετάβλητα (fresh grep):** `react-native-safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88, μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· `useColorScheme`/`ThemeProvider`/`useTheme` **0** (dark-only, το web κρατά light palette χωρίς mobile αντίστοιχο). **Reusable holdouts αμετάβλητα:** `<Chip>` **MISSING** (47 chip-refs· `chip`/`chipOn`/`chipText` Items+Settings· `cChip` Subscriptions· `sChip` Items)· ghost cyan/accent-border button variant ~7 sites (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) ΟΧΙ ενοποιημένα· raw `<TextInput>` **21** sites σε 7 screens (attended-preferred token-drift). **States:** in-button `ActivityIndicator` επικαλύπτεται με το ghost-button holdout, ΟΧΙ νέο debt. mobile `tsc --noEmit` → **EXIT 0**. **ΟΥΡΑ ΑΜΕΤΑΒΛΗΤΗ** — μηδέν item έκλεισε/άνοιξε. **Top-3 για builder:** (1) **Safe-area insets (P2/M)** — μοναδικό πραγματικό adaptive gap· package-add + simulator → semi-attended. (2) **`<Chip>` primitive (P2/M)** — τελευταίο reusable holdout· attended-preferred για οπτικό verify. (3) **Ghost `<Button variant>` (P2)** — κλείνει το button family (~7 sites), μαζί με το Chip item.
> **ui-auditor re-audit 2026-07-02 (32η σάρωση· ΝΕΟ mobile-src commit `d170d93` [Reports subs-by-category] → 0 νέο UI debt· CONFIRMATION):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (**16 screens**· `theme.ts` tokens `C`/`SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha` + `ui.tsx` primitives `Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` + `contentWidth` ΟΛΑ παρόντα). **`git log -- apps/mobile/src` τελευταίο πλέον `d170d93` (feat: Reports subs-by-category bars, monthly-equivalent)** — ΕΝΑ νέο mobile-src commit από την 31η (`e6ad795`). **Επιθεώρησα το diff (+9 γραμμές· `api.ts` +1 / `ReportsScreen.tsx` +8):** ξαναχρησιμοποιεί το υπάρχον `<Bar>` component με `color={C.purple}` + το υπάρχον `s.section` style, μηδέν νέο StyleSheet entry, **μηδέν hardcoded hex** → **0 νέο UI debt.** **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep 3/6/8-digit hex στα `screens/`+`nav.tsx`+`App.tsx`):** **0** (όλα `C.*`). **Δομικά TODO αμετάβλητα (fresh grep):** `react-native-safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88, μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· `useColorScheme`/`ThemeProvider`/`useTheme` **0** (dark-only, το web κρατά light palette χωρίς mobile αντίστοιχο). **Reusable holdouts αμετάβλητα:** `<Chip>` **MISSING** (`chip`/`chipOn`/`chipText` Items:465 + Settings:617· `cChip` Subscriptions:160· `sChip` Items:480)· ghost cyan/accent-border button variant ~7 sites (`aiBtn` Subscriptions:147/Vouchers:192/Items:488· `scanBtn` Money:232/Shopping:155· `rescanBtn` Receipts:264· `importBtn` Items:461) ΟΧΙ ενοποιημένα· raw `<TextInput>` 21 sites σε 7 screens (attended-preferred token-drift). **States:** in-button `ActivityIndicator` επικαλύπτεται με το ghost-button holdout, ΟΧΙ νέο debt. mobile `tsc --noEmit` → **EXIT 0**. **ΟΥΡΑ ΑΜΕΤΑΒΛΗΤΗ** — μηδέν item έκλεισε/άνοιξε. **Top-3 για builder:** (1) **Safe-area insets (P2/M)** — μοναδικό πραγματικό adaptive gap· package-add + simulator → semi-attended. (2) **`<Chip>` primitive (P2/M)** — τελευταίο reusable holdout· attended-preferred για οπτικό verify. (3) **Ghost `<Button variant>` (P2)** — κλείνει το button family (~7 sites), μαζί με το Chip item.
> **ui-auditor re-audit 2026-07-02 (31η σάρωση· ΝΕΟ mobile-src commit `e6ad795` [Reports parity] → 0 νέο UI debt):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (**16 screens**, `ui.tsx` primitives ΟΛΑ παρόντα: `Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` + `contentWidth`). **`git log -- apps/mobile/src` τελευταίο πλέον `e6ad795` (feat: Reports parity: spend-by-store, biggest-purchases, warranties-expiring)** — το ΠΡΩΤΟ νέο mobile-src commit από την 30ή σάρωση (`28c943c`). **Επιθεώρησα το ReportsScreen diff (νέος κώδικας):** χρώματα ΟΛΑ από `C.*` (μηδέν hex), χρησιμοποιεί `contentWidth` στο main ScrollView, state prims `Spinner`/`ErrorText` imported. Το inline numeric spacing (16/12/14/28/22) + `card:`/`netCard:` `borderRadius:16` ταιριάζει με το ΗΔΗ σημειωμένο Reports outlier (radius16, όχι `RADIUS.lg`=14) → **μηδέν νέο debt**. **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep):** 6/3/8-digit hardcoded hex στα `screens/`+`nav.tsx`+`App.tsx` → **0** (όλα `C.*`). **States dimension (fresh έλεγχος):** 13/16 screens χρησιμοποιούν το πλήρες trio `Spinner`/`Empty`/`ErrorText`· τα inline `ActivityIndicator` (11 sites) είναι in-button busy spinners (AI/save/unlink) που ΕΠΙΚΑΛΥΠΤΟΝΤΑΙ με το ήδη-queued ghost-button holdout, ΟΧΙ νέο state debt. **Δομικά TODO αμετάβλητα (fresh grep):** `safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88, μηδέν bottom inset· `nav.tsx:87` `paddingTop:60` magic)· `useColorScheme`/`ThemeProvider`/`useTheme` **0** (dark-only). **Reusable holdouts αμετάβλητα:** `<Input>` outliers 5 (Shopping/Receipts/Search/Assistant/Login) + 2 micro (Items `logInput`, Settings `budgetInput`)· **`<Chip>` MISSING** (`chip`/`chipOn`/`chipText` σε Items:465 + Settings:617· `cChip` Subscriptions:160· `sChip` Items:480)· ghost cyan/accent-border buttons ~7 sites ΟΧΙ ενοποιημένα. mobile `tsc --noEmit` → **EXIT 0**. **Top-3 για builder:** (1) Safe-area insets (P2/M, το μοναδικό πραγματικό adaptive gap που μένει ανοιχτό), (2) `<Chip>` primitive (P2/M, τελευταίο reusable-component holdout), (3) ghost `<Button variant>` (μέρος του Button+Chip item, κλείνει το button family). **ΟΥΡΑ ΑΜΕΤΑΒΛΗΤΗ από την 30ή** — μηδέν item έκλεισε/άνοιξε· καθαρό confirmation run.
>
> **ui-auditor re-audit 2026-07-02 (30ή σάρωση· ΝΕΟ mobile-src commit `28c943c` → «Max content width» item COMMITTED/DONE):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα. **16 mobile screens**, `ui.tsx` primitives ΟΛΑ παρόντα (`Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` + νέο `contentWidth` const @ ui.tsx:11). **`git log -- apps/mobile/src` τελευταίο πλέον `28c943c` (feat: shared max-content-width for list/scroll containers)** — αυτό είναι το ΠΡΩΤΟ νέο mobile-src commit από την 26η σάρωση (`0f69116` ListItem). Επιθεώρησα το diff: νέο `export const contentWidth: ViewStyle = { width:'100%', maxWidth:640, alignSelf:'center' }` + εφαρμογή σε **14 screens** (main scroll/list content-container μέσω named-style spread ή inline `[{…}, contentWidth]`)· **0 hardcoded numbers εκτός του single 640 token**, μηδέν token-drift, no-op σε phone (min(100%,640)). → **Το «Max content width για tablet / landscape» (P3/S) ΕΚΛΕΙΣΕ (DONE + committed).** Ήταν το #2 top-3 item της 29ης σάρωσης. **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep):** 6/8-digit hardcoded hex στα `screens/`+`nav.tsx` → **0** (όλα από `C.*`)· `theme.ts` εξάγει `SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha()` + τα web color tokens (`surface3`/`orange`/`onAccent`). **Δομικά TODO αμετάβλητα (fresh grep):** `safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88, μηδέν bottom inset)· `useColorScheme`/`ThemeProvider`/`useTheme` context **0** (dark-only, web light palette χωρίς mobile αντίστοιχο). **Reusable holdouts αμετάβλητα:** `<Input>`/`<TextArea>` σε **6/11** screens· 5 outliers raw (Shopping, Receipts `einput`, Search, Assistant composer, Login) + 2 micro (Items `logInput`, Settings `budgetInput`) = attended-preferred token-drift· **`<Chip>` MISSING** (6 StyleSheet entries σε Subscriptions/Items/Settings)· ghost cyan/accent-border buttons ~7 sites ΟΧΙ ενοποιημένα. mobile `tsc --noEmit` → **EXIT 0**. **Counts by dimension: hardcoded-hex 0 · missing-token 0 · duplicate-primitive 0 auto-buildable byte-identical (Chip = token-drift, όχι byte-identical) · touch-target 0 · safe-area 1 (TODO) · adaptive/max-width 0 (ΕΚΛΕΙΣΕ) · theme/dark-mode 1 (P3 TODO).** Κανένα committed secret (μόνο `.env.example` tracked). **Top-3 για τον builder:** (1) **Safe-area insets** (P2/M, **top unattended-safe**: `npx expo install react-native-safe-area-context` [additive dep, μηδέν token-drift] + `SafeAreaProvider`/`useSafeAreaInsets`, bottom inset σε bottom-sheet modals + κάτω κουμπιά)· (2) **`<Chip>` primitive** (P2, token-drift → θέλει simulator για οπτικό verify, attended-preferred)· (3) **Input outliers / ghost-button variants** (attended-preferred, οπτική αλλαγή). Auto-buildable byte-identical debt = **εξαντλημένο** (όλα τα primitives extracted+committed)· απομένοντα ενεργά items = είτε additive-dep (safe-area) είτε token-drift (Chip/Input/ghost) που θέλει attended simulator verify.
> **ui-auditor re-audit 2026-07-02 (29η σάρωση· CONFIRMATION, ουρά αμετάβλητη):** fresh read-only audit, inventory ξαναχτισμένο από τον κώδικα (όχι docs). **16 mobile screens**, `ui.tsx` primitives ΟΛΑ παρόντα (`Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` + `Header/Centered/Spinner/ErrorText/Empty` helpers). **`git log -- apps/mobile/src` τελευταίο ΑΚΟΜΑ `0f69116` (ListItem)** — μηδέν νέο mobile-src commit από την 26η/27η/28η. Οι ενδιάμεσες commits έως HEAD `7836f43` (`4819f97` SaaS billing routes, `9db3f05` docker-health, `c619123` web-debt docs, `7d4d287`/`7836f43` web pure-lib tests) = **web-only + SaaS server-scaffold + docs → μηδέν mobile UI impact**. **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh grep):** 6/8-digit hardcoded hex στα `screens/`+`nav.tsx` → **0** (όλα από `C.*`)· `theme.ts` εξάγει `SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha()` + τα web color tokens (`surface3`/`orange`/`onAccent`). Web light palette (globals.css) ΔΕΝ έχει mobile αντίστοιχο (dark-only → Light/dark theme P3 item). **Adaptive/theme TODO αμετάβλητα (fresh grep):** `safe-area-context` **0** imports + **εκτός** `package.json` (plain `SafeAreaView` @ App.tsx:88)· content `maxWidth` **0** (μόνο `nav.tsx:87` drawer panel + `AssistantScreen:69` bubble)· `useColorScheme`/`ThemeProvider`/`useTheme` context **0**. **Reusable holdouts αμετάβλητα:** `<Input>`/`<TextArea>` σε **6/11** screens (Items/Money/Settings/Subscriptions/Tasks/Vouchers)· 5 outlier screens raw (Shopping input padH14, Receipts `einput` bg surface, Search padH14/fs16, Assistant composer radius14/maxH120, Login block) + 2 micro (Items `logInput`, Settings `budgetInput`) = attended-preferred token-drift· **`<Chip>` MISSING** (clusters: Subscriptions `cChip`, Items `chip`/`sChip`, Tasks/Search/Settings chip patterns). mobile `tsc --noEmit` → **EXIT 0**. **Counts by dimension: hardcoded-hex 0 · missing-token 0 · duplicate-primitive 0 auto-buildable (Chip = token-drift, όχι byte-identical) · touch-target 0 · safe-area 1 (TODO) · adaptive/max-width 1 (P3 TODO) · theme/dark-mode 1 (P3 TODO).** Κανένα committed secret (μόνο `.env.example` tracked). **Top-3 για τον builder:** (1) **Safe-area insets** (P2/M, top unattended-safe· `SafeAreaProvider`+`useSafeAreaInsets`, χρειάζεται `npx expo install react-native-safe-area-context` = additive dep, μηδέν token-drift)· (2) **Max content width** `<Screen>` wrapper (P3/S, additive, no-op σε phone)· (3) **`<Chip>` ή Input-outliers** (attended-preferred, token-drift θέλει simulator για οπτικό verify).
> **ui-auditor re-audit 2026-07-02 (27η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** inventory ξαναχτισμένο από τον κώδικα (όχι docs). **16 mobile screens**, primitives στο `ui.tsx` = `Header/Centered/Spinner/ErrorText/Empty/Input/TextArea/Check/Button/IconButton/Card/ListItem/Badge` (ΟΛΑ παρόντα, `ListItem` @ ui.tsx:166, `Badge` @ 194). **`git log b6a6cda..HEAD -- apps/mobile/src` = ΚΕΝΟ** (μηδέν νέο mobile-src commit από την 26η σάρωση· τελευταίος mobile-src ΑΚΟΜΑ `0f69116` ListItem). Οι ενδιάμεσες commits = web pure-lib **tests** (`0b136b1` installments, `9550465` cards, `9ebeea2` apiBody), **SaaS scaffold** (`3c6bcc4` billing = server-only infra, μηδέν design-token προς port), landing (`e5e6ff7` FAQ, `ae2760b`/`c4beeae` showcase) + docs/marker → **μηδέν mobile UI impact, ουρά αμετάβλητη**. Working tree apps/mobile **καθαρό** (μόνο `.claude/launch.json` foreign tooling, δεν το αγγίζω). **Token hygiene ΠΛΗΡΩΣ καθαρό (fresh live grep):** 6/8-digit hardcoded hex στα `screens/` → **0** (όλα από `C.*`)· 11/11 web color tokens (globals.css:8-22) mirror-αρισμένα στο `theme.ts` (incl. `surface3`/`orange`/`onAccent`)· `SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha()` scales παρόντα. Web light palette (globals.css:37-50) ΔΕΝ έχει ακόμα mobile αντίστοιχο (dark-only· = Light/dark theme P3/L item). **Reusable holdouts (αμετάβλητα):** Input **6/11** (raw outliers Assistant[r14/maxH120]:77, Search[fs16/r12]:87, Shopping[r12]:154, Receipts[einput bg surface]:268, Login[block]:82 + micro logInput Items:517 / budgetInput Settings:632)· `<Chip>` MISSING (3 clusters με drift: Items chip pad14/7·r10 @465, Settings chip pad16/8·r10 @617, Subscriptions cChip pad11/6·r9 @160 + sChip Items:480)· ghost cyan/accent-border buttons ~7 sites ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports + **εκτός** `package.json` (ακόμα plain `SafeAreaView` @ App.tsx:88)· `maxWidth` στο content **0** (μόνο nav drawer:87 + Assistant bubble:69)· light/dark context **0**. mobile `tsc --noEmit` → **EXIT 0**. **Counts by dimension: hardcoded-hex 0 · missing-token 0 · duplicate-primitive 0 (Chip = token-drift, όχι byte-identical dup) · touch-target 0 (Check+hitSlop DONE) · safe-area 1 (TODO) · adaptive/max-width 1 (P3) · theme/dark-mode 1 (P3).** Κανένα committed secret (μόνο `.env.example` tracked). **Top-3 για τον builder:** (1) **Safe-area insets** (P2/M, **top unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`· χρειάζεται `npx expo install react-native-safe-area-context` = additive dep-add, μηδέν token-drift, πραγματικό UX win)· (2) **Max content width** (P3/S, νέο `<Screen>` wrapper maxWidth+center, additive, low-risk)· (3) **`<Chip>` ή Input-outliers** (attended-preferred· token-drift = οπτική αλλαγή → θέλει simulator για verify).
> **ui-auditor re-audit 2026-07-01 (25η σάρωση· ΠΡΟΟΔΟΣ: `<ListItem>` υπό υιοθέτηση [uncommitted WIP του Αχιλλέα]):** ξαναέχτισα το inventory από τον κώδικα. **16 mobile screens**, primitives στο `ui.tsx` = `Input`/`TextArea`/`Check`/`Button`/`IconButton`/`Card`/`ListItem`/`Badge` (8 exports, ΟΛΑ παρόντα). **Νέο εύρημα:** το top build item της 24ης, το **`<ListItem>` primitive, ΕΧΕΙ ΧΤΙΣΤΕΙ + υιοθετηθεί στο working tree αλλά ΔΕΝ έχει γίνει commit** — είναι ενεργό WIP του Αχιλλέα (`git status` = M σε CalendarScreen/ItemsScreen/MoneyScreen/SubscriptionsScreen/TasksScreen + ui.tsx). Επιβεβαιωμένα: `export function ListItem` @ `ui.tsx:166`, χρήση σε **6 screens** (Money/Subscriptions/Items/Calendar/Shopping/Tasks), και τα **5 byte-identical `row:` entries ΕΞΑΦΑΝΙΣΤΗΚΑΝ** (grep `row: {` στα screens → μένουν μόνο οι 4 outliers Shopping[marginTop]/Settings[space-between/hairline]/Search[radius12]/Receipts[padding12], ΟΛΟΙ σκόπιμα raw). **ΔΕΝ αγγίζω αυτά τα αρχεία** (μη-committed δουλειά του χρήστη· κανόνας git-hygiene). mobile `tsc --noEmit` → **EXIT 0** ΜΕ το WIP (καμία type regression από την υιοθέτηση). **Token hygiene αμετάβλητη & καθαρή:** grep 6/8-digit hardcoded hex στα `screens/` → **0** (όλα από `C.*`)· 11/11 web color tokens mirror-αρισμένα στο `theme.ts` (incl. `surface3`/`orange`/`onAccent`)· `SPACE`/`RADIUS`/`SIZE`/`scrim`/`alpha()` scales παρόντα. **Ουρά μετά το ListItem-in-tree:** το επόμενο unattended-safe TODO γίνεται **Safe-area insets** (ακόμα plain `SafeAreaView` @ `App.tsx:88`, `react-native-safe-area-context` ΔΕΝ στο `package.json` → θέλει dep-add, additive)· τα υπόλοιπα ανοιχτά = attended-preferred (token-drift, οπτικό verify): **`<Chip>`** (Items chip pad14/7·r10, Settings chip pad16/8·r10, Subscriptions cChip pad11/6·r9 — 3 clusters με drift) + **Input outliers** (5 screens Assistant[r14/maxH120]/Shopping[r12]/Search[fs16]/Login[block]/Receipts[einput bg surface]) + **Max content width** (P3) + **Light/dark theme** (P3/L). **Counts by dimension: hardcoded-hex 0 · missing-token 0 · duplicate-primitive 1 (ListItem, in-tree-uncommitted → effectively κλείνει με το commit) · touch-target 0 (Check + hitSlop DONE) · safe-area 1 (TODO) · adaptive/max-width 1 (P3) · theme/dark-mode 1 (P3).** Top-3 για τον builder: (1) **ΜΗΝ ξανακάνεις ListItem** — περίμενε να το commit-άρει ο Αχιλλέας· (2) Safe-area insets (μόνο αν εγκριθεί το dep-add)· (3) `<Chip>` ή Input-outliers όταν υπάρχει simulator για οπτικό verify.
> **ui-auditor re-audit 2026-07-01 (24η σάρωση· CONFIRMATION, μηδέν νέο mobile-src· επόμενο = `<ListItem>`):** `git log 3a272c1..HEAD -- apps/mobile/src` δείχνει **μόνο** το `ee9d413` (shared `<Badge>` primitive, ήδη καταγεγραμμένο στην 23η) → **μηδέν νέο mobile-src commit** από την προηγούμενη σάρωση, ουρά **αμετάβλητη**. Working tree apps/mobile **καθαρό** (μόνο `.claude/launch.json` foreign tooling, δεν το αγγίζω). **Foundation tokens = πλήρως καθαρά** (fresh grep: 8-digit alpha hex στα screens **0**, inline `rgba(0,0,0,…)` scrim **0**, inline `'#000'` onAccent **0**, άλλα 6-digit hex εκτός `theme.ts` **0**) → μηδέν token-drift regression. Primitives DONE: `Input` (6/11), `Button`, `IconButton`, `Card`, `Badge`, `Check`. mobile `tsc --noEmit` → **EXIT 0**. **ΝΕΟ ΕΥΡΗΜΑ (top unattended-safe win):** το `<ListItem>` (τελευταίο μισό του «Card + Badge + ListItem») είναι πλέον ο πιο καθαρός στόχος: **5 BYTE-IDENTICAL** `row:` StyleSheet entries (`flexDirection:'row', alignItems:'center', gap:12, backgroundColor:C.surface, borderWidth:1, borderColor:C.border, borderRadius:14, padding:14, marginBottom:10`) σε **SubscriptionsScreen:151, CalendarScreen:93, MoneyScreen:238, TasksScreen:191, ItemsScreen:469** — μηδέν token-drift, μηδέν οπτική αλλαγή, καθαρό dedup (= `<Card>` base + row-flex overlay) → **unattended-safe**. Outliers που ΜΕΝΟΥΝ raw (διαφορετικό pattern): SearchScreen:89 (radius12/pad12/mb8), SettingsScreen:609 (space-between/hairline row). **Δευτερεύοντα TODO:** Safe-area insets (χρειάζεται `npx expo install react-native-safe-area-context` — **δεν** είναι στο package.json· additive αλλά νέο dependency)· Max content width (μόνο 2 `maxWidth` usages, καμία στο content). **Attended-preferred** (token-drift = οπτικό verify): `<Chip>` (ItemsScreen chip padH14/7 vs SettingsScreen chip padH16/8 + cChip/sChip variants), ghost `<Button variant>` (7 sites), 5 Input outliers, Light/dark theme. **Top-3 για τον builder:** (1) `<ListItem>` primitive [P2/M, byte-identical 5 sites], (2) Safe-area insets [P2/M, +1 dep], (3) Max content width [P3/S].
> **ui-auditor re-audit 2026-07-01 (23η σάρωση· ΠΡΟΟΔΟΣ: `<Badge>` ΕΚΛΕΙΣΕ):** `git log 3a272c1..HEAD -- apps/mobile/src` δείχνει **1 νέο mobile-src commit** από την 22η σάρωση: `ee9d413` (**shared `<Badge>` primitive** για status/type chips· `--stat` 4 files/+37 −14). Τελευταίος mobile-src commit πλέον `ee9d413` (ήταν `3a272c1`). Το commit εξήγαγε `<Badge label color style? textStyle?>` στο `ui.tsx` (coloured 1px-border pill· `color` → border+text· base tokens `borderRadius:6`/padH6/padV2/fs9/w700/ls0.5 == το παλιό TasksScreen `chip`) και μετανάστευσε **byte-identical** τα 3 display-only status/type badges: **TasksScreen** (status), **SearchScreen** (type, `style` padV3 + border-fallback override), **ActivityScreen** (job-status, `style` padH7+marginLeft:auto + `textStyle` fs10)· έσβησε τα 3 τοπικά `chip/badge`+text StyleSheet pairs. Οι ενδιάμεσες μη-mobile commits (`f17f279` isObjectId dedup 4η/τελική + docs/marker/Badge review) = μηδέν επιπλέον UI impact. → Στο item **«Card + Badge + ListItem»: Card DONE + Badge DONE· απομένει μόνο `<ListItem>`**. Fresh live grep (ξανα-μετρημένα, όχι από docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**, 8-digit alpha hex **0**. **Reusable:** `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Input/TextArea/Check/Button/IconButton/Card/Badge` (+`CUR`) — **Chip / ListItem ΑΚΟΜΑ MISSING**· `<Badge>` **3** sites (Tasks/Search/Activity, DONE)· `<Button>` **9**· `<IconButton>` **7**· `<Card>` **11**· `<Input>/<TextArea>` **48** (6/11 screens)· raw `<TextInput>` **21** σε **7** screens (Assistant/Items/Settings/Shopping/Search/Login/Receipts)· `chip*`/`sChip`/`cChip` style-keys σε **3** screens (Subscriptions/Items/Settings· 2 clusters: filter-chip Items+Settings padding-drift 14/7 vs 16/8 + pill radius9 Subscriptions/Items)· base `card:` **2** outliers (Reports flex1/radius16 + Settings section-container)· ghost cyan/accent-border buttons (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) **~7** sites ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports + **εκτός** `package.json`· `maxWidth` στο content **0** (μόνο 2 σε drawer+bubble)· light/dark context (`useColorScheme`/`ThemeProvider`/`useTheme`) **0** (dark-only, ενώ web έχει `data-theme` πλήρες light palette — globals.css:37-50). **States:** συνεπή μέσω `ui.tsx` (0 violations). **Foundation 9 DONE (theme tokens + alpha + touch targets + scrim + Button + IconButton + Card + Button-family finish + Badge) · Input 🟡 6/11 · Chip / ListItem / Safe-area / Max-width / Light = TODO.** Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~3 ανοιχτά** (Chip + ListItem + ghost-buttons ~7 + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder: (1) **Safe-area insets** (P2/M, **unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift, real UX = κορυφαίο auto-buildable)· (2) **`<ListItem>` primitive** (P2/M, extract κοινό row, byte-identical → no οπτική αλλαγή)· (3) **`<Chip>`** (attended-preferred, token-drift = οπτική αλλαγή χωρίς simulator). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μόνο `.env.example` tracked). Read-only run: μηδέν app-code edit, μηδέν Docker, μηδέν AI. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
> **ui-auditor re-audit 2026-07-01 (22η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git log 3a272c1..HEAD -- apps/mobile/src` **κενό** + working tree apps/mobile **καθαρό** → κατάσταση **byte-identical με 21η/19η/18η σάρωση**· κανένα νέο violation, κανένα item δεν άλλαξε status (τελευταίος mobile-src commit ΑΚΟΜΑ `3a272c1` Button-family finish). Οι ενδιάμεσες commits από την 21η σάρωση (`c5cec57`/`f1413c3` isObjectId dedup 2η+3η παρτίδα, όλα web-api) = **μηδέν mobile UI impact**. Fresh live grep (ξανα-μετρημένα, όχι από docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**, 8-digit alpha hex **0**. **Reusable:** `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Input/TextArea/Check/Button/IconButton/Card` (+`CUR/money/shortDate`) — **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**· `<Button>` **9** · `<IconButton>` **7** · `<Card>` **11** · `<Input>/<TextArea>` **48** (6/11 screens) · raw `<TextInput>` **21** σε **7** screens (Items/Settings/Shopping/Assistant/Search/Login/Receipts) · `badge*` style-keys **5** refs (Activity + Statements) · base `card:` **2** (Reports flex1/radius16 + Settings section-container) · ghost cyan/accent-border buttons (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports + **εκτός** `package.json` · `maxWidth` **1** στο content (Assistant bubble· nav drawer ξεχωριστά) · light/dark context **0** (dark-only, ενώ web έχει `data-theme` light — globals.css:37-50 πλήρες light palette). **States:** συνεπή μέσω `ui.tsx` (0 violations). **Foundation 8 DONE · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο). Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~4 ανοιχτά** (Chip + Badge + ListItem + ghost-buttons + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder: (1) **Safe-area insets** (P2/M, **unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift, real UX = κορυφαίο auto-buildable)· (2) **`<ListItem>` primitive** (P2/M, extract κοινό row)· (3) **`<Chip>`/`<Badge>`** (attended-preferred, token-drift = οπτική αλλαγή). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μόνο `.env.example` tracked). Read-only run: μηδέν app-code edit, μηδέν Docker, μηδέν AI. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
> **ui-auditor re-audit 2026-07-01 (21η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git log 3a272c1..HEAD -- apps/mobile/src` **κενό** + working tree apps/mobile καθαρό → κατάσταση **byte-identical με 19η/18η σάρωση**· κανένα νέο violation, κανένα item δεν άλλαξε status (τελευταίος mobile-src commit ΑΚΟΜΑ `3a272c1` Button-family finish). Οι ενδιάμεσες commits (`f1413c3`/`d259a55` isObjectId/readBody dedup + docs) αφορούν ΜΟΝΟ web-api = μηδέν mobile UI impact. Fresh live grep (ξανα-μετρημένα, όχι από docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**, 8-digit alpha hex **0**. **Reusable:** `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Input/TextArea/Check/Button/IconButton/Card` (+`CUR/money/shortDate`) — **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**· `<Button>` **9** · `<IconButton>` **7** · `<Card>` **11** · `<Input>/<TextArea>` **48** (6/11 screens) · raw `<TextInput>` **21** σε **7** screens (Items/Settings/Shopping/Assistant/Search/Login/Receipts) · `badge*` style-keys **3** (Activity + Statements) · base `card:` **2** (Reports flex1/radius16 + Settings section-container) · ghost cyan/accent-border buttons (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports + **εκτός** `package.json` · `maxWidth` **2** (nav drawer + Assistant bubble, **καμία στο content**) · light/dark context **0** (dark-only, ενώ web έχει `data-theme` light). **States:** συνεπή μέσω `ui.tsx` (Spinner 14 / Empty 14 / ErrorText 22· 0 violations). **Foundation 8 DONE · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο). Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~4 ανοιχτά** (Chip + Badge + ListItem + ghost-buttons + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder: (1) **Safe-area insets** (P2/M, **unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift, real UX = κορυφαίο auto-buildable)· (2) **`<ListItem>` primitive** (P2/M, extract κοινό row)· (3) **`<Chip>`/`<Badge>`** (attended-preferred, token-drift = οπτική αλλαγή). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μόνο `.env.example` tracked). Read-only run: μηδέν app-code edit, μηδέν Docker, μηδέν AI. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
> **ui-auditor re-audit 2026-07-01 (19η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git log 3a272c1..HEAD -- apps/mobile/src` **κενό** + working tree apps/mobile καθαρό → η κατάσταση είναι **byte-identical με την 18η σάρωση**· κανένα νέο violation, κανένα item δεν άλλαξε status (τελευταίος mobile-src commit = `3a272c1` Button-family finish). Οι ενδιάμεσες app-code αλλαγές αφορούν ΜΟΝΟ το web (`cd3600f` pluggable notifiers + Beacon Sweep AI bar + i18n = μηδέν mobile UI impact). Fresh grep re-verify (όλα ξανα-μετρημένα live, όχι από docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**, 8-digit alpha hex **0**. **Reusable components:** `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Input/TextArea/Check/Button/IconButton/Card` (+`CUR/money/shortDate`) — **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**· `<Button>` **9** sites (core DONE)· `<IconButton>` **7** call sites (DONE)· `<Card>` **11** sites (DONE)· `<Input>/<TextArea>` **48** sites (6/11 screens)· raw `<TextInput>` **21** σε **7** screens (Items/Settings/Shopping/Assistant/Search/Login/Receipts)· `chip*`/`sChip` style-keys **8** σε **5** screens (Subscriptions/Tasks/Items/Search/Settings· 2 clusters: filter-chip Items+Settings padding drift 14/7 vs 16/8 + tiny tag-chip Search+Tasks radius6/fs9)· `badge*` **3** (Activity + Statements)· base `card:` **2** (Reports flex1/radius16 + Settings section-container outliers)· ghost cyan/accent-border buttons **8** sites (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) → ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports + **εκτός** `package.json`· `maxWidth` **2** (nav drawer panel + Assistant chat bubble, **καμία στο content**)· light/dark context (`useColorScheme`/`ThemeProvider`/`useTheme`) **0** (dark-only, ενώ το web έχει πλήρες `data-theme` light mode). **States:** συνεπή μέσω `ui.tsx` (0). **Foundation 8 DONE (theme tokens + alpha + touch targets + scrim + Button + IconButton + Card + Button-family finish) · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο από 18η). Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~4 ανοιχτά** (Chip + Badge + ListItem + ghost-buttons 8 + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder (unattended-safe πρώτο, τώρα που το Button έκλεισε): (1) **Safe-area insets** (P2/M, **unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift, real UX — bottom-sheets κάτω από home indicator· κορυφαίο auto-buildable)· (2) **`<ListItem>` primitive** (P2/M, extract κοινό row pattern)· (3) **`<Chip>`** ή **`<Badge>`** (attended-preferred, token-drift = οπτική αλλαγή χωρίς simulator). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μόνο `.env.example` template tracked, μηδέν πραγματικό `.env`). Read-only run: μηδέν app-code edit, μηδέν Docker, μηδέν AI. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
> **ui-auditor re-audit 2026-07-01 (18η σάρωση· ΠΡΟΟΔΟΣ: Button-family ΕΚΛΕΙΣΕ):** `git log 2b0422d..HEAD -- apps/mobile/src` δείχνει **1 νέο commit** από την 17η σάρωση: `3a272c1` (**Button-family finish** — ShoppingScreen `addBtnWide`/`addBtnText2` + ItemsScreen accent-pill `save` outlier → `<Button>`). Live re-verify ανά διάσταση (grep, όχι docs, όλα ξανα-μετρημένα): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**. **Reusable components:** `<Button>` **9** sites (7→9· addBtnWide + Items save migrated) → **Button πυρήνας DONE**· `addBtn` refs **1** (μόνο σχόλιο στο `ui.tsx`, μηδέν στα screens)· `<IconButton>` **15** refs → **DONE**· `<Card>` **13** refs → **DONE**· `<Input>/<TextArea>` **48** sites (6/11 screens)· raw `<TextInput>` **21** σε 7 screens· `chip*`/`chipOn` keys σε **4** screens (Items/Tasks/Search/Settings) + `sChip` Subscriptions· `badge*` **3** (Activity + Statements)· base `card:` **3** (Reports flex1/radius16 + Settings section-container outliers) → **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**· ghost cyan/accent-border buttons **7** sites (`aiBtn`/`scanBtn`/`rescanBtn`/`importBtn`) → ΟΧΙ ενοποιημένα. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports· `maxWidth` **2** (drawer panel + chat bubble, **καμία στο content**)· light/dark context (`useColorScheme`/`ThemeProvider`/`useTheme`) **0** (dark-only). **States:** συνεπή μέσω `ui.tsx` (0). **Foundation 8 DONE (theme tokens + alpha + touch targets + scrim + Button + IconButton + Card + [Button-family finish]) · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO.** Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~4 ανοιχτά** (Chip + Badge + ListItem + ghost-buttons 7 + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder: (1) **Safe-area insets** (P2/M, **unattended-safe**: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift — τώρα η κορυφαία unattended-safe μιας και το Button έκλεισε)· (2) **`<ListItem>` primitive** (P2/M, extract row pattern)· (3) **`<Chip>`** ή **`<Badge>`** (attended-preferred, token-drift = οπτική αλλαγή χωρίς simulator). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μηδέν tracked `.env`). Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (17η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git log 2b0422d..HEAD -- apps/mobile/src` **κενό** → καμία αλλαγή στον mobile κώδικα από την 16η σάρωση (builder inactive εδώ· τελευταίος mobile-src commit = `9d226cf` Card primitive). Οι μόνες ενδιάμεσες app-code αλλαγές = `0c866eb` (web: adopt `readBody`/`apiBody` σε notifications + lists PATCH = **Web Debt refactor, behaviorally identical**, μηδέν UI impact στο mobile). Live re-verify ανά διάσταση (grep, όχι docs, όλα ξανα-μετρημένα): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**. **Reusable components:** `<IconButton>` **15** refs (def + adopters) → **DONE**· `<Card>` **11** refs → **DONE**· `<Input>/<TextArea>` **48** sites (6/11 screens)· raw `<TextInput>` **23** σε 7 screens· `<Button>` **7** sites· `addBtn` **5** refs = μόνο ShoppingScreen `addBtnWide`/`addBtnText2` (wide text button `bg accent/radius12/padV12/padH18/onAccent/fs15/w700`, ΟΧΙ icon)· `chip*` keys σε **5** screens (Items/Search/Settings/Subscriptions/Tasks, μη ενοποιημένα)· `badge*` **5**, base `card:` **3** (2 outliers: Reports flex1/radius16 + Settings section-container) → **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports· `maxWidth` **2** (drawer + bubble, **καμία στο content**)· light/dark context (`useColorScheme`/`ThemeProvider`/`useTheme`) **0** (dark-only). **States:** συνεπή μέσω `ui.tsx` (0). **Foundation 7 DONE (theme tokens + alpha + touch targets + scrim + Button + IconButton + Card) · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο από 16η). Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~5 ανοιχτά** (Chip + Badge + ListItem + ghost-buttons ~6 + Input outliers 5 + addBtnWide), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder αμετάβλητο: (1) **Button-family finish** (P2/S, **unattended-safe**: ShoppingScreen `addBtnWide` → `<Button label="Add to list" style={{paddingHorizontal:18}}>` [byte-identical tokens] + ItemsScreen `save` outlier + SettingsScreen `saveText`)· (2) **Safe-area insets** (P2/M, unattended-safe: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift)· (3) **`<ListItem>` primitive** (P2/M) ή **`<Chip>`** (attended-preferred, token-drift). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μηδέν tracked `.env`). Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (16η σάρωση· ΠΡΟΟΔΟΣ: IconButton + Card ΕΚΛΕΙΣΑΝ):** `git log effd90d..HEAD -- apps/mobile/src` δείχνει **2 νέα foundation commits** από την 15η σάρωση: `929ca4d` (**IconButton** primitive + 7 adopters) + `9d226cf` (**Card** primitive + 6 sites)· ενδιάμεσα και `47a9574` (Tasks ←/→ quick-move) + `e0f7a97` (Tasks steps API+modal, ήδη queue-DONE). Live re-verify ανά διάσταση (grep, όχι docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, `#fff/#000` στα screens **0**, inline `rgba(` **0**. **Reusable components:** `<IconButton>` **13** refs / **7** call sites (add ×5 + addStep + Items `✦`/import) → **DONE**· `<Card>` **11** refs / 6 sites migrated, μόνο **2** τοπικά `card:` outliers (Settings section-container padH-only + Reports flex1/radius16) = attended → **Card DONE**· `<Input>/<TextArea>` **48** sites (6/11 screens)· raw `<TextInput>` **21** σε 7 screens· `<Button>` **7** sites· `addBtn` **4** refs = μόνο ShoppingScreen `addBtnWide` (text button, ΟΧΙ icon)· `chip*:` **14** (2 clusters), `badge*:` **3** → **Chip / Badge / ListItem ΑΚΟΜΑ MISSING**. **Adaptive/theme (δομικά TODO):** `safe-area-context` **0** imports· `maxWidth` content **0**· light/dark context **0** (dark-only). **States:** συνεπή μέσω `ui.tsx` (0). **Foundation 7 DONE (theme tokens + alpha + touch targets + scrim + Button + IconButton + Card) · Input 🟡 6/11 · Chip / Badge / ListItem / Safe-area / Max-width / Light = TODO.** Violations ανά διάσταση: Tokens **0**, States **0**, Touch **0**, Reusable-components **~5 ανοιχτά** (Chip 14 + Badge 3 + ListItem + ghost-buttons ~6 + Input outliers 5), Adaptive **2** (safe-area, max-width), Theme **1** (light). Top-3 builder: (1) **Button-family finish** (P2/S, unattended-safe: ShoppingScreen `addBtnWide` + ItemsScreen `save` outlier → `<Button>`, byte-identical-ish)· (2) **Safe-area insets** (P2/M, unattended-safe: `SafeAreaProvider`+`useSafeAreaInsets`, additive, no token-drift)· (3) **`<ListItem>` primitive** (P2/M) ή **`<Chip>`** (attended-preferred, token-drift). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μηδέν tracked `.env`). Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (15η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git log ff588b7..HEAD -- apps/mobile/src` **κενό** → καμία αλλαγή στον mobile κώδικα από την 14η σάρωση (builder inactive εδώ, τελευταίος mobile-src commit = `effd90d` Button primitive). Μόνη ενδιάμεση app-code αλλαγή = `83cc537` (web: adopt `apiBody` helpers σε `tasks` + `stores` POST = **Web Debt refactor, behaviorally identical**, μηδέν UI impact στο mobile). Live re-verify ανά διάσταση (grep, όχι docs, όλα ξανα-μετρημένα): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**. **Reusable components:** `<Input>/<TextArea>` **47** sites (6/11 screens)· raw `<TextInput>` **21** σε 7 screens (Assistant/Shopping/Search/Settings/Receipts/Items/Login)· `<Button>` primitive **7** sites· `addBtn` **18** refs σε **4** screens (Subscriptions/Items/Shopping/Vouchers)· ghost/scan/import buttons σε **6** screens (Subscriptions/Money/Shopping/Items/Receipts/Vouchers)· `chip*:` **14**, base `card:` **5** (το SettingsScreen:269 `card,` = function param, false-positive → grep δίνει 6), `badge*:` **3** → **IconButton/Chip/Card/Badge/ListItem ΑΚΟΜΑ MISSING**. **Adaptive/theme (δομικά TODO):** `safe-area-context` εκτός `package.json` + **0** imports· `maxWidth` **2** (drawer + bubble, καμία στο content)· light/dark context (`useColorScheme`/`ThemeProvider`/`useTheme`) **0** (dark-only). **States:** συνεπή μέσω `ui.tsx`. **Foundation 5 DONE (theme tokens + alpha + touch targets + scrim + Button) · Input 🟡 6/11 · IconButton+Chip / Card+Badge+ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο από 12η/14η). Top-3 builder αμετάβλητο: (1) **IconButton / `addBtn` variant** (P2/M, unattended-safe: byte-identical 46-wide `＋` accent ×4 + Items cyan `importBtn`)· (2) **Card + Badge + ListItem primitives** (P2/M, 5 card + 3 badge)· (3) **Input συνέχεια + `<Chip>`** (P1/M, 5 outlier screens token-drift + on/off chip, attended-preferred). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret (μηδέν tracked `.env`). Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (14η σάρωση· CONFIRMATION, μηδέν νέο mobile-src):** `git diff --stat e41fdff..HEAD -- apps/mobile/src/` **κενό** → καμία αλλαγή στον mobile κώδικα από την 12η σάρωση (builder inactive εδώ). Live re-verify ανά διάσταση (grep, όχι docs, όλα ξανα-μετρημένα): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, inline `rgba(` στα screens **0**. **Reusable components:** `<Input>/<TextArea>` **47** sites (6/11 screens)· raw `<TextInput>` **22** σε 7 screens (Items/Assistant/Shopping/Search/Settings/Receipts/Login)· `addBtn` **18** refs σε **4** screens (Subscriptions/Shopping/Vouchers/Items)· ghost/scan/import buttons σε 6 screens (Money/Subscriptions/Shopping/Items/Receipts/Vouchers)· `chip*:` **14**, base `card:` **5** (το SettingsScreen:269 `card,` = function param, false-positive → όχι style entry), `badge*:` **3** → **IconButton/Chip/Card/Badge/ListItem ΑΚΟΜΑ MISSING**. **Adaptive/theme (δομικά TODO):** `safe-area-context` εκτός `package.json` + **0** imports· `maxWidth` **1** στα screens (bubble, καμία στο content)· light/dark context (`useColorScheme`/`ThemeProvider`) **0** (dark-only). **States:** συνεπή μέσω `ui.tsx`. **Foundation 5 DONE (theme tokens + alpha + touch targets + scrim + Button) · Input 🟡 6/11 · IconButton+Chip / Card+Badge+ListItem / Safe-area / Max-width / Light = TODO** (αμετάβλητο από 12η). Top-3 builder αμετάβλητο: (1) **IconButton / `addBtn` variant** (P2/M, unattended-safe: byte-identical 46-wide `＋` accent ×4 + Items cyan `importBtn`)· (2) **Card + Badge + ListItem primitives** (P2/M, 5 card + 3 badge)· (3) **Input συνέχεια + `<Chip>`** (P1/M, 5 outlier screens token-drift + on/off chip, attended-preferred). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret. Read-only, μηδέν app-code / Docker / AI.
>
> **ui-auditor re-audit 2026-07-01 (12η σάρωση· ΝΕΟ: `Button` primitive ΕΚΛΕΙΣΕ, foundation 5 DONE):** Πρώτο ui-audit μετά τον builder commit `effd90d` (extract `<Button>`). Live re-verify ανά διάσταση (grep, όχι docs): **Tokens ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, short `#fff/#000` στα screens **0**, 8-digit alpha **0**, inline `rgba(` στα screens **0**. **Reusable components:** `<Button>` primitive πλέον στο `ui.tsx` + **7 sites migrated** (Money ×2, Receipts, Subscriptions, Tasks, Vouchers ×2)· `<Input>/<TextArea>` **47** sites (**6/11 screens**: Money/Subscriptions/Vouchers/Tasks/Items/Settings), raw `<TextInput>` **21** σε 7 screens (Assistant/Items/Shopping/Settings/Search/Receipts/Login· τα Items/Settings έχουν και τα δύο)· **ΑΠΟΜΕΝΟΥΝ ανοιχτά:** button-family duplication `addBtn` (46-wide `＋` accent) σε **4** screens (Subscriptions/Items/Vouchers/Shopping) + `scanBtn` (Money/Shopping) + ghost `aiBtn` (Subscriptions/Items/Vouchers) + `importBtn` cyan (Items) + Items `save` outlier (padH26/minWidth96) + Settings leftover `saveText` → **IconButton/Chip/ghost variants MISSING**· `chip*:` **14**, base `card:` **5**, `badge*:` **3** → **Chip/Card/Badge/ListItem MISSING**. **Adaptive/theme (δομικά TODO):** `safe-area-context` εκτός `package.json` (**0**)· `maxWidth` **2** (drawer + bubble, καμία στο content)· light/dark context **0** (dark-only). **States:** συνεπή μέσω `ui.tsx`. **Foundation 5 DONE (theme tokens + alpha + touch targets + scrim + Button) · Input 🟡 6/11 · IconButton+Chip / Card+Badge+ListItem / Safe-area / Max-width / Light = TODO.** Top-3 builder: (1) **IconButton / `addBtn` variant** (P2/M, unattended-safe: 4 byte-identical 46-wide `＋` accent + Items cyan `importBtn`)· (2) **Card + Badge + ListItem primitives** (P2/M, 5 card + 3 badge)· (3) **Input συνέχεια + `<Chip>`** (P1/M, 5 outlier screens token-drift + on/off chip, attended-preferred). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret. Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (11η σάρωση· confirmation, μηδέν νέο mobile-src):** `git log 91a5fe3..HEAD -- apps/mobile/src` **κενό** + working tree καθαρό → η κατάσταση είναι **byte-identical με την 10η σάρωση**· κανένα νέο violation, κανένα item δεν άλλαξε status. Fresh grep re-verify (όλα ξανα-μετρημένα live, όχι από docs): **Tokens dimension ΠΛΗΡΩΣ καθαρό** — 6-digit hex εκτός `theme.ts` **0**, `#000` στα screens **0**, 8-digit alpha **0**, inline `rgba(0,…)` στα screens **0**. **Reusable components (ανοιχτά):** `<Input>/<TextArea>` **47** sites (**6/11 screens**)· raw `<TextInput>` **21** σε 7 screens (Assistant/Items/Login/Receipts/Search/Settings/Shopping)· `chip*:` style-keys **14**, button-variant keys (save/add/ai/scan) **17**, base `card:` **6**, `badge*:` **3** — όλα per-screen, **Button/Chip/Card/Badge/ListItem ΑΚΟΜΑ MISSING** από `ui.tsx`. **Adaptive/theme (δομικά TODO):** `safe-area-context` εκτός `package.json` (grep **0**)· `maxWidth` **2** (drawer + bubble, καμία στο content)· light/dark context (`useColorScheme`/`useTheme`/`ThemeProvider`) **0** (dark-only). **States:** συνεπή μέσω `ui.tsx`. **Foundation 4 DONE (theme tokens + alpha + touch targets + scrim) · Input 🟡 6/11 · Button+Chip / Card+Badge+ListItem / Safe-area / Max-width / Light = TODO.** Top-3 builder αμετάβλητα: (1) **Button + Chip primitives** (P2/M, unattended-safe subset: 5 byte-identical `save` padV12/padH22 + 6 `addBtn`)· (2) **Card + Badge + ListItem primitives** (P2/M, 6 card + 3 badge entries)· (3) **Input primitive συνέχεια** (P1/M, 5 outlier screens token-drift, attended-preferred). mobile `tsc --noEmit` **EXIT 0**. Κανένα committed secret. Read-only, μηδέν app-code / Docker / AI.
> **ui-auditor re-audit 2026-07-01 (10η σάρωση· πιάνει το `91a5fe3`, scrim item ΕΚΛΕΙΣΕ):** πρώτη σάρωση μετά το `91a5fe3` (scrim token, ενοποίηση 10 backdrops). Τελευταίος mobile-src commit = `91a5fe3` (`git log 91a5fe3..HEAD -- apps/mobile/src` κενό). **Tokens dimension πλέον ΠΛΗΡΩΣ καθαρό:** hardcoded 6-digit hex εκτός `theme.ts` = **0** (ο τελευταίος `alpha('#000',0.67)` scrim @ Settings ενοποιήθηκε σε `scrim = 'rgba(0,0,0,0.6)'` στο `theme.ts`)· `#000` στα screens **0**· 8-digit alpha hex **0**· grep `rgba(0,0,0,…)` inline στα screens **0** (μόνο ο ένας token def + σχόλιο στο `theme.ts`). → το **scrim item κάτω ΕΚΛΕΙΣΕ ως DONE** (builder). **Reusable components dimension (τα ανοιχτά):** Input primitive **6/11 screens** (47 `<Input>/<TextArea>` sites· raw `<TextInput>` **21** sites σε **7 screens**: Assistant/Items/Login/Receipts/Search/Settings/Shopping· 6 τοπικά input-style entries με token-drift)· **Button/Chip/Card/Badge/ListItem ΑΚΟΜΑ MISSING** από το `ui.tsx` (εξάγει Header/Centered/Spinner/ErrorText/Empty/Check/Input/TextArea + CUR/money/shortDate)· chip-variant **27**, button-variant (save/add/scan/ai) **17** σε 9 screens, base `card:` **5** entries, badge-variant **3** — όλα per-screen. **Adaptive/theme (τα δομικά TODO):** `react-native-safe-area-context` **εκτός** `package.json` + **0** imports (plain RN `SafeAreaView` @ App.tsx:88, top-only, Android manual paddingTop, μηδέν bottom inset)· `maxWidth` **2** (drawer + bubble, **καμία στο content** → edge-to-edge σε tablet/landscape)· light/dark context (`useColorScheme`/`useTheme`/`ThemeProvider`) **0** (dark-only, ενώ web έχει πλήρες `data-theme` light mode). **States:** συνεπή μέσω `ui.tsx` (Spinner/Empty/ErrorText widely used). **Foundation 4 DONE (theme tokens + alpha + touch targets + scrim) · Input 🟡 6/11 · Button+Chip/Card+Badge/Safe-area/Max-width/Light = TODO.** Κορυφαίο unattended-safe = **Button primitive** (5 byte-identical `save` padV12/padH22 + 6 `addBtn` = οπτικά αμετάβλητο subset)· μετά Card+Badge+ListItem· τα Input-outlier screens + Safe-area/Light = attended-preferred (οπτικό verify / dep-add). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
> **Builder 2026-07-01 (Input primitive — byte-identical subset: Items + Settings):** συνέχισα τη μετανάστευση `<Input>`/`<TextArea>` στα **2 screens όπου τα τοπικά input styles είναι byte-identical με τα primitives** (μηδέν οπτική αλλαγή, unattended-safe, tsc-verifiable — δεν χρειάζεται simulator για οπτικό verify). **ItemsScreen**: `s.input` (`surface/radius12/padH12/padV11/fs15` == `inputSurface`) → `<Input style={{flex:1}}>` (1 site) + `s.minput` (`surface2/radius10/padH12/padV10/fs15` == `inputModal`) → `<Input variant="modal">` (4 sites) + το SPECS multiline → `<TextArea variant="modal" style={s.specs}>` (1 site)· έσβησα τα `input`/`minput` StyleSheet defs. **SettingsScreen**: `s.input` (`surface2/radius10/padH12/padV10/fs15` == `inputModal`) → `<Input variant="modal">` σε **11 sites** + το notes multiline → `<TextArea variant="modal" style={{height:64}}>`· έσβησα το `input` def. **Byte-identical**: κάθε migrated style ταιριάζει ακριβώς με το primitive base (RADIUS.md=12/RADIUS.sm=10, SPACE.md=12, SIZE.md=15)· τα composite `style={[s.input,{marginBottom:8}]}`/`{flex:1}` → `style={{…}}` passthrough. Έμειναν σκόπιμα raw (token-outliers = **οπτική** αλλαγή, attended-preferred): ItemsScreen `logInput` (radius10/padH10/padV8/fs14), SettingsScreen `budgetInput` (padV9), + ShoppingScreen (padH14)/ReceiptsScreen `einput` (bg surface όχι surface2)/AssistantScreen (radius14/padH14)/SearchScreen (padH14/fs16)/LoginScreen. **Verify**: mobile `npx tsc --noEmit` → **EXIT 0**· grep: μηδέν dangling `s.input`/`s.minput`· καμία αλλαγή web runtime → κανένα Docker rebuild, μηδέν AI. Input primitive πλέον **6/11 screens**. Επόμενο unattended-safe: το `scrim`/`backdrop` token για το εναπομείναν `alpha('#000',0.67)` @ SettingsScreen (byte-identical, P3/S)· τα υπόλοιπα input-outlier screens + Button/Chip component extraction = attended-preferred (οπτικό verify).
> **ui-auditor re-audit 2026-07-01 (9η σάρωση· πιάνει το `1791295`, scrim-drift formalized):** πρώτη σάρωση μετά το `1791295` (Input primitive migration Items+Settings, byte-identical). Foundation ξανα-επιβεβαιωμένο live: `theme.ts` mirror-άρει 1:1 το `globals.css:8-22` (13 χρώματα + `onAccent`) + `SPACE`/`RADIUS`/`SIZE`/`alpha`· `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Check/Input/TextArea` (+`CUR/money/shortDate`). **Tokens dimension σχεδόν καθαρό:** hardcoded hex εκτός `theme.ts` = **1** (`#000` @ SettingsScreen:677, το modal backdrop). **ΝΕΟ διακριτό εύρημα (formalized ως item παρακάτω):** ο **modal backdrop scrim ξαναγράφεται inline σε 9 screens με αποκλίνουσα διαφάνεια** — `rgba(0,0,0,0.6)` ×7 (Money/Items/Subscriptions/Shopping/Tasks/Statements/Vouchers), `rgba(0,0,0,0.7)` ×1 (Receipts:256), `alpha('#000',0.67)` ×1 (Settings:677) → 3-way drift + ο τελευταίος `#000` literal· ένα `scrim` token στο `theme.ts` τα ενοποιεί (byte-identical @0.6 για τα 7, μικρή οπτική για Receipts/Settings· unattended-safe). **Reusable components dimension:** Input **6/11** screens (5 outliers: Shopping/Receipts/Assistant/Search/Login, token-drift)· **Button MISSING** — το `{backgroundColor:C.accent, borderRadius:12, ...}` save/add/scan ξαναγράφεται σε **9 screens**, με **5 byte-identical `save`** (`padV12/padH22`: Money/Receipts/Subscriptions/Tasks/Vouchers)· **Chip/Badge MISSING** (7 screens). **Adaptive:** `react-native-safe-area-context` εκτός `package.json` (plain RN `SafeAreaView` @ App.tsx, μηδέν bottom inset)· **0** `maxWidth` στο content (tablet/landscape stretch). **States:** συνεπή μέσω `ui.tsx` (Spinner/Empty/ErrorText widely used· AssistantScreen/ShoppingScreen/LoginScreen input-driven → σκόπιμα minimal). **Foundation 3 DONE · Input 🟡 6/11 · scrim/Button/Chip/Card+Badge/Safe-area/Max-width/Light = TODO.** Κορυφαίο unattended-safe = **scrim token** (P2/S, σβήνει τον τελευταίο hex + 3-way drift)· μετά **Button primitive** (5 byte-identical `save` = unattended-safe subset). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
> **ui-auditor re-audit 2026-07-01 (8η σάρωση· πιάνει το `3bc8a3d`, onAccent chronic ΕΚΛΕΙΣΕ):** πρώτη σάρωση μετά το `3bc8a3d` (onAccent `#000` literals → `C.onAccent`, 32 sites/10 screens). Τελευταίος mobile commit = `3bc8a3d` (`git log 3bc8a3d..HEAD -- apps/mobile/src` κενό). **Καθοριστικό drift από την 7η σάρωση:** τα `#000` literals στα screens έπεσαν **33 → 1** — απομένει ΜΟΝΟ το `alpha('#000', 0.67)` modal backdrop scrim @ SettingsScreen:678 (σημασιολογικά black overlay, ΟΧΙ onAccent· θέλει ξεχωριστό `scrim`/`backdrop` token αντί inline literal = μικρό P3/S auto-buildable follow-up, byte-identical). Το `#000` του `theme.ts:18` είναι ο ίδιος ο `onAccent` token def (σωστό, όχι violation). Επομένως ο **χρόνιος onAccent follow-up = ΚΛΕΙΣΤΟΣ**· το Button+Chip item χρειάζεται πλέον ΜΟΝΟ το actual `<Button>`/`<Chip>` component extraction (μηδέν inline literals να μεταναστεύσουν). Υπόλοιπα dimensions live: `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Check` + **`Input`/`TextArea`** (γρ. 41/52) + `CUR/money/shortDate` — **μηδέν `Button/Chip/Card/Badge/ListItem`**. `<Input>/<TextArea>` σε **29 χρήσεις / 4 screens** (Money/Subscriptions/Tasks/Vouchers)· raw `<TextInput>` **39** σε **7 screens** (Assistant/Items/Login/Receipts/Search/Settings/Shopping)· τοπικά input-style entries **10** με αποκλίνοντα tokens (radius **10/12/14** [ItemsScreen:485=10, ItemsScreen:465=12, AssistantScreen:77=14], padH **10/12/14** [ItemsScreen:528=10, ItemsScreen:465=12, ShoppingScreen:157=14], padV **8/9/10/11** [logInput=8, budgetInput=9, minput=10, input=11], fs **14/15/16** [logInput=14, τα περισσότερα=15, SearchScreen:85=16]). non-`#000` 6-digit hex **0**· 8-digit alpha hex **0**· `card:` **6** / 5 screens (ActivityScreen/ReportsScreen/SettingsScreen/StatementsScreen/VouchersScreen)· badge-variant **3**· chip-variant **27** (per-screen)· `maxWidth` **2** (nav drawer panel + assistant bubble, **καμία στο content**)· `react-native-safe-area-context` **εκτός** `package.json` + **0** imports· light theme / `useColorScheme`/`useTheme`/`ThemeProvider` **0** (dark-only). **Foundation 3 DONE (theme tokens + alpha + touch targets) · Input primitive 🟡 IN PROGRESS (4/11) · Button+Chip onAccent-prereq DONE, component TODO · 4 δομικά TODO** (Card+Badge+ListItem, Safe-area, Max-width, Light/dark). Κορυφαίο ενεργό = **συνέχεια Input migration** στα 7 εναπομείναντα screens (attended-preferred, no simulator → οπτικό verify)· κορυφαίο unattended-safe = **Button + Chip primitives** (P2/M· ο onAccent prereq είναι πλέον done, μένει το component extraction). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
> **Builder 2026-07-01 (βραδινό — onAccent literal migration):** έκλεισα το χρόνιο «onAccent follow-up» (line 228: «τα υπόλοιπα ~26 inline `#000` onAccent σε άλλα screens μένουν») — τα **32** onAccent `#000` literals (23 `color:'#000'` StyleSheet/inline + 9 `<ActivityIndicator color="#000">`) σε **10 screens** → `C.onAccent`. **Byte-identical** (`C.onAccent === '#000'`, theme.ts:18) → μηδέν οπτική αλλαγή, tsc-verifiable, unattended-safe (το κύριο criterion που όλοι οι ui-auditors ανέφεραν). Τα `#000` literals έπεσαν **33 → 1** (μένει ΜΟΝΟ το `alpha('#000', 0.67)` modal backdrop scrim @ SettingsScreen:678, σημασιολογικά ΟΧΙ onAccent αλλά black overlay → σκόπιμα άθικτο· θέλει ξεχωριστό `scrim`/`backdrop` token = Needs Decision). Αυτό είναι **prerequisite cleanup** για το Button+Chip primitive item (πλέον όταν χτιστεί το `<Button>`, δεν θα υπάρχουν inline literals να μεταναστεύσουν)· ΔΕΝ κλείνει το item (χρειάζεται ακόμα actual `<Button>`/`<Chip>` component extraction). **Verify:** mobile `npx tsc --noEmit` → **EXIT 0**· καμία αλλαγή web runtime → κανένα Docker rebuild· μηδέν AI. Staged ΜΟΝΟ τα 10 screen files + docs (explicit paths).
> **ui-auditor re-audit 2026-07-01 (7η σάρωση· stable, μηδέν drift):** επανα-σάρωση από τον κώδικα (όχι docs). **Μηδέν νέο mobile code commit από την 6η σάρωση** (`git log 866d6e9..HEAD -- apps/mobile/src` κενό· τελευταίος mobile commit = `6a9dc33` Input/TextArea). Κατάσταση **byte-identical με την 6η σάρωση**, όλα τα dimensions ξανα-επιβεβαιωμένα live: `ui.tsx` εξάγει `Header/Centered/Spinner/ErrorText/Empty/Check` + **`Input`/`TextArea`** (γραμμές 41/52) + `CUR/money/shortDate` — **μηδέν `Button/Chip/Card/Badge/ListItem`** ακόμα. `<Input>/<TextArea>` σε **29 χρήσεις / 4 screens** (Money, Subscriptions, Tasks, Vouchers)· raw `<TextInput>` sites **41** σε **7 screens** (Assistant/Items/Login/Receipts/Search/Settings/Shopping) + οι 2 primitive defs στο `ui.tsx`· τοπικά input-style entries **13** (τα token-outliers: AssistantScreen radius14/maxH120, ItemsScreen `minput`/`logInput` micro padH10/padV8/fs14, SearchScreen fs16, SettingsScreen `budgetInput` padV9, ReceiptsScreen `einput`, ShoppingScreen padH14, LoginScreen). `#000` literals **33** (**23** `color:'#000'` text/pill + **9** `<ActivityIndicator color="#000">` + **1** `alpha('#000',0.6)` backdrop, ΟΛΑ onAccent → Button+Chip item)· non-`#000` 6-digit hex **0**· 8-digit alpha hex **0**· `card:` **5**· badge-variant **3**· `maxWidth` **2** (καμία στο content)· `react-native-safe-area-context` **0** imports + **εκτός** `package.json`· light theme / `useColorScheme`/`useTheme`/`ThemeProvider` **0** (dark-only). **Foundation 3 DONE (theme tokens + alpha + touch targets) · Input primitive 🟡 IN PROGRESS (4/11 screens) · 5 TODO** (Button+Chip, Card+Badge+ListItem, Safe-area, Max-width, Light/dark). Κορυφαίο ενεργό = **συνέχεια Input migration** στα 7 εναπομείναντα screens (ιδανικά attended run, no simulator → οπτικό verify)· αν μείνει attended-only, το κορυφαίο unattended-safe = **Button + Chip primitives** (P2/M, σβήνει τα 33 `#000` onAccent). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (6η σάρωση· μετά το `6a9dc33`, Input primitive IN PROGRESS):** πρώτη σάρωση που πιάνει τη μετανάστευση του builder — το `6a9dc33` (Input/TextArea primitives + migrate 4 record-form screens) προσγειώθηκε **μετά** την 5η σάρωση (`54c575e`). Επίδραση επιβεβαιωμένη live: (α) το `ui.tsx` εξάγει πλέον **`Input` + `TextArea`** (γραμμές 41/52) δίπλα στα `Header/Centered/Spinner/ErrorText/Empty/Check` (+`CUR/money/shortDate`) — το πρώτο form primitive υπάρχει· μηδέν ακόμα `Button/Chip/Card/Badge`. (β) **`<Input>`/`<TextArea>` σε 29 χρήσεις / 4 screens** (Money, Subscriptions, Tasks, Vouchers). (γ) Τα τοπικά input-style entries **έπεσαν 18 → 10 / 9 screens** (σβήστηκαν τα Money/Subscriptions/Vouchers/Tasks): απομένουν AssistantScreen:77 (radius **14**/padH14/padV11/fs15/maxH120), ItemsScreen:465 (radius12/padH12/padV11), :485 (`minput` radius10/padH12/padV10), :528 (`logInput` radius10/padH**10**/padV**8**/fs**14**), SearchScreen:85 (radius12/padH14/fs**16**), SettingsScreen:615 (radius10/padH12/padV10 surface2), :633 (`budgetInput` padV**9**), ShoppingScreen:157 (radius12/padH14), ReceiptsScreen:268 (`einput` radius10/padH12/padV10), LoginScreen:82 (distinct block). Αυτά είναι ακριβώς οι token-outliers που ο builder ανέβαλε ως attended-preferred (padH14→12, fs16→15, radius14, micro `logInput`/`budgetInput`). raw `<TextInput>` sites **41** (ήταν 68), σε **7** screens (Assistant/Items/Login/Receipts/Search/Settings/Shopping). Υπόλοιπα dimensions **σταθερά**: `#000` literals **33** (23 `color:'#000'` text/pill + 9 `<ActivityIndicator color="#000">` + 1 `alpha('#000',0.6)` backdrop, ΟΛΑ onAccent → Button item), non-`#000` 6-digit hex **0**, 8-digit alpha hex **0**, chip-variant **22**, `card:` **6**, badge-variant **7** (per-screen, μη ενοποιημένα), `maxWidth` **2** (καμία στο content), `react-native-safe-area-context` **0** imports + **εκτός** `package.json`, light theme **0** (dark-only). **Foundation 3 DONE (theme tokens + alpha + touch targets) · Input primitive 🟡 IN PROGRESS (4/11 screens) · 5 TODO** (Button+Chip, Card+Badge+ListItem, Safe-area, Max-width, Light/dark). Κορυφαίο ενεργό = **συνέχεια Input migration** στα 7 εναπομείναντα screens (ιδανικά attended, no simulator) ή, αν μείνει attended-only, **Button + Chip primitives** (P2/M, σβήνει τα 33 `#000` onAccent). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (5η σάρωση):** επανα-επαλήθευσα κάθε dimension από τον κώδικα. **Δύο counts ανέβηκαν από το `2223203` (Tasks tags/priority)**, όχι νέο δομικό πρόβλημα: (α) `#000` literals **32 → 33** — ο νέος PRIORITY picker πρόσθεσε ένα `color:'#000'` (TasksScreen:125, onAccent στο ενεργό priority pill, δίπλα στο υπάρχον status-pill :117)· καλύπτεται από το Button+Chip item (`onAccent` migration). (β) input style entries **17 → 18** — το edit-modal του Tasks απέκτησε `modalInput` (TasksScreen:161, radius 10)· ήδη στο Input-item scope. Πλήρης ανάλυση των 33 `#000`: **32 `color:'#000'`** text/pill (ΟΛΑ onAccent) + **9 `<ActivityIndicator color="#000">`** props + **1 `alpha('#000',0.6)`** backdrop (κάποιες γραμμές μετρώνται σε 2 buckets· το grep `#000` δίνει 33 μοναδικές γραμμές). Σταθερά: non-`#000` 6-digit hex **0**, 8-digit alpha hex **0**, `card:` **5**, chip-variant **8**, badge-variant **2**, `maxWidth` **2** (καμία στο content: AssistantScreen:69 bubble + nav.tsx:87 drawer panel), raw 24×24 checkbox Views **0** (το `<Check>` primitive τα ενοποίησε), TextInput sites **68** (11 screens). `ui.tsx` εξάγει ΑΚΟΜΑ μόνο `Header/Centered/Spinner/ErrorText/Empty/Check` (+`CUR/money/shortDate`) — **μηδέν Input/Button/Chip/Card/Badge primitive**· `react-native-safe-area-context` **0** imports + **εκτός** `package.json`. **3 foundation DONE (theme tokens + alpha + touch targets), 6 TODO**· κορυφαίο = **Input primitive (P1/M)** — απόκλιση tokens επιβεβαιωμένη live: borderRadius **10** (7 entries) vs **12** (7 entries) vs **14** (AssistantScreen:77), paddingHorizontal **10/12/14**, paddingVertical **8/9/10/11**, fontSize **14** (logInput) / **15** (τα περισσότερα) / **16** (SearchScreen:85). mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (αργά νύχτα, 4η σάρωση):** ΝΕΟΣ mobile commit ενδιάμεσα — `86c6ada` (feat parity: Items AI specs/info, mobile half) άγγιξε `ItemsScreen.tsx` (+30) + `api.ts` (+9). Επιθεώρησα το diff: πρόσθεσε **νέο ghost-button** `aiBtn` (`borderRadius:12, paddingVertical:10, borderColor:C.accent`) + `aiBtnText` (`color:C.accent, fontSize:14`) — χρώματα σωστά από tokens (μηδέν νέο hardcoded hex, μηδέν νέο `#000`), αλλά **radius/padding ξανά magic numbers** (12/10) και είναι **ακόμα μια ad-hoc button-variant** που θα αντικαθιστούσε το `Button` primitive → το πρόσθεσα στο edit list του Button+Chip item. Live counts ΑΜΕΤΑΒΛΗΤΑ από τη νυχτερινή σάρωση: `#000` **32** (23 text-style + 8 `ActivityIndicator color="#000"` + 1 `alpha('#000',0.6)` backdrop, ΟΛΑ onAccent), 8-digit alpha hex **0**, input style entries **17**, `card:` **5**, chip-variant **27**, badge-variant **4**, save/add-btn refs **21**, `maxWidth` **2** (καμία στο content), TextInput sites **78**. `ui.tsx` εξάγει ΑΚΟΜΑ μόνο `Header/Centered/Spinner/ErrorText/Empty/Check` (+`CUR/money/shortDate`) — μηδέν Input/Button/Chip/Card/Badge primitive· `react-native-safe-area-context` **εκτός** `package.json` + **0** imports. **3 foundation DONE, 6 TODO**, κορυφαίο = **Input primitive (P1/M)**. mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (νύχτα, 3η σάρωση):** κατάσταση κώδικα ΑΜΕΤΑΒΛΗΤΗ (clean tree, μηδέν mobile commits από `245be25` → `git log 245be25..HEAD -- apps/mobile/src` κενό· τελευταίος mobile commit = `fbda2b3` receipts re-scan). **ΔΙΟΡΘΩΣΗ μέτρησης:** οι προηγούμενες σαρώσεις **υπομετρούσαν** τα `#000` literals (έλεγαν 23) γιατί μετρούσαν μόνο τα `color:'#000'` StyleSheet entries· ο πλήρης grep `#000\b` δίνει **32** — τα 23 text-style + **8 inline `<ActivityIndicator color="#000">`** props (Login:69, Items:102/360/425, Vouchers:182, Settings:172/355/483/563) + **1 `alpha('#000',0.6)`** backdrop (Settings:678). ΟΛΑ τα 32 είναι σημασιολογικά onAccent (κείμενο/spinner πάνω σε accent/cyan button) → λύνονται με το Button primitive + migration σε `C.onAccent`· ο spinner sub-category (`color="#000"`→`color={C.onAccent}`) είναι μικρή προσθήκη στο ίδιο item. Άλλες διορθώσεις: input style entries **15→17** (ο ευρύτερος grep έπιασε MoneyScreen:232/249, SettingsScreen:633, ItemsScreen:460/500 — line refs του Input item refreshed παρακάτω), `card:` entries **5** (ActivityScreen/ReportsScreen/SettingsScreen/StatementsScreen/VouchersScreen), chip-variant **22**, badge-variant **5**, save/add-btn refs **34**, `maxWidth` **2** (καμία στο content). `theme.ts` scales ΟΚ (`SPACE`/`RADIUS`/`SIZE`/`alpha`), `ui.tsx` εξάγει ΑΚΟΜΑ μόνο `Header/Centered/Spinner/ErrorText/Empty/Check` (+`CUR/money/shortDate`) — μηδέν Input/Button/Chip/Card/Badge· `react-native-safe-area-context` **0** imports + εκτός `package.json`. 3 foundation DONE, **6 TODO**, κορυφαίο = **Input primitive (P1/M)**. mobile `tsc --noEmit` **EXIT 0**. Read-only, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (απόγευμα):** δεύτερη σάρωση της ημέρας, κατάσταση ΑΜΕΤΑΒΛΗΤΗ από το πρωί. Live counts: `#000` literals **23** (10 screens: Items 5, Subscriptions/Money/Tasks 3, Assistant/Shopping/Vouchers 2, Settings/Login/Receipts 1), 8-digit alpha hex **0** (grep καθαρό), input style entries **15**, `card:` entries **6**, chip-variant entries **54**. Το `ui.tsx` εξάγει ΑΚΟΜΑ μόνο `Header/Centered/Spinner/ErrorText/Empty/Check` (+`CUR/money/shortDate`) — μηδέν Input/Button/Chip/Card/Badge primitive· `react-native-safe-area-context` **0** imports + εκτός `package.json`. Foundation DONE μένουν 3 (theme tokens + alpha + touch targets), **6 TODO**. Κορυφαίο TODO = **Input primitive (P1/M)**: επιβεβαιωμένη απόκλιση tokens live, borderRadius **10/12/14** (SettingsScreen:615=10, ItemsScreen:440=12, AssistantScreen:77=14), paddingHorizontal **10/12/14** (ItemsScreen:500=10, ItemsScreen:440=12, SearchScreen:85=14), paddingVertical **8/9/10/11** (logInput=8, budgetInput=9, minput=10, input=11), fontSize **14/15/16** (logInput=14, input=15, SearchScreen:85=16). tsc `--noEmit` EXIT 0. Read-only audit, μηδέν app-code άλλαξε.
>
> **ui-auditor re-audit 2026-07-01 (πρωί):** σάρωση επιβεβαίωσε ότι ΤΙΠΟΤΑ νέο δεν υλοποιήθηκε από τον builder ενδιάμεσα στα 6 TODO items — το `ui.tsx` εξάγει ΑΚΟΜΑ μόνο `Header`/`Centered`/`Spinner`/`ErrorText`/`Empty`/`Check` (+`CUR`/`money`/`shortDate`), μηδέν Input/Button/Chip/Card/Badge primitive· `react-native-safe-area-context` ΑΚΟΜΑ εκτός `package.json`. **Μεταβολή από προηγούμενη σάρωση: τα `#000` literals έπεσαν 31→23** (το `<Check>` primitive ενοποίησε άλλα 8 onAccent literals). Τα 3 foundation items μένουν τα μόνα DONE (theme tokens + alpha + touch targets). Ο ενεργός κορυφαίος TODO παραμένει **Input primitive (P1/M)**: μετρήθηκαν live **15** input style entries / **11** screens με αποκλίνοντα tokens — borderRadius **10/12/14** (SettingsScreen:615=10, ItemsScreen:440=12, AssistantScreen:77=14), paddingHorizontal **12/14**, paddingVertical **10/11**, fontSize **15/16** (SearchScreen:85=16 vs τα υπόλοιπα 15). tsc `--noEmit` EXIT 0 (read-only). Read-only audit, μηδέν app-code άλλαξε.
>
> **Builder 2026-06-30 (cont. — Touch targets):** «Touch targets ≥44pt» **DONE** → shared `<Check>` primitive στο `ui.tsx` ενοποίησε τα 6 διπλά checkbox style-triplets + TasksScreen interactive box hitSlop 8→10 (44pt) + 5 `#000` literals → `C.onAccent`. Queue UI Debt: τα DONE πλέον **3** (theme tokens + alpha helper + touch targets), **6 TODO**. Ο ενεργός κορυφαίος TODO παραμένει **Input primitive (P1/M)** — μεγάλη μετανάστευση ~50 TextInput sites/11 screens, καλύτερη για attended run (no simulator → οπτικό verify ρίσκο)· εναλλακτικά Button+Chip primitives (P2/M, χτίζει πάνω στο onAccent). `#000` literals τώρα ~31 (ήταν 36).
>
> Mobile UI consistency audit (re-verified 2026-06-30 βραδινό — ui-auditor, 4η σάρωση ημέρας). Πόσο «universal» είναι το mobile UI σε σχέση με το web design system. Ranked: μικρά + P1 πρώτα. Το shared-theme item είναι foundation, άλλα εξαρτώνται από αυτό. Ο builder παίρνει το πρώτο TODO. Read-only audit, μηδέν app-code άλλαξε. **Re-audit: τα 2 foundation items παραμένουν τα ΜΟΝΑ DONE (commit `362efc5`): το `theme.ts` εξάγει `SPACE`/`RADIUS`/`SIZE` scales + `surface3`/`orange`/`onAccent` + `alpha(hex,n)` helper, 8-digit alpha hex = 0 (grep καθαρό). Κανένα από τα 7 εναπομείναντα items δεν υλοποιήθηκε από τον builder ενδιάμεσα: το `ui.tsx` έχει ΑΚΟΜΑ μόνο `Header`/`Centered`/`Spinner`/`ErrorText`/`Empty` (μηδέν Input/Button/Chip/Card/Badge primitive — επιβεβαιωμένο από τα 5 exported fns), `react-native-safe-area-context` ΑΚΟΜΑ εκτός `package.json`. Ο ενεργός κορυφαίος TODO = Input primitive (P1/M). Τα νούμερα ανανεώθηκαν.**
>
> Σύνοψη ευρημάτων (refreshed 2026-07-01 πρωί): **23** hardcoded hex εκτός `theme.ts`, **ΟΛΑ `#000`** (3-digit text-on-accent literals) σε 10 screens (Items 5, Subscriptions/Money/Tasks 3, Assistant/Shopping/Vouchers 2, Receipts/Settings/Login 1) — μηδέν non-`#000` 6-digit literal (grep καθαρό), μηδέν 8-digit alpha hex. Card-style entries **6**, chip-variant entries **27**, badge-variant entries **6** (local per-screen, μη ενοποιημένα). State handling ΗΔΗ συνεπές μέσω `ui.tsx` (Spinner 16 / Empty 10 / ErrorText 12 χρήσεις). 2 `maxWidth` (καμία στο content). [Ιστορικό: ΗΤΑΝ **36** literals, (Money/Assistant/Tasks/Subscriptions/Shopping/Receipts/Vouchers/Settings/Items/Login) — δεν έχει μείνει κανένα non-`#000` solid literal ούτε άλλο tinted hex· αυτά λύνονται με μετανάστευση σε `C.onAccent` (καλύπτεται από το Button primitive item + το onAccent follow-up). **0** alpha-tinted 8-digit hex (ΗΤΑΝ 8 → όλα `alpha()`). Το `theme.ts` έχει scales (✅) αλλά μηδέν reusable Input/Button/Chip/Card/Badge primitives στο `ui.tsx`: το input style ξαναγράφεται σε **15** entries (10 `input:` + 5 `minput`/`einput` variants) / **11** screens **με αποκλίνοντα tokens** (borderRadius 12 vs 10 vs 14, paddingVertical 11 vs 10, fontSize 15 vs 16 — επιβεβαιωμένο live ItemsScreen:426 vs :446, SearchScreen:85, AssistantScreen:77), `saveText`/`saveBtn`/`addBtn` **12×**, local `card`/`badge`/`chip` **11×**. Μηδέν light theme / theme context (το web έχει πλήρες light mode στο globals.css:37-50). `react-native-safe-area-context` **δεν είναι ακόμα dependency** → plain `SafeAreaView` (App.tsx:74) χωρίς bottom-inset (bottom-sheets κάτω από το home indicator). Μόνο **2** `maxWidth` usages σε όλο το app (καμία στο content → edge-to-edge σε tablet/landscape). ~7 touch targets <44pt (24×24 checkboxes ×6 + 30×30 lineDel + 36×36 rm + 40×36 backBtn) — τα περισσότερα έχουν ήδη hitSlop, το item έκλεισε DONE.] Re-confirmed 2026-07-01: tsc `--noEmit` EXIT 0.

### Theme tokens: spacing + radius + typography scale + missing colors
- Priority: P1
- Size: S
- Web ref: `@theme` tokens (font-display/body/mono, surface-3, orange) (file: apps/web/src/app/globals.css:3-23)
- Mobile files: apps/mobile/src/theme.ts (επέκταση του `C` + νέα exports `SPACE`, `RADIUS`, `FONT`/`SIZE`)
- Depends on: none
- Acceptance:
  - Το `theme.ts` εξάγει `surface3` (#242424) + `orange` (#ffa502) ώστε να καλύπτει όλα τα web color tokens (globals.css:8-22)
  - Νέα scale exports: spacing (π.χ. 4/8/12/16/24), radius (10/12/14 — τα 3 που ήδη χρησιμοποιούνται ασυνεπώς), font sizes (13/14/15/16/19) με σταθερά ονόματα
  - Νέο `onAccent` token (#000, το χρώμα κειμένου πάνω σε accent/cyan buttons) ώστε να μη γράφεται `'#000'` inline
- Status: DONE (2026-06-30) — `theme.ts` εξάγει `surface3`/`orange`/`onAccent` + `SPACE`/`RADIUS`/`SIZE` scales· τα 5 `#000` onAccent literals του SettingsScreen → `C.onAccent` + το `#f5f5f5` re-def → `C.text`. ΣΗΜ: τα υπόλοιπα ~26 inline `#000` onAccent σε άλλα screens μένουν (μετανάστευση σε `C.onAccent` ανά screen, low-priority follow-up).

### Alpha-tint helper για theme-derived backgrounds/borders
- Priority: P1
- Size: S
- Web ref: `color-mix(in srgb, var(--color-accent) 16%, transparent)` glow helpers (file: apps/web/src/app/globals.css:87-89)
- Mobile files: apps/mobile/src/theme.ts (νέο `alpha(hex, n)` helper), μετά αντικατάσταση των hardcoded alpha hex
- Depends on: Theme tokens: spacing + radius + typography scale + missing colors
- Acceptance:
  - Νέο `alpha()` (ή σταθερά tinted tokens) αντικαθιστά τα 11 hardcoded alpha hex: ActivityScreen.tsx:293,302,311,322 και SettingsScreen.tsx:644,653,664
  - Μηδέν `'#00ff88XX'` / `'#00d4ffXX'` / `'#ff4757XX'` / `'#ffd93dXX'` literals στα screens (grep καθαρό)
- Status: DONE (2026-06-30) — νέο `alpha(hex, n)` (0..1 → rgba) στο `theme.ts`· αντικατέστησε και τα 12 alpha-tinted literals (ActivityScreen ×7 + SettingsScreen ×5, incl. `#000000aa` backdrop). grep για 8-digit hex σε `src/**.tsx` → **NONE**.

### Input primitive (centralize 15 duplicate input styles)
- Priority: P1
- Size: M
- Web ref: shared input στυλ μέσω Tailwind tokens (file: apps/web/src/app/globals.css:8-22)
- Mobile files: apps/mobile/src/ui.tsx (νέο `<Input>` + `<TextArea>`), edits (**18 entries / 11 screens**, refs 2026-07-01 5η σάρωση) σε AssistantScreen.tsx:77, MoneyScreen.tsx:232,249, SubscriptionsScreen.tsx:147,163, TasksScreen.tsx:145,161 (input + νέο modalInput), ItemsScreen.tsx:465,485,528, ShoppingScreen.tsx:157, VouchersScreen.tsx:196,215, SettingsScreen.tsx:615,633, ReceiptsScreen.tsx:268, SearchScreen.tsx:85, LoginScreen.tsx:82
- Depends on: Theme tokens: spacing + radius + typography scale + missing colors
- Acceptance:
  - Ένα `Input` component εξάγεται από `ui.tsx`· τα παραπάνω screens το χρησιμοποιούν αντί για local `input`/`minput`/`einput` StyleSheet entry
  - Καμία απόκλιση borderRadius (τώρα 10 vs 12) ή padding (τώρα 12 vs 14) μεταξύ screens — όλα από το ένα primitive
- Status: 🟡 IN PROGRESS (**10/11 screens**· 2026-07-03 commit `53b861a` πρόσθεσε Login/Search/Assistant· `ad226a7` έκλεισε το ItemsScreen `logInput`· απομένει **ΜΟΝΟ** ReceiptsScreen `einput`/`cellInput` [9 raw `<TextInput>`]. **ΕΝΗΜΕΡΩΣΗ 2026-07-06 (46η σάρωση): το ReceiptsScreen ΕΓΙΝΕ COMMIT (`6835e6a`), working tree καθαρό → ΔΕΝ είναι πια WIP-blocked.** Ο builder μπορεί να μεταναστεύσει τα 5-6 top-level `einput` (store/date/pay/total/notes) σε `<Input>` [P3/S, tsc-verifiable]· τα 3 compact `cellInput` (qty/net/vat, radius10/pad12-10/fs15 tight grid) = attended-preferred visual delta) — **primitives χτίστηκαν** στο `ui.tsx`: `<Input variant="surface"|"modal">` + `<TextArea>` (base tokens `RADIUS.md/sm` + `SPACE.md` + `SIZE.md`, default `placeholderTextColor`, `style` passthrough για flex/minHeight). Μετανάστευσα **6 screens**: τα 4 standard record-form (**MoneyScreen** 2 surface + 9 modal + 1 TextArea, **SubscriptionsScreen** 2+3, **VouchersScreen** 2+6+1 TextArea, **TasksScreen** 1+2) + το byte-identical subset **ItemsScreen** (1 surface + 4 modal + 1 TextArea specs) + **SettingsScreen** (11 modal + 1 TextArea notes)· έσβησα τα τοπικά `input`/`minput`/`modalInput` StyleSheet entries. mobile `tsc --noEmit` **EXIT 0**. **Απομένουν 5 outlier screens** (token-drift = οπτική αλλαγή → attended-preferred, no-simulator verify): **ShoppingScreen** (input padH14), **ReceiptsScreen** (`einput` bg surface όχι surface2), **SearchScreen** (padH14/fs16), **AssistantScreen** (chat composer radius14/maxHeight120), **LoginScreen** (distinct block)· + τα micro `logInput` (Items, padH10/padV8/fs14) / `budgetInput` (Settings, padV9) που σκόπιμα έμειναν raw.

### Scrim / backdrop token (ενοποίηση 9 modal overlays)
- Priority: P2
- Size: S
- Web ref: modal overlay scrim (file: apps/web/src/app design-system, `bg-black/60` overlays)
- Mobile files: apps/mobile/src/theme.ts (νέο `scrim` token, π.χ. `alpha('#000', 0.6)`), edits στα 9 backdrop sites: MoneyScreen.tsx:244, ItemsScreen.tsx:480, SubscriptionsScreen.tsx:158, ShoppingScreen.tsx:172, TasksScreen.tsx:156, StatementsScreen.tsx:153, VouchersScreen.tsx:210, ReceiptsScreen.tsx:256, SettingsScreen.tsx:677
- Depends on: none
- Acceptance:
  - Νέο `scrim` (ή `C.scrim`) token στο `theme.ts`· τα 9 modal backdrops το χρησιμοποιούν αντί για inline `rgba(0,0,0,0.6)` / `rgba(0,0,0,0.7)` / `alpha('#000',0.67)`
  - Μηδέν `rgba(0, 0, 0, …)` ή `#000` literal στα screens (grep καθαρό — σβήνει και τον τελευταίο hardcoded hex @ SettingsScreen:677)
  - Ενιαία διαφάνεια σε όλα τα modals (τώρα 0.6 vs 0.67 vs 0.7 drift)· tsc καθαρό (mobile)
- Status: DONE (2026-07-01) — `scrim = 'rgba(0, 0, 0, 0.6)'` προστέθηκε στο `theme.ts`· και τα **10** backdrop sites (9 screens + `nav.tsx:86` drawer scrim, που είχε ξεφύγει από την αρχική λίστα) το χρησιμοποιούν πλέον. Grep καθαρό: μηδέν `rgba(0,0,0,…)` / `alpha('#000',…)` inline στα screens (μόνο σχόλιο στο `theme.ts`). Receipts (0.7→0.6) + Settings (0.67→0.6) ενοποιήθηκαν στο 0.6 (dominant, 7/9)· αμελητέα οπτική μεταβολή. mobile `tsc --noEmit` EXIT 0.

### Button + Chip primitives (add/save buttons + filter/status chips)
- Priority: P2
- Size: M
- Web ref: pill buttons + filter pills (file: apps/web/src/app/globals.css:120-132, design-system pills)
- Mobile files: apps/mobile/src/ui.tsx (νέα `<Button>`, `<Chip>`), edits σε ItemsScreen.tsx:429,436,453,456 + νέο `aiBtn`/`aiBtnText` (από 86c6ada, ghost-variant), SubscriptionsScreen.tsx:149,168, MoneyScreen.tsx:182,200, TasksScreen.tsx:99,119,142, VouchersScreen.tsx:198,222,226, ShoppingScreen.tsx:158,179, ReceiptsScreen.tsx:260,266, SettingsScreen.tsx:621,643, AssistantScreen.tsx:79, LoginScreen.tsx:89
- Depends on: Theme tokens: spacing + radius + typography scale + missing colors
- Acceptance:
  - Ένα `Button` (variant accent/cyan/ghost) με text χρώμα από `onAccent` token· μηδέν inline `color: '#000'` σε button text styles
  - Ένα `Chip` με on/off state αντικαθιστά τα per-screen `chip`/`chipOn`/`cChip`/`sChip` patterns
- Status: IN PROGRESS 🟡 (2026-07-01):
  - onAccent `#000` literal migration → `C.onAccent` DONE (32 sites/10 screens)
  - **`<Button>` primitive DONE** (builder, 2026-07-01): νέο `Button` στο `ui.tsx` (`label`/`onPress`/`disabled`/`busy`/`style`/`textStyle`, accent pill· `disabled||busy` → 0.4 dim + block, `busy` → spinner) ενοποιεί το byte-identical `save`+`saveText`+`dim` triplet. Migrated **5 screens** (Money ×2, Receipts, Subscriptions, Tasks, Vouchers ×2) → **7 save/scan sites**· τα per-screen `save:`/`saveText:` StyleSheet entries αφαιρέθηκαν (byte-identical output: `RADIUS.md`=12, `SPACE.md`=12, padH22, `SIZE.md`=15, weight700, `onAccent`). mobile `tsc --noEmit` EXIT 0.
  - **`<IconButton>` primitive DONE** (builder `929ca4d`, επιβεβαιωμένο 16η σάρωση): νέο `IconButton` στο `ui.tsx` (`glyph`/`onPress`/`disabled`/`busy`/`busyColor`/`style`/`textStyle`, 46-wide accent pill· `disabled||busy` → dim/block, `busy` → spinner). Adopted **7 call sites**: Subscriptions/Money/Tasks (add) + Tasks (addStep) + Items (`✦`/`＋` με cyan `importBtn` variant) + Vouchers + Shopping. Grep: μηδέν `addBtn` accent-icon StyleSheet entry (τα per-screen `addBtn`/`addBtnText` σβηστά). mobile `tsc --noEmit` EXIT 0.
  - **Button-family FINISH DONE** (builder `3a272c1`, επιβεβαιωμένο 18η σάρωση): ShoppingScreen `addBtnWide`/`addBtnText2` (wide «Add to list» accent text button) + ItemsScreen `save` accent-pill outlier → `<Button>`. Grep: `addBtnWide`/`addBtnText2` **GONE**, `addBtn` refs **0** στα screens (μόνο σχόλιο στο `ui.tsx`), `<Button>` sites **9** (5→7→9). mobile `tsc --noEmit` EXIT 0. → **Ο πυρήνας του `<Button>` (accent pills, save/scan) ΟΛΟΚΛΗΡΩΘΗΚΕ.**
  - **`<Chip>` primitive DONE + COMMITTED** (builder `681098f`, επιβεβαιωμένο 35η σάρωση 2026-07-02): νέο **`<Chip label on onPress style? textStyle?>`** στο `ui.tsx:229` (base padH12/padV6/radius10/fs12, surface2/1px-border → accent-fill/onAccent-text όταν `on`, ίδιο override idiom με Button/IconButton/Badge). Υιοθετήθηκε σε **5 screens** (Items `filterChip`/`statusChip`, Subscriptions `cycleChip`, Settings `optChip` ×3 call sites [currency/kind/type], Reports `rangeChip`, Tasks `tagChip` [zero-override, ταιριάζει το base]) — τα per-screen toggle-chip StyleSheet triplets (`chip`/`chipOn`/`chipText`/`chipTextOn` κ.ά.) σβήστηκαν, κάθε call site κρατά pixel-identical rendering μέσω minimal `style`/`textStyle` override. Grep 35η σάρωση: μηδέν toggle-chip cluster εκτός Chip· τα εναπομείναντα `addChip` (Settings, cyan ghost add-button) + `valChip` (Settings, removable ✕ value chip) είναι **σκόπιμα ΟΧΙ toggle chips** (διαφορετική semantics) → δεν είναι chip debt. mobile `tsc --noEmit` EXIT 0. → **Το `<Chip>` holdout ΕΚΛΕΙΣΕ.**
  - **Απομένει (μόνο attended-preferred):** (α) SettingsScreen `saveText` weight800 outlier (μοναδικό εναπομείναν `saveText:` entry· `<Button textStyle={{fontWeight:'800'}}>`)· (β) ghost cyan/accent-border variants → `<Button variant="ghost" tint?>`. **ΤΕΛΕΥΤΑΙΟ button-family holdout.** **ΕΝΗΜΕΡΩΣΗ 37η σάρωση 2026-07-02:** το commit `2201932` πρόσθεσε **ακόμα ένα** cyan-ghost site (`rescanBtn` MoneyScreen:273, radius10) → το footprint μεγάλωσε. Fresh grep όλων των border-ghost buttons: **~13 sites / 8 screens**: cyan-tint → Subscriptions `aiBtn`:145, Money `scanBtn`:259 + `rescanBtn`:273, Items `convertBtn`:481, Settings `testBtn`:627 + `addCardBtn`:646, Receipts `rescanBtn`:264 + `sumBtn`:271, Vouchers `aiBtn`:192 + `photoBtn`:208· accent-tint → Items `aiBtn`:479, Receipts `libBtn`:286· neutral `C.border` → Tasks `statusBtn`:241, Shopping `scanBtn`:155, Settings `cancelBtn`:673· plus fill outlier Items `importBtn`:457 (cyan bg). Drift: radius 8/10/11/12, padV 7-13, border cyan/accent/border. Το πυρήνα (τα ~7 core AI/scan/rescan cyan-ghosts) καλύπτει ένα `<Button variant="ghost" tint?>` με per-site override για pixel-identical. Unattended-safe (tsc-verifiable, style-override idiom). **ΕΝΗΜΕΡΩΣΗ 34η σάρωση 2026-07-02:** το chip footprint μεγάλωσε σε **7 StyleSheet clusters / 5 screens** (κάθε νέα οθόνη με chips προσθέτει ακόμα ένα inline cluster): **Items** `chip`/`chipOn`/`chipText`/`chipTextOn` (padH14/padV7/radius10) + `sChip*` (padH11/padV6/radius9)· **Settings** `chip*` (padH16/padV8/radius10) + `addChip`/`addChipText` (cyan-border ghost) + `valChip*` (with-× removable)· **Subscriptions** `cChip*` (padH11/padV6/radius9)· **Reports** `rangeChip`/`rangeChipOn`/`rangeText`/`rangeTextOn` (padH12/padV5/radius9· `bba44ff`)· **Tasks** `tagChip`/`tagChipOn`/`tagChipText`/`tagChipTextOn` (padH12/padV6/radius10· `268efb4`). Κοινή βάση σε ΟΛΑ: `backgroundColor:C.surface2`, `borderWidth:1`+`borderColor:C.border`, on-state `backgroundColor:C.accent`+`borderColor:C.accent`, text `C.dim`→on `C.onAccent`. Το padding/radius/fontSize ποικίλλει ελαφρά ανά cluster (token-drift). Πρόταση: ένα `<Chip label on onPress size?>` primitive στο `ui.tsx` (default padH12/padV6/radius9/fs12) καλύπτει τα 5 core filter/range/tag clusters· τα Settings `addChip` (ghost) + `valChip` (with-×) μένουν variants ή raw. Όλα token-drift = οπτική αλλαγή χωρίς simulator → attended-preferred.

### Card + Badge + ListItem primitives
- Priority: P2
- Size: M
- Web ref: card (border-radius 14, hover lift) + status badges (file: apps/web/src/app/globals.css, CLAUDE.md design-system)
- Mobile files: apps/mobile/src/ui.tsx (νέα `<Card>`, `<Badge>`, `<ListItem>`), edits στα screens που ορίζουν local `card`/`badge`/`row` (ItemsScreen, MoneyScreen, ReceiptsScreen, SettingsScreen, ActivityScreen, StatementsScreen κ.ά.)
- Depends on: Theme tokens: spacing + radius + typography scale + missing colors
- Acceptance:
  - `Card`/`Badge`/`ListItem` εξάγονται από `ui.tsx`· τα 5+ local `card:` και 2+ `badge:` StyleSheet entries αντικαθίστανται
  - Σταθερό border-radius/padding/border σε όλα τα cards (τώρα ποικίλλει ανά screen)
- Status: ✅ DONE (2026-07-01, committed — Card + Badge + ListItem όλα εξήχθησαν) — νέο `<Card>` στο `ui.tsx` (`children`/`onPress`/`onLongPress`/`style`· renders `Pressable` όταν υπάρχει handler, αλλιώς `View`· base `surface`/border/`RADIUS.lg`/padding14/marginBottom10 == το byte-identical `card:` triplet). Μετανάστευσα **3 screens / 6 sites**: **ActivityScreen** (4: alerts Pressable + trash View + jobs View + history Pressable, `style` passthrough για `unreadCard`), **StatementsScreen** (1 Pressable), **VouchersScreen** (1 Pressable + `faded` passthrough)· έσβησα τα τοπικά `card:` StyleSheet entries. Έμειναν σκόπιμα raw οι 2 outliers (**οπτική** διαφορά, attended-preferred): **ReportsScreen** (`flex:1`/`borderRadius:16`, καμία marginBottom) + **SettingsScreen** (μόνο `paddingHorizontal:14`, section container). mobile `tsc --noEmit` **EXIT 0**. **`<Badge>` DONE** (2026-07-01 builder, δεύτερο pass): νέο `<Badge label color style? textStyle?>` στο `ui.tsx` (coloured 1px-border pill + matching text, base tokens == TasksScreen `chip`: `borderRadius:6`/padH6/padV2/fs9/w700/ls0.5). Μετανάστευσα **byte-identical** τα 3 display-only status/type badges: **TasksScreen** (status chip, base ακριβώς), **SearchScreen** (type chip, `style` override για `paddingVertical:3` + separate border-fallback `C.border`), **ActivityScreen** (job-status badge, `style` `paddingHorizontal:7`+`marginLeft:'auto'` + `textStyle` `fontSize:10`)· έσβησα τα 3 local `chip`/`badge` + text StyleSheet pairs. Το per-status/type χρώμα διατηρείται μέσω του `color` prop (border+text). **Δεν** μεταναστεύθηκε το StatementsScreen inline installment badge (διαφορετικό pattern: text-only cyan count, όχι status pill) → μένει raw. mobile `tsc --noEmit` **EXIT 0**. **Απομένει: `<ListItem>` (unattended-safe, top build item — 24η σάρωση):** 5 **BYTE-IDENTICAL** `row:` entries (`flexDirection:'row', alignItems:'center', gap:12, backgroundColor:C.surface, borderWidth:1, borderColor:C.border, borderRadius:14, padding:14, marginBottom:10`) σε **SubscriptionsScreen:151, CalendarScreen:93, MoneyScreen:238, TasksScreen:191, ItemsScreen:469** → νέο `<ListItem>` στο `ui.tsx` (= `<Card>` base + `flexDirection:'row'`+`alignItems:'center'`+`gap:12` overlay, με `onPress`/`onLongPress`/`style` passthrough)· σβήσε τα 5 τοπικά `row:` StyleSheet entries. Outliers που ΜΕΝΟΥΝ raw: SearchScreen:89 (radius12/pad12/mb8, διαφορετικά tokens), SettingsScreen:609 (space-between/hairline, όχι card-row). Acceptance: μηδέν byte-identical `row:` duplicate στα 5 screens· card-row layout από ένα primitive· mobile `tsc --noEmit` EXIT 0· μηδέν οπτική αλλαγή (byte-identical output). **ΕΝΗΜΕΡΩΣΗ 2026-07-01 (25η σάρωση): `<ListItem>` ΧΤΙΣΤΗΚΕ + υιοθετήθηκε στο working tree ως uncommitted WIP του Αχιλλέα** (`export function ListItem` @ `ui.tsx:166`, χρήση σε 6 screens, τα 5 byte-identical `row:` entries σβήστηκαν, tsc EXIT 0). **ΜΗΝ το ξανακάνεις** — αναμονή commit από τον χρήστη· acceptance ικανοποιείται στο tree. **ΕΝΗΜΕΡΩΣΗ 2026-07-01 (26η σάρωση): `<ListItem>` ΕΓΙΝΕ COMMIT** `0f69116` (`refactor(mobile): extract shared <ListItem> row primitive`)· working tree καθαρό, acceptance ικανοποιείται στο committed HEAD. → **Ολόκληρο το «Card + Badge + ListItem» set ΕΚΛΕΙΣΕ (Card + Badge + ListItem, όλα DONE/committed).**

### Touch targets ≥44pt
- Priority: P2
- Size: S
- Web ref: mobile-first / FAB conventions (file: CLAUDE.md UI conventions)
- Mobile files: checkbox toggles 24×24 σε SubscriptionsScreen.tsx:170, TasksScreen.tsx:122, SettingsScreen.tsx:625, ShoppingScreen.tsx:165, VouchersScreen.tsx:220, ReceiptsScreen.tsx:258· menuBtn nav.tsx:65 (38×36)· backBtn ui.tsx:35 (40×36)· lineDel ReceiptsScreen.tsx:250 (30×30)
- Depends on: none
- Acceptance:
  - Όλα τα tap targets έχουν effective hit area ≥44×44 (μέσω μεγαλύτερου style ή `hitSlop`)
  - Τα 24×24 checkboxes ταυτίζονται σε ένα κοινό `Checkbox`/`Toggle` με σωστό hit area
- Status: DONE (2026-06-30) — νέο shared `<Check checked />` στο `ui.tsx` (24×24 box + ✓, `C.onAccent` mark) αντικαθιστά τα 6 διπλά `tbox/check + tboxOn/checkOn + tmark/mark/checkMark` triplets σε Tasks/Shopping/Subscriptions/Settings/Vouchers/Receipts (style triplets αφαιρέθηκαν). Ο μόνος interactive-box tap target (TasksScreen:75 Pressable-wrapped) → `hitSlop` 8→**10** (effective 44×44). Side-benefit: 5 hardcoded `#000` onAccent literals έφυγαν (ενοποιήθηκαν σε `C.onAccent` μέσα στο Check). Τα 5 View-based checkboxes είναι display-only μέσα σε μεγάλα pressable rows (≥44, ήδη OK). mobile `tsc --noEmit` EXIT 0. (menuBtn/backBtn/lineDel έχουν ήδη επαρκές hitSlop 12/12/8 → effective ≥44.)

### Touch targets — Settings `rm` ✕ + `swatch` missing hitSlop
- Priority: P2
- Size: S
- Web ref: mobile-first / ≥44pt tap conventions (file: CLAUDE.md UI conventions)
- Mobile files: apps/mobile/src/screens/SettingsScreen.tsx (`rm` buttons γρ.140/246/412 = 36×36· `swatch` γρ.336 = 32×32)
- Depends on: none
- Acceptance:
  - Τα 3 `rm` (✕ remove budget/list/store) Pressables παίρνουν `hitSlop={10}` (ή μεγαλύτερο container) → effective ≥44×44 (τώρα 36×36 raw, χωρίς hitSlop)
  - Το color `swatch` (card-color picker) παίρνει `hitSlop` ή μεγαλύτερο tap area → ≥44×44 (τώρα 32×32)
  - grep: κανένα 36×36/32×32 Pressable στο SettingsScreen χωρίς hitSlop
- Status: DONE (2026-07-06, pharos-daily-dev) — τα WIP screens του Αχιλλέα έγιναν commit (`6835e6a`), ξεμπλόκαρε. Τα 3 `rm` ✕ Pressables (36×36, γρ.140/246/412) πήραν `hitSlop={8}` → 52×52 effective· το `swatch` (32×32, γρ.336) πήρε `hitSlop={6}` → 44×44 (μικρό hitSlop ώστε να μη γίνεται overlap στο tight `chipsRow` gap:8· benign overlap σε color-picker αποδεκτό). grep: μηδέν 36×36/32×32 tap χωρίς hitSlop. mobile tsc EXIT 0.

### Modal container primitive (`<ModalSheet>`) + `RADIUS.xl` token
- Priority: P2
- Size: M
- Web ref: shared surface-card container pattern (analogue των web modal/dialog wrappers· mirror του ήδη-DONE `<Card>`/`<ListItem>` consolidation)
- Mobile files: apps/mobile/src/ui.tsx (νέο `<ModalSheet>` + προαιρετικά `<ModalBackdrop>`), apps/mobile/src/theme.ts (πρόσθεσε `RADIUS.xl = 18`), apps/mobile/src/screens/SubscriptionsScreen.tsx:154, apps/mobile/src/screens/TasksScreen.tsx:237, apps/mobile/src/screens/MoneyScreen.tsx:294, apps/mobile/src/screens/ItemsScreen.tsx:468, apps/mobile/src/screens/VouchersScreen.tsx:203. (ShoppingScreen.tsx:165 + ReceiptsScreen.tsx:257 = WIP-blocked → attended-preferred, ΜΗΝ τα αγγίξεις τώρα.)
- Depends on: none
- Acceptance:
  - Νέο `RADIUS.xl = 18` στο `theme.ts`· κανένα `borderRadius: 18` magic literal στα 5 non-WIP modal screens (χρησιμοποιούν `RADIUS.xl` ή το primitive).
  - Νέο `<ModalSheet>` στο `ui.tsx` που ενσωματώνει το byte-identical panel (`surface`/border/`RADIUS.xl`/padding20· optional `maxHeight` prop για Money/Items `'88%'`)· τα 5 τοπικά `modal:` StyleSheet entries σβήνονται.
  - Μηδέν οπτική αλλαγή (byte-identical output στα 5 screens)· mobile `npx tsc --noEmit` EXIT 0.
- Status: DONE (commit `c9fef1b`, 2026-07-06)· `<ModalSheet>` primitive + `RADIUS.xl=18` εξήχθησαν, 7 modals σε 5 screens μεταναστεύτηκαν. **ΕΝΗΜΕΡΩΣΗ 2026-07-06 (47η σάρωση): 1 holdout ΞΕΜΠΛΟΚΑΡΕ**, `ShoppingScreen.tsx:112` (raw `<Modal animationType="fade">` + `<View modalWrap>/<View modal borderRadius:18>`, χειροκίνητο αντίγραφο του pattern· γρ.165 = **το μοναδικό εναπομείναν `borderRadius:18` magic literal** στον κώδικα) έγινε πλέον commit (working tree καθαρό), οπότε ΔΕΝ είναι πια WIP-blocked. Ο builder μπορεί να το μεταναστεύσει σε `<ModalSheet>` [P3/S, tsc-verifiable, mobile-only]· κερδίζει tap-to-close backdrop + σβήνει το τελευταίο magic literal. (Οι raw `<Modal animationType="slide">` σε Statements/Settings/Receipts είναι bottom-sheet/full-panel, ΔΙΑΦΟΡΕΤΙΚΟ pattern → δεν είναι holdout του centered-fade ModalSheet.)

### Safe-area insets (bottom + notch) via react-native-safe-area-context
- Priority: P2
- Size: M
- Web ref: iOS-safe ambient/layout handling (file: apps/web/src/app/globals.css:69-84 comment)
- Mobile files: apps/mobile/App.tsx:74 (αντικατάσταση του plain `SafeAreaView`), apps/mobile/src/nav.tsx (Drawer paddingTop:60 magic number), bottom-sheet modals (π.χ. SettingsScreen.tsx:681 `modalBackdrop` flex-end)
- Depends on: none
- Acceptance:
  - `SafeAreaProvider` + `useSafeAreaInsets` αντί για το RN `SafeAreaView` (που δίνει μόνο top, μηδέν bottom, και Android-manual paddingTop)
  - Bottom-sheet content + κάτω κουμπιά δεν κάθονται κάτω από το home indicator (bottom inset εφαρμοσμένο)
- Status: TODO

### Max content width για tablet / landscape
- Priority: P3
- Size: S
- Web ref: centered max-width layouts (file: apps/web/src/app/settings, max-w 1080)
- Mobile files: apps/mobile/src/ui.tsx (νέο `<Screen>`/content wrapper με maxWidth + center), εφαρμογή στα list screens
- Depends on: none
- Acceptance:
  - Το main content έχει maxWidth (π.χ. 640) και κεντράρεται σε wide viewport· δεν τεντώνεται edge-to-edge σε tablet/landscape (τώρα μόνο 2 maxWidth usages σε όλο το app, καμία στο content)
- Status: ✅ DONE + COMMITTED `28c943c` (επιβεβαιωμένο 30ή σάρωση 2026-07-02· `git log -- apps/mobile/src` τελευταίο = `28c943c`, working tree clean, acceptance ικανοποιείται στο HEAD) — νέο `contentWidth: ViewStyle` const στο `ui.tsx` (`{ width:'100%', maxWidth:640, alignSelf:'center' }`, JSDoc): `width:'100%'` γεμίζει το phone, `maxWidth` καπάρει το tablet, `alignSelf:'center'` κεντράρει το capped column → **no-op σε phone** (ήδη κάτω από 640), centering μόνο σε tablet/landscape. Εφαρμόστηκε στο main scroll/list content-container **14 screens**: named-style spread (ActivityScreen `s.list` [τα 4 tab FlatLists], HomeScreen `s.content`) + inline array `[{…}, contentWidth]` (Reports/Settings root ScrollView + Calendar/Items/Money/Receipts/Search/Shopping/Statements/Subscriptions/Tasks/Vouchers κύριο FlatList). Modals/inner-scrolls + Assistant(chat)/Login(centered form) αφέθηκαν σκόπιμα. mobile `tsc --noEmit` **EXIT 0**. ΣΗΜ: μη-verifiable χωρίς tablet simulator, αλλά byte-safe σε phone (min(100%,640)).

### Light / dark theme via theme context
- Priority: P3
- Size: L
- Web ref: light theme overrides + `data-theme` toggle (file: apps/web/src/app/globals.css:32-51)
- Mobile files: apps/mobile/src/theme.ts (light palette + `ThemeProvider`/`useTheme`), refactor ΟΛΩΝ των screens (το `C` είναι const-imported σε 19 αρχεία) + SettingsScreen theme toggle
- Depends on: Theme tokens: spacing + radius + typography scale + missing colors, Input primitive (centralize 15 duplicate input styles), Button + Chip primitives (add/save buttons + filter/status chips), Card + Badge + ListItem primitives
- Acceptance:
  - `useTheme()` hook δίνει το active palette· μηδέν direct `import { C }` σε screen styles
  - Settings toggle εναλλάσσει light/dark, persisted· όλα τα screens ακολουθούν (τώρα dark-only, μηδέν `useColorScheme`/context)
- Status: TODO

### RADIUS token adoption (swap borderRadius literals → RADIUS.*)
- Priority: P2
- Size: S
- Web ref: consistent radius scale (mobile `theme.ts:26` RADIUS = sm10/md12/lg14/xl18, mirror του web card/pill radii)
- Mobile files: apps/mobile/src/ui.tsx (chip:279 `borderRadius:10`, iconBtn:287 `borderRadius:12`) + 12 screens (47 sites, π.χ. ItemsScreen aiBtn/wrap/storeRow, MoneyScreen scanBtn/bigImg, SettingsScreen card/saveBtn/testBtn, StatementsScreen plan/totBox, Subscriptions/Vouchers aiBtn, Assistant example/send, Activity tab/restore, Login btn)
- Depends on: none
- Acceptance:
  - μηδέν `borderRadius: (10|12|14|18)` numeric literal σε `apps/mobile/src/screens/*.tsx` και `ui.tsx` (όλα → `RADIUS.sm/md/lg/xl`)
  - non-scale radii (16/9/8/6/5/4/3 = cards/chips/tracks/dots) μένουν literal σκόπιμα (δεν υπάρχει token) — μην τα αγγίξεις
  - mobile `npx tsc --noEmit` EXIT 0· byte-identical rendered output (μηδέν οπτική αλλαγή)
- Status: TODO

### Brand typography (Outfit / Manrope / IBM Plex Mono) via expo-font
- Priority: P2
- Size: M
- Web ref: font families Outfit (display/titles), Manrope (body), IBM Plex Mono (labels/amounts) — design system (CLAUDE.md «Fonts») + web layout font loading
- Mobile files: apps/mobile/App.tsx (`useFonts` load + splash gate), apps/mobile/src/theme.ts (νέο `FONT = { display, body, mono }` token), εφαρμογή σε headers/titles (`FONT.display`) + labels/amounts/eyebrows (`FONT.mono`) στα screens + `ui.tsx` (Header/Badge/Chip). package.json: `expo-font` + `@expo-google-fonts/outfit|manrope|ibm-plex-mono`
- Depends on: none
- Acceptance:
  - `theme.ts` εξάγει `FONT` με display/body/mono· App φορτώνει τα fonts με `useFonts` και δεν renderάρει πριν φορτωθούν (splash gate)
  - grep `fontFamily` στο `apps/mobile/src` > 0 (τώρα **0**)· titles = display, mono labels/ποσά = IBM Plex Mono, mirror του web
  - mobile `npx tsc --noEmit` EXIT 0
- Status: TODO

### Full-screen loader consistency (`<Centered><Spinner/>`)
- Priority: P3
- Size: S
- Web ref: single loading treatment ανά route
- Mobile files: apps/mobile/src/screens/ShoppingScreen.tsx:69, apps/mobile/src/screens/SettingsScreen.tsx:91 (hand-rolled `<View style={center}><ActivityIndicator color={C.accent}/></View>`)
- Depends on: none
- Acceptance:
  - τα full-screen initial loaders χρησιμοποιούν `<Centered><Spinner/></Centered>` (τα primitives υπάρχουν στο `ui.tsx:28/31`)· τα τοπικά `center`/`loadWrap` StyleSheet entries σβήνουν αν μένουν αχρησιμοποίητα
  - inline button-busy `<ActivityIndicator>` (μέσα σε save/scan/AI buttons) ΜΕΝΟΥΝ ως έχουν (διαφορετικό pattern)
  - mobile `npx tsc --noEmit` EXIT 0
- Status: TODO

---

## 2026-07-19 (49η σάρωση — ui-auditor read-only grep audit)

**Σκοπός**: αυτόνομη αναμέτρηση mobile UI consistency μετά από 10 commits (από την 48η σάρωση στις 2026-07-09). Κανένα νέο item, αλλά **λεπτομέρειες των υπαρχόντων P2/P3** items που χρειάζονται μεγαλύτερη ποσοστιαία κάλυψη.

**Ευρήματα (ανά διάσταση):**

1. **Token adoption (RADIUS)**: μη-deterministic grep `borderRadius:` across 12 screens + ui.tsx → **~47 hardcoded literals** (6/8/9/10/12/14/16/18/20) αντί RADIUS.*. Τα non-scale radii (5/4/3 για dots/tracks σκόπιμα) → P2/S item υπάρχει.

2. **Font sizes**: `fontSize:` grep → **229 hardcoded values** (10..22) σε 19 screens, αντί SIZE.* tokens (SIZE = xs11/sm13/base14/md15/lg16/xl19). Αυτό είναι μεγάλο consistency gap και ζει υπό το P2/M "Brand typography" item (που καλύπτει ΟΛΕΣ τις font πτυχές: fontFamily + fontSize token).

3. **Padding/margin**: χιλιάδες literals (4..24) σκορπισμένα αντί SPACE.*. Δεν έχει ξεχωριστό item — αυτό κρύβεται ίσως κάτω από τη routing-priority άρνησης (ο builder κάνει RADIUS πρώτα, μετά Typography, τα margins παρακολουθούνται ως "secondary gaps" του Typography migration όπου τα μεγάλα padding blocks εναλλάσσονται σε StyleSheet)· δεν δημιουργώ νέο item για αυτό.

4. **fontFamily**: grep → **0 results**. Σύστημα font παντού. Υπάρχει P2/M item, **status TODO**.

5. **Light theme**: χωρίς `useColorScheme`/`ColorScheme`/context. P3/L item υπάρχει, **status TODO**.

6. **Safe-area handling**: χωρίς SafeAreaView/useSafeAreaInsets. Δεν έχει item (κατάταξη απόφαση: mobile notches < desktop breakpoint risk, αναβάλλεται). ΣΗΜ: `react-native-safe-area-context` ΔΕΝ installed.

7. **Touch targets**: `hitSlop` καθαρό (8–10), button widths 40–46 → PASS. Αποδεκτό.

8. **ReceiptsScreen WIP**: γραμμές 211–213 (3 `<TextInput>` raw, qty/net/vat fields) → Achilleas uncommitted, παραμένει blocked.

9. **ModalSheet**: ShoppingScreen holdout **ΚΛΕΙΣΙΜΕΝΟ** μετά την 48η σάρωση.

**Σύνολο δεδομένων:**
- mobile `npx tsc --noEmit` → **EXIT 0** ✓
- Working tree: clean (εκτός ReceiptsScreen WIP του χρήστη)
- 19 screens, ~1450 lines logic σε screens/, 310 lines primitives (`ui.tsx`)

**Κατεύθυνση**: η UI Debt Queue παραμένει ακριβής. Δεν προσθέτω νέα items, αλλά τα P2 items (RADIUS + Typography) χρειάζονται **μεγαλύτερη κάλυψη σε στατιστικά**. Ο επόμενος builder pass πρέπει να κατηγοριοποιήσει:
- **P2/S RADIUS**: ~47 sites σε screens + ui.tsx, byte-safe, μηδέν οπτική αλλαγή
- **P2/M Typography**: fontFamily (0 μέρη), fontSize (229 σημεία, 19 files), margin/padding δευτερογενής
- **P3 items**: ασφαλή αργότερα

Επόμενο suggested: builder να αρχίσει με P2/S (RADIUS), μετά P2/M (Typography: fontFamily load + SIZE.*).

