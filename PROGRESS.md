# PROGRESS — Pharos autonomous dev log

Καθημερινό unattended run (03:03). Κάθε run: διάλεξε ΕΝΑ task, validate (tsc + safe Docker rebuild), commit ΜΟΝΟ τα δικά σου αρχεία, push, κατέγραψε εδώ.

<!-- reviewed: 261a4fb -->
<!-- docker-validated: 0b53e82 -->

## 2026-06-30 (parity-auditor — 5η σάρωση ημέρας, queue ξαναμετρημένη από κώδικα)
- **Inventory (από κώδικα, όχι docs)**: **48 v1 routes** (από 46 — προστέθηκαν `items/[id]/convert-to-task` + `receipts/[id]/rescan`), **16 mobile screens**, **70** exported api fns (η παλιά «78» ήταν χαλαρή μέτρηση). Το mobile `api.ts` καταναλώνει **47/48** routes· το μοναδικό χωρίς mobile consumer = `POST /api/v1/receipts/[id]/rescan`.
- **DONE / GAP / NEEDS DECISION**: από τα 6 Build Queue items → **4 DONE** (Statements installment-plan overview, Items convert-to-task, Notifications unread badge, Expenses/Income full-field edit — όλα επιβεβαιωμένα στον κώδικα, όχι μόνο στα docs), **2 GAP/TODO**, **0 νέα NEEDS DECISION** (τα γνωστά μένουν, βλ. παρακάτω).
- **ΚΡΙΣΙΜΗ διόρθωση doc (stale)**: το queue item «Receipts re-scan» έλεγε `API exists: no`. Το web endpoint **χτίστηκε σήμερα** (`d24be27`) και είναι registered + serving (no-token → 401). Διόρθωσα σε `exists: yes` + ξανα-έγραψα το item ώστε να λέει ρητά ότι **απομένει ΜΟΝΟ το mobile half** (api.ts `rescanReceipt` + ReceiptsScreen buttons), να μην ξαναχτίσει ο builder το endpoint.
- **Top 3 για τον builder (με σειρά)**:
  1. **Receipts re-scan — mobile half** (P2/M, AI → δομικό verify μόνο): το web endpoint ΕΤΟΙΜΟ· λείπει `rescanReceipt(id,ocr)` στο api.ts + «Re-scan OCR/text» buttons στο ReceiptsScreen detail (re-prefill in-place από το response, ίδιο shape με `getReceipt`). ΜΗΝ τρέξεις πραγματικό AI scan.
  2. **Items AI specs** (P3/M, AI, full-stack): το `POST /api/v1/items/[id]/ai-fill` **ΔΕΝ υπάρχει** ακόμα → πρώτα νέο web endpoint (port των `aiFillSpecs`/`aiFillInfo`), μετά mobile button. Δομικό verify μόνο.
  3. **UI Debt → Input primitive** (P1/M, pure-mobile, no AI): centralize ~15 διπλά input styles σε `<Input>`/`<TextArea>` στο `ui.tsx`. Top της UI Debt ουράς.
- **Read-only checks**: mobile `npx tsc --noEmit` → **EXIT 0** (μηδέν P1 type errors). ΔΕΝ έτρεξα Docker, ΔΕΝ trigger-αρα AI job, ΔΕΝ άγγιξα app code (μόνο τα 2 docs).
- **Needs Achilleas**: τίποτα νέο. Παραμένουν τα γνωστά (απαιτούν απόφαση/device, ΔΕΝ πάνε στον builder): theme toggle + 8-language switcher στο mobile (product decision + L migration), AI engine / prompts settings στο mobile, Storage/OneDrive settings, Statements PDF import (mobile δεν κάνει upload PDF), remote push pipeline (χτίστηκε αλλά αδοκίμαστο — χρειάζεται EAS dev build + APNs key). Μηδέν committed secrets βρέθηκαν.

## 2026-06-30 (docker-health — safe rebuild σε νέο web code, stack healthy)
- **Health**: mongo `healthy`, web RestartCount **0**, mongo RestartCount 43 (σωρευτικό 2 εβδομάδων, αυτή τη στιγμή healthy/up, όχι loop). flaresolverr **Exited** εδώ και 27h (καμία ενέργεια χρειάστηκε, μένει σβηστό).
- **Disk**: `docker system df` → Images 3.49GB, Build Cache 565.9MB πριν. Άνετα μέσα στο ~31GB VM.
- **Rebuild απόφαση**: marker ήταν `4ca5649`· `git diff 4ca5649..HEAD -- apps/web` αγγίζει web runtime (`api/v1/{items,items/[id],items/[id]/convert-to-task,expenses/[id],receipts/[id]/rescan,shopping-list/[id]}/route.ts`, `shopping-list/actions.ts`, `models/Item.ts`) → rebuild δικαιολογημένο.
- **Safe rebuild**: `docker compose build web` → **fully cached** (η εικόνα ήταν ήδη χτισμένη σε HEAD, web container 3min old) → mongo `healthy` → `up -d web` → poll `curl /login` → **200** στη 2η προσπάθεια → web RestartCount παρέμεινε **0**. Μετά `docker builder prune -f` → reclaimed ~3.2MB (το υπόλοιπο cache active/in-use).
- **Needs Achilleas**: κανένα. Stack υγιές, κανένα regression.
- Marker → `0b53e82` (HEAD).

## 2026-06-30 (builder — WEB_DEBT P3#5: shared body-coercion helpers, web-only no-AI)
- **Τι**: χτίστηκε το WEB_DEBT «Shared body-coercion helpers» (P3/S). Στην αρχή του run το tree είχε βαρύ παράλληλο WIP άλλων routines: `apps/mobile/src/api.ts`, `items/route.ts` (commit ως listEnvelope alignment), `shopping-list/*` (commit ως P3#4 DONE), untracked `receipts/[id]/rescan/route.ts` (παράλληλος builder). **Απέφυγα όλες αυτές τις ζώνες** → καθαρά web-only task χωρίς κανένα WIP αρχείο.
- **Web** (νέο `lib/apiBody.ts`): `readBody(req)` + `strField`/`numField`/`enumField`/`boolField` — typed, behaviour-identical helpers για το επαναλαμβανόμενο body-parse pattern σε ~25 mutation routes. Κανένα δεν throw-άρει.
- **Web** (`api/v1/expenses/route.ts` + `api/v1/subscriptions/route.ts` POST): refactor ως **απόδειξη σε 2 routes**. Το acceptance έλεγε «items + expenses» αλλά το `items/route.ts` ήταν active WIP → **swap σε subscriptions** (ίδιο pattern, μηδέν conflict). Αμιγώς dedup: vendor/name `strField(...,true)`+required, `amount=numField()` με `===null` guard (ίδιο με `!Number.isFinite`), `enumField` για kind/recurringCycle/billingCycle, `boolField` για recurring, `strField`+fallback για τα optional strings. **Μηδέν αλλαγή σε validation/response shape**.
- **Verify**: `apps/web npm run type-check` → **EXIT 0**. Safe rebuild: `build web` → mongo **healthy** → `up -d web` → `curl /login`=**200** → web running. Structural: POST expenses/subscriptions + GET expenses no-token → **401** (auth πριν το body-parse). Logs μόνο γνωστά stale-bundle + canvas warning. flaresolverr σβηστό· `docker builder prune -f`. Μηδέν AI/token· δεν έτρεξα authed write.
- **Staged ΜΟΝΟ**: `lib/apiBody.ts`, `api/v1/expenses/route.ts`, `api/v1/subscriptions/route.ts`, `WEB_DEBT.md`, `PROGRESS.md` (explicit paths). ΔΕΝ άγγιξα api.ts / items/route.ts / shopping-list / receipts-rescan / secrets / `.env`.
- **Επόμενο task**: επέκταση των body-helpers σε ακόμη ~23 mutation routes (incl. items όταν ελεύθερο) σε μικρά batches· ή UI Debt → Input primitive (P1/M, pure-mobile).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — UI Debt: Touch targets ≥44pt + shared `<Check>` primitive, no AI)
- **Τι**: χτίστηκε καθαρό UI Debt Queue item (P2/S) αντί για τα παρ-τοπ Build Queue items, επειδή στην αρχή του run το working tree είχε παράλληλα uncommitted edits του Αχιλλέα/άλλου routine ΑΚΡΙΒΩΣ στη ζώνη που τα εναπομείναντα parity TODOs αγγίζουν: `apps/mobile/src/api.ts` (M), `apps/web/.../api/v1/items/route.ts` (M), `apps/web/.../api/v1/shopping-list/[id]/route.ts` (M), `shopping-list/actions.ts` (M), + **untracked `api/v1/receipts/[id]/rescan/`** (= το P2 Receipts re-scan, χτίζεται παράλληλα). Receipts re-scan + Items AI-specs (τα 2 ενεργά parity TODOs) θέλουν api.ts + items/receipts routes → conflict zone. Το «Touch targets» είναι pure-mobile UI (ui.tsx + screens), μηδέν επαφή με τη ζώνη, fully tsc-verifiable.
- **Mobile** (`src/ui.tsx`): νέο shared `<Check checked />` (display-only 24×24 box + ✓, χρώματα από `C.borderLight`/`C.accent`/`C.onAccent`, `SIZE.md` mark) + JSDoc που λέει «wrap σε Pressable με hitSlop 10 για ≥44pt». Νέα styles `checkbox`/`checkboxOn`/`checkboxMark`. Import `SIZE` από theme.
- **Mobile** (6 screens): αντικαταστάθηκαν τα 6 πανομοιότυπα checkbox inline-`View` (+ τα `tbox/check + tboxOn/checkOn + tmark/mark/checkMark` style triplets, **όλα αφαιρέθηκαν**) με `<Check checked={!!x} />` σε Tasks/Shopping/Subscriptions/Settings/Vouchers/Receipts. Ο μόνος interactive checkbox που ΗΤΑΝ ο ίδιος ο tap target (TasksScreen:75, Pressable-wrapped) → `hitSlop` 8→**10** (effective 24+20=44pt). Τα άλλα 5 είναι `<View>` display-only μέσα σε μεγάλα pressable rows/toggles (≥44pt ήδη). Side-benefit: 5 hardcoded `#000` onAccent literals έφυγαν (ενοποιήθηκαν σε `C.onAccent`).
- **Verify**: `apps/mobile npx tsc --noEmit` → **EXIT 0**. grep: μηδέν leftover refs στα removed style keys· 6 `<Check>` usages στη θέση τους. Καμία αλλαγή σε web runtime → **κανένα Docker rebuild** (βήμα 4: web diff κενό), μηδέν AI/token call. Mobile UI δεν auto-testable unattended (no simulator) → βασίστηκα σε tsc + careful review· το `<Check>` αναπαράγει ΑΚΡΙΒΩΣ το ίδιο visual (ίδια tokens) → μηδέν οπτική αλλαγή πέρα από το μεγαλύτερο tap area στο Tasks.
- **Staged ΜΟΝΟ**: `apps/mobile/src/ui.tsx`, `apps/mobile/src/screens/{TasksScreen,ShoppingScreen,SubscriptionsScreen,SettingsScreen,VouchersScreen,ReceiptsScreen}.tsx`, `MOBILE_PARITY.md`, `PROGRESS.md` (explicit paths, όχι `-A`). ΔΕΝ άγγιξα τα παράλληλα `api.ts`/`items`/`shopping-list`/`receipts-rescan` WIP ούτε secrets/`.env`.
- **Επόμενο task**: UI Debt Queue → **Button + Chip primitives** (P2/M — `<Button variant=accent/cyan/ghost>` με text από `C.onAccent` + `<Chip on/off>`· μηδενίζει τα ~22 inline `#000` button-text literals + τα per-screen chip patterns, χτίζει πάνω στο onAccent token). Το Input primitive (P1/M, top TODO) είναι σπρωγμένο (~50 TextInput sites/11 screens) → προτιμότερο σε attended run λόγω no-simulator οπτικού verify. Build Queue parity: Receipts re-scan + Items AI-specs μένουν (AI cost + conflict zone με τα τρέχοντα api.ts/items WIP).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (reviewer — έλεγχος range 4ead61b..261a4fb)
- **Range**: 9 commits, **5 με app code** (4 builder features + 1 refactor)· τα υπόλοιπα 4 docs-only (monitor/review). Κανένα `up --build`, μηδέν Docker, μηδέν AI.
- **Checks**: web `npm run type-check` → **EXIT 0**, mobile `npx tsc --noEmit` → **EXIT 0** (και τα δύο πέρασαν παρά το ταυτόχρονο uncommitted WIP στο tree — Input primitive `ui.tsx`/Shopping/Subscriptions/Tasks screens + `apiBody.ts` + expenses/subscriptions routes· δεν είναι δικά μου, ΔΕΝ τα άγγιξα). **Καμία διόρθωση χρειάστηκε.**
- **Review ευρήματα (όλα PASS, μηδέν regression)**:
  - `c3c9fae` **listEnvelope alignment** (breaking shape change `{items}`→`{data}`): web route + ο μοναδικός mobile consumer (`getItems` στο `api.ts`) αλλάζουν **στο ΙΔΙΟ commit** → μηδέν in-between divergence. `listEnvelope(docs, total, p)` παράγει `{data,total,limit,offset}` (επιβεβαιωμένο από `apiList.ts:26`). Σωστό.
  - `804a60a` **POST /api/v1/items/[id]/convert-to-task** (νέο route, additive): id-guard `^[a-f0-9]{24}$`→400, `withAuth`, wraps το proven `convertItemToTask` (επιστρέφει `{ok,taskId?,error?}`), `!r.ok`→404. Item ανέγγιχτο. Καθαρό.
  - `261a4fb` **shopping-list/[id] id-guard + 404**: PATCH+DELETE κάνουν `ID_RE` guard (το ΜΟΝΟ [id] route που έλειπε)· τα actions γυρνούν `{ok,found}` από `matchedCount`. Επαλήθευσα ότι ο soft-delete plugin **όντως hook-άρει `updateOne`** (`softDelete.ts:21`) → already-trashed ή ανύπαρκτο id = matchedCount 0 = 404, όπως τεκμηριώνεται. Web UI αγνοεί το return (additive). PATCH χωρίς πεδίο→400. Σωστό.
  - `bba9b1c` **expenses/[id] PATCH full-field** (period/recurring/recurringCycle/paymentMethod): αμιγώς additive στο `$set`, `recurringCycle` enum-guarded (`''`|monthly|quarterly|yearly|weekly) — ταιριάζει 1:1 με τα mobile `CYCLES` (MoneyScreen:8) που στέλνει `''` όταν off. id-guard ήδη παρών (route:14,40). Ίδιο response shape.
  - `8b8c0f2` **mobile AppBar bell badge**: pure-mobile UI, tsc καθαρό.
  - Secret-scan στο diff: **καθαρό** (τα μόνα matches = builder prose «μηδέν secrets/.env», όχι literals· μόνο `apiToken`/`x-api-token`/401-structural references).
- **Flagged**: τίποτα νέο. Καμία regression, κανένα type error, κανένα committed secret, καμία ασυντόνιστη API shape-break. Ουρές (WEB_DEBT, MOBILE_PARITY) μένουν ως έχουν.
- **Git hygiene**: staged ΜΟΝΟ `PROGRESS.md` (reviewed marker `4ead61b`→`261a4fb` + αυτή η εγγραφή). ΔΕΝ άγγιξα το παράλληλο WIP (ui.tsx/Shopping/Subscriptions/Tasks/apiBody.ts/expenses+subscriptions routes) ούτε secrets/.env.

## 2026-06-30 (builder — WEB_DEBT P2: GET /api/v1/items → shared listEnvelope {data})

- **Συμφραζόμενα / έντονη παράλληλη σύγκρουση**: το working tree ήταν αυτό το run πολύ contested — ο αρχικός μου στόχος (Items convert-to-task, ο προτεινόμενος επόμενος task) είχε ήδη uncommitted WIP (`?? api/v1/items/[id]/convert-to-task/` + `M api.ts` + `M ItemsScreen.tsx`)· τον validate-άρισα (tsc web+mobile EXIT 0, safe rebuild → /login 200, no-token/bogus → 401) αλλά ΕΝΟΣΩ δούλευα τον commit-άρισε παράλληλο routine ως `804a60a`. Παράλληλα τρέχοντα: Notifications badge (`8b8c0f2`), shopping-list id-guard (`261a4fb`), Receipts re-scan + `apiBody.ts` (untracked WIP). Διάλεξα task που **δεν αγγίζει κανένα contested αρχείο**.
- **Τι**: WEB_DEBT P2 — το `GET /api/v1/items` ήταν το ΜΟΝΟ v1 list endpoint που γύριζε `{ items, ... }` αντί του shared `listEnvelope` `{ data, total, limit, offset }` (το χρησιμοποιούν ήδη receipts/tasks/expenses/subscriptions/statements/vouchers). Άλλαξα το route να καλεί `listEnvelope(docs.map(trim), total, p)` (+ import + JSDoc) και ενημέρωσα τον **μοναδικό** mobile consumer (`getItems` στο `apps/mobile/src/api.ts`: `{items}`→`{data}`, `.items`→`.data`). Κλείνει την τελευταία απόκλιση response-shape· web + mobile αλλάζουν μαζί σε ΕΝΑ commit, μηδέν in-between deploy.
- **Verify**: `apps/web npm run type-check` → **EXIT 0**· `apps/mobile npx tsc --noEmit` → **EXIT 0**. Safe Docker rebuild: `docker compose build web` (mongo πήγε στιγμιαία unhealthy κατά το build — το γνωστό CPU-starvation, ανέκαμψε healthy πριν το up) → `up -d web` (mongo waited healthy) → `curl /login` = **200 σε 1s** → web `running` → `docker builder prune -f`. Structural: `GET /api/v1/items` + `?status=shopping` no-token → **401** (route registered, serves). flaresolverr σβηστό. ΔΕΝ έτρεξα authed round-trip (token=secret) ούτε AI job.
- **Staged ΜΟΝΟ** (explicit paths, όχι `-A`): `apps/web/src/app/api/v1/items/route.ts` + `apps/mobile/src/api.ts` (commit `c3c9fae`) + `PROGRESS.md`. **ΔΕΝ άγγιξα** το `WEB_DEBT.md` (είχε ταυτόχρονα uncommitted edits από παράλληλο routine — ο επόμενος auditor ξαναδιαβάζει τον κώδικα και θα δει το listEnvelope ούτως ή άλλως) ούτε κανένα άλλο contested working-tree αρχείο/secret/.env.
- **Επόμενο task**: WEB_DEBT P3 **Shared body-coercion helpers** (`apiBody.ts` str/num/enum/bool) — ΣΗΜ: ξεκίνησε ήδη ως untracked WIP (`apps/web/src/lib/apiBody.ts`) από παράλληλο run· αν παραμένει uncommitted, προτίμησε **Receipts re-scan mobile wiring** (το web endpoint `receipts/[id]/rescan` χτίστηκε ήδη — λείπει το `apps/mobile/src/screens/ReceiptsScreen.tsx` κουμπί + `api.ts rescanReceipt`, P2/M, AI → δομικό verify μόνο).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — Receipts re-scan WEB endpoint, P2/M AI → δομικό verify, web-only)

- **Συμφραζόμενα / αποφυγή σύγκρουσης**: το working tree ήταν έντονα contested από πολλά παράλληλα routines — uncommitted edits σε `App.tsx`, `src/nav.tsx`, `src/api.ts`, `src/screens/ItemsScreen.tsx`, `MOBILE_PARITY.md`, `web/src/app/shopping-list/{actions.ts,[id]/route.ts}` + untracked `api/v1/items/[id]/convert-to-task/`. Δηλαδή ΚΑΙ τα δύο επόμενα queue tasks (Items convert-to-task **και** Notifications unread badge) υλοποιούνταν ήδη από άλλους runs ταυτόχρονα (το `nav.tsx` είχε ήδη το bell+badge, το `App.tsx` το `unread` poll· το shopping-list id-guard ήταν επίσης ήδη υπό commit). Για να μην σύρω ξένο WIP, διάλεξα task που **δεν αγγίζει κανένα modified αρχείο**: νέο standalone web endpoint.
- **Τι**: νέο `POST /api/v1/receipts/[id]/rescan` (`apps/web/src/app/api/v1/receipts/[id]/rescan/route.ts`) — port του web `rescanReceipt(id, useOcr)`. Body `{ocr?:boolean}` (`ocr:true` → OCR path, αλλιώς embedded text/vision). `withAuth` + 24-hex id-guard (`bad id` 400). Επιστρέφει **ΑΚΡΙΒΩΣ το ίδιο shape με το GET /api/v1/receipts/[id]** (re-query με `trimReceipt` + notes + normalized `lineItems[{name,qty,price,vatRate}]`, refinedName||name) ώστε το mobile detail να κάνει in-place re-prefill χωρίς reopen, + `aiUsed/model/aiError`. Failure → 404 σε not-found/missing-file, αλλιώς 500.
- **Αμιγώς additive**: μόνο ένα νέο αρχείο, μηδέν edit σε υπάρχον/contested αρχείο, μηδέν αλλαγή response shape άλλου endpoint, μηδέν mobile touch.
- **Verify** (δομικό μόνο — AI cost, ΔΕΝ έτρεξα πραγματικό scan): `apps/web npm run type-check` → **EXIT 0**. Safe rebuild: `docker compose build web` (image, cached) → mongo `healthy` → `up -d web` → `curl /login` = **200 σε 2s** → web `.State.Status=running` (αυτό το engine δεν εκθέτει `.RestartCount` — γνωστό αβλαβές quirk· χωρίς restart-loop). Structural API: `POST .../rescan` no-token → **401**, bogus-token + non-hex id → **401** (auth gate πριν το id-check). `docker builder prune -f` (0B, in-use). flaresolverr σβηστό.
- **Staged ΜΟΝΟ**: `apps/web/src/app/api/v1/receipts/[id]/rescan/route.ts` + `PROGRESS.md` (explicit paths, όχι `-A`). **ΔΕΝ άγγιξα** κανένα παράλληλο-contested αρχείο ούτε secrets/.env.
- **Επόμενο task**: **Receipts re-scan — mobile half** (όταν το `apps/mobile/src/api.ts` δεν είναι πλέον contested): `rescanReceipt(id, ocr)` στο `api.ts` + «Re-scan: OCR / text» buttons στο ReceiptsScreen detail → spinner → re-prefill των store/date/total/lineItems από το response (ίδιο shape με GET). Το web endpoint είναι έτοιμο. Εναλλακτικά: Items AI specs web endpoint (`POST /api/v1/items/[id]/ai-fill`, νέο αρχείο, ίδιο standalone μοτίβο).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — WEB_DEBT P3: shopping-list/[id] id-guard + 404)
- **Τι**: ο προτεινόμενος επόμενος task (Items convert-to-task) είχε **παράλληλο uncommitted WIP** στην έναρξη (`?? api/v1/items/[id]/convert-to-task/route.ts` + `M apps/mobile/src/api.ts` + `M apps/mobile/src/screens/ItemsScreen.tsx` — όχι δικό μου). Απέφυγα εντελώς items/mobile και διάλεξα το πιο καθαρό ανεξάρτητο item της WEB_DEBT ουράς: **shopping-list/[id] id-validation + 404** (P3/S, web-only, μηδέν mobile coupling).
- **Web** (`api/v1/shopping-list/[id]/route.ts`): PATCH+DELETE κάνουν πλέον `^[a-f0-9]{24}$` guard (`apiError('bad id')` 400, ίδιο μοτίβο με ΟΛΑ τα άλλα `[id]` routes) → malformed id δεν φτάνει σε Mongoose query (απέτρεψε πιθανό CastError→500). PATCH χωρίς έγκυρο `checked`/πεδίο → 400 `no valid fields`. Όταν δεν βρεθεί live εγγραφή → **404** αντί σιωπηλό `{ok:true}`.
- **Web** (`shopping-list/actions.ts`): τα `toggleListItem`/`updateListItem`/`deleteListItem` γυρνούν πλέον `{ ok, found }` (`found` από `matchedCount` του updateOne· ο soft-delete pre-hook βάζει `deletedAt:null` στο filter → match μόνο live, άρα ήδη-trashed = not found = 404, σωστό). **Additive** — το `ShoppingListClient.tsx` καλεί με `void`/αγνοεί το return → μηδέν regression στο web UI.
- **Verify**: `apps/web npm run type-check` → **EXIT 0**. Safe rebuild: `docker compose build web` → mongo `healthy` → `up -d web` → `curl /login` = **200** (2η προσπάθεια) → web `Status=running` (αυτό το engine δεν εκθέτει RestartCount). Structural: no-token PATCH non-hex / DELETE valid-hex / bogus-token PATCH → όλα **401** (auth gate πριν το id-check, αναμενόμενο). Το 400/404 path θέλει real token (=secret) → επαληθεύεται από code review + tsc + serving build. flaresolverr σβηστό· `docker builder prune -f` (cache-only, ανέκτησε ~2.6GB). Μηδέν AI job.
- **Staged ΜΟΝΟ**: `apps/web/src/app/api/v1/shopping-list/[id]/route.ts`, `apps/web/src/app/shopping-list/actions.ts`, `WEB_DEBT.md`, `PROGRESS.md` (explicit paths, όχι `-A`). ΔΕΝ άγγιξα τα παράλληλα WIP (items convert-to-task, mobile api.ts/ItemsScreen, mobile Notifications) ούτε secrets/.env.
- **Επόμενο task**: WEB_DEBT P3 **Shared body-coercion helpers** (str/num/enum/bool σε νέο `lib/apiBody.ts`, refactor items+expenses ως απόδειξη· no AI, web-only, fully testable). Η εναπομείνασα P2 (`GET /items → listEnvelope {data}`) είναι **breaking για mobile** (`api.ts` διαβάζει `.items`) + ο Αχιλλέας/παράλληλα routines έχουν συχνά WIP εκεί → άφησέ την όσο υπάρχει mobile WIP στο tree.
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — MOBILE_PARITY: Notifications unread badge στο AppBar, P3/S pure-mobile)
- **Τι**: χτίστηκε το «Notifications — unread badge στο AppBar» (Build Queue P3/S, no AI, pure-mobile). Στην αρχή του run υπήρχαν παράλληλα uncommitted edits στην περιοχή items (`convert-to-task/route.ts` untracked + `api.ts` + `ItemsScreen.tsx` modified — όχι δικά μου, παράλληλο builder routine). Ο προηγούμενος builder είχε ρητά γράψει «αν τα items routes έχουν uncommitted edits, προτίμησε Notifications unread badge (pure-mobile, μηδέν web touch)» → ακριβώς αυτό επέλεξα ώστε να μην ακουμπήσω τη ζώνη conflict. (Ενδιάμεσα τα items edits έγιναν commit αλλού· στο τέλος του run το ενεργό παράλληλο WIP ήταν `shopping-list` — επίσης δεν το άγγιξα.)
- **Mobile** (`src/nav.tsx`): το `AppBar` δέχεται πλέον `onBell?`/`unread?` props → νέο 🔔 Pressable (πριν το `PharosMark`) με κόκκινο badge (`C.red`, white-on-red `C.text`) που δείχνει τον unread count (cap `99+`)· κρύβεται όταν `unread===0`. Νέα styles `bellIcon`/`badge`/`badgeText` (absolute-positioned badge top-right του κουμπιού).
- **Mobile** (`App.tsx`): νέο `unread` state· `refreshUnread` (useCallback) καλεί το υπάρχον `getNotifications()` (γυρνά ήδη `{items, unread}`, **καμία αλλαγή σε api.ts**) και θέτει το count· poll κάθε 60s όσο `authed` + refresh σε ΚΑΘΕ screen change (ώστε μετά το mark-read στο Activity→Alerts το badge να ενημερώνεται). `onBell={() => setScreen('activity')}` → η ActivityScreen ανοίγει default στο tab `alerts`. Reset σε 0 όταν unauthed.
- **Verify**: `apps/mobile npx tsc --noEmit` → **EXIT 0** (με το παράλληλο WIP μέσα, καθαρό). Καμία αλλαγή σε web runtime → **κανένα Docker rebuild** (σωστά ανά βήμα 4: web diff κενό). Καμία κλήση AI/token. Mobile UI δεν auto-testable unattended (χωρίς simulator) → βασίστηκα σε tsc + code review (το `getNotifications` συμβόλαιο + ActivityScreen default tab επαληθεύτηκαν από τον κώδικα).
- **Staged ΜΟΝΟ**: `apps/mobile/App.tsx`, `apps/mobile/src/nav.tsx`, `MOBILE_PARITY.md`, `PROGRESS.md` (explicit paths, όχι `-A`). ΔΕΝ άγγιξα τα παράλληλα `shopping-list/*` / items edits / secrets / `.env`.
- **Επόμενο task**: Build Queue → **Receipts re-scan OCR/text** (P2/M, AI → δομικό verify μόνο: `POST /api/v1/receipts/[id]/rescan` port του `rescanReceipt` + ReceiptsScreen buttons· endpoint registered + no-token 401, ΧΩΡΙΣ πραγματικό AI scan). Εναλλακτικά, αν τα items routes είναι ελεύθερα, **Items convert-to-task** (αν δεν έχει ήδη γίνει commit από το παράλληλο routine).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — MOBILE_PARITY: Items convert-to-task, P3/S no-AI)

- **Τι**: χτίστηκε το επόμενο καθαρό Build Queue item (το suggested-next του προηγ. builder run). Working tree ΚΑΘΑΡΟ στην αρχή (μηδέν παράλληλο WIP) → δεν χρειάστηκε αποφυγή περιοχής.
- **Web** (νέο `api/v1/items/[id]/convert-to-task/route.ts`): `POST` wrapper του proven `convertItemToTask` (apps/web/src/app/items/actions.ts:649) — 24-hex id-guard, `withAuth` gate, επιστρέφει `{ok, taskId}`· `r.ok===false` → `apiError(..., 404)`. Δημιουργεί Task (title + price + links σε HTML content, tag 'shopping', status 'todo')· το item μένει αμετάβλητο. Ίδιο μοτίβο με το `items/[id]/link-plan` route. Μηδέν AI.
- **Mobile** (`api.ts`): νέα `convertItemToTask(id)` → `POST /api/v1/items/${id}/convert-to-task`, type `{ok, taskId?}`.
- **Mobile** (`ItemsScreen.tsx`): κουμπί «＋ Convert to task» (cyan outline) στο item edit modal, κάτω από SPECS· `converting` state + handler → Alert επιβεβαίωση «Converted to task» (ή error). Νέα styles `convertBtn`/`convertBtnText`.
- **Verify**: `apps/web npm run type-check` → **EXIT 0**· `apps/mobile npx tsc --noEmit` → **EXIT 0**. Safe Docker rebuild: `docker compose build web` (cached, built) → mongo `healthy` → `up -d web` → `curl /login` = **200**· web `.State.Status=running` (αυτό το engine δεν εκθέτει `.State.RestartCount` — γνωστό αβλαβές quirk). Structural: `POST /api/v1/items/<24hex>/convert-to-task` no-token → **401**, bogus-token + non-hex id → **401** (auth πριν το id-check → endpoint registered). Logs μόνο τα γνωστά stale-bundle «Failed to find Server Action» (ανοιχτό browser tab), κανένα crash. flaresolverr σβηστό· `docker builder prune -f` (cache-only, ~2GB). ΔΕΝ έτρεξα authed write (token = secret) ούτε AI job.
- **Staged ΜΟΝΟ**: `apps/web/src/app/api/v1/items/[id]/convert-to-task/route.ts`, `apps/mobile/src/api.ts`, `apps/mobile/src/screens/ItemsScreen.tsx`, `MOBILE_PARITY.md`, `PROGRESS.md` (explicit paths, όχι `-A`). Μηδέν αλλαγή σε secrets/.env.
- **Επόμενο task**: MOBILE_PARITY queue → απομένουν **3 GAP** (1 P2 + 2 P3). Καθαρότερο no-AI: **Notifications unread badge** (P3/S, pure-mobile — `GET /api/v1/notifications` γυρνά ήδη `unread` count, απλώς badge στο HomeScreen/tab· μηδέν web touch). Τα Receipts re-scan (P2/M) + Items AI specs (P3/M) θέλουν AI → μόνο δομικό verify (μην triggάρεις AI job unattended).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (builder — Build Queue: Expenses/Income full-field edit, P3/S no-AI)

- **Τι**: χτίστηκε το επόμενο καθαρό (no-AI, fully verifiable) Build Queue item. Στην αρχή του run υπήρχαν uncommitted edits στα `api/v1/items/*` + `models/Item.ts` (παράλληλο builder routine — όχι του Αχιλλέα) → απέφυγα εντελώς την περιοχή items· ενδιάμεσα έγιναν commit ως `4ead61b` (item-status whitelist).
- **Web** (`api/v1/expenses/[id]/route.ts`): το `PATCH` δέχεται πλέον `period` (string), `recurring` (boolean), `recurringCycle` (ίδιος enum-guard με το POST: monthly/quarterly/yearly/weekly ή '' για clear), `paymentMethod` (string), επιπλέον των vendor/amount/category/kind/notes/date. Αμιγώς additive στο `set` object — μηδέν αλλαγή σε response shape (`{ok,id}`).
- **Mobile** (`api.ts`): `Expense` type += `recurringCycle`/`paymentMethod`/`notes` (τα γυρνά ήδη το list endpoint `trim()`, απλώς δεν ήταν typed)· `updateExpense` signature += `period`/`recurring`/`recurringCycle`/`paymentMethod`/`notes`.
- **Mobile** (`MoneyScreen.tsx`): το edit modal (πριν μόνο vendor/amount/category) → full-field: DATE (YYYY-MM-DD), PERIOD (YYYY-MM), PAYMENT, **Recurring toggle ON/OFF** + **cycle picker** (ορατός μόνο όταν recurring=ON), NOTES (multiline). `openEdit` prefill + `saveEdit` (cycle='' όταν recurring off). Wrapped σε `ScrollView` (+ `maxHeight:'88%'`). Νέα styles + `CYCLES` const.
- **Verify**: `apps/web npm run type-check` → **EXIT 0**· `apps/mobile npx tsc --noEmit` → **EXIT 0**. Safe Docker rebuild: `docker compose build web` (cached) → mongo `healthy` → `up -d web` → `curl /login` = **200 σε 1s** → `homepage-web .RestartCount` = **0**. Structural: `PATCH /api/v1/expenses/<24hex>` no-token → **401**, bogus-token + non-hex id → **401** (auth πριν το id-check). flaresolverr σβηστό· `docker builder prune -f` (cache-only). ΔΕΝ έτρεξα authed round-trip (real token/secret) ούτε AI job.
- **Staged ΜΟΝΟ**: `apps/web/src/app/api/v1/expenses/[id]/route.ts`, `apps/mobile/src/api.ts`, `apps/mobile/src/screens/MoneyScreen.tsx`, `MOBILE_PARITY.md`, `PROGRESS.md` (explicit paths, όχι `-A`). Μηδέν αλλαγή σε items/secrets/.env.
- **Επόμενο task**: Build Queue → **Items convert-to-task** (P3/S, no AI — νέο `POST /api/v1/items/[id]/convert-to-task` port του `convertItemToTask` + mobile button). Αν τα items routes έχουν uncommitted edits εκείνη τη στιγμή, προτίμησε **Notifications unread badge** (P3/S, pure-mobile, μηδέν web touch).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (reviewer — range 5457339..4ead61b clean, item-status whitelist verified)
- Εύρος: `5457339..4ead61b` (7 commits· 6 docs/docker/monitor, **1 code**: `4ead61b` web — whitelist item status σε POST /api/v1/items). Read-only review + αμφότερα type-checks.
- **Type-checks**: `apps/web` `npm run type-check` → **EXIT 0**· `apps/mobile` `npx tsc --noEmit` → **EXIT 0** (το tree περιείχε και το παράλληλο builder WIP — βλ. κάτω — και παρ' όλα αυτά καθαρό). Καμία διόρθωση χρειάστηκε.
- **Diff review του `4ead61b`** (`models/Item.ts` + `items/route.ts` + `items/[id]/route.ts`): καθαρό refactor, αμιγώς προστατευτικό. Επαλήθευσα:
  - `ITEM_STATUSES` export = ΑΚΡΙΒΩΣ οι 8 τιμές του παλιού schema enum, ίδια σειρά → μηδέν αλλαγή στο enum validation. `enum: [...ITEM_STATUSES]` = κανένα διπλό literal.
  - POST whitelist: `b.status` invalid/undefined/'' → fallback `'researching'` (ίδιο αποτέλεσμα με το παλιό για τις νόμιμες περιπτώσεις· μόνο το invalid-verbatim bug διορθώθηκε). Μηδέν regression. `category` σκόπιμα free string (relaxed enum) — σωστό, τεκμηριωμένο.
  - PATCH: το local `STATUS` literal αντικαταστάθηκε με το import· **εξακολουθεί να χρησιμοποιείται** (line 113 `STATUS.includes(b.status)`) → κανένα dead const, tsc `strict` καθαρό. Το παλιό literal είχε διαφορετική σειρά (sold/broken πριν deferred) αλλά ίδιο σύνολο → `.includes()` αμετάβλητο.
  - Μηδέν αλλαγή σε response shape (POST επιστρέφει ίδιο `{item}`), μηδέν secrets.
- **Παράλληλο uncommitted builder WIP** (Expenses/Income full-field edit): `apps/mobile/src/api.ts`, `apps/mobile/src/screens/MoneyScreen.tsx`, `apps/web/src/app/api/v1/expenses/[id]/route.ts`. **ΔΕΝ είναι committed → εκτός review range· ΔΕΝ τα άγγιξα**. Type-checks πέρασαν με αυτά μέσα. Θα τα δει το επόμενο reviewer run όταν committed-αριστούν.
- **Fixed**: τίποτα (δεν χρειάστηκε). **Flagged**: κανένα νέο εύρημα (regression/secret/shape-break). Ουρές (WEB_DEBT P2#2 → DONE από builder, MOBILE_PARITY 5 GAP) αμετάβλητες πέρα από το ήδη-καταγεγραμμένο progress.
- Staged ΜΟΝΟ `PROGRESS.md` (explicit path, όχι `-A`). Marker `5457339 → 4ead61b`. Κανένα Docker/AI job/secret.

## 2026-06-30 (builder — WEB_DEBT P2#2: whitelist item status σε POST /api/v1/items)
- Τι: χτίστηκε το επόμενο TODO της WEB_DEBT ουράς (P2/S, api). Το `POST /api/v1/items` έγραφε αυθαίρετο `status` verbatim (`String(b.status||'researching')`) ενώ το `PATCH items/[id]` έκανε ήδη `STATUS.includes()` έλεγχο — άρα invalid status περνούσε μέσω POST. Εξήγαγα **`ITEM_STATUSES`** (+ `ItemStatus` type) ως single source of truth στο `models/Item.ts`, που **τροφοδοτεί και το schema enum** (`enum: [...ITEM_STATUSES]`, μηδέν διπλό literal). Τώρα το POST κάνει whitelist (invalid → fallback `'researching'`, ίδιο μοτίβο με `tasks/route.ts`) και το PATCH αντικατέστησε το local `STATUS` array με το import. Το `category` παραμένει **σκόπιμα free string** (relaxed enum, custom categories από Settings → Lists) — προστέθηκε σχόλιο.
- **Καμία αλλαγή σε response shape / route logic** πέρα από το να μην αποθηκεύεται πλέον invalid status. Μηδέν αλλαγή στο mobile↔web συμβόλαιο (POST επιστρέφει ίδιο `{item}`).
- Verify: `apps/web npm run type-check` → **EXIT 0**. Safe rebuild: `docker compose build web` (EXIT 0) → mongo `healthy` → `docker compose up -d web` → `curl /login` = **200** σε 1s → web `restarts=0` → `docker builder prune -f`. Structural API: `POST` + `GET /api/v1/items` no-token → **401** (auth gate ΟΚ, endpoint registered). ΔΕΝ έκανα authenticated write (API token = secret)· η whitelist logic επαληθεύεται από code review + tsc + serving build.
- **ΠΡΟΣΟΧΗ working tree**: υπήρχαν 3 παράλληλα uncommitted αρχεία του Αχιλλέα (`apps/mobile/src/api.ts`, `apps/mobile/src/screens/MoneyScreen.tsx`, `apps/web/src/app/api/v1/expenses/[id]/route.ts` — μάλλον Expenses/Income full-field-edit WIP). **ΔΕΝ τα άγγιξα**. Staged ΜΟΝΟ τα 3 δικά μου (`models/Item.ts` + `items/route.ts` + `items/[id]/route.ts`) με explicit paths (όχι `-A`). Commit `4ead61b`.
- **Επόμενο task:** WEB_DEBT P2#3 (GET /api/v1/items → `listEnvelope` `{items}`→`{data}`) είναι **breaking για το mobile** (`api.ts` διαβάζει `.items`) + ο Αχιλλέας έχει παράλληλο WIP στο `api.ts` → ρίσκο conflict. Καθαρότερη/ανεξάρτητη επιλογή: WEB_DEBT P3 **`shopping-list/[id]` id-guard (24-hex regex) + 404** (μηδέν mobile coupling, fully testable).
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (docker-health — health OK, χωρίς rebuild)
- Health (read-only): mongo **healthy**, web **running** (`/login` → **200**), και τα δύο Up 2 ώρες. searxng up (default stack). **flaresolverr ΣΒΗΣΤΟ** (όχι στο ps) → καμία ενέργεια. ΣΗΜ: αυτό το Docker engine δεν εκθέτει `.State.RestartCount` (template parsing error «map has no entry» → αβλαβές quirk έκδοσης)· χρησιμοποίησα `.State.Status=running` ως proxy, κανένα restart-loop σύμπτωμα.
- Disk: `docker system df` → Images 3.49GB, Build Cache **565.7MB με 0B reclaimable**, Volumes 502MB. VM άνετο → **κανένα prune** (τίποτα να ανακτηθεί + κανένα build).
- Rebuild decision: `git diff --name-only 01230bd..HEAD -- apps/web` → **κενό** (τα ενδιάμεσα commits docs/mobile-only) → **SKIP rebuild** ανά βήμα 4. Δεν έτρεξα κανένα build/AI.
- Marker: `01230bd` → **`4ca5649`** (current HEAD). Staged ΜΟΝΟ PROGRESS.md.
- Needs Achilleas: κανένα νέο. (Παραμένει εκκρεμές από monitor runs: RAM bump στο μικρό Docker VM ~1.9GB λόγω επαναλαμβανόμενου mongo OOM σε βαριά builds.)

## 2026-06-30 (parity-auditor — 3η σάρωση ημέρας, re-confirm read-only)
- Read-only re-audit web↔mobile από τον κώδικα (όχι docs). Μηδέν app code, μηδέν Docker, μηδέν AI jobs. Inventory: **46 v1 route files** (login + 45 bearer), **16 mobile screens**, **78** exported api fns.
- mobile `npx tsc --noEmit` → **EXIT 0** (καθαρό → κανένα P1 type-gap).
- **Endpoint coverage 1:1**: grep των v1 paths στο mobile `api.ts` ταιριάζει με ΟΛΟ το route list → κανένα «web endpoint χωρίς mobile consumer» gap· ό,τι λείπει χρειάζεται **νέο/επεκταμένο** endpoint.
- **Επαλήθευσα ξανά από directory listings + κώδικα και τα 5 GAP ως γνήσια:**
  - Receipts re-scan (P2/M): `api/v1/receipts/[id]/` = {route.ts, add-to-library} → κανένα rescan → valid TODO (AI cost → δομικό verify μόνο).
  - Items convert-to-task (P3/S): `api/v1/items/[id]/` = {route.ts, link-plan, plans, price} → κανένα convert-to-task → valid (no AI).
  - Items AI specs (P3/M): ίδιο listing → κανένα ai-fill → valid (AI cost).
  - Expenses/Income full-field edit (P3/S): `PATCH /api/v1/expenses/[id]` (route.ts:11-29, διάβασα το body) δέχεται ΜΟΝΟ vendor/amount/category/kind/notes/date → ΛΕΙΠΟΥΝ period/recurring/recurringCycle/paymentMethod → «PARTIAL» ισχύει (builder επεκτείνει ΠΡΩΤΑ το PATCH).
  - Notifications unread badge (P3/S): pure-mobile UI· `GET /api/v1/notifications` υπάρχει + γυρνά ήδη `unread` (api.ts:412) → valid.
- **Πρώην P1 = DONE**: Statements installment-plan overview (route `statements/plans` υπάρχει, api.ts:274 το καλεί). Πυρήνας parity κλειστός (μηδέν P1/S).
- **Από το προηγ. parity audit (`afbccb3`) μόνο 1 code commit άγγιξε web runtime**: `5457339` (updatedAt indexes = db perf, ΟΧΙ parity) → queue σωστά αμετάβλητο.
- **Σύνολο: 0 DONE-corrections, 5 GAP (1 P2 + 4 P3), 0 νέα NEEDS DECISION.**
- **Top 3 για τον builder:** (1) Receipts re-scan OCR/text [P2/M, AI → δομικό verify], (2) Items convert-to-task [P3/S, no AI], (3) Expenses/Income full-field edit [P3/S, no AI — επέκταση PATCH πρώτα].
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (explicit paths, όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (docker-health guard — rebuild μετά τα updatedAt indexes, υγιές)
- Health πρώτα: mongo **healthy** (up 16'), web restarts **0**, mongo restarts 36 (ιστορικό σωρευτικό από παλιά OOM, όχι ενεργό loop). flaresolverr **όχι running**. Disk υγιές: images 3.49GB, build cache 565MB.
- Diff `d897698..HEAD` (=01230bd) άγγιξε `apps/web/src/models/*` (τα `updatedAt:-1` indexes του 5457339) = web runtime code → δικαιολογήθηκε rebuild.
- Ασφαλές rebuild: `docker compose build web` (layers cached) → mongo healthy → `up -d web` → `/login` **200 σε 1η προσπάθεια** → web restarts παρέμειναν **0**. `docker builder prune -f` μετά (reclaimed ~3MB, το υπόλοιπο cache in-use).
- Marker → `01230bd`. Needs Achilleas: κανένα.

## 2026-06-30 (reviewer — range 362efc5..5457339 clean, updatedAt indexes verified)
- Εύρος: `362efc5..5457339` (8 commits· 7 docs/monitor/docker/audit, **1 code**: `5457339` web — `updatedAt: -1` index σε 7 synced models). Read-only review + αμφότερα type-checks.
- **Type-checks**: `apps/web` `npm run type-check` → **EXIT 0**· `apps/mobile` `npx tsc --noEmit` → **EXIT 0**. Καμία διόρθωση χρειάστηκε.
- **Diff review του `5457339`** (Item/Task/Receipt/Expense/Subscription/Statement/Voucher): αμιγώς additive `Schema.index({ updatedAt: -1 })`, ένα ανά model, με σχόλιο. Μηδέν αλλαγή σε route logic / response shape / mobile↔web συμβόλαιο. Μηδέν secrets.
  - Επαλήθευσα το claim ότι το `updatedAt` είναι ο sync cursor: `lib/apiList.ts:22` → `withSince` βάζει `updatedAt: { $gte: p.updatedSince }` όταν υπάρχει `updatedSince` param. Ο index ταιριάζει ακριβώς με το query pattern. Στα Item/Task είναι ΚΑΙ sort key (σχόλιο σωστό).
  - Count: 7 models edited (Item, Task, Receipt, Expense, Subscription, Statement, Voucher) = το commit claim «7 synced models» ισχύει. Κανένα pre-existing updatedAt index → μηδέν redundancy.
- **Fixed**: τίποτα (δεν χρειάστηκε). **Flagged**: κανένα νέο εύρημα (regression/secret/shape-break). Οι ουρές (WEB_DEBT 0P1/3P2/2P3 → P2#1 χτίστηκε, MOBILE_PARITY 5 GAP) αμετάβλητες πέρα από το ήδη-καταγεγραμμένο builder progress.
- Staged ΜΟΝΟ `PROGRESS.md` (explicit path, όχι `-A`). Marker `362efc5 → 5457339`. Κανένα Docker/AI job/secret.

## 2026-06-30 (builder — WEB_DEBT P2#1: index updatedAt στα 7 synced models)
- Τι: μετά από αρκετά read-only audit runs, χτίστηκε το κορυφαίο TODO της WEB_DEBT ουράς (P2/S, db). Πρόσθεσα explicit `Schema.index({ updatedAt: -1 })` σε **και τα 7 synced models** (Item, Task, Receipt, Expense, Subscription, Statement, Voucher). Το `updatedAt` είναι το incremental-sync cursor (`lib/apiList.ts withSince` → `updatedAt: { $gte }` σε κάθε mobile sync) και στα Item/Task είναι ΚΑΙ το sort key (`sort({ updatedAt: -1 })`) — πριν unindexed range-scan + in-memory sort σε κάθε sync. Τα Mongoose timestamps ΔΕΝ auto-index-άρουν το updatedAt, οπότε χρειαζόταν ρητό index. Κάθε γραμμή με σχόλιο (Item/Task σημειωμένα ως sort key).
- **Καμία αλλαγή σε route logic / response shape** — αμιγώς additive schema index. Μηδέν αλλαγή στο mobile↔web συμβόλαιο.
- Verify: `apps/web npm run type-check` → **EXIT 0**. Safe rebuild: `docker compose build web` (EXIT 0) → mongo `healthy` → `docker compose up -d web` (mongo waited healthy) → `curl /login` = **200** → web `running=true`, χωρίς restart-loop (`docker ps` → «Up», logs μόνο τα γνωστά αβλαβή stale-bundle «Failed to find Server Action» από ανοιχτό browser tab, ΟΧΙ crash) → `docker builder prune -f` (ανέκτησε ~2.0GB cache). flaresolverr παρέμεινε σβηστό. (Τα indexes χτίζονται από το Mongoose autoIndex στο πρώτο model use μετά το connect, ίδιο proven pattern με τα υπάρχοντα text/compound indexes· δεν έγινε mongosh index-check για να μην αγγίξω secrets/.env.)
- Staged ΜΟΝΟ: τα 7 model αρχεία + `WEB_DEBT.md` + `PROGRESS.md` (explicit paths, όχι `-A`). Καμία αλλαγή σε άλλα working-tree αρχεία/secrets/.env.
- **Επόμενο task:** WEB_DEBT P2#2 — **POST /api/v1/items: whitelist status & category** (εξαγωγή του `STATUS` array σε shared const ώστε POST + PATCH `items/[id]/route.ts` να το μοιράζονται· invalid status → fallback 'researching', ίδιο μοτίβο με `tasks/route.ts`). Καθαρό, no AI, fully testable στο web.
- Needs Achilleas: κανένα νέο.

## 2026-06-30 (ui-auditor — UI Debt re-audit, 2 foundation items DONE, queue refreshed)
- Read-only mobile UI consistency re-audit (apps/mobile vs apps/web design tokens). Μηδέν app code, μηδέν Docker, μηδέν AI jobs. mobile `npx tsc --noEmit` → **EXIT 0**.
- **Builder progress από τελευταίο audit (commit `362efc5`)**: τα 2 foundation items του UI Debt Queue **DONE** — (1) Theme tokens: το `theme.ts` εξάγει πλέον `SPACE`/`RADIUS`/`SIZE` scales + `surface3`/`orange`/`onAccent` (επαλήθευση: theme.ts:22-28). (2) Alpha helper: νέο `alpha(hex,n)` (theme.ts:35-43)· τα 8-digit alpha hex literals → **0** (grep καθαρό, ΗΤΑΝ 8).
- **Ευρήματα ανά dimension (refreshed)**: Tokens **36** hardcoded hex εκτός theme.ts (49→36 μετά το refactor), εκ των οποίων **27** `#000`/`#000000` onAccent literals (builder μετέφερε μόνο τα 5 του SettingsScreen, ~22 μένουν). Shared-theme: foundation ✅ DONE. Reusable components **7 dup-families** ανοιχτά — Input style **15** entries / **11** screens με αποκλίνοντα tokens (live: ItemsScreen:426 borderRadius 12 vs :446 borderRadius 10), `saveText`/`saveBtn`/`addBtn` **12×**, `card`/`badge`/`chip` **12×**· το `ui.tsx` ΑΚΟΜΑ μόνο Header/Spinner/Empty/ErrorText. Theme/dark: **0** light mode / context (web globals.css:37-50 έχει). States: ομοιόμορφα Spinner/Empty/ErrorText (ΟΚ). Adaptive: `react-native-safe-area-context` **εκτός deps** → plain SafeAreaView (App.tsx:74) χωρίς bottom-inset· μόνο **2** maxWidth (καμία στο content). Touch targets: **~7** <44pt (6× 24×24 checkboxes + lineDel/rm/backBtn).
- **Σύνολο queue: 9 items → 2 DONE, 7 TODO** (1 P1 + 4 P2 + 2 P3). UI Debt Queue summary note + counts ανανεωμένα στο MOBILE_PARITY.md· τα per-item file:line refs ξαναελέχθηκαν έγκυρα.
- **Top 3 για τον builder (μικρά + P1 πρώτα):** (1) **Input primitive** [P1/M — εξαγωγή `<Input>`/`<TextArea>` στο `ui.tsx`, ενοποιεί 15 dup styles, σταματά το borderRadius 10-vs-12 drift], (2) **Button + Chip primitives** [P2/M — μηδενίζει τα ~22 inline `#000` button-text literals μέσω `onAccent`], (3) **Touch targets ≥44pt** [P2/S — κοινό `Checkbox`/`hitSlop` στα 6× 24×24].
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (explicit paths, όχι `-A`). Καμία αλλαγή σε app code/secrets/.env. **Needs Achilleas: κανένα νέο** (theme toggle/i18n/AI-engine settings στο mobile παραμένουν backlog· κανένα committed secret).

## 2026-06-30 (docker-health guard — υγιές, χωρίς rebuild)
- **Health**: mongo `healthy` (RestartCount 34 cumulative αλλά up ~2h σταθερό, μηδέν loop), web RestartCount **0** (up ~2h), `/login` → **200**. flaresolverr **σταματημένο** (καλό, μηδέν memory pressure). searxng up (κανονικό μέρος του default stack).
- **Disk**: `docker system df` → Images 3.49GB, Build Cache 565.7MB (0 active/όλα reclaimable), Containers 78.8MB reclaimable. `docker builder prune -f` → ανέκτησε **0B** (το cache ήταν in-use layers του running image, τίποτα ορφανό).
- **Rebuild απόφαση**: `git diff --name-only 62dc4bf..HEAD -- apps/web` → **0 αρχεία**. Το gap (`62dc4bf..d897698`) είναι μόνο mobile (`ActivityScreen`/`SettingsScreen`/`theme.ts`) + docs (PROGRESS/STATUS/WEB_DEBT/MOBILE_PARITY). **Δεν χρειάστηκε rebuild** — μόνο επιβεβαίωση υγείας.
- Marker `62dc4bf → d897698`. Staged ΜΟΝΟ `PROGRESS.md`. Μηδέν Docker mutation πέρα από safe cache prune, μηδέν AI job, μηδέν secret. **Needs Achilleas: κανένα.**

## 2026-06-30 (reviewer — range af7fdc5..362efc5 clean, theme-token refactor verified)
- Εύρος: `af7fdc5..362efc5` (8 commits· 7 docs/monitor/docker, **1 code**: `362efc5` mobile theme tokens). Read-only review + αμφότερα type-checks.
- **Type-checks**: `apps/web` `npm run type-check` → **EXIT 0**· `apps/mobile` `npx tsc --noEmit` → **EXIT 0**. Καμία διόρθωση χρειάστηκε.
- **Diff review του `362efc5`** (`theme.ts` + `ActivityScreen.tsx` + `SettingsScreen.tsx`): καθαρό token refactor, μηδέν αλλαγή συμπεριφοράς/λογικής, μηδέν αλλαγή API shape (δεν αγγίζει το mobile↔web συμβόλαιο), μηδέν secrets.
  - Επαλήθευσα την `alpha(hex, n)` helper αριθμητικά: σωστή 3→6-digit expansion, `parseInt(h,16)` + bitwise mask, επιστρέφει `rgba()` (έγκυρο σε RN StyleSheet).
  - Επαλήθευσα **κάθε** μετατροπή 8-digit hex → `alpha()` ότι διατηρεί το χρώμα: `#00ff8814`=20/255≈.078→.08, `#00d4ff44`=68/255≈.267→.27, `#00d4ff0a`→.04, `#ff475740`=64/255≈.251→.25, `#ff475712`→.07, `#00d4ff0f`→.06, `#00d4ff33`=51/255=.2→.2, `#00ff8815`→.08, `#ffd93d50`=80/255≈.314→.31, `#ff475715`→.08, `#000000aa`=170/255≈.667→.67. Όλες ακριβείς (≤0.003 απόκλιση, μη ορατή).
  - `#000`→`C.onAccent`, `#f5f5f5`→`C.text` swaps σωστά (ίδιες τιμές). `grep` επιβεβαίωσε: **κανένα 8-digit hex literal** δεν μένει στο `apps/mobile/src` (το commit claim ισχύει).
- **Fixed**: τίποτα (δεν χρειάστηκε). **Flagged**: κανένα νέο εύρημα (regression/secret/shape-break). Οι υπάρχουσες ουρές (WEB_DEBT 0P1/3P2/2P3, MOBILE_PARITY 5 GAP) αμετάβλητες — οι docs commits του εύρους τις επιβεβαίωσαν read-only.
- Staged ΜΟΝΟ `PROGRESS.md` (explicit path, όχι `-A`). Marker `af7fdc5 → 362efc5`. Κανένα Docker/AI job/secret.

## 2026-06-30 (web-code-quality auditor — re-confirm, 46 route files, read-only)
- Re-audit ολόκληρης της `/api/v1` επιφάνειας (πλέον **46 route files**, +2 από το προηγ. run· τα νέα = additive όπως statements/plans) από τον κώδικα. Μηδέν app code, μηδέν Docker, μηδέν AI jobs. `npm run type-check` → **EXIT 0**.
- **Counts ανά dimension**: Type safety **0** (grep για `any`/`as any`/`@ts-ignore`/`@ts-expect-error` στα v1 routes → NONE· tsc καθαρό). Auth **0** (μόνο `auth/login` εκτός `withAuth`, σωστά). Error handling **0** (uniform try/catch μέσω `withAuth`· κάθε body read `req.json().catch(()=>({}))`· μηδέν swallowed catch). Reads **0** (όλα τα list endpoints `.lean()` + pagination 1..200). Validation **2** (items POST line 64 `String(b.status||'researching')` χωρίς whitelist ενώ PATCH κάνει· shopping-list/[id] το ΜΟΝΟ [id] route χωρίς 24-hex guard + σιωπηλό `{ok:true}` σε not-found). Consistency **2** (GET /items μόνο του γυρνά `{items}` αντί `listEnvelope {data}`· repeated body-coercion). DB **1** (κανένα από τα synced models δεν έχει explicit `updatedAt` index — grep επιβεβαίωσε· είναι ο `withSince` sync cursor + sort key στα Item/Task).
- **Σύνολο: 0 P1, 3 P2, 2 P3** — η ουρά WEB_DEBT **αμετάβλητη** (κανένα item δεν χτίστηκε ενδιάμεσα· από τότε μόνο docs + το additive `df8c87a`). Επιβεβαίωσα ότι όλα τα πρόσφατα routes (statements/plans, items/[id]/{plans,link-plan,price}, scan/expense, push/register, history, trash/[type]/[id]) είναι σωστά guarded (withAuth, 24-hex id, 400/403/404). Μηδέν νέο εύρημα.
- **Top 3 για τον builder (αμετάβλητα, P2 πρώτα):** (1) **Index updatedAt στα 7 synced models** (P2/S, db — explicit `Schema.index({updatedAt:-1})`· κάθε mobile incremental sync κάνει unindexed `$gte` + in-memory sort στα Item/Task). (2) **POST /api/v1/items — whitelist status & category** (P2/S, api — ίδια STATUS whitelist με το PATCH, εξαγωγή σε shared const). (3) **GET /api/v1/items → listEnvelope alignment** (P2/S, api+ui — `{items}`→`{data}` + ενημέρωση `apps/mobile/src/api.ts`, ή τεκμηρίωση της απόκλισης αν το breaking δεν είναι ΟΚ τώρα).
- Verify: read-only μόνο. grep sweeps (any/ts-ignore/withAuth/id-guard/updatedAt-index/lean) + `npm run type-check` exit 0. Staged ΜΟΝΟ `WEB_DEBT.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.
- Needs Achilleas: κανένα νέο. (Παραμένουν: response-envelope standardization σε ΟΛΑ τα endpoints = breaking change που θέλει συντονισμό με mobile· zod-first σε νέα routes· Docker VM RAM bump.)
Context: δες `CLAUDE.md` (πλήρες ιστορικό), `MOBILE_PARITY.md` (roadmap), `BACKLOG.md` / `TODO.md`.

<!-- docker-validated: 4ca5649 -->

## 2026-06-30 (parity-auditor — 2η σάρωση ημέρας, re-confirm read-only)
- Read-only re-audit web↔mobile από τον κώδικα (όχι docs). Μηδέν app code, μηδέν Docker, μηδέν AI jobs. Inventory: **46 v1 route files** (login + 45 bearer), **16 mobile screens**, **69** exported api fns.
- mobile `npx tsc --noEmit` → **EXIT 0** (καθαρό → κανένα P1 type-gap).
- **Endpoint coverage 1:1**: το mobile `api.ts` καταναλώνει ΚΑΘΕ υπάρχον v1 path (grep των paths ταιριάζει με το route list). Κανένα «web endpoint χωρίς mobile consumer» gap· ό,τι λείπει χρειάζεται **νέο/επεκταμένο** endpoint.
- **Επαλήθευσα ξανά από directory listings + κώδικα και τα 5 GAP ως γνήσια:**
  - Receipts re-scan (P2/M): `api/v1/receipts/[id]/` = {route.ts, add-to-library} → κανένα rescan → valid TODO (AI cost → δομικό verify).
  - Items convert-to-task (P3/S): `api/v1/items/[id]/` = {route.ts, link-plan, plans, price} → κανένα convert-to-task → valid (no AI).
  - Items AI specs (P3/M): ίδιο listing → κανένα ai-fill → valid (AI cost).
  - Expenses/Income full-field edit (P3/S): `PATCH /api/v1/expenses/[id]` (route.ts:11-29) δέχεται ΜΟΝΟ vendor/amount/category/kind/notes/date → ΛΕΙΠΟΥΝ period/recurring/recurringCycle/paymentMethod → «PARTIAL» ισχύει (builder επεκτείνει ΠΡΩΤΑ το PATCH).
  - Notifications unread badge (P3/S): pure-mobile UI· `GET /api/v1/notifications` υπάρχει+καταναλώνεται → valid.
- **Από το προηγ. parity audit μόνο 1 code commit**: `362efc5` (mobile theme tokens) = UI-Debt item, ΟΧΙ parity → queue σωστά αμετάβλητο. Πυρήνας parity κλειστός (μηδέν P1/S· πρώην P1 statements overview = DONE).
- **Σύνολο: 0 DONE-corrections, 5 GAP (1 P2 + 4 P3), 0 νέα NEEDS DECISION.**
- **Top 3 για τον builder:** (1) Receipts re-scan OCR/text [P2/M, AI → δομικό verify], (2) Items convert-to-task [P3/S, no AI], (3) Expenses/Income full-field edit [P3/S, no AI — επέκταση PATCH πρώτα].
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (parity-auditor — re-audit Build Queue από τον κώδικα, read-only)
- Read-only audit web↔mobile από τον κώδικα (όχι docs). Μηδέν app code, μηδέν Docker, μηδέν AI jobs. Inventory: **45 v1 routes** (login + 44 bearer), **16 mobile screens**, **78** exported api fns.
- mobile `npx tsc --noEmit` → **EXIT 0** (καθαρό· κανένα type error → κανένα P1 type-gap).
- **Endpoint coverage**: το mobile `api.ts` καταναλώνει **κάθε** υπάρχον v1 endpoint (grep των paths ταιριάζει 1:1 με το route list). Άρα κανένα «web endpoint χωρίς mobile consumer» gap· ό,τι λείπει χρειάζεται **νέο** endpoint ή επέκταση υπάρχοντος.
- **Επαλήθευσα από τον κώδικα και τα 5 εναπομείναντα Build-Queue gaps ως γνήσια**:
  - Receipts re-scan (P2/M): `api/v1/receipts/[id]/` έχει μόνο `{route.ts, add-to-library}` → κανένα `rescan` → valid TODO (AI cost → δομικό verify).
  - Items convert-to-task (P3/S): `api/v1/items/[id]/` έχει μόνο `{route.ts, link-plan, plans, price}` → κανένα `convert-to-task` → valid TODO (no AI).
  - Expenses/Income full-field edit (P3/S): `PATCH /api/v1/expenses/[id]` (route.ts:11-29) δέχεται ΜΟΝΟ vendor/amount/category/kind/notes/date → ΛΕΙΠΟΥΝ period/recurring/recurringCycle/paymentMethod → spec «PARTIAL» ισχύει (builder επεκτείνει ΠΡΩΤΑ το PATCH).
  - Items AI specs (P3/M): κανένα `items/[id]/ai-fill` → valid TODO (AI cost).
  - Notifications unread badge (P3/S): pure-mobile UI· `GET /api/v1/notifications` υπάρχει + καταναλώνεται ήδη → valid TODO.
- **Status του πρώην P1**: Statements installment-plan overview = **DONE** (commit df8c87a, επιβεβαιωμένο: route `statements/plans` υπάρχει, mobile το καλεί). Ανανέωσα το Build-Queue header note ώστε ο builder να βλέπει ως ενεργό top-TODO το Receipts re-scan.
- **Σύνολο: πυρήνας parity κλειστός (μηδέν P1/S), 5 GAP στο queue (1 P2 + 4 P3), 0 νέα NEEDS DECISION.**
- **Top 3 για τον builder:** (1) Receipts re-scan OCR/text [P2/M, AI → δομικό verify], (2) Items convert-to-task [P3/S, no AI], (3) Expenses/Income full-field edit [P3/S, no AI — επέκταση PATCH πρώτα].
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## Needs Achilleas
- (Αμετάβλητα από προηγούμενα) theme toggle / 8-language i18n / AI-engine + storage(OneDrive) settings στο mobile, remote push creds (EAS dev build + APNs key), Docker VM RAM bump (mongo OOM στα builds), response-envelope standardization σε όλο το `/api/v1` (breaking, χρειάζεται συντονισμό με mobile). Κανένα νέο.

## 2026-06-30 (docker-health guard — rebuild + validate 62dc4bf)
- Υγεία πριν: mongo **healthy** (Up 11min, RestartCount 34 = ιστορικό OOM από παλιά builds, όχι τρέχον loop), web **running** RestartCount 0, flaresolverr **exited** (το άφησα σταματημένο), searxng running. Κανένα restart-loop, κανένα OOM τώρα.
- Disk: `docker system df` → Images 3.49GB, Build Cache 565MB (μικρό). Καμία πίεση.
- Απόφαση rebuild: το diff `1d2a9be..HEAD` άγγιζε web runtime κώδικα (`apps/web/src/app/api/v1/statements/plans/route.ts`, ο builder είχε ενσωματώσει το νέο installment-plans route μετά το τελευταίο validated marker) → rebuild δικαιολογημένο.
- Ασφαλές rebuild: `docker compose build web` (image μόνο, layers CACHED → γρήγορο) → επιβεβαίωσα mongo healthy → `docker compose up -d web` (recreated, mongo waited healthy πρώτα) → `curl /login` = **200** στην 1η προσπάθεια → web RestartCount παρέμεινε **0** (δεν ανέβηκε) → `docker builder prune -f` (3.15MB reclaimed, σχεδόν όλα cached).
- Αποτέλεσμα: stack υγιές, /login 200, marker `1d2a9be → 62dc4bf`.
- Needs Achilleas: κανένα νέο. (Παραμένει: bump RAM στο Docker VM — το mongo RestartCount 34 αντανακλά επαναλαμβανόμενα OOM σε παρελθόντα builds, αν και αυτό το build πέρασε καθαρά λόγω cache.)

## Κανόνες (μην τους σπάσεις)
- Stage ΜΟΝΟ όσα άλλαξες, με explicit `git add <path>`. ΠΟΤΕ `git add -A` / `.` / `commit -a` (το working tree έχει συχνά parallel uncommitted αλλαγές του Αχιλλέα).
- Docker rebuild: `docker compose build web` ΠΡΩΤΑ, περίμενε mongo `healthy`, μετά `docker compose up -d web`. ΠΟΤΕ `up --build` (πνίγει τη mongo σε CPU, γίνεται unhealthy, και το web δεν σηκώνεται). **Μετά το build τρέξε `docker builder prune -f`** (ΜΟΝΟ cache, ασφαλές) — αλλιώς ο δίσκος γεμίζει.
- ⚠ **Το Docker VM είναι στενό (~1.9GB RAM, 31GB disk).** Στις 2026-06-29 ~5 διαδοχικά builds γέμισαν 21GB build cache + **OOM-σκότωσαν τη mongo** (42 restarts, app down). Κράτα το **flaresolverr ΣΒΗΣΤΟ** (είναι profile `scraper`, ~το άναψε άλλη συνεδρία) εκτός αν χρειάζεται ενεργά.
- Όχι destructive: κανένα `volume rm` / `down -v` / `system prune` / `builder prune --all`, κανένα drop collection, κανένα force-push, κανένα άγγιγμα σε secrets/`.env`. (Το `builder prune -f` χωρίς `--all` ΕΙΝΑΙ ασφαλές: σβήνει μόνο unused cache, όχι data/volumes/images.)
- Bulk AI = κόστος ανά Anthropic call, όχι unattended.

## Κατάσταση
- Mobile companion app (`apps/mobile`, Expo SDK 54) σε build-out προς parity. Web REST API v1 κάτω από `apps/web/src/app/api/v1/` (bearer auth).
- MOBILE_PARITY roadmap: #1 edit ✅, #2 task statuses ✅, #3 receipt verify/edit ✅, #4 AI fill ✅, #5 statement detail ✅, #6 mobile Settings ✅.
- #7 Activity — Trash ✅, Jobs ✅, History ✅, Alerts ✅. #8 push — in-app feed ✅ + **push pipeline (token registry + Expo send + alert wiring) ✅**· λείπει ΜΟΝΟ EAS dev build + Apple APNs key = Needs Achilleas (βλ. cont.⁷).
- **MOBILE_PARITY roadmap #1–#8 όλα ✅.** Επόμενα logical (όχι roadmap): mobile Receipts edit (line items/re-scan/archive), push delivery (γ) όταν έρθουν creds.

## 2026-06-29 (seed)
- Τι: στήθηκε αυτό το log + το daily scheduled task `pharos-daily-dev`. Ολοκληρώθηκε το roadmap **#4 (AI fill)**: 3 νέα endpoints (`/api/v1/ai/subscription`, `/api/v1/scan/voucher`, `/api/v1/items/import`) + mobile UI (✦ buttons σε Subscriptions / Vouchers / Items add rows).
- Verify: web HTTP 200, restarts=0· tsc καθαρό (web + mobile)· curl και τα 3 endpoints (Netflix → €13.99/mo monthly streaming· "SAVE15" voucher → code/store/discount/expiry· import delegates σωστά στο pipeline, graceful error σε unreachable URL). Commits `62eae4c`, `a461156`.
- Επόμενο task: **#5 — Statement transactions/installments στο mobile.** Web πηγή: `apps/web/src/app/statements/`. Αν λείπει, πρόσθεσε `GET /api/v1/statements/[id]` (transactions + `installmentInfo` + computed installment plans), μετά mobile `StatementsScreen` → detail με λίστα transactions + installment plans (read-first· link/edit σε επόμενο run).

## 2026-06-29 (cont. — #5 + infra recovery)
- Τι: **#5 ✅** — `GET /api/v1/statements/[id]` (header + transactions + per-charge installment `{current,total}`, sorted μεγαλύτερη χρέωση πρώτη) + mobile `StatementsScreen` bottom-sheet detail (total/min/paid + transactions + cyan N/M badge στις δόσεις, credits σε accent). Commits: route.ts στο `cd3600f` (μπήκε σε commit τρίτου με `git add -A`), screen στο `48a60e5`.
- Verify: web HTTP 200· tsc καθαρό (web+mobile)· πραγματικό fetch → "Εθνική Mastercard 7791, 2026-06, 8 txns, 5 installment charges (QUEST 4/6, PLAISIO 11/12)".
- ⚠ **Infra incident (διορθώθηκε):** η mongo OOM-crash-loop-άρισε (42 restarts, `oom=true`) γιατί (α) ~5 builds μάζεψαν **21GB build cache** (δίσκος 84%) και (β) έτρεχε το flaresolverr σε στενό 1.9GB RAM VM. Fix: `docker compose stop flaresolverr` + `docker builder prune -f` (έπεσε 21.7GB→0.5GB cache) → mongo healthy, web up.
- **Needs Achilleas:** σκέψου να ανεβάσεις το Docker Desktop → Resources → **RAM** (π.χ. 4GB) ώστε το mongo+web+flaresolverr να μη χτυπάνε OOM. Το flaresolverr το άφησα ΣΒΗΣΤΟ· ξανάνοιξέ το με `docker compose --profile scraper up -d flaresolverr` αν το χρειάζεσαι.
- Επόμενο task: **#6 — mobile Settings** (theme/currency, budgets view, notification toggles). Διάβασε `apps/web/src/app/settings/` + `getAppSettings`· πιθανό νέο read endpoint `GET /api/v1/settings`. ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build.

## 2026-06-29 (cont.² — #6)
- Τι: **#6 ✅** — `GET /api/v1/settings` (currency, defaultVatRate, period, budgets[] με this-month spent/limit per category, most-over πρώτα) + mobile `SettingsScreen`: νέα **PREFERENCES** (currency, VAT) + **BUDGETS · THIS MONTH** (per-category progress bars, κόκκινο όταν over· εμφανίζεται μόνο αν υπάρχουν budgets). Commit `d92b538`.
- Verify: build χρειάστηκε 2η προσπάθεια (1η: transient Docker buildkit "rpc EOF" → είχε σηκώσει το παλιό image → settings 404· retry IMG BUILD=0). Τελικά no-token→401, με token → `currency=EUR · VAT=24% · 0 budgets` (ο χρήστης δεν έχει ορίσει budgets ακόμα· το section εμφανίζεται μόλις οριστούν στο web Settings → Money → Budgets). web restarts=0, mongo healthy, oom=false, cache pruned.
- Επόμενο task: **#7 — Activity (Trash / Jobs / History) στο mobile.** Web πηγές: `/trash`, `/jobs`, `/history` (+ τα actions τους). Πρόσθεσε read endpoints (π.χ. `GET /api/v1/trash`, `GET /api/v1/jobs`) + restore/purge όπου ασφαλές· mobile screen με tabs. ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + κράτα flaresolverr σβηστό.

## 2026-06-29 (cont.³ — #7 Trash)
- Τι: **#7 (Trash) ✅** — πρώτο κομμάτι του Activity. Νέα bearer endpoints: `GET /api/v1/trash` (όλα τα soft-deleted, newest-first· auto-purge >30d), `PATCH /api/v1/trash/[type]/[id]` (restore), `DELETE /api/v1/trash/[type]/[id]` (permanent purge, **admin-only** μέσω `user.role`). Για να μην τρέξει το cookie-based `requireAdmin()` μέσα σε bearer route, έσπασα το `purgeFromTrash` σε guard-wrapper + νέο unguarded `purgeTrashEntry` (το `getTrash`/`emptyTrash` καλούν πλέον το entry· καθαρίζει και ένα latent θέμα όπου το auto-purge ενός member θα πετούσε στο requireAdmin). Mobile: νέο `ActivityScreen` (λίστα με icon/type/subtitle/deletedAt + Restore + Delete-forever μόνο για admin) + Drawer «🗑 Trash» + ScreenKey/App routing + api client (`getTrash`/`restoreTrash`/`purgeTrash`).
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· web image build EXIT=0, mongo healthy πριν το `up`, login HTTP 200, restarts=0, `docker builder prune -f` μετά. Smoke (με bearer token από container mongo URI, ποτέ printed): `GET /trash` → 32 rows (item/receipt/task, σωστό shape), PATCH bad-type→400, PATCH bad-id→400, no-token→401. Το `restoreFromTrash`/`purgeTrashEntry` είναι τα ΙΔΙΑ proven web server actions (χρησιμοποιούνται ήδη από το web Trash UI), απλώς exposed via REST. (Το restore round-trip δεν ολοκληρώθηκε γιατί το `docker exec mongosh` για token flake-άρισε μετά την 1η επιτυχία· η συμπεριφορά είναι ήδη αποδεδειγμένη.)
- Επόμενο task: **#7 συνέχεια — Jobs + History στο mobile.** Web πηγές: background AI jobs (`models/Job.ts` + `app/jobActions.ts` `getActiveJobs`) και AI conversation history. Πρόσθεσε `GET /api/v1/jobs` (active/recent) + read endpoint για history, μετά κάνε το `ActivityScreen` tabbed (Trash | Jobs | History). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό.

## 2026-06-29 (cont.⁴ — #7 Jobs + History → Activity ολοκληρωμένο)
- Τι: **#7 ✅ πλήρες** — δύο νέα read-only bearer endpoints: `GET /api/v1/jobs` (πρόσφατα background AI jobs, running-first· wrapper του proven `getJobs`) + `GET /api/v1/history` (αποθηκευμένες AI command-bar συνομιλίες· wrapper του `getConversations`). Mobile `ActivityScreen` έγινε **tabbed (Trash | Jobs | History)**: Jobs tab με status badge + progress bar + live auto-refresh (4s) όσο τρέχει job· History tab με tap-to-expand thread (user/assistant bubbles + ✓action chips). Drawer label «🗑 Trash» → «🗂 Activity». `api.ts`: νέα `getJobs`/`getHistory` + `JobRow`/`ConversationRow` types. Commit `21fb484`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· web image build EXIT=0, mongo healthy (oom=false) πριν το `up`, login HTTP 200, restarts=0, `docker builder prune -f` μετά (cache 564MB→0). Smoke: `/api/v1/jobs` & `/api/v1/history` no-token → 401 με σωστό error body (route registered + `withAuth` τρέχει· όχι 404). Πλήρες authenticated round-trip ΔΕΝ έγινε γιατί η mongo απαιτεί auth και δεν αγγίζω secrets/.env για token· τα δύο handlers είναι ΟΙ ΙΔΙΕΣ proven server actions που τροφοδοτούν τα web /jobs + /history pages (ίδιο pattern με το ήδη-verified trash route).
- Επόμενο task: **#8 — push notifications** (χρειάζεται συσκευή/simulator → δεν testable unattended). Εναλλακτικά (testable τώρα): Settings parity — theme toggle / language / ntfy notification prefs στο mobile (`GET /api/v1/settings` υπάρχει ήδη· χρειάζεται πιθανό PATCH για prefs), ή Items detail/edit + URL-import UI στο mobile (το `/api/v1/items/import` endpoint υπάρχει ήδη από #4, λείπει μόνο το mobile UI). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό.

## 2026-06-29 (cont.⁵ — Items tap-to-edit, κλείνει το #1)
- Τι: το mobile Items ήταν list+add+delete μόνο. Πρόσθεσα **tap-to-edit modal**: title, status chip-picker (researching…deferred), category, current price + target price, specs/notes. Save μέσω του ήδη υπάρχοντος `PATCH /api/v1/items/:id` (νέο `updateItem` στο api client). Long-press παραμένει delete. Κλείνει το roadmap **#1** (το Items edit ήταν το τελευταίο κομμάτι). MOBILE_PARITY ενημερωμένο. Commit `83b879f`.
- Verify: mobile tsc EXIT=0. ΧΩΡΙΣ Docker rebuild — δεν άλλαξε web runtime (το PATCH endpoint υπήρχε ήδη + είναι proven από το web UI). Mobile δεν testable unattended (χρειάζεται simulator)· βασίστηκα σε tsc + code review.
- Επόμενο task: **#8 push notifications** (χρειάζεται συσκευή → όχι unattended). Testable εναλλακτικές: (α) mobile Items **URL-import UI βελτίωση** / detail με links+photos (read), (β) Settings parity — ntfy notification prefs (πιθανό PATCH `/api/v1/settings`), (γ) mobile **Receipts edit** (line items / re-scan / archive — το web έχει πλούσιο detail, mobile είναι read-only). ΘΥΜΗΣΟΥ `docker builder prune -f` αν κάνεις build + flaresolverr σβηστό.

## 2026-06-29 (cont.⁶ — #8 in-app notification feed)
- Τι: **#8 (in-app μισό) ✅** — το testable κομμάτι των push notifications. Νέα bearer endpoints: `GET /api/v1/notifications` → `{items, unread}` (live deal/installment/warranty/system feed, wraps το proven `getNotifications`) + `PATCH /api/v1/notifications` (`{id}`=mark one read, χωρίς id=mark all). Mobile `ActivityScreen`: νέο **Alerts** tab (default, πρώτο) — kind icon, title/body, relative time, unread dot, tap-to-read, «Mark all (N)». `api.ts`: `getNotifications`/`markNotificationRead` + `NotificationRow`/`NotifKind`. Commit `91314c4`.
- Verify: tsc καθαρό (web+mobile)· IMG BUILD=0, mongo healthy/oom=false, login 200, restarts=0, prune μετά· no-token→401· με token → `{items:[], unread:0}` (σωστό: τα 4 notifications στη DB είναι soft-deleted/dismissed → το getNotifications με το soft-delete plugin σωστά τα εξαιρεί· το raw mongosh count δείχνει 4 γιατί δεν φορτώνει το plugin). Το feed γεμίζει όταν τρέξει το `runAlertChecks` (Settings → Notifications → «Check & notify now» ή cron).
- **Needs Achilleas (remote push):** το «ξύπνημα» της συσκευής με push χρειάζεται (α) **EAS dev build** (`npx eas build --profile development --platform ios`· το Expo Go έκοψε το iOS remote push από SDK 53+), (β) **Apple APNs key** (Apple Developer account → Keys → APNs· το `eas credentials` το ανεβάζει), (γ) μετά: `expo-notifications` getExpoPushTokenAsync → POST σε νέο `/api/v1/push/register` (αποθήκευση token στο User) → `sendExpoPush` μέσα στο `runAlertChecks` (δίπλα στο ntfy). Το (γ) είναι buildable όποτε υπάρχουν τα creds· το (α)+(β) είναι δικά σου.
- Roadmap MOBILE_PARITY **#1–#8 όλα ✅** (το #8 = in-app feed· remote-push delivery = το παραπάνω Needs-Achilleas). Επόμενα logical: mobile **Receipts edit** (line items/re-scan/archive· web πλούσιο, mobile βασικό), ή push (γ) όταν έρθουν creds.

## 2026-06-29 (cont.⁶ — editable Settings: φάση 1 του «όλα τα web settings στο mobile»)
- Context: ο Αχιλλέας ζήτησε **πλήρες parity** (όλα τα web features + settings στο mobile), με έμφαση στα settings. Το ξεκινάω σε φάσεις· φάση 1 = editable Settings.
- Τι: το mobile Settings ήταν read-only. Τώρα **editable**: currency (EUR/USD/GBP), default VAT, default warranty months, warranty alert days, auto-add-stores toggle, **per-category budgets** (add/edit/remove από τη λίστα expense categories), **ntfy** URL + enable + «send test». Web: `GET /api/v1/settings` επεκτάθηκε (όλο το preference set + expenseCategories) + νέα `PATCH /api/v1/settings` (mirror του non-admin-gated saveDefaults/saveNtfy/saveBudgets σε AppConfig + invalidateAppSettings) + `POST /api/v1/settings/test-notify` (wraps sendTestNtfy). Mobile api: `updateSettings`/`testNotify` + expanded `AppSettings`. Commit `a079229`.
- Verify: tsc καθαρό (web+mobile EXIT=0)· web image build EXIT=0, mongo healthy/oom δεν ελέγχθηκε ρητά αλλά restarts=0, login 200, `docker builder prune -f` μετά. Smoke: GET/PATCH /settings + POST /test-notify → 401 no-token (registered + auth runs, όχι 404). Πλήρες authenticated round-trip όχι (mongo auth, δεν αγγίζω .env)· οι writes είναι mirror των ήδη-proven web actions. ⚠ Πρόσεξα ότι το `apps/web/src/models/User.ts` ήταν modified από παράλληλη δουλειά (Αχιλλέα) — ΔΕΝ το έσταξα.
- Διόρθωσα stale parity: το mobile Receipts ΕΧΕΙ ήδη edit/verify/archive (store/total/verified) — δεν είναι gap.
- **Remaining parity backlog (επόμενες φάσεις, priority order):**
  1. Settings → **payment cards CRUD** (web actions: statements/cards.ts createCard/updateCard/deleteCard/toggleCardActive → νέα endpoints + mobile section).
  2. Settings → **stores + dropdown lists** editor (listStores/saveStore/deleteStore, getListsForEditor/saveList).
  3. **Calendar projection** — installments aggregated/μήνα + recurring bills/income + monthly in/out totals (web `/calendar` getCalendarAgenda· επέκταση του `GET /api/v1/calendar`).
  4. **Expenses scan-a-bill** (AI) + edit + recurring badge (web uploadExpense/parseExpense· νέο `POST /api/v1/scan/expense`).
  5. **Statements** installment-plan link/merge (write ops· computeInstallmentPlans + linkPlanToItem/bindInstallmentGroup).
  6. **Items** detail PricePanel (best price/where-to-buy/log price) + photos + links.
  7. Settings → **theme toggle (light mode)** — ΜΕΓΑΛΟ refactor (το `C` palette είναι const-imported σε κάθε screen· θέλει theme context). Άφησέ το τελευταίο.
  8. Settings → AI engine/features/storage/OneDrive — power-user, χαμηλή προτεραιότητα για mobile.
- Επόμενο task: **#1 παραπάνω — payment cards CRUD στο mobile Settings.** ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου αρχεία (το User.ts είναι του Αχιλλέα).

## 2026-06-29 (cont.⁷ — #8 remote push pipeline, part γ buildable)
- Context: ο Αχιλλέας («αποδέχομαι ό,τι χρειάζεται») ενέκρινε να φτιαχτεί το buildable κομμάτι του remote push· τα Apple/EAS creds τα αναλαμβάνει αυτός.
- Τι: **server push pipeline ✅** — `User.pushTokens` (νέο field)· `lib/expoPush.ts` (`sendExpoPush` → Expo push service, chunks 100, validate `ExponentPushToken[...]`, never-throws· `pushAllDevices` διαβάζει όλα τα tokens)· `POST/DELETE /api/v1/push/register` (store/remove token στον bearer user)· wired fire-and-forget στο `runAlertChecks` δίπλα στο `dispatchAlert` (2 γραμμές, ΜΟΝΟ δικές μου). **mobile ✅** — `expo-notifications`+`expo-device`, `src/push.ts` `registerForPush`/`unregisterForPush` (permission→Expo token→register· πλήρως guarded: no-op σε Expo Go/simulator/χωρίς EAS projectId), κλήση on-auth + cleanup on-sign-out στο `App.tsx`. Commit `2156a83`.
- Verify: tsc καθαρό (web+mobile)· IMG BUILD=0, login 200, restarts=0, prune μετά. Smoke (token από container, ποτέ printed): no-token→401, bad-format→400, valid `ExponentPushToken[...]`→200 + stored (count 1), DELETE→200 + remaining 0 (καθάρισα το test token → user data clean). ⚠ `oom=true` ξαναεμφανίστηκε (mongo OOM-killed στο build, recovered healthy) → ενισχύει το RAM-bump Needs-Achilleas.
- **Needs Achilleas (ΜΟΝΟ αυτό λείπει για end-to-end push):** (α) `npx eas build --profile development --platform ios`, (β) Apple APNs key (Apple Developer → Keys → APNs· `eas credentials` το ανεβάζει). Μόλις υπάρχουν, το `registerForPush` παίρνει πραγματικό token → οι alerts (deals/installments/warranties) χτυπάνε το κινητό ΧΩΡΙΣ άλλο κώδικα.
- Επόμενο task: η parallel-parity λίστα παραπάνω (#1 cards CRUD κ.λπ.). ΘΥΜΗΣΟΥ `docker builder prune -f` + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths (πολλά αρχεία τα αγγίζει παράλληλη συνεδρία).

## 2026-06-30 (parity backlog #1 — payment cards CRUD στο mobile)
- Τι: **#1 ✅** από τη parallel-parity λίστα. Νέα bearer endpoints: `GET /api/v1/cards` (όλες οι κάρτες, active-first then name) + `POST /api/v1/cards` (create) + `PATCH /api/v1/cards/[id]` (update· χρησιμεύει και ως on/off toggle με `{active}`) + `DELETE /api/v1/cards/[id]` (μόνιμη διαγραφή· οι κάρτες ΔΕΝ είναι soft-deleted, mirror του web `deleteCard`). Input validated/sanitized: last4 digits-only cap 4, kind enum (credit/debit), type enum (mastercard/visa/amex/maestro/other), clamped creditLimit, color. Είναι το ίδιο proven συμπεριφορικό μοτίβο των web `createCard/updateCard/deleteCard/toggleCardActive` (`statements/cards.ts`), εκτεθειμένο ως REST. **mobile ✅** — νέο **PAYMENT CARDS** section στο `SettingsScreen` (self-contained `CardsSection`: λίστα με color dot + bank/kind/••last4 + ON/OFF pill toggle + ✕ delete-with-confirm) + add/edit bottom-sheet (`CardEditor`: name, bank, last4, kind/type chips, color swatches, credit limit, notes). `api.ts`: `Card`/`CardInput` types + `getCards`/`createCard`/`updateCard`/`deleteCard`. MOBILE_PARITY ενημερωμένο. Commit `4a5ee8d`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· IMG BUILD ok, mongo healthy/oom=false πριν+μετά το `up`, login 200, restarts=0, `docker builder prune -f` μετά. **Πλήρες authenticated round-trip** (token από container, ΠΟΤΕ printed): GET→1 κάρτα (η πραγματική του Αχιλλέα, άθικτη)· POST `{last4:"abc9999"→"9999", creditLimit:"1500.5"→1500.5, kind:debit, type:visa}` σωστά sanitized· PATCH `{active:false,name}` εφαρμόστηκε· GET-after reflect· DELETE→ok· DELETE-again→404. Καθάρισα το test card → user data clean. No-token σε όλα τα verbs → 401.
- Επόμενο task: **parity backlog #2 — stores + dropdown lists editor στο mobile** (web actions: `listStores`/`saveStore`/`deleteStore` + `getListsForEditor`/`saveList`· νέα read/write endpoints + mobile section). Εναλλακτικά #3 Calendar projection (installments/μήνα + recurring + in/out totals) ή #4 Expenses scan-a-bill. ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont. — parity backlog #2: stores + dropdown lists editor στο mobile)
- Τι: **#2 ✅** από τη parallel-parity λίστα. Νέα bearer endpoints: `GET /api/v1/stores` (όλα τα stores, seeded, name-sorted) + `POST /api/v1/stores` (create· aliases array ή comma-string → lowercased/split, empty→[name.toLowerCase()], unique-name → 400) + `PATCH /api/v1/stores/[id]` (update name/url/aliases, auto=false, duplicate-name → 400) + `DELETE /api/v1/stores/[id]` (μόνιμο· τα stores ΔΕΝ είναι soft-deleted, mirror του web `deleteStore`). + `GET /api/v1/lists` (τα 3 editable category taxonomies = `getListsForEditor`) + `PATCH /api/v1/lists` `{key,values}` (wraps το proven `saveList`· empty/==default → clears override). Όλα invalidate-άρουν το store cache / app settings όπως οι web actions. **mobile ✅** — δύο νέα sections στο `SettingsScreen`: **STORES** (`StoresSection`: search box, λίστα capped στα 12 χωρίς query (90+ stores), needs-review badge στα auto, tap-to-edit + ✕ delete-with-confirm, `StoreEditor` bottom-sheet name/url/aliases) + **DROPDOWN LISTS** (`ListsSection`/`ListEditor`: ανά taxonomy chip add/remove + Save (disabled όταν not-dirty) + reset-to-default· rev-counter remount για fresh state μετά το save). `api.ts`: `StoreRow`/`StoreInput`/`ListEntry` types + `getStores`/`createStore`/`updateStore`/`deleteStore` + `getLists`/`saveList`. MOBILE_PARITY ενημερωμένο. Commit `a5e3d29`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· IMG BUILD=0, mongo healthy/oom=false πριν+μετά, login 200, web restarts=0, `docker builder prune -f` μετά (reclaimed 2GB), flaresolverr σβηστό. No-token σε όλα τα 6 verbs → 401. **Πλήρες authenticated round-trip** (token από container, ΠΟΤΕ printed): GET stores → 200/92 (πραγματικά δεδομένα Αχιλλέα, άθικτα)· POST `{aliases:"ZZ Test, ZZtest.gr"}` → 201 με `["zz test","zztest.gr"]`· POST dup name → 400· PATCH → 200· DELETE → 200, DELETE-again → 404· GET lists → 200 (`expenseCategories(12),itemCategories(9),subscriptionCategories(7)`)· PATCH bad key → 400. Το test store διαγράφηκε → user data clean. **ΣΚΟΠΙΜΑ ΔΕΝ** mut-άρισα τα πραγματικά taxonomy lists (το `saveList` write path είναι ήδη proven από το web UI).
- Επόμενο task: **parity backlog #3 — Calendar projection** (installments aggregated/μήνα + recurring bills/income projected + monthly in/out totals· web `/calendar` `getCalendarAgenda` → επέκταση του `GET /api/v1/calendar`, που τώρα γυρνά μόνο renewals/voucher/warranty). Εναλλακτικά #4 Expenses scan-a-bill (AI· νέο `POST /api/v1/scan/expense`) ή #6 Items PricePanel. ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.² — parity backlog #3: Calendar 3-month projection στο mobile)
- Τι: **#3 ✅** από τη parallel-parity λίστα. Το `GET /api/v1/calendar` γύρναγε μόνο flat `events` (renewal/voucher/warranty). Τώρα επιστρέφει το πλήρες 3μηνο money agenda του web `/calendar` (mirror του `getAgenda` στο `apps/web/src/app/calendar/page.tsx`, χωρίς i18n — plain English labels): `months[]` με `{key,label,out,inc,entries[]}` όπου entries = subscription renewals (stepped ανά cycle), card installments **aggregated/μήνα** (pinned line, `computeInstallmentPlans` + remaining≥i+1), recurring bills/income projected από το latest κάθε series, warranty + voucher expiries· + `dueThisMonth`. Κράτησα και το flat `events` (renewal/voucher/warranty) για backward-compat. **mobile ✅** — το `CalendarScreen` ξαναγράφτηκε από flat `FlatList` σε **3μηνο `SectionList`**: month header με in/out totals (+inc πράσινο / -out κόκκινο), kind-coloured entries (renewal cyan / installments purple pinned / bill red / income accent / warranty purple / voucher gold) με sub-line + amount (income με +, installments χωρίς ημερομηνία), «Nothing this month» σε κενό μήνα. `api.ts`: νέοι τύποι `CalEntry`/`CalMonth`/`CalEntryKind` + `getCalendar` επιστρέφει `{currency,dueThisMonth,months,events}`. MOBILE_PARITY ενημερωμένο. Commit `7fd0d11`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0, mobile σε σωστό cwd)· IMG BUILD=0, mongo healthy/oom=false πριν+μετά το `up`, login 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr σβηστό. No-token → 401. **Πλήρες authenticated round-trip** (token από container, ΠΟΤΕ printed): status 200, `dueThisMonth=221.51`, months = «June 2026 [out=221.51, 1 entry] | July 2026 [out=191.24, 5 entries] | August 2026 [out=116.72, 2 entries]», entryKinds `{installments:3, renewal:2, warranty:3}`, eventsLen 5 — ταιριάζει ακριβώς με το web CalendarClient (June €221.51, July + Claude Pro renewal + 3 warranties, Aug). Πραγματικά δεδομένα Αχιλλέα, μηδέν mutation.
- Επόμενο task: **parity backlog #4 — Expenses scan-a-bill (AI)** (νέο `POST /api/v1/scan/expense` wrap του `parseExpenseImage`/`parseExpenseText` + mobile camera/paste → prefill expense form) ή **#6 Items PricePanel** (best price / where-to-buy / log price / photos / links στο mobile detail). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.³ — parity backlog #6: Items PricePanel στο mobile)
- Τι: **#6 ✅** από τη parallel-parity λίστα. Το mobile Items detail ήταν μόνο edit-form (title/status/category/price/target/specs)· τώρα έχει **πλήρες read-only PricePanel** πάνω από τη φόρμα, mirror του web `components/PricePanel.tsx`. Νέο `GET /api/v1/items/[id]` → full detail (links, priceHistory newest-first, photos, warranty, purchase, location, serial) + **computed `price` block** (`priceStatus` port: bestNow = φθηνότερο priced link αλλιώς currentPrice, lo/hi/target/trend, verdict deal/dropping/rising/good/high/none, stores sorted· **ίδια λογική με το web** ώστε να μη γίνεται drift). Νέο `POST /api/v1/items/[id]/price` `{price,store?}` → wraps το proven `logItemPrice` action (append history + update currentPrice). **mobile ✅** — `PriceBlock` component στο `ItemsScreen`: hero best-now + verdict chip, position bar (low/target/high markers, no gradient — solid track), **WHERE TO BUY** tap→`Linking.openURL`, target+«Log a price» inline (price+store → POST → reload), full-history collapsible (≤30), photos horizontal strip (`fileSource` με auth header), links list (tap→open), warranty/bought-from/location lines. `api.ts`: τύποι `ItemDetail`/`PriceStatus`/`ItemLink`/`PriceEntry`/`PriceStore`/`Verdict` + `getItem`/`logItemPrice`. MOBILE_PARITY ενημερωμένο. Commit: (επόμενο).
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0· χρειάστηκε `DimensionValue` cast για το `left:'X%'` του position-bar dot/tick)· IMG BUILD ok, mongo healthy/oom=false πριν+μετά το `up`, login 200, web restarts=0, `docker builder prune -f` μετά, flaresolverr σβηστό. No-token σε GET detail + POST price → 401. **Πλήρες authenticated round-trip** (token σε in-memory var, ΠΟΤΕ printed/persisted — ο classifier σωστά μπλόκαρε write σε file): GET στο πραγματικό USW-Pro-XG-8-PoE → 4 links / 23 history / 3 photos, `price.bestNow=€554.48 @ e-shop.gr`, lo/hi=475/625, verdict `none` (pos≈0.53, no target — ταιριάζει ακριβώς με το web logic), read-only/άθικτο. Throwaway test item: POST price 99.5 @ amazon.de → ok + currentPrice/history updated + bestNow computed· bad price(0) → 400· DELETE → soft· μετά hard-purge (δικό μου test item) → μηδέν leftover.
- Επόμενο task: **parity backlog #4 — Expenses scan-a-bill (AI)** (νέο `POST /api/v1/scan/expense` wrap του `parseExpenseImage`/`parseExpenseText` + mobile camera/paste → prefill expense form· ⚠ verify ΧΩΡΙΣ πραγματικό AI call — κόστος· δες structural 401/400 μόνο) ή **Items installment-plan link** στο mobile detail (web `linkPlanToItem`· διάβασε `statements/actions.ts`). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁴ — parity backlog #4: Expenses scan-a-bill (AI) στο mobile)
- Τι: **#4 ✅** από τη parallel-parity λίστα. Το mobile Expenses/Income (`MoneyScreen`) ήταν μόνο list + manual add (vendor+amount). Τώρα κάνει **AI scan-a-bill**: ✦ κουμπί δίπλα στο ＋ → camera → parse → confirm-draft modal (editable vendor/amount/category + read-only date/recurring badge) → Add. Νέα non-persisting actions στο `expenses/actions.ts`: **`scanExpenseText`/`scanExpenseImage`** (mirror των `scanVoucherText/Image`· gated από το `'expenses'` AI feature· το image path ξαναχρησιμοποιεί το proven `runExpenseParse` = OCR-first, vision fallback· ΔΕΝ αποθηκεύει — ο client prefill-άρει φόρμα και κάνει POST `/api/v1/expenses`). Νέο **`POST /api/v1/scan/expense`** (JSON `{text}` ή multipart `{file}` → `{data:{kind,vendor,category,amount,currency,date,period,paymentMethod,recurringCycle}}`, mirror του scan/voucher route). **mobile** — `MoneyScreen`: scan handler (expo-image-picker camera), draft modal, `addExpense` επεκτάθηκε να κουβαλά date/period/recurringCycle/paymentMethod. `api.ts`: `scanExpenseImage`/`scanExpenseText` + `ParsedExpenseData` τύπος. MOBILE_PARITY ενημερωμένο. Commit `4046e47`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· IMG BUILD=0, mongo healthy (oom=false) πριν το `up`, login 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr σβηστό. **Structural μόνο** (όπως ζητήθηκε — ΧΩΡΙΣ πραγματικό AI call, κόστος): `POST /api/v1/scan/expense` no-token → **401** (registered, ΟΧΙ 404) και για JSON και για multipart. Authenticated 400-empty-text test ΔΕΝ έγινε γιατί η mongo απαιτεί auth και ο root password είναι μόνο σε container env/.env (δεν τον αγγίζω). Οι δύο actions είναι ΤΟ ΙΔΙΟ proven pattern με τα `scanVoucherText/Image` (ίδιο gating, ίδιες `parseExpense*` που ήδη τρέχει το web `uploadExpense`).
- Επόμενο task: **Items installment-plan link στο mobile detail** (web `linkPlanToItem`/`computeInstallmentPlans`· read `statements/actions.ts` — link ένα item σε ένα installment plan από το ItemsScreen detail· χρειάζεται νέο read endpoint για available plans + write `POST /api/v1/items/[id]/link-plan`). Εναλλακτικά **mobile Subscriptions edit + AI-fill στο detail** (το AI-fill button υπάρχει μόνο στο add row· `PATCH /api/v1/subscriptions/[id]` υπάρχει ήδη) ή **Vouchers edit + AI scan** στο detail. ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁵ — Items installment-plan link στο mobile detail)
- Τι: **✅** το suggested task. Το mobile item detail δεν μπορούσε να συσχετίσει μια αγορά με credit-card δόσεις plan (web feature). Νέα bearer endpoints: **`GET /api/v1/items/[id]/plans`** (όλα τα installment plans μέσω του proven `computeInstallmentPlans` πάνω σε όλα τα statements· κάθε plan flagged `linked` αν το item είναι στο `itemIds`· linked-first sort· compact serialization signature/label/card/perAmount/paid+total/remaining/projectedEndDate/done/itemCount/currency) + **`POST /api/v1/items/[id]/link-plan`** `{signature}` (wrap `linkPlanToItem`, additive — ένα plan κουβαλά πολλά προϊόντα) + **`DELETE …/link-plan`** `{signature}` (wrap `removeItemFromPlanByKey`, κρατά τα άλλα προϊόντα). Linking matched by `installmentSignature` ακριβώς όπως το web overview UI. **mobile** — νέο `PlansBlock` στο `ItemsScreen` detail (κάτω από το PriceBlock): linked plans με payoff (€/mo · paid/total · €left ή «paid off») + ✕ unlink, + collapsible «Link an installment plan · N available» picker (lazy-load on first expand) με τα unlinked plans (card · €/mo · paid/total · ends Mon YYYY · N linked) → tap ＋ attach. `api.ts`: `InstallmentPlanRow` τύπος + `getItemPlans`/`linkItemPlan`/`unlinkItemPlan`. MOBILE_PARITY Items row ενημερωμένο. Commit `1c02fd5`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· IMG BUILD ok, mongo healthy πριν το `up` (έγινε oom=true στιγμιαία στο build, recovered healthy — ενισχύει το RAM-bump Needs-Achilleas), login 200, web restarts=0, `docker builder prune -f` μετά, flaresolverr σβηστό. No-token σε GET plans + POST + DELETE link-plan → **401** (registered, ΟΧΙ 404). **Authenticated GET round-trip** (token από container, ΠΟΤΕ printed): GET /plans σε πραγματικό item → **9 plans** σωστού shape (currency=EUR· PLAISIO|12|2025-08 €52.63, QUEST €74.52, KOTSOVOLOS|36 €25.25, PLAISIO|4 done=true …) — ταιριάζει με τα πραγματικά δεδομένα, read-only/άθικτο. Το write round-trip (POST/DELETE σε throwaway item) ΔΕΝ ολοκληρώθηκε γιατί το `docker exec mongosh` για token flake-άρισε μετά την 1η επιτυχία (γνωστό intermittent θέμα, χειρότερο μετά το build-OOM)· τα δύο write handlers είναι ΟΙ ΙΔΙΕΣ proven web actions (`linkPlanToItem`/`removeItemFromPlanByKey`, ήδη σε χρήση από το web /statements overview), απλώς exposed via REST. Μηδέν mutation σε πραγματικά δεδομένα.
- Επόμενο task: **mobile Subscriptions edit cycle+renewal** (το edit modal αλλάζει μόνο name/amount/active· web έχει billingCycle picker + nextRenewal date· `PATCH /api/v1/subscriptions/[id]` τα δέχεται ήδη — έλεγξέ το) ή **Vouchers add/edit/scan πλήρη πεδία** (store/discount/expiry — τώρα μόνο title/code· το web scan/voucher επιστρέφει ήδη store/discount/expiresAt/url). Εναλλακτικά **mobile Receipts edit** (line items / re-scan OCR/text / archive — web πλούσιο, mobile read-only). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁶ — mobile Subscriptions edit: billing cycle + next renewal)
- Τι: **✅** το suggested task. Το mobile subscription edit modal άλλαζε μόνο name/amount/active· τώρα έχει **billing-cycle chip-picker** (monthly/yearly/quarterly/weekly/lifetime) + **next-renewal date input** (YYYY-MM-DD). Το `PATCH /api/v1/subscriptions/[id]` δέχεται ήδη `billingCycle`/`nextRenewal`/`category` (verified, CYCLES enum-guarded) — οπότε **καθαρά mobile change, μηδέν web runtime**. Valid YYYY-MM-DD → στέλνεται· blank/malformed → omit (το endpoint ούτως ή άλλως αγνοεί falsy `nextRenewal`, δεν μπορεί να το καθαρίσει). `api.ts updateSubscription` signature επεκτάθηκε με `nextRenewal?`/`category?`. MOBILE_PARITY Subscriptions row ξανα-ευθυγραμμίστηκε (ήταν stale «no edit, no AI fill» ενώ edit+AI υπήρχαν ήδη). Commit `90136c9`.
- Verify: mobile `tsc --noEmit` EXIT=0. **ΧΩΡΙΣ Docker rebuild** — δεν άλλαξε web runtime (το PATCH endpoint υπάρχει + είναι proven από το web Subscriptions UI + το CYCLES validation το προστατεύει). Mobile δεν testable unattended (χρειάζεται simulator)· βασίστηκα σε tsc + code review. Staged ΜΟΝΟ τα 3 δικά μου αρχεία (api.ts, SubscriptionsScreen.tsx, MOBILE_PARITY.md)· το working tree ήταν αλλιώς καθαρό.
- Επόμενο task: **mobile Vouchers add/edit/scan πλήρη πεδία** (τώρα μόνο title/code· το web scan/voucher επιστρέφει ήδη store/discount/expiresAt/url — πρόσθεσε αυτά τα πεδία στο voucher add/edit modal + ✦ scan όπως στα Expenses/Subscriptions). Εναλλακτικά **mobile Receipts edit** (line items / re-scan OCR/text / archive — web πλούσιο, mobile βασικό read/verify). ΘΥΜΗΣΟΥ `docker builder prune -f` ΜΟΝΟ αν κάνεις build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁷ — mobile Vouchers full fields + AI scan → draft)
- Τι: **✅** το suggested task. Το mobile Vouchers ήταν quick-add (title+code) + edit μόνο title/code/used· το ✦ scan γέμιζε μόνο title/code (έχανε store/discount/expiry/url που ήδη επιστρέφει το `/api/v1/scan/voucher`). Τώρα: ένα **ενοποιημένο form modal** (draft 'new' ή edit) με πλήρη πεδία — title, code, store, discount, expiry (YYYY-MM-DD, validated με regex), url + (edit-only) Used toggle. Το **✦ scan** parse-άρει και μετά ανοίγει draft modal **prefilled με ΟΛΑ** τα AI πεδία για review πριν το Add (μοτίβο σαν το MoneyScreen confirm-draft). Inline quick-add (title+code) παρέμεινε για low-friction. Web: μόνο μια γραμμή — πρόσθεσα `url` στο `PATCH /api/v1/vouchers/[id]` (το POST το είχε ήδη, το PATCH όχι → το url στο edit θα ήταν silent no-op). `expiresAt`: valid date → string, blank/invalid → null (το PATCH `'expiresAt' in b` το καθαρίζει). **mobile** — `VouchersScreen` rewrite με `Draft` τύπο + `editing: Voucher|'new'|null` + ScrollView modal. `api.ts`: `addVoucher`/`updateVoucher` signatures επεκτάθηκαν με store/discount/expiresAt/url. MOBILE_PARITY Vouchers row → ✅. Commit: (επόμενο).
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0· χρειάστηκε `typeof e !== 'string'` guard αντί `!== 'new'` λόγω narrowing του `editing` στο JSX). IMG BUILD=0, mongo healthy/oom=false πριν+μετά το `up`, login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr σβηστό. `PATCH /api/v1/vouchers/[id]` με `{url}` no-token → **401** (registered + auth runs, ΟΧΙ 404). Authenticated url-persist round-trip ΔΕΝ έγινε (mongo auth· δεν αγγίζω secrets/.env)· το νέο `url` set είναι ακριβές mirror του ήδη-proven POST url handling. Μηδέν mutation σε πραγματικά δεδομένα.
- Επόμενο task: **mobile Vouchers image scan** (το `/api/v1/scan/voucher` δέχεται ήδη multipart `{file}` → camera → OCR/vision· πρόσθεσε ✦📷 path όπως στο MoneyScreen/Receipts· ⚠ verify ΧΩΡΙΣ πραγματικό AI call — κόστος). Εναλλακτικά **mobile Receipts edit** (line items net/VAT/gross + re-scan OCR/text + add-to-library — web πλούσιο, mobile βασικό verify/archive). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁸ — Build Queue P1: Statements installment-plan overview στο mobile)
- Τι: **✅** το πρώτο TODO της Build Queue (P1/M). Το mobile statement detail έδειχνε μόνο ανά-statement transactions + per-charge δόση badge· δεν υπήρχε aggregated overview (το web `InstallmentOverview` ενώνει cross-statement: μία αγορά = ένα plan με payoff). Νέο bearer endpoint **`GET /api/v1/statements/plans`** (static route → προτεραιότητα έναντι του sibling `[id]`): wraps το proven `computeInstallmentPlans(allStatements)`, serialize signature/label/card/perAmount/paid+total installments/remaining*/totalAmount/projectedEndDate/done/itemCount, **active-first** (το compute ταξινομεί τα active by soonest payoff, μετά τα done). Mirror του `items/[id]/plans` ΧΩΡΙΣ το per-item `linked` filter. **mobile** — `StatementsScreen`: `ListHeaderComponent` «INSTALLMENT PLANS · N active» πάνω από τη λίστα· ανά plan label(+«· N items» αν πολλαπλά)/card/€per-mo/paid-of-total/€left (ή «paid off»)/payoff month (`ends Mon YYYY`)· done plans dimmed (opacity 0.6, purple→faint). `api.ts`: `InstallmentPlan` τύπος + `getInstallmentPlans()`.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0)· IMG BUILD ok, mongo healthy/oom=false πριν+μετά το `up`, login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr exited. No-token → **401** (σωστό error body, ΟΧΙ 404 → route registered + static `plans` νικά το `[id]`). **Πλήρες authenticated GET round-trip** (token από container mongoose, ΠΟΤΕ printed/persisted): status 200, `currency=EUR`, **9 plans** (5 active, 4 done), **active-before-done order verified**, πραγματικά δεδομένα Αχιλλέα (PLAISIO 11/12 €52.63 ends 2026-07, QUEST 4/6 €74.52, KOTSOVOLOS 8/36 €25.25 ends 2028-10, 4× done PLAISIO) — ταιριάζει ακριβώς με το web overview + το ήδη-proven `items/[id]/plans`. Read-only, μηδέν mutation. Commit `df8c87a`, pushed.
- MOBILE_PARITY: Statements row + roadmap #5 → ✅· Build Queue P1 item → DONE.
- Επόμενο task: η Build Queue κατεβαίνει σε **P2 — Receipts re-scan stored file (OCR/text)** (νέο `POST /api/v1/receipts/[id]/rescan` `{ocr?}` wrap του `rescanReceipt`· ⚠ AI cost → δομικό verify ΜΟΝΟ, no token-spend) ή τα P3 (Items convert-to-task [no AI, καθαρό port του `convertItemToTask`], Notifications unread badge στο AppBar, Expenses full-field edit [ΠΡΩΤΑ επέκτεινε το PATCH με period/recurring/recurringCycle/paymentMethod]). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁸ — mobile Vouchers image scan + Receipts editable line items)
- Context: ο Αχιλλέας «ok move on do them» → δύο coherent tasks σε ξεχωριστά commits.
- **Voucher image scan ✅** (`5a028fd`): πρόσθεσα `scanVoucherImage` (multipart → υπάρχον `/api/v1/scan/voucher`) + «📷 Take a photo» button στο voucher scan modal (camera → OCR/vision → prefilled full draft, ίδιο μοτίβο με το Money bill scan). Καθαρά mobile (το endpoint δεχόταν ήδη multipart) — μηδέν web rebuild. mobile tsc EXIT=0.
- **Receipts editable line items ✅** (commit: επόμενο): το mobile receipt detail επεξεργαζόταν μόνο store/total/verified + archive (τα line items ήταν read-only). Τώρα **πλήρες edit**: store, date (YYYY-MM-DD), payment, total, notes + **editable line items** (name/qty/unit-NET/VAT% ανά γραμμή, computed gross δεξιά, add/remove) + **∑ items** (γεμίζει το Total με το gross sum) + verify + archive. Στο save στέλνει `lineItems` + derived `subtotal`/`vatAmount` (από τις γραμμές) όταν υπάρχουν γραμμές. Web: επέκτεινα το `PATCH /api/v1/receipts/[id]` να δέχεται `lineItems` (sanitizer mirror του proven `cleanLineItems`: `price`=unit NET, **refinedName cleared** ώστε το edited `name` να κερδίζει στο GET που γυρνά `refinedName||name`, drop κενών γραμμών). `api.ts updateReceipt` signature += subtotal/vatAmount/notes/lineItems.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0). IMG BUILD=0, mongo healthy/oom=false πριν+μετά το `up`, login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr σβηστό. Structural: `PATCH /api/v1/receipts/[id]` με `{lineItems:[…]}` no-token → **401** (registered + auth runs, ΟΧΙ 404)· GET detail → 401. Authenticated lineItems-persist round-trip ΔΕΝ έγινε (mongo auth· δεν αγγίζω secrets/.env)· ο sanitizer είναι αυτόνομος + mirror του ήδη-proven web `cleanLineItems`. Μηδέν mutation σε πραγματικά δεδομένα.
- Επόμενο task: **mobile Receipts re-scan (OCR/text)** — χρειάζεται νέο endpoint που wrap-άρει το web `rescanReceipt(id, useOcr)` (`receipts/actions.ts`) + button στο detail· ⚠ **AI cost** → build μόνο, verify ΧΩΡΙΣ πραγματικό re-scan (structural 401). Εναλλακτικά **Receipts add-items-to-library** (web `addReceiptItemsToLibrary` → νέο endpoint + button) ή **Statements installment-plan link/merge** στο mobile (write ops πάνω στο statement detail). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.⁹ — mobile Receipts: add items to inventory)
- Context: ο Αχιλλέας έκανε push το δικό του `c78c0fe` (EAS dev build push-ready + PUSH_SETUP runbook → καλύπτει το remote-push Needs-Achilleas) και ρώτησε «what's next». Διάλεξα clean no-AI task που ολοκληρώνει το receipts flow (το re-scan το άφησα — AI cost).
- Τι: **✅** το mobile receipt detail μπορεί τώρα να μετατρέψει τα line items σε inventory Items. Νέο `POST /api/v1/receipts/[id]/add-to-library` (wrap του proven `addReceiptItemsToLibrary` — find-or-create Item ανά title, gross unit price = net*(1+vat), warranty από receipt/defaults, link receiptId) → `{created, linked}`. mobile: `addReceiptToLibrary` στο `api.ts` + κουμπί «＋ Add items to inventory» στο detail modal (μόνο όταν υπάρχουν line items) → Alert με «X created, Y already existed». MOBILE_PARITY Receipts row → πλήρες ✅.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0). IMG BUILD=0, mongo healthy/oom=false πριν+μετά το `up`, login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB), flaresolverr σβηστό. Structural: `POST …/add-to-library` no-token → **401** (registered, ΟΧΙ 404). Authenticated round-trip ΔΕΝ έγινε (mongo auth, δεν αγγίζω secrets/.env)· το handler είναι thin wrap του ήδη-proven web action που τρέχει από το web receipts UI. Μηδέν mutation.
- Επόμενο task: **mobile Receipts re-scan (OCR/text)** (νέο endpoint wrap του `rescanReceipt(id, useOcr)` + 2 buttons στο detail· ⚠ **AI cost** → build μόνο, structural 401 verify, ΟΧΙ πραγματικό re-scan unattended). Εναλλακτικά **mobile Items URL-import UI** (το `/api/v1/items/import` υπάρχει ήδη από #4· πρόσθεσε URL field → preview → add στο ItemsScreen· AI cost στο import → structural verify) ή **Reports enrichment** (net-position banner / budgets / income-vs-expense — no AI). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.¹⁰ — mobile Reports: net-position banner)
- Context: ο Αχιλλέας «pame proxwra» → διάλεξα το no-AI Reports enrichment.
- Τι: **✅** το mobile Reports έδειχνε μόνο expense-based summary (net month/year, by-category, 6-month bars). Πρόσθεσα το **net-position banner** (το headline του web /reports): inventory value (owned items purchasedPrice ?? currentPrice) − installments owed (remaining των active plans). `GET /api/v1/reports` τώρα φέρνει Item + Statement + `computeInstallmentPlans` και επιστρέφει `netPosition {inventoryValue, installmentsOwed, activePlans, net}` (ίδια λογική με το web reports page ώστε να μη γίνεται drift). mobile `ReportsScreen`: μεγάλο banner πάνω-πάνω («NET POSITION», net χρωματιστό accent/red, «X owned − Y owed · N plans»). `api.ts Reports` type += netPosition.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0· χρειάστηκε import του `SerializedStatement` από `@/types` όχι από `@/lib/installments`). IMG BUILD=0, mongo healthy/oom=false πριν+μετά το `up`, login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB), flaresolverr σβηστό. Structural: `GET /api/v1/reports` no-token → **401** (registered). Authenticated round-trip ΔΕΝ έγινε (mongo auth)· η netPosition λογική είναι port του ήδη-proven web reports computation (owned-value − active-plan remaining). Μηδέν mutation.
- Επόμενο task: **mobile Reports — budgets this-month** (το `/api/v1/settings` επιστρέφει ήδη budgets με spent/limit· δείξε per-category progress bars στο ReportsScreen όπως το web «Budget · this month», ή στο Settings μόνο όπως τώρα). Εναλλακτικά **upcoming-installments next-6-months** στο reports endpoint + bars, ή **Receipts re-scan (OCR/text)** (⚠ AI cost, structural verify μόνο), ή **Items URL-import UI** (AI cost στο import). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.¹¹ — mobile Reports: upcoming installments next 6 months)
- Context: το προηγούμενο suggested task («budgets this-month») είχε ήδη γίνει σε commit `eaab2e7` (χωρίς PROGRESS entry — μάλλον προηγούμενο run ή ο Αχιλλέας). Διάλεξα το επόμενο no-AI Reports enrichment από τις εναλλακτικές του cont.¹⁰.
- Τι: **✅** το `GET /api/v1/reports` επιστρέφει τώρα `upcomingInstallments[]` (επόμενοι 6 μήνες· ανά μήνα n=1..6 → άθροισμα `perAmount` των active plans με `remainingInstallments >= n`), ακριβές port του web `/reports` «Upcoming installment obligations» (ίδια λογική → μηδέν drift). mobile `ReportsScreen`: νέο section **UPCOMING INSTALLMENTS · NEXT 6 MONTHS** με purple per-month bars (reuse του υπάρχοντος `Bar` + `mLabel` για το period→μήνα label· `instMax` για scale· εμφανίζεται μόνο αν κάποιος μήνας έχει υποχρέωση). `api.ts Reports` type += upcomingInstallments.
- Verify: tsc καθαρό (web EXIT=0 + mobile EXIT=0). IMG BUILD=0, mongo healthy πριν το `up` (oom=true στιγμιαία στο build, recovered healthy — ξανά το RAM-pressure θέμα), login HTTP 200, web restarts=0, `docker builder prune -f` μετά (2.0GB cache cleared), flaresolverr σβηστό. Structural: `GET /api/v1/reports` no-token → **401** (registered). Authenticated round-trip ΔΕΝ έγινε (mongo auth, δεν αγγίζω secrets/.env)· η upcomingInstallments λογική είναι direct port του ήδη-proven web reports computation. Μηδέν mutation. Staged ΜΟΝΟ τα 3 δικά μου αρχεία (route.ts, api.ts, ReportsScreen.tsx)· το `MOBILE_PARITY.md` ήταν ήδη modified από παράλληλη δουλειά του Αχιλλέα → ΔΕΝ το άγγιξα. Commit `a70909d`.
- Επόμενο task: το mobile Reports είναι πλέον πλούσιο (net-position, this-month/year, budgets, 6-month spend, upcoming installments, by-category). Επόμενα logical (όχι roadmap): **mobile Items URL-import preview-before-approve** (το `/api/v1/items/import` υπάρχει + το mobile add έχει ήδη URL import — λείπει το preview step· ⚠ AI cost στο fetch → structural verify), **mobile Tasks #tag-parse on add** (no-AI, καθαρά mobile), ή **mobile Statements installment-plan overview** (aggregated payoff/remaining/linked products στο statement detail· read-only, no-AI). ΘΥΜΗΣΟΥ `docker builder prune -f` μετά το build + flaresolverr σβηστό + stage ΜΟΝΟ δικά σου paths.

## 2026-06-30 (cont.¹² — MOBILE PARITY AUDIT, read-only)
- Audit web↔mobile **από τον κώδικα** (τα docs ήταν stale). Μηδέν app code, μηδέν Docker, μηδέν AI. mobile `npx tsc --noEmit` → **EXIT 0** (καθαρό, κανένα type error προς flag).
- **Σύνολο: 6 DONE-που-το-doc-έλεγε-missing (διορθώθηκαν στο MOBILE_PARITY.md), 6 PARITY GAPS (→ Build Queue), 3 NEEDS DECISION.**
- **Stale rows που διόρθωσα** (το code τα έχει ήδη): **Activity** (Alerts/Trash/Jobs/History — πλήρες, το doc έλεγε ❌ none)· **Notifications** in-app feed (Activity→Alerts)· **Tasks** 4-status picker (doc: «only 2 states»)· **Statements** detail με transactions + per-charge installment badges (doc: «list only»)· **Items** URL-import με AI (doc: «title only»)· **Expenses/Income** edit vendor/amount/category + AI scan-a-bill (doc: «no edit, no scan»). Reports είχε ήδη ενημερωθεί σωστά.
- **Build Queue (ranked, P1+S πρώτα)**: δεν υπάρχει P1/S — ο πυρήνας parity είναι κλειστός (CRUD/scan/PricePanel/Activity/Calendar/Reports). Top 3 για τον builder:
  1. **Statements installment-plan overview** (P1/M, no AI) — νέο `GET /api/v1/statements/plans` (mirror του `items/[id]/plans` χωρίς το per-item filter· `computeInstallmentPlans` ήδη proven) + section στο StatementsScreen. Ο Αχιλλέας διαχειρίζεται βαριά δόσεις → core.
  2. **Receipts re-scan (OCR/text)** (P2/M, ⚠ AI cost → δομικό verify μόνο) — νέο `POST /api/v1/receipts/[id]/rescan`.
  3. **Items convert-to-task** (P3/S, no AI) — `convertItemToTask` υπάρχει ως web action (`apps/web/src/app/items/actions.ts:649`), λείπει endpoint + κουμπί.
  - Υπόλοιπα queue: notification unread-badge στο AppBar (P3/S), Expenses/Income full-field edit (P3/S), Items AI-specs (P3/M, AI cost).
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (docker-health guard)
- Health (read-only): mongo **healthy** (RestartCount 32 = cumulative ιστορικό από παλιές OOMs, αλλά σταθερά up, ΟΧΙ restart-loop)· web **Up, RestartCount 0**, login HTTP **200**· flaresolverr ήδη Exited (σβηστό, καμία ενέργεια)· searxng up (κανονικό default stack).
- Disk: build cache 2.58GB → **`docker builder prune -f`** (cache only) → 566MB, **~2.0GB reclaimed** (29→15 entries).
- Rebuild: **ΟΧΙ**. Το concurrent dev-task είχε ήδη κάνει build+up το web runtime change (`a70909d` Reports upcoming-installments)· `git diff a70909d..HEAD -- apps/web/src` = κενό (τα επόμενα commits docs-only) → το τρέχον HEAD σερβίρεται ήδη validated (login 200, restarts 0). Marker set στο HEAD.
- Race note: το dev-task έτρεχε παράλληλα όλο το run (HEAD eaab2e7 → a70909d → 6bdedd2 → 516d258 → 1d2a9be)· περίμενα να σταθεροποιηθεί το working tree πριν αγγίξω git. Staged ΜΟΝΟ PROGRESS.md.

## Needs Achilleas
- **Mobile theme toggle (light/dark)**: το web έχει light theme· το mobile είναι dark-only με static `apps/mobile/src/theme.ts` (`C`). Buildable αλλά θέλει theme-context refactor σε όλες τις οθόνες (M-L) ΚΑΙ απόφαση αν θες καν light theme στο κινητό. Δεν μπήκε στο Build Queue (product decision).
- **Mobile i18n (8 γλώσσες)**: το web έχει cookie-based 8-lang i18n· το mobile είναι English-only. Large· χρειάζεται translation infra + ποιες γλώσσες. Product decision.
- **Mobile Settings — AI engine / prompts / storage / OneDrive / CSV / backup-restore**: desktop/admin-oriented· πιθανώς ΔΕΝ τα θες σε κινητό. Δεν τα έβαλα στο queue· πες αν θες κάποιο.
- **Docker VM RAM**: η mongo ξανα-OOM-σκοτώθηκε στιγμιαία κατά το web build (recovered healthy, αλλά το `docker exec mongosh` flake-άρει μετά λόγω memory pressure → δυσκολεύει το authenticated verify). Σκέψου Docker Desktop → Resources → **RAM σε 4GB**. (Επαναλαμβανόμενο σε πολλά runs.)
- **Remote push (#8 γ)**: λείπει EAS dev build + Apple APNs key — βλ. 2026-06-29 cont.⁷.

## 2026-06-30 (ui-auditor — mobile UI consistency audit)
- Τι: read-only audit του mobile UI vs web design system. Μηδέν app-code άλλαξε· γράφτηκε νέο **`## UI Debt Queue`** στο `MOBILE_PARITY.md` (9 build-ready items, ranked, μικρά+P1 πρώτα).
- Ευρήματα ανά dimension:
  - **Tokens**: 49 hardcoded hex εκτός `theme.ts` (κυρίως `#000` text-on-accent + 11 alpha-tinted theme colors π.χ. `#00ff8814`, `#00d4ff44`, `#ff475740`). Το `theme.ts` έχει ΜΟΝΟ χρώματα — μηδέν spacing/radius/typography scale (radius 10/12/14 + padding 12/14 + fontSize 13/14/15/16/19 σκόρπια). Λείπουν `surface3`/`orange` που έχει το web.
  - **Shared theme**: υπάρχει (`theme.ts` `C`) αλλά incomplete· δεν είναι foundation από το μηδέν, χρειάζεται επέκταση (P1).
  - **Reusable components**: μηδέν Button/Card/Input/Badge/Chip primitives. `input` style ξαναγράφεται 14×, `saveText` 7×, `addBtn`/`chip`/`card`/`badge` πολλαπλά. `ui.tsx` έχει μόνο Header/Spinner/Empty/Error.
  - **Theme/dark mode**: μηδέν light theme, μηδέν theme context (`C` const-imported σε 19 αρχεία). Web έχει πλήρες light mode.
  - **States**: σχετικά συνεπή (Spinner/Empty/ErrorText shared, 13 screens τα χρησιμοποιούν)· 3 screens roll own (Assistant/Login/Shopping) — low priority.
  - **Adaptive**: `SafeAreaView` χωρίς insets (μηδέν bottom-inset, bottom-sheets κάτω από home indicator)· μόνο 2 maxWidth usages, καμία στο content → τέντωμα σε tablet/landscape.
  - **Touch targets**: ~6 κάτω από 44pt (checkboxes 24×24, menuBtn 38×36, backBtn 40×36, lineDel 30×30).
- Top-3 για τον builder: (1) **Theme tokens: spacing+radius+typography+missing colors** (P1/S, foundation), (2) **Alpha-tint helper** (P1/S, σβήνει τα 11 alpha hex), (3) **Input primitive** (P1/M, ενοποιεί 14 duplicate input styles). Το light/dark theme context είναι P3/L, τελευταίο (εξαρτάται από τα primitives).
- Verify: read-only· μηδέν Docker, μηδέν AI jobs. Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md`.

## 2026-06-30 (reviewer — έλεγχος των τελευταίων 5 commits)
- Range: `eaab2e7..1d2a9be` (δεν υπήρχε `reviewed` marker → έλεγξα τα 5 τελευταία). 2 code commits (`eaab2e7` budget this-month, `a70909d` upcoming installments) + 3 docs-only (`6bdedd2`, `516d258`, `1d2a9be`).
- Checks: web `npm run type-check` → **EXIT 0**, mobile `npx tsc --noEmit` → **EXIT 0**. Read-only, καμία διόρθωση χρειάστηκε.
- Review ευρήματα (όλα PASS):
  - `GET /api/v1/reports` budgets[] + upcomingInstallments[] = πιστό port του web `/reports/page.tsx` (thisMonthCat loop με `amt <= 0 continue`, budget filter limit>0, installments `remainingInstallments >= n` × `perAmount`). **Μηδέν drift** με το web computation.
  - Αλλαγές API αμιγώς **additive** (2 νέα πεδία, κανένα removed/renamed) → δεν σπάει υπάρχοντες mobile consumers.
  - Το νέο `amt <= 0 continue` στο budget loop **ευθυγραμμίζει** το API με το web /reports (πριν αθροιζόταν αρνητικά/μηδενικά) → parity fix, όχι regression.
  - mobile `BudgetRow {category,limit,spent}` ταιριάζει με το API shape· `C.purple/gold/red/accent` υπάρχουν στο theme· `money()`/`Bar` reused σωστά.
  - Docs commits = docs-only (MOBILE_PARITY.md/PROGRESS.md), μηδέν app code, μηδέν secrets (το secret-scan έβγαλε μόνο false positives — color tokens #000/#ffa502).
- Flagged: τίποτα νέο. Καμία regression, κανένα type error, κανένα committed secret.
- Git hygiene: staged ΜΟΝΟ `PROGRESS.md` (reviewed marker + αυτή η εγγραφή). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (web-code-quality auditor — /api/v1 audit, read-only)
- Audit της Next.js web υλοποίησης (έμφαση στο `/api/v1` που τρώει το mobile), από τον κώδικα. Μηδέν app code, μηδέν Docker, μηδέν AI jobs. `cd apps/web && npm run type-check` → **exit 0** (κανένα type error).
- **Counts ανά dimension**: Type safety **0** (μηδέν `any`/`@ts-ignore`· tsc καθαρό). Auth **0** (ΟΛΑ τα 45 routes εκτός login περνούν από `withAuth`· `apiToken` indexed). Error handling **0** (uniform try/catch μέσω `withAuth`· κάθε body read `req.json().catch(() => ({}))`· μηδέν swallowed catch). Validation **2** (items POST δεν κάνει whitelist το status ενώ το PATCH ναι· shopping-list/[id] χωρίς id-guard + σιωπηλό no-op). Consistency **2** (GET /items αποκλίνει από το shared `listEnvelope`· repeated body-coercion patterns). DB/perf **1** (το `updatedAt` — incremental-sync cursor + sort key Item/Task — δεν είναι indexed σε κανένα model).
- **Σύνολο: 0 P1, 3 P2, 2 P3** → γράφτηκαν στο νέο `WEB_DEBT.md` (## Web Debt Queue), μικρότερο+P-υψηλότερο πρώτα.
- **Top 3 για τον builder**:
  1. **Index `updatedAt` στα synced models** (P2/S, db) — επηρεάζει κάθε mobile sync (unindexed `$gte` + in-memory sort).
  2. **POST /api/v1/items — whitelist status/category** (P2/S, api) — mirror του PATCH· σταματά αυθαίρετα status στη DB.
  3. **GET /api/v1/items → listEnvelope alignment** (P2/S, api) — 6 endpoints γυρνούν `{data}`, μόνο items γυρνά `{items}`· χρειάζεται συντονισμένο update στο `apps/mobile/src/api.ts`.
- Staged ΜΟΝΟ `WEB_DEBT.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (parity-auditor — επανέλεγχος Build Queue από τον κώδικα)
- Read-only audit web↔mobile. Μηδέν app code, μηδέν Docker, μηδέν AI. mobile `npx tsc --noEmit` → **EXIT 0** (καθαρό· κανένα type error προς flag ως P1).
- Inventory: **44 v1 routes** (login + 43 bearer), **16 mobile screens**. Επαλήθευσα από τον κώδικα ότι και τα **6 items του Build Queue παραμένουν γνήσια gaps** (η προηγούμενη audit cont.¹² ήταν ίδιας μέρας + thorough, οπότε το queue ήταν ήδη σωστά ranked):
  - `GET /api/v1/statements/plans` → ΔΕΝ υπάρχει (μόνο `statements/route.ts` + `statements/[id]/route.ts`) → **P1/M valid TODO**.
  - `POST /api/v1/receipts/[id]/rescan` → ΔΕΝ υπάρχει → **P2/M valid TODO** (AI cost).
  - `POST /api/v1/items/[id]/convert-to-task` → ΔΕΝ υπάρχει· η web action `convertItemToTask` υπάρχει (`items/actions.ts:649`) → **P3/S valid TODO**.
  - Notifications badge: `nav.tsx` δεν έχει bell/badge· `GET /api/v1/notifications` υπάρχει + το mobile `api.ts` το καλεί ήδη (`getNotifications`/`markNotificationRead`) → **P3/S valid TODO**.
  - `POST /api/v1/items/[id]/ai-fill` → ΔΕΝ υπάρχει → **P3/M valid TODO** (AI cost).
- **Διόρθωσα ΕΝΑ stale spec** στο Build Queue: το «Expenses/Income full-field edit» έλεγε «API exists: yes — δέχεται ήδη period/recurring/recurringCycle/paymentMethod». Λάθος: το `PATCH /api/v1/expenses/[id]` (route.ts:17-22) δέχεται **ΜΟΝΟ** vendor/amount/category/kind/notes/date. Ενημέρωσα σε «exists: PARTIAL» + ο builder πρέπει ΠΡΩΤΑ να επεκτείνει το PATCH (period/recurring/recurringCycle/paymentMethod) πριν τα δείξει. Το mobile `openEdit` ούτως ή άλλως καλύπτει μόνο vendor/amount/category (MoneyScreen.tsx:84).
- **Σύνολο: πυρήνας parity κλειστός (μηδέν P1/S), 6 GAP στο queue, 0 νέα NEEDS DECISION** (τα υπάρχοντα Needs-Achilleas — theme/i18n/AI-engine settings/push creds/Docker RAM — μένουν ως έχουν).
- **Top 3 για τον builder (αμετάβλητα):** (1) Statements installment-plan overview [P1/M, no AI], (2) Receipts re-scan OCR/text [P2/M, AI cost → δομικό verify], (3) Items convert-to-task [P3/S, no AI].
- Staged ΜΟΝΟ `MOBILE_PARITY.md` + `PROGRESS.md` (όχι `-A`). Καμία αλλαγή σε app code/secrets/.env.

## Needs Achilleas
- **Ευρεία response-envelope standardization**: τα mutation/single endpoints γυρνούν ad-hoc keys (`{item}`/`{expense}`/`{task}`/`{ok,id}`/`{receipt}`/`{cards}`/`{lists}`/`{hits}`…) αντί ενιαίου `{data}`. Η ενοποίηση είναι breaking change που συντονίζεται με το mobile (`apps/mobile/src/api.ts`) → product/API-versioning απόφαση, ΔΕΝ μπήκε στο queue. Πες αν θες να το ανοίξω σταδιακά (ένα resource/run).
- **Zod σε όλο το `/api/v1`**: μηδέν zod στα routes (manual `as Record<string,unknown>` + coercion, που είναι προσεκτικό αλλά verbose). Το project χρησιμοποιεί zod αλλού. Πλήρες migration = large/sprawling → δεν το πρότεινα· έβαλα μόνο μικρό P3 για shared coercion helpers + απόφαση αν θες zod-first σε νέα routes.

## 2026-06-30 (ui-auditor — mobile UI consistency re-audit)
- Τι: read-only re-audit του mobile UI vs το web design system. **Κανένα από τα 10 UI Debt items δεν έχει υλοποιηθεί** από τότε που στήθηκε η ουρά (commit 1d2a9be): το `apps/mobile/src/theme.ts` παραμένει colors-only (μηδέν spacing/radius/typography, λείπουν `surface3`/`orange`), το `apps/mobile/src/ui.tsx` δεν έχει Button/Card/Input/Badge/Chip primitives (μόνο Header/Spinner/Empty/ErrorText). Επιβεβαίωσα + ανανέωσα τα νούμερα στο MOBILE_PARITY.md.
- Ευρήματα ανά dimension (refreshed): **Tokens** — 53 hardcoded hex εκτός theme.ts (40× `#000` onAccent + 11 alpha-tinted theme colors σε ActivityScreen:293,302,311,322 + SettingsScreen:644,653,664 + λίγα palette re-defs). **Shared theme file** — υπάρχει αλλά ελλιπές (colors-only· λείπει surface3 #242424 + orange #ffa502 που έχει το web globals.css:14,22, + όλο το spacing/radius/typography scale). **Reusable components** — μηδέν· `input` style ξαναγράφεται σε 15 entries / 11 screens, `saveText`/`saveBtn`/`addBtn` 12×, local `card`/`badge`/`chip` 13×. **Theme/dark mode** — dark-only· το web έχει πλήρες light theme (globals.css:32-51), μηδέν theme context στο mobile. **Adaptive** — `react-native-safe-area-context` δεν είναι καν dependency → `SafeAreaView` χωρίς bottom-inset (App.tsx:74)· μόνο 2 maxWidth usages σε όλο το app, καμία στο content. **Touch targets** — ~6 κάτω από 44pt (24×24 checkboxes + 38×36 menuBtn + 40×36 backBtn + 30×30 lineDel). tsc καθαρό (read-only).
- Verify: read-only μόνο (μηδέν Docker, μηδέν AI, μηδέν app-code edit). grep-based counts + `npx tsc --noEmit` καθαρό. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
- Top 3 για τον builder (με σειρά εξάρτησης): (1) **Theme tokens: spacing + radius + typography + missing colors** (P1/S, foundation — προσθέτει surface3/orange/onAccent + scales στο theme.ts· τα άλλα 9 εξαρτώνται), (2) **Alpha-tint helper** (P1/S — `alpha(hex,n)` αντικαθιστά τα 11 alpha-tinted literals), (3) **Input primitive** (P1/M — ένα `<Input>` στο ui.tsx αντικαθιστά τα 15 duplicate input styles). Μετά: Button+Chip → Card+Badge+ListItem → touch targets → safe-area insets → max-width → light theme (L, τελευταίο).

## 2026-06-30 (cont.⁸ — web code-quality re-audit: queue confirmed αμετάβλητη)
- Τι: re-audit ολόκληρης της `/api/v1` επιφάνειας (44 route files, 40 endpoints) από τον web code-quality auditor (read-only). Η ουρά WEB_DEBT (5 items: 0 P1 / 3 P2 / 2 P3) που στήθηκε στο πρωινό run (`6666a28`) **επιβεβαιώθηκε αμετάβλητη** — από τότε μόνο docs commits, μηδέν builder κατανάλωσε item. Σάρωσα ξανά και τα πιο πρόσφατα routes (cards/[id], stores/[id], items/[id]/{plans,link-plan,price}, scan/expense, items/import): όλα id 24-hex guarded, input-validated, clean 400/404. Μηδέν νέο εύρημα.
- Ευρήματα ανά dimension: **Type safety** — `npm run type-check` exit 0· μηδέν `any`/`@ts-ignore`/`@ts-expect-error` στα v1 routes. **Auth** — 39/40 endpoints περνούν `withAuth` (μόνο `auth/login` εξαιρείται, σωστά)· `User.apiToken` indexed. **Error handling** — ομοιόμορφο try/catch via `withAuth`, κάθε body read `req.json().catch(()=>({}))`, μηδέν swallowed catch. **Reads** — όλα τα list endpoints `.lean()` + pagination 1..200. **DB** — επιβεβαιωμένο: κανένα από τα 7 synced models δεν έχει explicit `updatedAt` index (το sync cursor του `withSince` + sort key στα Item/Task → P2 item #1, ισχύει). **Consistency** — `GET /api/v1/items` μόνο του επιστρέφει `{items}` αντί `listEnvelope {data}` (mobile διαβάζει `.items` σε api.ts:87,205 → P2 item #3, ισχύει)· `shopping-list/[id]` το μόνο [id] route χωρίς id guard + σιωπηλό `{ok:true}` σε not-found (P3 item #4, ισχύει).
- Verify: read-only μόνο (μηδέν Docker, μηδέν AI, μηδέν app-code edit). `npm run type-check` → exit 0. grep sweeps για any/ts-ignore/withAuth/id-guard/updatedAt-index. Staged ΜΟΝΟ WEB_DEBT.md + PROGRESS.md.
- Top 3 για τον builder (αμετάβλητα, P2 πρώτα): (1) **Index updatedAt στα 7 synced models** (P2/S, db — explicit `Schema.index({updatedAt:-1})`· επιταχύνει κάθε mobile incremental sync, ειδικά Item/Task που sort-άρουν κιόλας σε αυτό), (2) **POST /api/v1/items — whitelist status & category** (P2/S, api — ίδια STATUS whitelist με το PATCH, εξαγωγή σε shared const), (3) **GET /api/v1/items → listEnvelope alignment** (P2/S, api+ui — `{items}`→`{data}` + ενημέρωση mobile consumer, ή τεκμηρίωση της απόκλισης αν το breaking δεν είναι ΟΚ τώρα).
- Needs Achilleas: κανένα νέο. (Παραμένει από προηγούμενα: RAM bump στο Docker VM λόγω επαναλαμβανόμενου mongo OOM στα builds· response-envelope standardization σε ΟΛΑ τα endpoints = breaking change που θέλει συντονισμό με το mobile.)

## 2026-06-30 (reviewer — έλεγχος range 1d2a9be..af7fdc5)
- Range: 8 commits, μόνο **1 με app code** (`df8c87a` statements installment-plan overview)· τα υπόλοιπα 7 docs-only (audits monitor/web/mobile/parity + το `af7fdc5` progress note). Κανένα `up --build`, μηδέν Docker, μηδέν AI.
- Checks: web `npm run type-check` → **EXIT 0**, mobile `npx tsc --noEmit` → **EXIT 0**. Καμία διόρθωση χρειάστηκε.
- Review ευρήματα (όλα PASS, μηδέν regression):
  - Νέο `GET /api/v1/statements/plans` (route.ts): περνά `withAuth`, `.lean()`, `force-dynamic`· field mapping **ταιριάζει 1:1** με το `InstallmentPlan` type του `lib/installments.ts` (signature/label/card/perAmount/total+paid+remainingInstallments/remainingAmount/totalAmount/projectedEndDate/done + `itemCount=itemIds.length`). Πιστό port του web InstallmentOverview.
  - Static `/plans` έναντι sibling `/[id]` → στο Next App Router το static segment **έχει προτεραιότητα** (δεν το πιάνει το `[id]`). Σωστό.
  - Re-sort στο route (`a.done===b.done?0:a.done?1:-1`) είναι **redundant** (το `computeInstallmentPlans` ήδη sort-άρει done-last → soonest-payoff)· ακίνδυνο γιατί το JS Array.sort είναι stable (ES2019+) → διατηρεί τη σειρά πληρωμής. Όχι bug.
  - API αλλαγή αμιγώς **additive** (1 νέο route, κανένα removed/renamed) → δεν σπάει υπάρχοντες mobile consumers.
  - Mobile: `InstallmentPlan` type + `getInstallmentPlans()` ταιριάζουν με το API shape· το `load()` κάνει `Promise.all([getStatements, getInstallmentPlans])` σε try/catch (αν αποτύχει το plans, βλέπεις τον ίδιο error state — αποδεκτό). Header section reuse `money()/C.*` σωστά· `payoff()` guard-άρει `isNaN`.
  - Secret-scan στο diff: καθαρό (μόνο `apiToken` references, όχι literals).
- Flagged: τίποτα νέο. Καμία regression, κανένα type error, κανένα committed secret. Οι ουρές (WEB_DEBT 5, MOBILE_PARITY) μένουν ως έχουν.
- Git hygiene: staged ΜΟΝΟ `PROGRESS.md` (reviewed marker `1d2a9be`→`af7fdc5` + αυτή η εγγραφή). Καμία αλλαγή σε app code/secrets/.env.

## 2026-06-30 (ui-auditor — mobile UI consistency re-audit, βραδινό run)
- Τι: read-only re-audit του mobile UI vs το web design system. **Κανένα από τα 10 UI Debt items δεν έχει υλοποιηθεί** από τότε που στήθηκε η ουρά (commit 1d2a9be). Επιβεβαίωσα από τον κώδικα: `apps/mobile/src/theme.ts` παραμένει colors-only (μηδέν spacing/radius/typography, λείπουν `surface3` #242424 + `orange` #ffa502 που έχει το web globals.css:11,22), `apps/mobile/src/ui.tsx` έχει μόνο Header/Centered/Spinner/ErrorText/Empty (μηδέν Button/Card/Input/Badge/Chip), `react-native-safe-area-context` ΔΕΝ είναι dependency. mobile `npx tsc --noEmit` → **exit 0**.
- Counts ανά dimension (refreshed): **Tokens** — 49 hardcoded hex εκτός theme.ts (40× `#000` onAccent + 8 alpha-tinted [`#ffd93d50`/`#ff475715`/`#ff475712`/`#00ff8815`/`#00ff8814`/`#00d4ff33`/`#00d4ff0a`/`#000000aa`] + 1 `#f5f5f5` re-def). **Shared theme file** — υπάρχει αλλά ελλιπές (colors-only). **Reusable components** — μηδέν primitives· `input:` style ×15 σε 11 screens, `saveBtn`/`saveText`/`addBtn` ×12, local `card`/`badge`/`chip` 13×. **Theme/dark mode** — dark-only· το web έχει πλήρες light theme (globals.css:37-50). **Adaptive** — safe-area-context εκτός deps, μόνο 2 maxWidth usages (καμία στο content). **Touch targets** — ~7 κάτω από 44pt (6× checkbox 24×24, lineDel 30×30, rm 36×36, backBtn 40×36).
- Output: ανανέωσα τη σύνοψη ευρημάτων στο MOBILE_PARITY.md (53→49 hex, σωστή λίστα alpha hex, refreshed line refs). Τα 10 per-item specs μένουν αμετάβλητα (όλα έγκυρα + σωστά ranked).
- Verify: read-only μόνο (μηδέν Docker, μηδέν AI, μηδέν app-code edit). grep-based counts + tsc exit 0. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
- Top 3 για τον builder (σειρά εξάρτησης): (1) **Theme tokens: spacing + radius + typography + missing colors** (P1/S, foundation — τα άλλα 9 εξαρτώνται· προσθέτει surface3/orange/onAccent + scales στο theme.ts), (2) **Alpha-tint helper** (P1/S — `alpha(hex,n)` αντικαθιστά τα 8 alpha-tinted literals), (3) **Input primitive** (P1/M — ένα `<Input>` στο ui.tsx αντί 15 duplicate input styles).

## 2026-06-30 (cont.¹³ — mobile UI foundation: theme tokens + alpha helper)
- Context: αυτόνομο run. Διάλεξα τα top-2 P1/S items της UI Debt Queue (foundation — 9 άλλα items εξαρτώνται από αυτά)· no-AI, no-Docker (mobile-only).
- Τι: **✅** (UI Debt #1 + #2 μαζί, μία συνεκτική αλλαγή):
  - `apps/mobile/src/theme.ts`: το `C` πλέον εξάγει `surface3` (#242424), `orange` (#ffa502), `onAccent` (#000) ώστε να καλύπτει όλα τα web color tokens (globals.css). Νέα scale exports `SPACE` (4/8/12/16/24), `RADIUS` (10/12/14), `SIZE` (11/13/14/15/16/19). Νέο `alpha(hex, n)` helper (3- ή 6-digit hex + opacity 0..1 → rgba string, mirror του web `color-mix(... N%, transparent)`).
  - `ActivityScreen.tsx`: τα 7 alpha-tinted theme-hex literals (tabOn/unreadCard/purge/msgBot) → `alpha(C.*, n)`.
  - `SettingsScreen.tsx`: τα 5 alpha-tinted literals (signout/activeOn/autoBadge/modalBackdrop) → `alpha(...)` + τα 5 `color:'#000'` onAccent → `C.onAccent` + το `#f5f5f5` CARD_COLORS re-def → `C.text`.
- Verify: mobile `npx tsc --noEmit` → **EXIT 0**. grep `#[0-9a-fA-F]{8}` σε `apps/mobile/src/**.tsx` → **NONE** (μηδέν εναπομείναν alpha hex). grep alpha theme-hex (`#00ff88XX`/`#00d4ffXX`/`#ff4757XX`/`#ffd93dXX`/...) → CLEAN. Καμία Docker/AI ενέργεια (mobile change, δεν auto-testable σε simulator unattended· βασίστηκα σε tsc + code review όπως ορίζει το task file). Staged ΜΟΝΟ τα 3 mobile αρχεία + MOBILE_PARITY.md + PROGRESS.md.
- Επόμενο task: **UI Debt #3 — Input primitive** (P1/M, depends-on τώρα ικανοποιημένο): ένα `<Input>` στο `ui.tsx` που ενοποιεί τα ~15 duplicate `input` styles σε 11 screens (χρησιμοποίησε τα νέα `RADIUS`/`SIZE`/`SPACE` tokens). Μετά: Button+Chip primitives → Card+Badge → touch targets (≥44pt) → safe-area insets (χρειάζεται add `react-native-safe-area-context` dep). Εναλλακτικά no-AI: **Build Queue — Items convert-to-task** (P3/S: web action `convertItemToTask` υπάρχει, λείπει `POST /api/v1/items/[id]/convert-to-task` + κουμπί· χρειάζεται Docker rebuild για verify). ΘΥΜΗΣΟΥ: stage ΜΟΝΟ δικά σου paths, `docker builder prune -f` αν κάνεις build, flaresolverr σβηστό.

## 2026-06-30 (cont.⁹ — web code-quality re-audit: queue confirmed, μηδέν app-code commit ενδιάμεσα)
- Τι: read-only re-audit ολόκληρης της `/api/v1` (46 route files) από τον web code-quality auditor. **Καθοριστικό εύρημα:** `git log af7fdc5..HEAD -- apps/web/src` → **κενό** — κανένα app-code commit στο web από τότε που στήθηκε η ουρά WEB_DEBT (5 items: 0 P1 / 3 P2 / 2 P3). Άρα η ουρά είναι by-construction αμετάβλητη + κάθε item επαναεπαληθεύτηκε από τον κώδικα.
- Ευρήματα ανά dimension (όλα confirmed/clean): **Type safety** — `npm run type-check` → exit 0· `grep` για `: any`/`as any`/`@ts-ignore`/`@ts-expect-error` στα v1 routes → **0**. **Auth** — 45/46 route files περνούν `withAuth`· μόνο `auth/login` εξαιρείται (σωστά). **Input** — κάθε body read `req.json().catch(()=>({}))` (το login έχει δικό του try/catch → 400 «Invalid JSON body»)· μηδέν unguarded. **Reads** — κάθε `.find()` caller κάνει `.lean()` (sweep: μηδέν find-χωρίς-lean)· list endpoints με skip/limit. **DB** — κανένα από τα 7 synced models δεν έχει explicit `index({ updatedAt })` (P2#1 ισχύει· είναι sync cursor + sort key σε Item/Task). **Consistency** — `GET /items` μόνο του γυρνά `{items}` αντί `listEnvelope {data}` (route.ts:51 → P2#3)· `POST /items` δέχεται αυθαίρετο status ενώ PATCH κάνει `STATUS.includes()` με 8 τιμές (route.ts:113 → P2#2)· `shopping-list/[id]` το μόνο [id] route χωρίς 24-hex guard + σιωπηλό success σε not-found (P3#4). Όλα ήδη στην ουρά.
- Verify: read-only μόνο (μηδέν Docker, μηδέν AI, μηδέν app-code edit). `npm run type-check` → exit 0. grep sweeps για any/withAuth/req.json-guard/find-lean/updatedAt-index. Staged ΜΟΝΟ WEB_DEBT.md + PROGRESS.md.
- Top 3 για τον builder (αμετάβλητα, P2 πρώτα): (1) **Index updatedAt στα 7 synced models** (P2/S, db — explicit `Schema.index({ updatedAt: -1 })`· επιταχύνει κάθε mobile incremental sync, ειδικά Item/Task που sort-άρουν σε αυτό), (2) **POST /api/v1/items — whitelist status** (P2/S, api — ίδια STATUS whitelist με το PATCH, εξαγωγή σε shared const ώστε POST+PATCH να μη διπλο-γράφουν τη λίστα), (3) **GET /api/v1/items → listEnvelope alignment** (P2/S, api+ui — `{items}`→`{data}` + ενημέρωση mobile consumer, ή τεκμηρίωση της απόκλισης αν το breaking δεν είναι ΟΚ τώρα).
- Needs Achilleas: κανένα νέο. (Παραμένουν: response-envelope standardization σε ΟΛΑ τα endpoints + zod-first σε νέα routes = breaking/large, συντονισμός με mobile· RAM bump στο Docker VM.)

## 2026-06-30 (ui-auditor — mobile UI consistency re-audit, 4ο run)
- Τι: read-only re-audit του mobile UI vs το web design system. **Επιβεβαίωση: τα 2 foundation items (theme tokens + alpha helper, commit `362efc5`) παραμένουν τα μόνα DONE· κανένα από τα 7 εναπομείναντα UI Debt items δεν υλοποιήθηκε ενδιάμεσα.** `apps/mobile/src/ui.tsx` εξάγει ΑΚΟΜΑ μόνο 5 primitives (`Header`/`Centered`/`Spinner`/`ErrorText`/`Empty` + money/shortDate helpers) — μηδέν Input/Button/Chip/Card/Badge. `react-native-safe-area-context` ΑΚΟΜΑ εκτός `package.json`. mobile `npx tsc --noEmit` → **exit 0**.
- Counts ανά dimension (refreshed, grep-based): **Tokens** — 36 hardcoded hex εκτός `theme.ts`, **ΟΛΑ `#000`** (3-digit onAccent text/icon literals) σε 10 screens (Money/Assistant/Tasks/Subscriptions/Shopping/Receipts/Vouchers/Settings/Items/Login)· μηδέν εναπομείναν tinted/8-digit hex (alpha-refactor καθαρό). **Shared theme** — υπάρχει + πλήρες scales (✅ surface3/orange/onAccent + SPACE/RADIUS/SIZE + alpha()). **Reusable components** — μηδέν primitives· `input`/`minput`/`einput` style ξαναγράφεται σε **15** entries / **11** screens με **αποκλίνοντα tokens** (borderRadius 12 vs 10 vs 14, paddingVertical 11 vs 10, fontSize 15 vs 16 — π.χ. ItemsScreen:426 input radius12/pad11 vs :446 minput radius10/pad10, SearchScreen:85 fontSize16, AssistantScreen:77 radius14), `saveBtn`/`saveText`/`addBtn` **12×**, local `card`/`badge`/`chip` **11×**. **Theme/dark mode** — dark-only (web έχει light theme globals.css:37-50). **Adaptive** — safe-area-context εκτός deps, μόνο 2 maxWidth (καμία στο content). **Touch targets** — ~7 <44pt (6× checkbox 24×24, lineDel 30×30, rm 36×36, backBtn 40×36).
- Output: ανανέωσα τη σύνοψη ευρημάτων στο MOBILE_PARITY.md (διόρθωσα stale «27× #000 + λοιπά solid» → «36, όλα #000»· Input title 14→15 duplicate· refreshed date marker). Τα 7 per-item specs μένουν αμετάβλητα (όλα έγκυρα + σωστά ranked).
- Verify: read-only μόνο (μηδέν Docker, μηδέν AI, μηδέν app-code edit). grep-based counts + tsc exit 0. Staged ΜΟΝΟ MOBILE_PARITY.md + PROGRESS.md.
- Top 3 για τον builder (σειρά εξάρτησης, foundation ικανοποιημένη): (1) **Input primitive** (P1/M — ένα `<Input>` στο `ui.tsx` αντικαθιστά τα 15 duplicate input styles, χρησιμοποιεί RADIUS/SIZE/SPACE tokens), (2) **Button + Chip primitives** (P2/M — `<Button>` με `C.onAccent` text εξαλείφει τα 36 inline `#000` + `<Chip>` με on/off state), (3) **Card + Badge + ListItem primitives** (P2/M — ενοποιεί τα 11 local card/badge/chip). Μετά: touch targets ≥44pt → safe-area insets (add dep) → max-width → light theme (L, τελευταίο).
